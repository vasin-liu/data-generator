# 数据集名称，各个文件中唯一
name: parking_black_list
#生成数量
amount: 20
#批次数量
batchSize: 20
#全局配置
global:
  #线程池配置
  executor:
    #核心线程数
    coreSize: 8
    #最大线程数
    maxSize: 16
    #队列大小
    queueCapacity: 16

# 数据集元数据配置
table:
  fields:
    - name: ID
      stages:
        - type: READ
          readers:
            - type: SPEL
              content: "#faker.snowflake.next"
        - type: SELECT
    - name: VEHICLE_ID
      stages:
        - type: READ
          inMemory: true
          readers:
            - type: JDBC
              dataSourceId: 'tocc_parking'
              content: "SELECT * FROM TOCC_PARKING_LOT.ARCHIVE_VEHICLE_INFO WHERE STATUS = 'S0A' LIMIT 20"
        - type: SELECT
        - type: SCRIPT
          scriptType: SPEL
          content: "#dataset.VEHICLE_ID"
    - name: PLATE_NUMBER
      dependsOn:
        - VEHICLE_ID
      stages:
        - type: SCRIPT
          scriptType: SPEL
          content: "#dataset.PLATE_NUMBER"
    - name: PLATE_COLOR
      dependsOn:
        - VEHICLE_ID
      stages:
        - type: SCRIPT
          scriptType: SPEL
          content: "#dataset.PLATE_COLOR"
    - name: VEHICLE_TYPE
      dependsOn:
        - VEHICLE_ID
      stages:
        - type: SCRIPT
          scriptType: SPEL
          content: "#dataset.VEHICLE_TYPE"
    - name: PHONE
      dependsOn:
        - VEHICLE_ID
      stages:
        - type: SCRIPT
          scriptType: SPEL
          content: "#dataset.PHONE"
    - name: APPEAL_STATUS
      stages:
        - type: READ
          inMemory: true
          readers:
            - type: CONSTANT
              content: [ 0,1 ]
    - name: STATUS
      stages:
        - type: READ
          inMemory: true
          readers:
            - type: CONSTANT
              content: [ 'S0A' ]
    - name: CREATE_TIME
      stages:
        - type: READ
          readers:
            - type: SPEL
              content: >-
                #faker.expression("#{date.past '1','DAYS','yyyy-MM-dd HH:mm:ss'}")
    - name: UPDATE_TIME
      dependsOn:
        - CREATE_TIME
      stages:
        - type: SCRIPT
          scriptType: SPEL
          content: "#dataset"


# 数据输出配置
output:
  writers:
#    - type: CONSOLE
    - type: JDBC
      dataSourceId: 'tocc_parking'
      target: 'PARKING_BLACK_LIST'
      template: >-
        ID,PHONE,PLATE_NUMBER,PLATE_COLOR,VEHICLE_TYPE,STATUS,CREATE_TIME,UPDATE_TIME