---
phase: 20-pipeline-proof-docs-p1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/geospatial-overview.md
  - docs/geo-synthetic-v2-source.md
autonomous: true
requirements:
  - GEO-04
must_haves:
  truths:
    - "docs/geospatial-overview.md lists geo_synthetic / GEO_SYNTHETIC and distinguishes it from GEOJSON and ITERATOR+GEO per D-05"
    - "Maintainers see a minimal V2 YAML example adapted from the design spec per D-06"
    - "Output formats columns/wkt/geojson documented; SQL companion references existing V2_GEO_* only per D-07"
    - "Docs note V2 geo_synthetic preference for new work without V1 hard removal"
  artifacts:
    - path: docs/geospatial-overview.md
      provides: Primary maintainer landing page with geo_synthetic entry
      contains: "geo_synthetic"
    - path: docs/geo-synthetic-v2-source.md
      provides: Dedicated short reference when overview would exceed ~40 lines of YAML/modes
      min_lines: 30
  key_links:
    - from: docs/geospatial-overview.md
      to: docs/geo-synthetic-v2-source.md
      via: relative markdown link
      pattern: "geo-synthetic-v2-source.md"
    - from: docs/geospatial-overview.md
      to: docs/superpowers/specs/2026-07-30-geo-synthetic-v2-source-design.md
      via: design spec reference
      pattern: "geo-synthetic-v2-source-design"
---

<objective>
Deliver GEO-04 maintainer documentation: clarify `geo_synthetic` vs `geojson`, document output formats, and provide a minimal Template V2 YAML example (D-05, D-06, D-07).

Purpose: Operators and maintainers need a single landing page plus enough detail to author pipelines without reading Phase 19 source code.
Output: Updated `docs/geospatial-overview.md`; optional sibling `docs/geo-synthetic-v2-source.md` when content exceeds overview budget per D-06 discretion.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pipeline-proof-docs-p1/20-CONTEXT.md
@docs/superpowers/specs/2026-07-30-geo-synthetic-v2-source-design.md
@docs/geospatial-overview.md
@.planning/phases/19-v2-geo-synthetic-source/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend geospatial-overview.md source types and modules</name>
  <files>docs/geospatial-overview.md</files>
  <read_first>docs/geospatial-overview.md, docs/superpowers/specs/2026-07-30-geo-synthetic-v2-source-design.md</read_first>
  <action>
    Per D-05 update docs/geospatial-overview.md. Add v2.2 milestone note in Phase status or a short v2.2 row. In Modules table add GeoSyntheticSourceVO in data-generator-core and GeoSyntheticSourceFactory/GeoSyntheticRowSource in data-generator-calcite. In Template V2 source types table add row for geo_synthetic / GEO_SYNTHETIC: synthesize boundary/line/bbox/circle points without V1 iterator. Clarify split: geojson = read Feature/FeatureCollection files; geo_synthetic = synthesize points; ITERATOR+GEO = legacy V1 path — prefer geo_synthetic for new Template V2 work (docs preference only, no V1 removal). Add link to design spec. Update Verification section to mention TemplateV2RunnerGeoSyntheticSourceTests alongside existing geo tests. Do not document upload, polygons, console map, or new ST_* APIs.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Select-String -Path docs/geospatial-overview.md -Pattern 'geo_synthetic' -SimpleMatch).Count -ge 2"</automated>
  </verify>
  <done>Overview distinguishes three geo entry paths and lists Phase 19 modules per D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal YAML example (overview section or dedicated page)</name>
  <files>docs/geospatial-overview.md, docs/geo-synthetic-v2-source.md</files>
  <read_first>docs/superpowers/specs/2026-07-30-geo-synthetic-v2-source-design.md</read_first>
  <action>
    Per D-06 copy/adapt the design-spec YAML example (sources.pts.type geo_synthetic, four mode comments, bbox/center arrays, output block) using documented classpath fixture names (e.g. classpath:geo/district.geojson or the 南沙区 fixtures used in tests). If combined YAML + per-mode notes exceed ~40 lines in overview, create docs/geo-synthetic-v2-source.md with full example and mode validation table excerpt; link from overview with one-line summary. If content stays short, embed as a new section in overview only and omit creating geo-synthetic-v2-source.md (delete empty file if created then unused). Include minimal transform + sink snippet showing passthrough SQL select lon, lat from pts matching Phase 20 pipeline IT shape. Executor discretion per D-06 on inline vs dedicated file.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$o=(Select-String -Path docs/geospatial-overview.md -Pattern 'type:\s*geo_synthetic' -AllMatches).Matches.Count; if ($o -ge 1) { exit 0 } else { if (Test-Path docs/geo-synthetic-v2-source.md) { (Select-String -Path docs/geo-synthetic-v2-source.md -Pattern 'type:\s*geo_synthetic' -AllMatches).Matches.Count -ge 1 } else { exit 1 } }"</automated>
  </verify>
  <done>Minimal V2 template YAML visible to maintainers per D-06; dedicated page linked when used.</done>
</task>

<task type="auto">
  <name>Task 3: Document output formats and SQL companion</name>
  <files>docs/geospatial-overview.md, docs/geo-synthetic-v2-source.md</files>
  <read_first>docs/superpowers/specs/2026-07-30-geo-synthetic-v2-source-design.md</read_first>
  <action>
    Per D-07 document GeoSyntheticSourceOutputVO format enum: columns (default lon/lat columnNames), wkt (geometry column), geojson (geometry column). Place in overview Template V2 section or dedicated page (whichever holds the YAML example). Add SQL companion subsection: existing in-memory V2_GEO_* functions from overview Built-in SQL functions list may filter/join synthetic output — documentation only; explicitly state no new ST_* surface this milestone. Cross-link geospatial-phase1-usage.md only if helpful; do not duplicate large SpEL/V1 iterator guides.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f=@('docs/geospatial-overview.md'); if (Test-Path docs/geo-synthetic-v2-source.md) { $f+='docs/geo-synthetic-v2-source.md' }; $c=0; foreach ($p in $f) { $c+=(Select-String -Path $p -Pattern 'columns|wkt|geojson' -AllMatches).Matches.Count }; if ($c -ge 3) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <done>Output formats and SQL companion documented per D-07; GEO-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator templates | YAML examples use classpath fixtures only; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Information Disclosure | example YAML paths | low | accept | Use public classpath fixture names only |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
Read geospatial-overview.md end-to-end: geo_synthetic row present, split from geojson clear, YAML example reachable, output formats listed.
</verification>

<success_criteria>
- GEO-04 checklist: split documented, output formats, minimal V2 example, SQL companion = docs only
- D-05, D-06, D-07 citations honored in doc structure
- No scope creep (upload, polygons, map UI, new ST_*)
</success_criteria>

<output>
Create `.planning/phases/20-pipeline-proof-docs-p1/20-02-SUMMARY.md` when done
</output>
