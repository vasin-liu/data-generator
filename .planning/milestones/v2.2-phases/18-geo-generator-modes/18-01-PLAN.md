---
phase: 18-geo-generator-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationMode.java
  - data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationRequest.java
  - data-generator-geo/src/test/java/org/gensokyo/data/geo/GeoGenerationRequestValidationTests.java
autonomous: true
requirements:
  - GEO-02
must_haves:
  truths:
    - "GeoGenerationMode exposes BBOX and CIRCLE alongside existing boundary/line modes"
    - "GeoGenerationRequest carries bbox and circle geometry fields mappable to Phase 19 VO"
    - "Illegal BBOX/CIRCLE config fails in validate() with IllegalArgumentException naming the field"
    - "seed is always taken from request.getSeed() with 0 valid (no wall-clock fallback)"
  artifacts:
    - path: data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationMode.java
      provides: BBOX and CIRCLE enum constants
      contains: "BBOX"
    - path: data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationRequest.java
      provides: bbox/circle fields and mode-specific validate branches
      contains: "validateBbox"
    - path: data-generator-geo/src/test/java/org/gensokyo/data/geo/GeoGenerationRequestValidationTests.java
      provides: automated proof of validation failures and count/seed rules
      min_lines: 40
  key_links:
    - from: data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationRequest.java
      to: data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationMode.java
      via: validate() switch on mode
      pattern: "case BBOX|case CIRCLE"
---

<objective>
Extend the geo generator contracts so BBOX and CIRCLE modes are first-class configuration types with strict validation before any sampling runs.

Purpose: Phase 19 V2 source maps YAML directly to GeoGenerationRequest; validation must fail fast with clear field names per D-11.
Output: Extended enum, request fields, validation tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-geo-generator-modes/18-CONTEXT.md
@.planning/ROADMAP.md
@docs/superpowers/specs/2026-07-30-geo-synthetic-v2-source-design.md
@data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationMode.java
@data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationRequest.java
@data-generator-geo/src/main/java/org/gensokyo/data/geo/generate/BoundaryPointGenerator.java
@.cursor/rules/java-copyright-class-javadoc.mdc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GeoGenerationMode with BBOX and CIRCLE</name>
  <files>data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationMode.java</files>
  <behavior>
    - Enum contains BOUNDARY_POINTS, LINE_SAMPLE, BBOX, CIRCLE (existing order preserved, new modes appended)
    - No changes to boundary/line semantics
  </behavior>
  <action>
    Add BBOX and CIRCLE constants to GeoGenerationMode per D-09 foundation. Update class Javadoc to mention all four modes (not only GEO iterator). Keep copyright block and file layout per java-copyright-class-javadoc rule. Do not touch GeoSyntheticGenerator yet.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-geo -Dtest=GeoSyntheticGeneratorTests test</automated>
  </verify>
  <done>GeoGenerationMode compiles with four values; existing module tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add bbox and circle fields to GeoGenerationRequest</name>
  <files>data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationRequest.java</files>
  <action>
    Add flat WGS84 fields (readable for Phase 19 VO mapping per discretion): bboxMinLon, bboxMinLat, bboxMaxLon, bboxMaxLat, centerLon, centerLat, radiusMeters — each with getter/setter and Javadoc. seed remains required long on request; document that callers always pass explicit seed and 0 is valid per D-06. Extend validate() switch with BBOX and CIRCLE branches calling private validateBbox() and validateCircle(). Do not add sampling logic in this task.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-geo -DskipTests compile</automated>
  </verify>
  <done>Request exposes all geometry params; validate() dispatches to new mode branches (may throw once rules added in Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement BBOX and CIRCLE validation rules</name>
  <files>data-generator-geo/src/main/java/org/gensokyo/data/geo/GeoGenerationRequest.java, data-generator-geo/src/test/java/org/gensokyo/data/geo/GeoGenerationRequestValidationTests.java</files>
  <behavior>
    - BBOX: count &gt; 0; minDistanceMeters &gt;= 0; minLon &lt; maxLon and minLat &lt; maxLat or IllegalArgumentException naming bbox fields (D-03)
    - BBOX degenerate: minLon &gt;= maxLon or minLat &gt;= maxLat fails validate()
    - CIRCLE: count &gt; 0; radiusMeters &gt; 0; center lon in [-180,180] lat in [-90,90] finite (D-05)
    - CIRCLE: radiusMeters &lt;= 0 fails with field name in message
    - minDistanceMeters &lt; 0 on BBOX fails like boundary mode
    - Existing BOUNDARY_POINTS and LINE_SAMPLE validation unchanged
  </behavior>
  <action>
    Implement validateBbox() and validateCircle() per D-01 context (no file paths required for bbox/circle), D-03, D-05, D-11. Use IllegalArgumentException messages that include the offending field name (e.g. bboxMinLon, radiusMeters, centerLat). Create GeoGenerationRequestValidationTests with @Test methods for each illegal config and one happy-path BBOX/CIRCLE request that validate() accepts without sampling. Follow repository copyright/Javadoc rules on the new test class.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-geo -Dtest=GeoGenerationRequestValidationTests test</automated>
  </verify>
  <done>All validation behaviors proven; GeoSyntheticGeneratorTests still green (modes not wired yet).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Request config → validate() | Untrusted numeric bounds from future YAML mapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | GeoGenerationRequest.validate | low | accept | Pure local validation; no external IO in Phase 18 |
| T-18-02 | Denial of Service | count cap MAX_COUNT | low | mitigate | Keep existing 1_000_000 cap in validate() |
</threat_model>

<verification>
Run full geo module tests after all three plans complete. Plan 01 isolated: GeoGenerationRequestValidationTests + GeoSyntheticGeneratorTests green.
</verification>

<success_criteria>
- GeoGenerationMode has BBOX and CIRCLE
- GeoGenerationRequest validates bbox/circle configs per D-01, D-03, D-05, D-06, D-11
- Automated validation tests exist and pass
</success_criteria>

<output>
Create `.planning/phases/18-geo-generator-modes/18-01-SUMMARY.md` when done
</output>
