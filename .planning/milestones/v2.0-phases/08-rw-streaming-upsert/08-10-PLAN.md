---
id: 08-10
phase: 08
title: Calcite Unit Tests for Streaming Pipelines & SQL Builder
wave: 4
depends_on: [08-04, 08-05]
requirements: [RW-01, RW-02, RW-03]
autonomous: true
files_modified:
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/StreamingPipelineTests.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineTests.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/TemplateV2RunnerTests.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java
must_haves:
  truths:
    - StreamingPipelineTests covers CSV and JSON source batch streaming (D-04)
    - ChunkedPipelineTests covers CSV/JSON ROW_LOCAL per-chunk SQL
    - JdbcSinkSqlBuilderTests full upsertKeys matrix for PG and MySQL (D-13)
    - Multi-source and cross-chunk shapes still rejected (D-04)
  artifacts:
    - Extended StreamingPipelineTests
    - Extended ChunkedPipelineTests
    - Extended TemplateV2RunnerTests chunked paths
    - Extended JdbcSinkSqlBuilderTests
---

# Plan 08-10: Calcite Unit Tests for Streaming Pipelines & SQL Builder

## Objective

Expand calcite module unit test coverage for CSV/JSON streaming pipelines and upsert SQL builder dialect matrix (RW-01–RW-03, D-04).

**Output:** Green unit tests runnable without Docker in default `mvn test` slice.

## Context

@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@.planning/phases/08-rw-streaming-upsert/08-PATTERNS.md
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/StreamingPipelineTests.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineTests.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java

## Tasks

<task>
  <read_first>
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/StreamingPipelineTests.java (streamsQuerySourceToJdbcSinkInBatches)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/source/ChunkedCsvRowSourceTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/source/ChunkedJsonRowSourceTests.java
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-04
  </read_first>
  <action>
    Add `StreamingPipelineTests`:
    - `streamsCsvSourceInBatches` — inject ChunkedCsvRowSource via test registry, assert chunksProcessed and peakRowsInMemory <= chunkSize
    - `streamsNdjsonSourceInBatches` — JSON NDJSON equivalent
    - `rejectsMultipleCsvSourcesInStreamingMode` — preserve v1 scope
    Add `ChunkedPipelineTests`:
    - `chunkedCsvSourceAppliesSqlPerChunk` — transform modifies column per chunk independently (proves no cross-chunk state)
    - `chunkedJsonArraySourceWritesAllRows`
    Use injected parsers/fixtures from 08-01/08-02 tests — avoid 10 MB fixtures here (OOM in 08-09).
  </action>
  <acceptance_criteria>
    - At least 4 new test methods across StreamingPipelineTests and ChunkedPipelineTests
    - `peakRowsInMemory <= 100` (or configured chunk size) for streaming CSV test with 500 rows / chunk 100
    - Multi-source rejection test throws IllegalArgumentException
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=StreamingPipelineTests,ChunkedPipelineTests -q` exits 0
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/TemplateV2RunnerTests.java (readsCsvSource*, readsJsonSource*)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-13, D-14, D-21
  </read_first>
  <action>
    Complete `JdbcSinkSqlBuilderTests` matrix:
    - Composite upsertKeys [id, tenant_id] in PG ON CONFLICT clause
    - MySQL multi-column upsert update clause
    - Legacy conflictColumns still works for PG (backward compat)
    - Unsupported dialect clickhouse throws or returns base insert only per Phase 8 scope
    Extend `TemplateV2RunnerTests`:
    - `readsCsvSourceChunkedModeViaInjectedParser` — policy CHUNKED returns empty rows() on result, metrics show chunks
    - Confirm IN_MEMORY tests unchanged (D-21)
  </action>
  <acceptance_criteria>
    - JdbcSinkSqlBuilderTests has ≥6 upsert-related test methods passing
    - TemplateV2RunnerTests chunked CSV test asserts `result.getRows().isEmpty()` for streaming/chunked pipeline result shape
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcSinkSqlBuilderTests,TemplateV2RunnerTests -q` exits 0
  </acceptance_criteria>
</task>

## Verification criteria

- Unit tests cover CSV/JSON streaming without Docker
- Upsert SQL builder dialect matrix complete for PG/MySQL
- IN_MEMORY regression tests still pass

## Artifacts this phase produces

- Extended `StreamingPipelineTests`
- Extended `ChunkedPipelineTests`
- Extended `JdbcSinkSqlBuilderTests`
- Extended `TemplateV2RunnerTests`
