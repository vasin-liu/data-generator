---
id: 08-08
phase: 08
title: V2 Scenario YAML Fixtures & Scenario IT Harness
wave: 4
depends_on: [08-04, 08-05, 08-06, 08-07]
requirements: [RW-01, RW-02, RW-03, RW-04]
autonomous: true
files_modified:
  - data-generator-service/src/main/resources/template/v2-scenarios/scenario-f-streaming-csv.yaml
  - data-generator-service/src/main/resources/template/v2-scenarios/scenario-f-streaming-ndjson.yaml
  - data-generator-service/src/main/resources/template/v2-scenarios/scenario-g-upsert-pg.yaml
  - data-generator-service/src/main/resources/template/v2-scenarios/scenario-g-upsert-mysql.yaml
  - data-generator-service/src/main/resources/template/v2-scenarios/fixtures/**
  - data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java
  - docs/template-v2-scenario-template-catalog.md
must_haves:
  truths:
    - New scenarios follow scenario-e-streaming-jdbc.yaml pattern (D-07)
    - V2ScenarioTemplateIT validates and runs all Phase 8 scenarios green
    - Scenario catalog documents GF-F/GF-G entries
    - Fixtures support 10 MB OOM bar generation in 08-09 (D-06)
    - Upsert scenarios use H2 MODE=PostgreSQL|MySQL or @EnabledIf skip when dialect unavailable (W-01)
    - scenario-e-partial-sink asserts Phase 8 errorSample rowsFailed and actionable sink errors — mandatory not optional (W-08)
  artifacts:
    - scenario-f-streaming-csv.yaml
    - scenario-f-streaming-ndjson.yaml
    - scenario-g-upsert-pg.yaml
    - scenario-g-upsert-mysql.yaml
    - Updated V2ScenarioTemplateIT.scenarioResources and assertScenarioOutcome
    - Large CSV/NDJSON fixtures under v2-scenarios/fixtures/
---

# Plan 08-08: V2 Scenario YAML Fixtures & Scenario IT Harness

## Objective

Extend the V2 scenario catalog and `V2ScenarioTemplateIT` harness for CSV/JSON streaming and JDBC upsert paths (D-07, RW-01–RW-04).

**Output:** Four new scenario YAMLs; IT assertions for execution mode, row counts, and upsert metrics.

## Context

@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@.planning/phases/08-rw-streaming-upsert/08-PATTERNS.md
@data-generator-service/src/main/resources/template/v2-scenarios/scenario-e-streaming-jdbc.yaml
@data-generator-service/src/main/resources/template/v2-scenarios/scenario-c-csv-export.yaml
@data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java
@docs/template-v2-scenario-template-catalog.md

## Tasks

<task>
  <read_first>
    - data-generator-service/src/main/resources/template/v2-scenarios/scenario-e-streaming-jdbc.yaml
    - data-generator-service/src/main/resources/template/v2-scenarios/scenario-c-csv-export.yaml
    - data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java (prepareScenarioFixtures, assertScenarioOutcome)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-07, D-02, D-08, D-12
  </read_first>
  <action>
    Create scenario YAMLs per D-07:
    1. `scenario-f-streaming-csv.yaml` — mode CHUNKED or STREAMING, CSV source → CSV or console sink, explicit sourceChunkSize/sinkBatchSize (small values for fast IT, e.g. 40 like scenario-e)
    2. `scenario-f-streaming-ndjson.yaml` — mode STREAMING, NDJSON source, SQL transform, sink NDJSON or CSV
    3. `scenario-g-upsert-pg.yaml` — CHUNKED, inline or H2/Testcontainers PG JDBC sink with upsert:true upsertKeys:[id]
    4. `scenario-g-upsert-mysql.yaml` — same for MySQL (may use Testcontainers JDBC URL in prepareScenarioFixtures)
    **H2 upsert dialect (W-01):** when scenarios run against embedded H2 (not Testcontainers), configure JDBC URL with `MODE=PostgreSQL` for PG upsert scenario and `MODE=MySQL` for MySQL upsert scenario so `ON CONFLICT` / `ON DUPLICATE KEY` SQL paths execute. Alternatively gate PG/MySQL upsert scenario IT methods with `@EnabledIf` when only generic H2 is available — document skip reason in test Javadoc.
    Add fixtures under `template/v2-scenarios/fixtures/`:
    - `streaming-orders.csv` (moderate size for IT)
    - `streaming-events.ndjson`
    - `large-streaming-orders.csv` generator hook for 08-09 (≥10 MB — generate in prepareScenarioFixtures if repo size concern)
    Register scenarios in `docs/template-v2-scenario-template-catalog.md` as GF-F / GF-G.
  </action>
  <acceptance_criteria>
    - Four YAML files exist and load via `TemplateKit` without parse errors
    - Each scenario name unique and matches `template.getName()` in IT switch
    - Catalog markdown lists all four with mode and requirement trace RW-01..04
    - Fixture paths referenced in YAML resolve under classpath or temp dir in IT
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java (full file — scenarioResources, assertScenarioOutcome switch)
    - data-generator-service/src/main/resources/template/v2-scenarios/scenario-e-partial-sink.yaml (partial error pattern)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-07, D-15, D-16
  </read_first>
  <action>
    Add new scenario resource names to `scenarioResources()` Stream.
    Extend `prepareScenarioFixtures()` for PG/MySQL upsert table DDL + seed rows; materialize large CSV path for streaming scenarios.
    For upsert scenarios in `prepareScenarioFixtures()`: set H2 datasource JDBC URLs to `jdbc:h2:...;MODE=PostgreSQL` (PG scenario) and `jdbc:h2:...;MODE=MySQL` (MySQL scenario) when not using Testcontainers — required for dialect upsert SQL in IT (W-01).
    Extend `assertScenarioOutcome()` switch cases:
    - `scenario-f-streaming-csv`: assert executionMode CHUNKED or STREAMING, rowsWritten expected count, peakRowsInMemory > 0
    - `scenario-f-streaming-ndjson`: assert STREAMING, rowsWritten count
    - `scenario-g-upsert-pg`: run template twice via helper; assert row count unchanged second run; rowsUpserted > 0 in report metrics (D-15)
    - `scenario-g-upsert-mysql`: same idempotency assertion
    **Mandatory partial sink failure assertions (W-08):** extend existing `scenario-e-partial-sink` case (or add Phase 8 partial-sink variant) to assert Phase 8 error fields — `errorSample` contains sink writer name and actionable message; `rowsFailed > 0` on failed sink; `rowsOk` on successful sink unchanged. Do not leave partial failure as optional add-on; update `assertScenarioOutcome` switch is required for RW-04.
  </action>
  <acceptance_criteria>
    - `.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=V2ScenarioTemplateIT -q` exits 0
    - Upsert scenarios assert second run produces zero duplicate rows (row count stable)
    - Upsert scenarios use H2 MODE=PostgreSQL|MySQL or `@EnabledIf` skip with documented reason (W-01)
    - `scenario-e-partial-sink` (or Phase 8 equivalent) asserts errorSample with sink identifier and rowsFailed > 0 (W-08, RW-04)
    - Streaming CSV scenario asserts `getMetrics().getExecutionMode()` matches YAML mode
    - `scenario-e-streaming-jdbc` case still passes (regression)
  </acceptance_criteria>
</task>

## Verification criteria

- Scenario harness extended, not parallel new IT style (D-07)
- All Phase 8 scenario YAMLs green in V2ScenarioTemplateIT
- Upsert idempotency asserted in harness (D-15)
- H2 upsert scenarios use MODE=PostgreSQL|MySQL or EnabledIf skip (W-01)
- scenario-e-partial-sink asserts Phase 8 errorSample and rowsFailed (W-08, mandatory)

## Artifacts this phase produces

- `scenario-f-streaming-csv.yaml`
- `scenario-f-streaming-ndjson.yaml`
- `scenario-g-upsert-pg.yaml`
- `scenario-g-upsert-mysql.yaml`
- Fixture files under `template/v2-scenarios/fixtures/`
- Updated `V2ScenarioTemplateIT.java`
- Updated `docs/template-v2-scenario-template-catalog.md`
