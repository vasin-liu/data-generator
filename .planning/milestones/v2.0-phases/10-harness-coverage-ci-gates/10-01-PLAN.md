---
phase: 10-harness-coverage-ci-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/test-matrix.yaml
autonomous: true
requirements:
  - TEST-07
must_haves:
  truths:
    - test-matrix.yaml contains two independent P0 streaming rows for CSV and JSON (D-01, D-09)
    - test-matrix.yaml contains one P0 JDBC upsert row linking Phase 8 PG/MySQL evidence (D-02, D-10)
    - test-matrix.yaml contains five independent P0 dialect rows for DM, Kingbase, HighGo, PG, CK (D-03, D-11)
    - Each new row uses tier P0, owner_module data-generator-calcite, and linked_tests from Phase 8/9 classes only (D-12)
    - Dameng row covers via MERGE unit tests; ChunkedPipelineDamengUpsertIT is notes-only, never linked (D-05, D-08)
    - Kingbase and HighGo rows cover via PG-proxy IT plus JdbcSinkSqlBuilderTests dialect-key units (D-06)
    - PostgreSQL and ClickHouse rows cover via Testcontainers ITs per Phase 9 evidence bars (D-07)
    - No Phase 6–7 datasource catalog/governance rows added this phase (D-13)
  artifacts:
    - .planning/test-matrix.yaml with 8 new P0 rows (15 total including existing 7)
  key_links:
    - linked_tests simple class names must resolve under data-generator-calcite/src/test
    - Multiple linked_tests on one row require all classes green for computed covered status
---

<objective>
Register eight new P0 matrix rows for Phase 8 streaming/upsert and Phase 9 dialect capabilities in `.planning/test-matrix.yaml` (TEST-07, D-01–D-03, D-05–D-12).

Purpose: The harness gate can only enforce regressions on capabilities that exist as tier P0 rows with linked Maven evidence.
Output: Expanded test-matrix.yaml with strict TEST-07 row set; no harness script or workflow changes (D-14 deferred to plan 02 verification).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md
@.planning/phases/10-harness-coverage-ci-gates/10-PATTERNS.md
@.planning/test-matrix.yaml
@.planning/ROADMAP.md
@scripts/verify-phase8-uat-rw-streaming-upsert.ps1
@scripts/verify-phase9-uat-jdbc-dialect.ps1
</context>

<artifacts_this_phase_produces>
- `.planning/test-matrix.yaml` — 8 new P0 rows (2 streaming + 1 upsert + 5 dialects); 15 P0 rows total with legacy 7
- `target/test-matrix-summary.json` — produced by plan 02 after harness run (not edited in this plan)
- `docs/test-harness.md` — updated in plan 03
- `AGENTS.md` — updated in plan 03
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add P0 streaming CSV and JSON matrix rows</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md (D-01, D-09, D-12)
    - .planning/phases/10-harness-coverage-ci-gates/10-PATTERNS.md (v2-streaming-csv/json row table)
    - .planning/test-matrix.yaml (existing P0 row shape: transform-json, udf-script)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/CsvJsonStreamingSinkTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/StreamingPipelineTests.java
  </read_first>
  <action>
    Append two independent P0 rows per D-09 (not one combined streaming row):
    1. id v2-streaming-csv — capability v2-streaming-csv, adapter csv, test_types [unit, integration], owner_module data-generator-calcite, status covered, tier P0, linked_tests [CsvJsonStreamingSinkTests, StreamingPipelineTests], notes cite RW-01/TEST-07 and per-chunk CSV sink + streaming pipeline evidence.
    2. id v2-streaming-json — capability v2-streaming-json, adapter json, same module/tier/status, linked_tests [CsvJsonStreamingSinkTests, StreamingPipelineTests], notes cite RW-01/TEST-07 and NDJSON/array streaming evidence.
    Do not link CsvJsonStreamingOomIT (separate low-heap profile; out of scope D-12). Do not add Playwright linked_tests (D-12). Match YAML indentation and field order of existing P0 rows.
  </action>
  <verify>
    <automated>rg -n "v2-streaming-csv|v2-streaming-json" .planning/test-matrix.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Exactly two new rows with tier P0 for CSV and JSON streaming (D-01, D-09)
    - Both rows list CsvJsonStreamingSinkTests and StreamingPipelineTests in linked_tests (D-12)
    - CsvJsonStreamingOomIT absent from linked_tests
    - owner_module is data-generator-calcite on both rows
  </acceptance_criteria>
  <done>Streaming CSV and JSON are registered as separate merge-blocking P0 capabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Add P0 JDBC upsert matrix row (PG + MySQL)</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md (D-02, D-10, D-12)
    - .planning/phases/10-harness-coverage-ci-gates/10-PATTERNS.md (v2-jdbc-upsert-pg-mysql row)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineMySqlUpsertTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcUpsertSmokeTests.java
  </read_first>
  <action>
    Append one P0 row per D-10:
    - id v2-jdbc-upsert-pg-mysql — capability v2-jdbc-upsert, adapter jdbc, test_types [integration, unit], owner_module data-generator-calcite, status covered, tier P0.
    - linked_tests: ChunkedPipelinePostgresUpsertTests, ChunkedPipelineMySqlUpsertTests, JdbcUpsertSmokeTests (multi-class pattern like udf-script row).
    - notes: cite RW-03/TEST-07, Testcontainers idempotency for PG and MySQL, embedded-first per docs/testing-embedded-components.md.
    Do not fold Phase 9 dialect upsert ITs into this row (D-11 separates dialect traceability).
  </action>
  <verify>
    <automated>rg -n "v2-jdbc-upsert-pg-mysql" .planning/test-matrix.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Single P0 upsert row exists with tier P0 (D-02, D-10)
    - linked_tests includes all three Phase 8 upsert classes (D-12)
    - Row notes reference RW-03 and TEST-07
  </acceptance_criteria>
  <done>JDBC upsert (PG/MySQL path) is a merge-blocking P0 capability with linked Phase 8 tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add five P0 dialect matrix rows with evidence-bar linked_tests</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md (D-03, D-05–D-08, D-11, D-12, D-13)
    - .planning/phases/10-harness-coverage-ci-gates/10-PATTERNS.md (dialect row table, D-05–D-08)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineKingbaseDialectTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/ClickHouseInsertBulkWriterIntegrationTests.java
  </read_first>
  <action>
    Append five independent P0 rows per D-11 (one per engine), all owner_module data-generator-calcite, tier P0, status covered:
    1. v2-dialect-dameng — linked_tests [JdbcSinkSqlBuilderTests] only; notes cite D-05 MERGE unit (buildsDamengMergeInto), RW-05/TEST-07; mention optional ChunkedPipelineDamengUpsertIT gated by -Ddm.it=true in notes only — never in linked_tests (D-08).
    2. v2-dialect-kingbase — linked_tests [ChunkedPipelineKingbaseDialectTests, JdbcSinkSqlBuilderTests]; notes cite D-06 PG-proxy IT + buildsKingbaseUsesOnConflictPath; no licensed Kingbase image required.
    3. v2-dialect-highgo — linked_tests [ChunkedPipelineKingbaseDialectTests, JdbcSinkSqlBuilderTests]; notes cite D-06 PG-proxy IT + buildsHighgoUsesOnConflictPath.
    4. v2-dialect-postgres — linked_tests [ChunkedPipelinePostgresUpsertTests]; notes cite D-07 Testcontainers upsert IT.
    5. v2-dialect-clickhouse — linked_tests [ClickHouseInsertBulkWriterIntegrationTests, JdbcSinkSqlBuilderTests]; notes cite D-07 insert-bulk IT plus clickhouseUpsertIsUnsupported unit reject.
    Do not add Phase 6–7 datasource catalog/governance rows (D-13). Do not retire or rewrite unrelated pending reader/writer rows (D-16). Optional P1 companion rows are discretion — skip unless required for a failing P0 link.
    After append, confirm P0 count in file is 15 (7 existing + 8 new) via manual count or rg tier P0.
  </action>
  <verify>
    <automated>rg -c "tier: P0" .planning/test-matrix.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Five dialect P0 rows exist with ids v2-dialect-dameng, v2-dialect-kingbase, v2-dialect-highgo, v2-dialect-postgres, v2-dialect-clickhouse (D-03, D-11)
    - Dameng linked_tests excludes ChunkedPipelineDamengUpsertIT (D-08)
    - Kingbase/Highgo rows use PG-proxy IT pattern per D-06
    - PostgreSQL and ClickHouse rows match D-07 evidence bars
    - Total tier P0 count is 15
    - No Phase 6–7 datasource rows added (D-13)
  </acceptance_criteria>
  <done>All five target dialects are merge-blocking P0 rows with Phase 9 evidence linked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix author → CI gate | YAML linked_tests defines which tests prove capability coverage |
| Matrix notes → operators | Free-form notes must not contain credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | test-matrix.yaml linked_tests | high | mitigate | Use only verified Phase 8/9 class simple names from tree; plan 02 runs full harness slice |
| T-10-02 | Information disclosure | matrix notes field | medium | mitigate | Notes cite requirement ids and evidence bars only — no JDBC URLs, passwords, or internal hostnames |
| T-10-03 | Elevation of privilege | P0 tier assignment | medium | mitigate | Only TEST-07 locked rows get tier P0; do not promote unrelated pending rows (D-13, D-16) |
</threat_model>

<verification>
- rg confirms all eight new row ids present in test-matrix.yaml
- P0 tier count equals 15 before harness run
- No ChunkedPipelineDamengUpsertIT in any linked_tests list
</verification>

<success_criteria>
- TEST-07: Feature matrix adds P0 rows for streaming CSV/JSON, JDBC upsert, and five dialects with embedded-first linked tests
- D-01 through D-12 implemented in matrix YAML
- Ready for plan 02 harness verification
</success_criteria>

<output>
Create `.planning/phases/10-harness-coverage-ci-gates/10-01-SUMMARY.md` when done
</output>
