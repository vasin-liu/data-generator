---
phase: 7.1
slug: close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-28
backfilled: 2026-07-28 (Phase 13, DIAL-02)
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Backfill note (Phase 13, DIAL-02, 2026-07-28):** This VALIDATION.md did not exist during Phase 07.1 execution. Phase 07.1 shipped and passed goal verification on 2026-07-24 (`07.1-VERIFICATION.md`, status: passed, 7/7 truths). This document is built entirely from the existing `07.1-VERIFICATION.md` report and the `07.1-01-SUMMARY.md`..`07.1-03-SUMMARY.md` Verification/coverage sections — no new tests were written and no DS-03 execute-path routing implementation was reopened (D-11). Phase 07.1's evidence base is small and concentrated on execute-path routing (one dedicated integration test plus several code-trace/compile checks); the map below is honest about that concentration rather than padded to look broad.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 5 (Maven Surefire) — `data-generator-service`; plain `@SpringBootTest`, no Playwright/E2E in this phase |
| **Config file** | `data-generator-service/src/test/resources/application-phase7-test.yaml` (reused from Phase 7, `JdbcSnapshotExecutePathIT`) |
| **Quick run command** | `.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q` (07.1-01/07.1-02 — compile-only proof for resolver/adapter code-trace tasks) |
| **Full suite command** | `.\mvnw-jdk25.ps1 -pl "data-generator-service" -am test -Dtest=JdbcSnapshotExecutePathIT -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true` (07.1-03-SUMMARY.md: `Tests run: 1, Failures: 0`) |
| **E2E command** | None — this phase records only one Maven slice (the mid-flight IT); no Playwright/UAT script was authored or required for the DS-03 execute-path gap closure |
| **Estimated runtime** | Compile-only checks: a few seconds; full reactor `-am` compiles observed 12–64 min in 07.1-01/07.1-02 (dominated by `data-generator-console-web` frontend-maven-plugin rebuild, not test execution); `JdbcSnapshotExecutePathIT` itself: single test, sub-second assertion time within a ~28 min plan session |

---

## Sampling Rate

- **After every task commit:** Run the task's recorded compile/grep/IT command (07.1-01/07.1-02: compile + code trace; 07.1-03: `JdbcSnapshotExecutePathIT`)
- **After every plan wave:** Re-run `JdbcSnapshotExecutePathIT` once it exists (07.1-03); prior waves (07.1-01, 07.1-02) have no runtime IT of their own — compile is the phase-appropriate gate for resolver-return and adapter-trace changes
- **Before `/gsd-verify-work`:** `JdbcSnapshotExecutePathIT` green + decision-coverage check (16/16 per `07.1-VERIFICATION.md`)
- **Max feedback latency:** 120 seconds (compile) / single-test IT run

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|----------|-----------|-------------------|-------------|--------|
| 07.1-01-01 | 01 | 1 | DS-03 | Managed JDBC resolve returns `connectionName()` (`snap:{instanceId}:{name}`) for both source and sink, after `registerIfAbsent` — no fallback to logical `dataSourceId` | code-trace + compile | `.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q` plus `grep 'jdbc.connectionName()\|resolveManagedDataSourceId' DefaultRuntimeJdbcEndpointResolver.java` (07.1-01-SUMMARY.md coverage D1, both `status: pass`) | ✅ | ✅ green |
| 07.1-01-02 | 01 | 1 | DS-03 | Class Javadoc documents `DefaultRuntimeJdbcEndpointResolver` as V2 execute-path authority vs `JdbcCatalogResolver` as catalog-side resolver, no implied delegation | compile + doc review | `.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q` (07.1-01-SUMMARY.md coverage D3, `status: pass`; wording clarity itself flagged `human_judgment: true`) | ✅ | ✅ green (compile) — wording clarity is a manual judgment, see Manual-Only Verifications |
| 07.1-02-01 | 02 | 1 | DS-03 | `QuerySourceFactory` / `PostGisQuerySourceFactory` push the resolver's `resolveSourceDataSourceId` return value onto their VOs, not the original logical `dataSourceId` | code-trace + compile | `.\mvnw-jdk25.ps1 -pl "data-generator-calcite,data-generator-service" -am -DskipTests compile -q` (07.1-02-SUMMARY.md coverage D1, `status: pass`; verify-only plan, zero code changes) | ✅ | ✅ green |
| 07.1-02-02 | 02 | 1 | DS-03 | `JdbcRowSinkAdapter.writeBatch` pushes `resolveSinkDataSourceId(writer)` return value to `DynamicDataSourceContextHolder.push`, not the writer's logical field | code-trace + compile | Same compile command as 07.1-02-01 plus `grep 'setDataSourceId\|DynamicDataSourceContextHolder' JdbcRowSinkAdapter.java` (07.1-02-SUMMARY.md coverage D2, `status: pass`) | ✅ | ✅ green |
| 07.1-03-01 | 03 | 1 | DS-03 | `JdbcSnapshotExecutePathIT` asserts `resolveSourceDataSourceId`/`resolveSinkDataSourceId` return `snap:{instanceId}:` keys before and after mid-flight `persistAndReload` under bound `WorkflowRunContext` | IT | `.\mvnw-jdk25.ps1 -pl "data-generator-service" -am test -Dtest=JdbcSnapshotExecutePathIT -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true` (07.1-03-SUMMARY.md: `Tests run: 1, Failures: 0`; 07.1-VERIFICATION.md Behavioral Spot-Checks: same result) | ✅ | ✅ green |
| 07.1-03-02 | 03 | 1 | DS-03 | Kafka/ES snapshot paths remain unchanged this phase (ROADMAP SC3); post-clear resolve returns the live catalog name, not a `snap:` key | scope-check + IT (same run) | `git show --stat 12f36f0` (only `JdbcSnapshotExecutePathIT.java` added — no kafka/elasticsearch source diffs; 07.1-03-SUMMARY.md coverage D2, `status: pass`); post-clear assertion is part of the same `JdbcSnapshotExecutePathIT` test run above | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Only one dedicated behavioral test file exists for this phase (`JdbcSnapshotExecutePathIT`, 1 test). The 07.1-01/07.1-02 rows are transcribed from code-trace + compile evidence recorded in their SUMMARY `coverage` blocks — this phase never authored additional JUnit test classes for the resolver/adapter tasks because those tasks were fix + verify-only in nature (D-11: transcribe only, no new tests written to pad the map).*

---

## Wave 0 Requirements

Phase 07.1 reused all Phase 7 test infrastructure — no greenfield harness install:

- [x] `application-phase7-test.yaml` — reused IT profile (Phase 7)
- [x] `HotReloadTests` queue/markRunning/captureConnectionSnapshot/bind/persistAndReload pattern — reused as the structural template for `JdbcSnapshotExecutePathIT` (Phase 7)
- [x] `WorkflowRunContext`, `ConnectionCatalog`, `DynamicDataSourceContextHolder` — existing runtime collaborators, unchanged interfaces (Phase 7)

No Wave 2+ new infrastructure — Phase 07.1 is a single-wave gap-closure phase (3 plans, no wave subdivision beyond "1").

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Class Javadoc wording clarity for `DefaultRuntimeJdbcEndpointResolver` (execute-path vs catalog-side ownership) | DS-03 | `07.1-01-SUMMARY.md` coverage D3 explicitly flags `human_judgment: true` — "wording clarity/unambiguity for a future planner is a qualitative judgment call" even though compile confirms syntactic validity | Read the class Javadoc and confirm a future maintainer can distinguish `DefaultRuntimeJdbcEndpointResolver` (V2 execute-path) from `JdbcCatalogResolver` (datasource-module catalog-side) without ambiguity |
| Dual DB-A/DB-B observable JDBC URL divergence across mid-flight reload | DS-03 | `07.1-03-SUMMARY.md` coverage D3: explicitly deferred per CONTEXT.md; routing-key (`snap:`) retention is the accepted proof bar for this phase, not dual-DB URL divergence — `human_judgment: true`, no automated verification | Deferred; not required for phase close. A future phase could add a second DB fixture and assert distinct live JDBC URLs pre/post reload if stronger proof is ever needed |

*07.1-VERIFICATION.md records "Human Verification: N/A — Infrastructure/foundation phase... Behavior-dependent mid-flight invariant exercised by green named IT." The two items above are judgment-call footnotes from the SUMMARY `coverage` blocks, carried forward rather than resolved (D-11).*

---

## Validation Sign-Off

- [x] All tasks have an `<automated>`-equivalent verify (compile/grep/IT) or Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 fully reuses Phase 7 infrastructure — no gap
- [x] No watch-mode flags
- [x] Feedback latency < 120s (compile) / single-test IT run
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** Backfilled 2026-07-28 (Phase 13, DIAL-02) from `07.1-VERIFICATION.md` (status: passed, 2026-07-24) and `07.1-01-SUMMARY.md`..`07.1-03-SUMMARY.md` coverage/Verification sections — retrospective, not pre-execution. This is a new file; Phase 07.1 had no VALIDATION.md before this backfill.
