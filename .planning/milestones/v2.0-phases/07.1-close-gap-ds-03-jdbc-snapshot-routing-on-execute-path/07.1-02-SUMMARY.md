---
phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
plan: 02
subsystem: datasource
tags: [jdbc, connection-catalog, snapshot-routing, dynamic-datasource, verify-only]

# Dependency graph
requires:
  - phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
    provides: "07.1-01: DefaultRuntimeJdbcEndpointResolver managed resolve now returns connectionName() (snap:{instanceId}:{name} under an active run) instead of the logical dataSourceId"
provides:
  - Confirmed QuerySourceFactory.create resolves via runtimeJdbcEndpointResolver.resolveSourceDataSourceId and mutates QuerySourceVO.dataSourceId with the resolver return before RowSource construction (D-05, D-06, D-08)
  - Confirmed PostGisQuerySourceFactory.resolveEndpoint resolves via the same interface and mutates PostGisQuerySourceVO.dataSourceId with the resolver return, and PostGisQuerySourceSupport.toQuerySource propagates the mutated id into the QuerySourceVO handed to QueryRowSource/ChunkedQueryRowSource (D-05, D-07, D-08)
  - Confirmed JdbcRowSinkAdapter.writeBatch resolves via runtimeJdbcEndpointResolver.resolveSinkDataSourceId(writer) and pushes that return value (not the writer's logical dataSourceId field) to DynamicDataSourceContextHolder.push before batch writes (D-05, D-07)
  - No code changes were necessary in any of the three adapters — all already implemented the resolve-then-mutate/push pattern correctly prior to this plan
affects: [07.1-03]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "V2 JDBC source factories: resolve via RuntimeJdbcEndpointResolver.resolveSourceDataSourceId(VO) -> mutate VO.dataSourceId with the return value (only if non-blank) -> build RowSource from the mutated VO. Confirmed present, no changes needed."
    - "V2 JDBC sink adapter: resolve via RuntimeJdbcEndpointResolver.resolveSinkDataSourceId(writer) -> DynamicDataSourceContextHolder.push(resolverReturn) -> batch write -> clear(). Confirmed present, no changes needed."

key-files:
  created: []
  modified: []

key-decisions:
  - "No fixes applied — all three adapters (QuerySourceFactory, PostGisQuerySourceFactory, JdbcRowSinkAdapter) already used the resolver return value exclusively; the DS-03 gap was entirely inside DefaultRuntimeJdbcEndpointResolver's managed-path return, which plan 07.1-01 already fixed"
  - "Did not add writer.setDataSourceId(dataSourceId) to JdbcRowSinkAdapter — PATTERNS.md/CONTEXT.md D-07 note this is optional and only warranted if a same-path reader of writer.getDataSourceId() exists; no such reader was found on the write path, so no speculative mutate was added"

requirements-completed: [DS-03]

coverage:
  - id: D1
    description: "QuerySourceFactory and PostGisQuerySourceFactory push the RuntimeJdbcEndpointResolver.resolveSourceDataSourceId return value (not the original logical dataSourceId) onto their VOs before RowSource construction"
    requirement: "DS-03"
    verification:
      - kind: other
        ref: "Read of QuerySourceFactory.java lines 43-53 and PostGisQuerySourceFactory.java lines 60-76 — both call resolveSourceDataSourceId and conditionally call setDataSourceId(effectiveDataSourceId) with the resolver's return, never the original querySource/source getDataSourceId() value"
        status: pass
      - kind: other
        ref: "Read of PostGisQuerySourceSupport.java lines 28-36 (toQuerySource) — reads source.getDataSourceId() AFTER resolveEndpoint mutated the same PostGisQuerySourceVO instance, so the resolved id propagates into the QuerySourceVO used by QueryRowSource/ChunkedQueryRowSource"
        status: pass
      - kind: other
        ref: ".\\mvnw-jdk25.ps1 -pl \"data-generator-calcite,data-generator-service\" -am -DskipTests compile -q"
        status: pass
    human_judgment: false
  - id: D2
    description: "JdbcRowSinkAdapter pushes the RuntimeJdbcEndpointResolver.resolveSinkDataSourceId(writer) return value to DynamicDataSourceContextHolder.push, not the writer's logical dataSourceId field or a template literal"
    requirement: "DS-03"
    verification:
      - kind: other
        ref: "Read of JdbcRowSinkAdapter.java lines 136-149 (writeBatch) — dataSourceId is captured strictly from runtimeJdbcEndpointResolver.resolveSinkDataSourceId(writer) and passed directly to DynamicDataSourceContextHolder.push; no alternate source of the pushed key exists on this path"
        status: pass
      - kind: other
        ref: "grep 'setDataSourceId|DynamicDataSourceContextHolder' data-generator-calcite/.../sink/JdbcRowSinkAdapter.java — only push/clear calls present, confirming no speculative writer.setDataSourceId was added"
        status: pass
    human_judgment: false
  - id: D3
    description: "No V1 writer files modified; Kafka and Elasticsearch snapshot sink paths remain untouched by this plan"
    requirement: "DS-03"
    verification:
      - kind: other
        ref: "git diff --stat -- data-generator-calcite/.../source/QuerySourceFactory.java data-generator-calcite/.../source/PostGisQuerySourceFactory.java data-generator-calcite/.../sink/JdbcRowSinkAdapter.java (empty output — zero changes to any of the three in-scope files)"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-07-24
status: complete
---

# Phase 07.1 Plan 02: Verify QuerySource/PostGIS/JdbcRowSink push resolved ids Summary

**Verify-only plan: all three V2 JDBC adapters (`QuerySourceFactory`, `PostGisQuerySourceFactory`, `JdbcRowSinkAdapter`) already push `RuntimeJdbcEndpointResolver`'s return value — not the logical catalog name — so plan 07.1-01's `connectionName()` fix now flows end-to-end without any adapter code changes.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-07-24T04:20:00Z
- **Completed:** 2026-07-24T04:32:00Z
- **Tasks:** 2
- **Files modified:** 0 (verify-only)

## Accomplishments
- Traced `QuerySourceFactory.create` and confirmed it calls `runtimeJdbcEndpointResolver.resolveSourceDataSourceId(querySource)` and conditionally calls `querySource.setDataSourceId(effectiveDataSourceId)` using only the resolver's return value — matches D-06/D-08 exactly, no fix needed.
- Traced `PostGisQuerySourceFactory.resolveEndpoint` and confirmed the identical resolve-then-mutate pattern against `PostGisQuerySourceSupport.endpointLookup(source)`, and confirmed `PostGisQuerySourceSupport.toQuerySource` reads the already-mutated `source.getDataSourceId()` so the resolved id survives into the `QuerySourceVO` passed to `QueryRowSource`/`ChunkedQueryRowSource` — matches D-07/D-08, no fix needed.
- Traced `JdbcRowSinkAdapter.writeBatch` and confirmed `dataSourceId` is captured strictly from `runtimeJdbcEndpointResolver.resolveSinkDataSourceId(writer)` and passed directly to `DynamicDataSourceContextHolder.push(...)`, with no alternate/logical-name code path — matches D-05/D-07, no fix needed. No `writer.setDataSourceId(...)` was added since no same-path consumer of that field exists on the write path (per PATTERNS.md guidance, this mutate is optional and was correctly omitted).
- Confirmed via `git diff --stat` that none of the three in-scope files had any prior uncommitted changes, and this plan introduced zero code diffs to them.
- Confirmed `data-generator-calcite` and `data-generator-service` compile cleanly together (`.\mvnw-jdk25.ps1 -pl "data-generator-calcite,data-generator-service" -am -DskipTests compile -q`, exit 0).
- Confirmed Kafka and Elasticsearch sink/source modules were not touched (out of scope, unchanged) and no V1 writer files were examined or modified.

## Task Commits

No production-code commits were made — both tasks were verify-only with zero required changes (all acceptance criteria were already satisfied by the existing code). Per the plan's explicit instruction ("If both factories already correct per PATTERNS.md, document verification in SUMMARY without speculative edits" / "Fix only if push uses logical name instead of resolver return"), no commit is warranted for either task individually.

**Plan metadata:** _pending — committed alongside this SUMMARY_

## Files Created/Modified
None. This was a verify-only plan; all three target files (`QuerySourceFactory.java`, `PostGisQuerySourceFactory.java`, `JdbcRowSinkAdapter.java`) were confirmed correct as-is.

## Decisions Made
- No fixes applied to any of the three adapters — the DS-03 gap was fully isolated to `DefaultRuntimeJdbcEndpointResolver`'s managed-path return, already fixed in plan 07.1-01. All adapter-level consumers were already correctly wired to use the resolver's return value.
- Did not add `writer.setDataSourceId(dataSourceId)` to `JdbcRowSinkAdapter` — PATTERNS.md/CONTEXT.md (D-07) explicitly note this mutate is optional and warranted only if a same-path reader of `writer.getDataSourceId()` exists; no such reader was found, so the speculative addition was correctly omitted per D-08 (accept existing behavior, no unnecessary VO mutation).

## Deviations from Plan

None - plan executed exactly as written. Both tasks resolved as verify-only with no code changes required.

## Issues Encountered
- `.\mvnw-jdk25.ps1 -pl data-generator-calcite,data-generator-service -am -DskipTests compile -q` (unquoted comma-separated module list) failed with `Unknown lifecycle phase "data-generator-service"` — PowerShell's argument parsing split the unquoted comma-separated value. Re-ran with the module list quoted (`-pl "data-generator-calcite,data-generator-service"`), which succeeded with exit code 0. Environment/shell-parsing quirk, not a code issue.

## Known Stubs

None.

## Threat Flags

None — both STRIDE mitigations for this plan (T-07.1-01 source factory setDataSourceId, T-07.1-02 JdbcRowSinkAdapter ContextHolder push) were confirmed already correctly mitigated by existing code; T-07.1-04 (wrong pool key on sink) is resolved by the compile + code trace confirmation performed here. No new unmitigated surface introduced since no code was changed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 07.1-03 (execute-path `@SpringBootTest` IT asserting `snap:{instanceId}:{name}` for both source and sink resolution under mid-flight reload) can proceed with full confidence: both the resolver (07.1-01) and all three adapters/factories that consume its return value (this plan) are now verified correct end-to-end.
- No regressions expected: zero code changes in this plan; V1 writers, Kafka, and Elasticsearch snapshot paths were not examined or touched.
- No blockers.

## Self-Check: PASSED

- FOUND: `.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-02-SUMMARY.md` (this file)
- CONFIRMED: `git diff --stat` for all three in-scope files returns empty (zero code changes, as expected for a verify-only plan)
- CONFIRMED: `.\mvnw-jdk25.ps1 -pl "data-generator-calcite,data-generator-service" -am -DskipTests compile -q` exits 0
- All acceptance criteria for both Task 1 and Task 2 re-verified via source read + grep + compile above — all PASS

---
*Phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path*
*Completed: 2026-07-24*
