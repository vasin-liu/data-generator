---
phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
plan: 03
type: execute
wave: 3
depends_on:
  - 07.1-01
  - 07.1-02
files_modified:
  - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java
autonomous: true
requirements:
  - DS-03
must_haves:
  truths:
    - SpringBootTest IT binds WorkflowRunContext and asserts resolveSourceDataSourceId and resolveSinkDataSourceId return snap:{instanceId}: keys (D-09, D-10, D-11)
    - Mid-flight catalog persistAndReload does not change in-flight resolved keys to live catalog name (D-11)
    - IT uses application-phase7-test.yaml profile and HotReloadTests queue/markRunning/snapshot pattern (D-12)
    - Test template includes managed JDBC source and sink dataSourceId refs (D-10)
    - No secrets logged; Kafka/ES paths not modified (roadmap success criterion 3)
  artifacts:
    - org.gensokyo.data.datasource.catalog.JdbcSnapshotExecutePathIT (or equivalent focused class name)
  key_links:
    - TaskExecutionService queue + RunLineageSupport captureConnectionSnapshot + WorkflowRunContext.bind
    - ConnectionCatalog persistAndReload mid-flight trigger from HotReloadTests pattern
---

# Plan 07.1-03: Mid-flight SpringBootTest IT for source+sink snap routing keys

<objective>
Add a focused `@SpringBootTest` integration test proving JDBC source and sink resolution return and retain `snap:{instanceId}:{name}` routing keys through mid-flight catalog reload (D-09..D-12).

Purpose: Provide executable proof for DS-03 execute-path snapshot isolation without dual-DB observable divergence or Playwright UAT.
Output: Green IT in data-generator-service asserting snap: keys for source+sink before and after mid-flight reload.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-CONTEXT.md
@.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/HotReloadTests.java
@data-generator-service/src/test/resources/application-phase7-test.yaml
@data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JdbcSnapshotExecutePathIT with source+sink snap key assertions</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java</files>
  <read_first>
    - HotReloadTests.java (queue, markRunning, captureConnectionSnapshot, WorkflowRunContext.bind, persistAndReload)
    - application-phase7-test.yaml embedded H2 and governance profile
    - 07.1-CONTEXT.md D-09, D-10, D-11, D-12
    - 07.1-PATTERNS.md IT analog table
    - DefaultRuntimeJdbcEndpointResolver after plan 07.1-01 fix
  </read_first>
  <action>
    Per D-12: create new focused IT class JdbcSnapshotExecutePathIT under org.gensokyo.data.datasource.catalog with @SpringBootTest(classes = DataGeneratorApplication.class) and @TestPropertySource or spring.config.location classpath:/application-phase7-test.yaml.
    Autowire RuntimeJdbcEndpointResolver (DefaultRuntimeJdbcEndpointResolver), ConnectionCatalog, TaskExecutionService, DataSourceConfigService, DynamicRoutingDataSource, ConnectionCatalogImpl as needed per HotReloadTests wiring.
    Per D-10: use or construct a minimal Template V2 fixture with managed JDBC dataSourceId on both a JDBC query source VO and JDBC sink VO referencing the same or distinct catalog-managed datasources from phase7 test profile.
    Per D-09 and D-11: flow — queue execution, markRunning, captureConnectionSnapshot, WorkflowRunContext.bind(instanceId, snapshot), then call resolveSourceDataSourceId on source VO and resolveSinkDataSourceId on sink VO; assert both returned strings startWith snap: and contain instanceId.
    Per D-11: invoke mid-flight persistAndReload on catalog entry (same hook as HotReloadTests); re-call both resolve methods under still-bound WorkflowRunContext; assert keys still startWith snap: and match pre-reload values.
    Prefer new class over extending HotReloadTests to keep scope focused. Dual DB-A/DB-B behavioral proof not required. Do not log passwords or secret values in assertions or failure messages.
    Document in class Javadoc that Kafka/ES snapshot paths are unchanged this phase.
  </action>
  <acceptance_criteria>
    - IT class exists with @SpringBootTest and phase7 test profile (D-12)
    - Assertions cover both resolveSourceDataSourceId and resolveSinkDataSourceId (D-10)
    - Pre-reload and post-reload mid-flight assertions use snap: prefix routing keys (D-11)
    - HotReloadTests not bloated with duplicate scenarios
    - No plaintext secrets in test output
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=JdbcSnapshotExecutePathIT -Dsurefire.failIfNoSpecifiedTests=false</automated>
  </verify>
  <done>Focused IT proves in-flight JDBC source and sink resolution retains snap routing keys through mid-flight reload.</done>
</task>

<task type="auto">
  <name>Task 2: Run IT green and document post-reload isolation bar</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java</files>
  <read_first>
    - JdbcSnapshotExecutePathIT from Task 1
    - 07.1-CONTEXT.md D-11 deferred dual-DB proof
    - ROADMAP.md Phase 07.1 success criteria 2 and 3
    - HotReloadTests post-reload patterns if any
  </read_first>
  <action>
    Run JdbcSnapshotExecutePathIT until green; fix test wiring or flakiness without weakening snap: assertions.
    Optionally add secondary assertion that after WorkflowRunContext.clear a fresh resolve without bound context returns live catalog key or differs from in-flight snap key — minimum bar remains in-flight still snap: after reload (D-11).
    In SUMMARY document that Kafka/ES snapshot paths were not modified and success criterion 3 is satisfied by scope exclusion plus existing Phase 7 coverage.
    Note dual DB-A/DB-B observable divergence explicitly deferred per CONTEXT.md.
  </action>
  <acceptance_criteria>
    - mvn test -Dtest=JdbcSnapshotExecutePathIT exits 0
    - In-flight snap: assertions remain strict after any optional post-clear check
    - SUMMARY cites D-09..D-12 and DS-03 closure evidence
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=JdbcSnapshotExecutePathIT -Dsurefire.failIfNoSpecifiedTests=false</automated>
  </verify>
  <done>IT green; DS-03 execute-path snap routing proven for JDBC source+sink under mid-flight reload.</done>
</task>

</tasks>

<threat_model>
## Security Configuration

ASVS Level: 1 | block_on: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IT logs and assertion messages | Must not emit JDBC passwords or resolved secret values |
| Mid-flight reload in test | Uses test catalog entries only; no production datasource IDs |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Information Disclosure | high | IT logging | mitigate | Assert on routing key prefix only; no credential fields in logs or assert messages |
| T-07.1-02 | Tampering | medium | Mid-flight isolation proof | mitigate | Assert snap: keys unchanged after persistAndReload under bound context (D-11) |
| T-07.1-03 | Repudiation | low | Missing sink assertion | mitigate | Require both source and sink resolve assertions (D-10) |
| T-07.1-04 | Denial of Service | low | Flaky IT blocking CI | accept | Reuse HotReloadTests stable hooks; embedded H2 only |
| T-07.1-SC | Tampering | n/a | Kafka/ES IT scope | accept | Document unchanged; no Kafka/ES test additions this phase |
</threat_model>

<verification>
- JdbcSnapshotExecutePathIT passes on JDK 25 with phase7 profile
- Assertions grep confirms snap: prefix and instanceId presence
- No kafka/elasticsearch test or main sources modified
</verification>

<success_criteria>
- DS-03: mid-flight IT proves JDBC execute path uses snap routing keys for source and sink
- Roadmap success criterion 2 satisfied via routing-key assertion bar (D-11)
- Roadmap success criterion 3 satisfied — Kafka/ES unchanged
</success_criteria>

## Artifacts this phase produces

- `org.gensokyo.data.datasource.catalog.JdbcSnapshotExecutePathIT` — SpringBootTest proving snap: keys for source+sink survive mid-flight reload
- `07.1-03-SUMMARY.md` — evidence summary citing D-09..D-12 and deferred dual-DB proof

<output>
Create `.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-03-SUMMARY.md` when done
</output>
