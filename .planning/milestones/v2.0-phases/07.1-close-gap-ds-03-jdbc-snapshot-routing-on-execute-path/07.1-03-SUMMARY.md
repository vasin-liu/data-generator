---
phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
plan: 03
subsystem: datasource
tags: [jdbc, connection-catalog, snapshot-routing, hot-reload, springboot-test, mid-flight]

# Dependency graph
requires:
  - phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
    provides: "07.1-01: DefaultRuntimeJdbcEndpointResolver managed resolve returns connectionName() (snap:{instanceId}:{name} under active run); 07.1-02: QuerySource/PostGIS/JdbcRowSink already push resolver return"
provides:
  - SpringBootTest JdbcSnapshotExecutePathIT proving resolveSourceDataSourceId and resolveSinkDataSourceId return snap:{instanceId}: keys under WorkflowRunContext.bind
  - Mid-flight persistAndReload isolation proof — in-flight snap keys unchanged after catalog reload (D-11 routing-key bar)
  - Optional post-clear assertion that unbound resolve returns live catalog name (not snap:)
  - DS-03 execute-path closure evidence without dual DB-A/DB-B or Playwright UAT
affects: [phase-07.1-complete, verify-work]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Focused SpringBootTest (phase7 profile) separate from HotReloadTests: queue → markRunning → captureConnectionSnapshot → WorkflowRunContext.bind → resolveSource/Sink → mid-flight persistAndReload → re-resolve assert equality"

key-files:
  created:
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java
  modified: []

key-decisions:
  - "Completed interrupted draft IT rather than rewriting from scratch — draft already matched HotReloadTests wiring and D-09..D-12 shape"
  - "Added optional post-clear live-key assertion (plan Task 2) without weakening in-flight snap: equality checks"
  - "Dual DB-A/DB-B observable divergence explicitly deferred per CONTEXT.md; Kafka/ES paths untouched (roadmap success criterion 3)"

patterns-established:
  - "DS-03 execute-path proof bar is routing-key snap: retention under mid-flight reload, not dual-DB URL divergence"

requirements-completed: [DS-03]

coverage:
  - id: D1
    description: "JdbcSnapshotExecutePathIT asserts resolveSourceDataSourceId and resolveSinkDataSourceId return snap:{instanceId}: keys before and after mid-flight persistAndReload under bound WorkflowRunContext (D-09, D-10, D-11, D-12)"
    requirement: "DS-03"
    verification:
      - kind: integration
        ref: "data-generator-service/.../JdbcSnapshotExecutePathIT.java#sourceAndSinkResolveRetainSnapshotKeyAcrossMidFlightReload"
        status: pass
      - kind: other
        ref: ".\\mvnw-jdk25.ps1 -pl \"data-generator-service\" -am test -Dtest=JdbcSnapshotExecutePathIT -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true (exit 0; Tests run: 1, Failures: 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Kafka and Elasticsearch snapshot paths unchanged this phase; success criterion 3 satisfied by scope exclusion plus existing Phase 7 coverage"
    requirement: "DS-03"
    verification:
      - kind: other
        ref: "git show --stat 12f36f0 — only JdbcSnapshotExecutePathIT.java added; no kafka/elasticsearch main or test sources modified"
        status: pass
    human_judgment: false
  - id: D3
    description: "Dual DB-A/DB-B observable divergence deferred per CONTEXT.md; routing-key bar fulfills roadmap success criterion 2 for this phase"
    requirement: "DS-03"
    verification: []
    human_judgment: true
    rationale: "CONTEXT.md explicitly defers dual-DB behavioral proof; documenting deferral is a scope judgment, not an automated assertion."

duration: 28min
completed: 2026-07-24
status: complete
---

# Phase 07.1 Plan 03: Mid-flight SpringBootTest IT for source+sink snap routing keys Summary

**Focused `@SpringBootTest` proves JDBC source and sink `RuntimeJdbcEndpointResolver` returns retain `snap:{instanceId}:` routing keys through mid-flight catalog reload (DS-03 / D-09..D-12).**

## Performance

- **Duration:** ~28 min
- **Started:** 2026-07-24T05:17:00Z
- **Completed:** 2026-07-24T05:45:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Completed interrupted draft `JdbcSnapshotExecutePathIT` against HotReloadTests queue/markRunning/snapshot/bind/persistAndReload pattern with phase7 test profile (D-12).
- Asserted both `resolveSourceDataSourceId` and `resolveSinkDataSourceId` return `snap:{instanceId}:` keys pre- and post-mid-flight reload with equality retention (D-09, D-10, D-11).
- Added optional post-clear check that unbound resolve returns the live catalog name (`jdbc-snap-execute-it-ds`), not a `snap:` key.
- IT green: `Tests run: 1, Failures: 0, Errors: 0` under JDK 25; Kafka/ES paths not modified; dual DB-A/DB-B deferred per CONTEXT.md.

## Task Commits

Each task was committed atomically:

1. **Task 1 + Task 2: Create IT + run green** - `12f36f0` (test) — single commit because Task 2 only added the optional post-clear assertions in the same new file and verified the suite green; no separate production surface.

**Plan metadata:** _pending — committed alongside this SUMMARY_

## Files Created/Modified
- `data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java` — Mid-flight SpringBootTest for source+sink snap routing keys

## Decisions Made
- Finished the existing UNTRACKED draft rather than discarding it — copyright, SpringBootTest skeleton, and HotReloadTests-aligned flow already matched the plan.
- Kept a focused new class instead of extending `HotReloadTests` (plan preference).
- Documented Kafka/ES unchanged and dual-DB deferred in class Javadoc and this SUMMARY (roadmap success criteria 2–3 / CONTEXT.md).

## Deviations from Plan

None - plan executed exactly as written. Combined Task 1/2 into one `test(07.1-03)` commit because Task 2 had no separate artifact beyond optional assertions + green verification already folded into the new IT file.

---

**Total deviations:** 0 auto-fixed (1 intentional commit-batching note only)
**Impact on plan:** None — acceptance criteria and verify commands fully satisfied.

## Issues Encountered
- `-Dskip.console.frontend=true` skips only the service-module antrun console copy, not `data-generator-console-web` frontend-maven-plugin `npm ci`/`npm run build` when `-am` pulls that module. Build still succeeded; duration dominated by vite (~18 min of reactor time).

## Known Stubs

None.

## Threat Flags

None — T-07.1-01 mitigated (assert messages use routing-key prefixes only, no passwords); T-07.1-02 mitigated (snap keys unchanged after persistAndReload); T-07.1-03 mitigated (both source and sink asserted); T-07.1-SC accepted (Kafka/ES out of scope, documented).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 07.1 plans 01–03 all have SUMMARYs — phase complete for DS-03 execute-path snap routing closure.
- Ready for `/gsd-verify-work` on phase 07.1 and milestone progress toward next open work (Phases 8–10 already complete on roadmap).
- No blockers. Dual DB-A/DB-B remains explicitly deferred if a future phase wants observable URL divergence.

## Self-Check: PASSED

- FOUND: `.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-03-SUMMARY.md` (this file)
- FOUND: `data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java`
- CONFIRMED: `git log --oneline --grep="07.1-03"` returns ≥1 commit (`12f36f0`)
- CONFIRMED: `.\mvnw-jdk25.ps1 -pl "data-generator-service" -am test -Dtest=JdbcSnapshotExecutePathIT -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true` → exit 0, Tests run: 1, Failures: 0
- All Task 1/2 acceptance criteria re-checked — PASS

---
*Phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path*
*Completed: 2026-07-24*
