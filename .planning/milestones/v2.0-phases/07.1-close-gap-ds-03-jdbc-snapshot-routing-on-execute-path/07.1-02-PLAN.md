---
phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
plan: 02
type: execute
wave: 2
depends_on:
  - 07.1-01
files_modified:
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/QuerySourceFactory.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/PostGisQuerySourceFactory.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcRowSinkAdapter.java
autonomous: true
requirements:
  - DS-03
must_haves:
  truths:
    - QuerySourceFactory calls resolveSourceDataSourceId then setDataSourceId with returned value (D-05, D-06, D-07, D-08)
    - PostGisQuerySourceFactory follows same resolve-then-mutate pattern (D-05, D-07, D-08)
    - JdbcRowSinkAdapter uses resolveSinkDataSourceId return for DynamicDataSourceContextHolder.push (D-05, D-07)
    - No V1 writer changes; no speculative writer.setDataSourceId without same-path reader (D-07)
    - Kafka and ES snapshot paths unchanged (roadmap success criterion 3)
  artifacts:
    - Verified or minimally fixed QuerySourceFactory, PostGisQuerySourceFactory, JdbcRowSinkAdapter
  key_links:
    - RuntimeJdbcEndpointResolver bean from plan 07.1-01 feeds resolved snap keys into adapters
    - VO mutate side effects accepted; no copy/restore wrapper (D-08)
---

# Plan 07.1-02: Verify QuerySource/PostGIS/JdbcRowSink push resolved ids

<objective>
Verify V2 JDBC source and sink adapters push the resolved routing id from RuntimeJdbcEndpointResolver, not the logical catalog name (D-05..D-08). Fix only if a code path skips the resolver return.

Purpose: Ensure plan 07.1-01 snap keys actually reach DynamicDataSourceContextHolder and JDBC RowSource/RowSink execution.
Output: Confirmed adapter wiring with surgical fixes only where resolver return is dropped; calcite + service compile green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-CONTEXT.md
@.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/QuerySourceFactory.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/PostGisQuerySourceFactory.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcRowSinkAdapter.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/RuntimeJdbcEndpointResolver.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify QuerySourceFactory and PostGisQuerySourceFactory resolve-then-mutate</name>
  <files>data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/QuerySourceFactory.java,data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/PostGisQuerySourceFactory.java</files>
  <read_first>
    - QuerySourceFactory.java create/build path for JDBC managed dataSourceId
    - PostGisQuerySourceFactory.java parallel path
    - 07.1-PATTERNS.md QuerySourceFactory and PostGisQuerySourceFactory analog table
    - 07.1-CONTEXT.md D-05, D-06, D-07, D-08
    - RuntimeJdbcEndpointResolver.java resolveSourceDataSourceId contract
  </read_first>
  <action>
    Per D-06 and D-08: trace each managed JDBC source factory path from VO with dataSourceId through runtimeJdbcEndpointResolver.resolveSourceDataSourceId (or equivalent) to vo.setDataSourceId with the returned String — not the original logical name.
    Per D-05 and D-07: confirm both QuerySourceFactory and PostGisQuerySourceFactory cover managed dataSourceId; no V1 reader/writer paths touched.
    Per D-08: accept existing VO mutation side effects; do not add copy/restore around setDataSourceId.
    If either factory returns RowSource without calling resolver or sets logical name after resolve, apply minimal fix so setDataSourceId receives resolver return value only.
    If both factories already correct per PATTERNS.md, document verification in SUMMARY without speculative edits.
  </action>
  <acceptance_criteria>
    - QuerySourceFactory managed path uses resolver return for setDataSourceId (D-06, D-08)
    - PostGisQuerySourceFactory managed path uses resolver return for setDataSourceId (D-07, D-08)
    - No V1 source factory files modified
    - No VO copy/restore wrapper added
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite,data-generator-service -am -DskipTests compile -q</automated>
  </verify>
  <done>Both V2 JDBC source factories push resolved snap routing ids into VO before RowSource creation.</done>
</task>

<task type="auto">
  <name>Task 2: Verify JdbcRowSinkAdapter pushes resolveSinkDataSourceId return</name>
  <files>data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcRowSinkAdapter.java</files>
  <read_first>
    - JdbcRowSinkAdapter.java write/open path and DynamicDataSourceContextHolder.push usage
    - 07.1-PATTERNS.md JdbcRowSinkAdapter analog (already pushes resolver return)
    - 07.1-CONTEXT.md D-05, D-07, D-08
    - RuntimeJdbcEndpointResolver.java resolveSinkDataSourceId contract
  </read_first>
  <action>
    Per D-05 and D-07: trace sink managed dataSourceId path through resolveSinkDataSourceId to DynamicDataSourceContextHolder.push using the resolver return value — not sink VO logical name or template literal.
    Do not add writer.setDataSourceId unless a same-path reader of writer.getDataSourceId() exists on the identical execution path; PATTERNS indicates push is the sink routing surface.
    Confirm no V1 JDBC writer classes are modified.
    Explicitly note in SUMMARY that Kafka and ES snapshot adapters are out of scope and unchanged (roadmap success criterion 3).
    Fix only if push uses logical name instead of resolver return.
  </action>
  <acceptance_criteria>
    - JdbcRowSinkAdapter DynamicDataSourceContextHolder.push uses resolveSinkDataSourceId return (D-05, D-07)
    - No V1 writer files modified
    - No speculative writer.setDataSourceId added without proven same-path consumer
    - Kafka/ES modules untouched in git diff
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite,data-generator-service -am -DskipTests compile -q</automated>
  </verify>
  <done>JdbcRowSinkAdapter routes JDBC writes through resolved snap key from execute-path resolver.</done>
</task>

</tasks>

<threat_model>
## Security Configuration

ASVS Level: 1 | block_on: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolver return → VO dataSourceId / ContextHolder push | Adapters must not revert to logical catalog name after resolve (D-05) |
| V2 calcite adapters only | V1 writers excluded to limit blast radius (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Tampering | high | Source factory setDataSourceId | mitigate | Verify/fix paths use resolver return only (D-06, D-08) |
| T-07.1-02 | Tampering | high | JdbcRowSinkAdapter ContextHolder push | mitigate | Verify/fix push uses resolveSinkDataSourceId return (D-05) |
| T-07.1-03 | Information Disclosure | low | VO mutate logging | accept | No new logging of connection params |
| T-07.1-04 | Denial of Service | medium | Wrong pool key on sink | mitigate | Compile + code trace confirmation before marking verify-only |
| T-07.1-SC | Tampering | n/a | Kafka/ES paths | accept | Explicitly out of scope; no modifications this phase |
</threat_model>

<verification>
- data-generator-calcite and data-generator-service compile
- Code trace or grep confirms resolver return flows to setDataSourceId and ContextHolder.push
- Git diff excludes kafka, elasticsearch, V1 writer modules
</verification>

<success_criteria>
- DS-03: V2 JDBC source and sink adapters use resolved snap routing keys end-to-end
- D-05..D-08 satisfied with verify-only or minimal surgical fixes
- Kafka/ES snapshot paths remain unchanged
</success_criteria>

## Artifacts this phase produces

- Verified QuerySourceFactory and PostGisQuerySourceFactory resolve-then-mutate wiring (D-06, D-07, D-08)
- Verified JdbcRowSinkAdapter resolveSinkDataSourceId → DynamicDataSourceContextHolder.push (D-05)
- 07.1-02-SUMMARY.md noting verify-only vs fix scope and Kafka/ES unchanged

<output>
Create `.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-02-SUMMARY.md` when done
</output>
