# Phase 07.1: Close gap DS-03 — JDBC snapshot routing on execute path - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-24
**Phase:** 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
**Areas discussed:** Fix locus, Source + sink coverage, Proof bar for mid-flight isolation, Duplicate JDBC resolvers

---

## Fix locus

| Option | Description | Selected |
|--------|-------------|----------|
| Resolver only | Snap keys enter via DefaultRuntimeJdbcEndpointResolver | ✓ |
| Adapters also | Adapters construct snap keys | |
| catalog.resolve → connectionName() | Managed path returns snap routing key | ✓ |
| Build snap key in resolver manually | Hand-build `snap:` string | |
| Managed dataSourceId only | Inline deferred | ✓ |
| Include inline | Inline blocks in this phase | |
| Fail fast | No fallback to logical name | ✓ |
| Fallback to logical name | Soft degrade on resolve failure | |

**User's choice:** Resolver only; catalog.resolve then connectionName(); managed only; fail fast.
**Notes:** Resumed from prior checkpoint for this area.

---

## Source + sink coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Source + Sink | Both paths; push resolved id | ✓ |
| Source only / Sink only | Partial coverage | |
| QuerySourceFactory VO mutation | Existing setDataSourceId pattern | ✓ |
| Adapter rewrite | Alternate wiring | |
| QuerySource + PostGIS + JdbcRowSink | V2 surface; no V1 | ✓ |
| Accept mutate | No copy/restore | ✓ |

**User's choice:** Source+Sink; factory mutation; Query+PostGIS+JdbcRowSink; accept mutate.
**Notes:** Resumed from prior checkpoint for this area.

---

## Proof bar for mid-flight isolation

| Option | Description | Selected |
|--------|-------------|----------|
| Execute-path IT | Resolver + JDBC under mid-flight reload | ✓ |
| Unit only / Full UAT | Weaker or broader bars | |
| Both source and sink assertions | Dual assertions in IT | ✓ |
| Pool/routing key assertion | Assert `snap:{instanceId}:{name}` | ✓ |
| Observable endpoint divergence | DB-A vs DB-B behavioral proof | |
| Service Spring IT | `@SpringBootTest` in data-generator-service | ✓ |
| Focused calcite/datasource IT | Lighter double-based test | |

**User's choice:** Execute-path IT; both source+sink; routing-key assertion; service Spring IT.
**Notes:** After four questions, user chose next area (no more proof-bar questions). Replies continued in Chinese from this point.

---

## Duplicate JDBC resolvers

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both, clear ownership | No merge this phase | ✓ |
| Merge this phase | Unify into one resolver | |
| Align semantics inside DefaultRuntime | Copy connectionName() pattern; no dependency | ✓ |
| Delegate to JdbcCatalogResolver | Inject catalog resolver for managed path | |
| Zero changes to JdbcCatalogResolver | Already returns connectionName() | ✓ |
| Docs/comments only on JdbcCatalogResolver | Non-behavioral | |
| Document execute authority | DefaultRuntime is V2 execute entry | ✓ |
| No extra docs | Behavior + IT only | |

**User's choice:** Keep both; fix inside DefaultRuntime; zero JdbcCatalogResolver changes; document authority.
**Notes:** User then chose ready for CONTEXT (no more gray areas).

---

## Claude's Discretion

- IT class/package placement and mid-flight reload trigger mechanics (within D-09–D-12).
- Exact Javadoc wording for resolver ownership (D-16).

## Deferred Ideas

- Inline dataSource snap routing
- Resolver consolidation
- Dual-DB observable isolation proof
- VO copy/restore; V1 writers; Playwright for this gap
