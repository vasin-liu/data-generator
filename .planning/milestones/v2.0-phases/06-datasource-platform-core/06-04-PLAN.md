---
phase: 06-datasource-platform-core
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - data-generator-service/pom.xml
  - data-generator-service/src/main/java/org/gensokyo/data/datasource/**
  - data-generator-service/src/main/java/org/gensokyo/data/messaging/**
  - data-generator-service/src/main/java/org/gensokyo/data/config/CoreConfig.java
  - data-generator-service/src/test/java/org/gensokyo/data/datasource/**
autonomous: true
requirements:
  - DS-02
must_haves:
  truths:
    - ConnectionCatalog implementation merges BOOTSTRAP yaml + MANAGED DB entries; MANAGED wins on name collision (D-06, D-23)
    - Catalog exposes resolve-only API; CRUD remains on DataSourceConfigService and MessagingClusterConfigService (D-05, D-13)
    - Yaml bootstrap entries registered at application startup (D-25)
    - Phase 6 resolve uses live lookup against current registry/DB with no snapshot-at-run-start stub (D-09)
  artifacts:
    - org.gensokyo.data.datasource.catalog.ConnectionCatalogImpl (or equivalent service bean)
  key_links:
    - CoreConfig registers ConnectionCatalog bean wired to adapters (D-34 strangler)
    - data-generator-service depends on datasource parent/adapters (D-15)
---

# Plan 06-04: Service Catalog Implementation & Bootstrap Wiring

<objective>
Implement production `ConnectionCatalog` in service layer delegating to JDBC/Kafka/ES adapters and existing persistence services per D-05, D-06, D-11, D-13, D-23..D-26, D-34, D-35.

Purpose: Strangler catalog becomes the runtime resolve/list hub while CRUD APIs stay unchanged.
Output: Spring-wired catalog with bootstrap registration and merged list view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-datasource-platform-core/06-CONTEXT.md
@data-generator-service/src/main/java/org/gensokyo/data/datasource/DataSourceBootstrap.java
@data-generator-service/src/main/java/org/gensokyo/data/datasource/DataSourceConfigService.java
@data-generator-service/src/main/java/org/gensokyo/data/messaging/MessagingClusterConfigService.java
@data-generator-service/src/main/java/org/gensokyo/data/config/CoreConfig.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ConnectionCatalogImpl delegating to existing services</name>
  <files>data-generator-service/src/main/java/org/gensokyo/data/datasource/catalog/**,data-generator-service/pom.xml</files>
  <read_first>
    - DataSourceConfigService.java (list/find/register pool methods)
    - MessagingClusterConfigService.java (cluster CRUD + runtime registration)
    - DataSourceBootstrap.java
    - ConnectionCatalog api types from 06-01
    - JDBC/Kafka/ES adapters from 06-02/06-03
  </read_first>
  <action>
    Per D-05/D-13: create `ConnectionCatalogImpl` implementing `ConnectionCatalog` with `resolve` and `listAll` only — no create/update/delete on Catalog.
    Per D-06/D-23/D-26: `listAll()` merges yaml BOOTSTRAP + DB MANAGED; on duplicate name MANAGED overrides BOOTSTRAP in both list and resolve.
    Per D-24: mark BOOTSTRAP entries read-only at API layer (reject mutate attempts targeting bootstrap-only names in existing services if not already enforced).
    Per D-10: resolve builds runtime handles via adapters calling SecretResolver internally; Catalog list entries omit secrets.
    Per D-15: add service POM dependencies on `data-generator-datasource-jdbc`, `-kafka`, `-elasticsearch`, `-api`.
    Per D-35: remove parallel legacy resolve paths in catalog code — single delegation chain.
    Per D-09: `resolve` reads live registry/DB state on each call — do NOT introduce snapshot/cache layer (deferred to Phase 7).
  </action>
  <acceptance_criteria>
    - `ConnectionCatalogImpl` class exists and implements `ConnectionCatalog`
    - Service integration test or unit test proves MANAGED entry wins over BOOTSTRAP for same name in `listAll()`
    - `DataSourceConfigService` and `MessagingClusterConfigService` still own CRUD methods (grep `save|delete|update` remains in those classes)
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=DataSourceConfigServiceTests -q</automated>
  </verify>
  <done>Catalog impl merges sources and delegates CRUD to existing services.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Catalog bean and startup bootstrap registration</name>
  <files>data-generator-service/src/main/java/org/gensokyo/data/config/CoreConfig.java,data-generator-service/src/main/java/org/gensokyo/data/datasource/DataSourceBootstrap.java,data-generator-service/src/test/java/**</files>
  <read_first>
    - CoreConfig.java (existing registry beans)
    - DataSourceBootstrap.java
    - MessagingClusterConfigService @PostConstruct registration
  </read_first>
  <action>
    Per D-25: register yaml JDBC/Kafka/ES bootstrap connections into Catalog at application startup (extend DataSourceBootstrap and messaging bootstrap hooks).
    Per D-34: CoreConfig exposes `@Bean ConnectionCatalog connectionCatalog(...)` wiring adapters + services.
    Ensure catalog bean available for console list delegation (next plan) and calcite wiring (plan 05).
    Add/adjust service tests verifying bootstrap entries appear in `listAll()` with source BOOTSTRAP.
  </action>
  <acceptance_criteria>
    - CoreConfig defines a `ConnectionCatalog` bean
    - Test asserts at least one BOOTSTRAP-tagged entry from test yaml profile in `listAll()`
    - `.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=DataSourceConfigServiceTests,ConsoleDataSourceControllerTest -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=DataSourceConfigServiceTests,ConsoleDataSourceControllerTest -q</automated>
  </verify>
  <done>Catalog wired in Spring; bootstrap + managed list verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Console CRUD → persistence | Operators mutate MANAGED entries only |
| Catalog list → console UI | List responses must not leak secrets (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | BOOTSTRAP entry mutation | mitigate | Reject API mutations on BOOTSTRAP-only names (D-24) |
| T-06-08 | Information Disclosure | listAll metadata | mitigate | No password/apiKey in CatalogEntry payloads |
| T-06-SC | Tampering | Service wiring | mitigate | Regression tests on existing console datasource tests |
</threat_model>

<verification>
- DataSourceConfigServiceTests and ConsoleDataSourceControllerTest pass
- Catalog bean resolvable from Spring context in service tests
</verification>

<success_criteria>
- DS-02 partial: managed catalog resolves through abstraction layer in service
- D-05, D-06, D-11, D-13, D-23..D-26, D-34, D-35 delivered
</success_criteria>

## Artifacts this phase produces

- `org.gensokyo.data.datasource.catalog.ConnectionCatalogImpl`
- Updated `CoreConfig` ConnectionCatalog `@Bean`
- Bootstrap registration hooks tagging BOOTSTRAP entries

<output>
Create `.planning/phases/06-datasource-platform-core/06-04-SUMMARY.md` when done
</output>
