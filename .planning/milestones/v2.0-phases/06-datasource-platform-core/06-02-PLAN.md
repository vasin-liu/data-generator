---
phase: 06-datasource-platform-core
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - data-generator-datasource/data-generator-datasource-jdbc/pom.xml
  - data-generator-datasource/data-generator-datasource-jdbc/src/main/java/**
  - data-generator-datasource/data-generator-datasource-jdbc/src/test/java/**
  - data-generator-datasource/pom.xml
autonomous: true
requirements:
  - DS-01
must_haves:
  truths:
    - JDBC adapter resolves managed dataSourceId via Catalog and supports inline InlineDataSourceVO fallback (D-19, D-20)
    - datasource-jdbc owns Druid + dynamic-datasource pool construction (Claude's Discretion)
  artifacts:
    - data-generator-datasource/data-generator-datasource-jdbc/pom.xml
    - org.gensokyo.data.datasource.jdbc.JdbcCatalogResolver (or equivalent)
  key_links:
    - datasource-jdbc depends on datasource-api and data-generator-database-core (D-16)
    - SecretResolver used internally when building pools (D-10, D-22)
---

# Plan 06-02: JDBC Datasource Adapter

<objective>
Implement `data-generator-datasource-jdbc` adapter: JDBC pool construction, managed resolve, and inline fallback per D-16, D-19, D-20, D-22, D-34.

Purpose: Self-contained JDBC resolution logic relocated from service inline resolver patterns.
Output: Tested JDBC adapter module delegating secret resolution internally.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-datasource-platform-core/06-CONTEXT.md
@data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
@data-generator-common/data-generator-database-core/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold datasource-jdbc module and dependencies</name>
  <files>data-generator-datasource/pom.xml,data-generator-datasource/data-generator-datasource-jdbc/pom.xml</files>
  <read_first>
    - data-generator-datasource/data-generator-datasource-api/pom.xml
    - data-generator-common/data-generator-database-core/pom.xml
    - data-generator-service/pom.xml (Druid + dynamic-datasource versions)
  </read_first>
  <action>
    Per D-12/D-16: add `data-generator-datasource-jdbc` submodule to parent POM.
    Dependencies: `data-generator-datasource-api`, `data-generator-database-core`, Druid, `dynamic-datasource-spring-boot4-starter` (compile where needed for routing types), `SecretResolver` from core (or SPI callback interface in api to avoid circular deps — prefer callback injected from service).
    Per discretion: adapter owns `DruidDataSource` creation mirroring `DefaultRuntimeJdbcEndpointResolver.createDataSource` behavior.
  </action>
  <acceptance_criteria>
    - Module `data-generator-datasource-jdbc` listed in parent POM
    - POM depends on `data-generator-datasource-api` and `data-generator-database-core`
    - `.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-jdbc -am compile` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-jdbc -am compile -q</automated>
  </verify>
  <done>JDBC adapter module compiles with correct dependency graph.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JDBC resolve with managed-first and inline fallback</name>
  <files>data-generator-datasource/data-generator-datasource-jdbc/src/main/java/org/gensokyo/data/datasource/jdbc/**,data-generator-datasource/data-generator-datasource-jdbc/src/test/java/**</files>
  <read_first>
    - DefaultRuntimeJdbcEndpointResolver.java (full file)
    - org.gensokyo.data.model.v2.InlineDataSourceVO
    - org.gensokyo.data.secret.SecretResolver
    - ConnectionCatalog api from plan 06-01
  </read_first>
  <action>
    Per D-20: resolution order — if `dataSourceId` present, resolve managed entry from catalog delegate; else build/register inline datasource from `InlineDataSourceVO`.
    Per D-19/D-22: inline `passwordSecretRef` flows through `SecretResolver.resolveInlinePassword`.
    Per D-07: unknown managed id throws `IllegalArgumentException` with connection name and kind JDBC.
    Add unit tests in adapter module (D-18): managed resolve, inline fallback, missing id error message, secret ref path.
    Do NOT move CRUD/persistence here (D-13).
  </action>
  <acceptance_criteria>
    - Unit test proves blank dataSourceId + inline block registers pool key equal to inline name
    - Unit test proves unknown managed id throws IllegalArgumentException containing connection name
    - `.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-jdbc test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-jdbc test -q</automated>
  </verify>
  <done>JDBC adapter encodes managed/inline parity with tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inline JDBC config → pool builder | Untrusted template inline blocks may supply credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | JDBC pool passwords | mitigate | Resolve via SecretResolver only; never log secrets (D-10) |
| T-06-04 | Elevation | Inline datasource registration | accept | Same trust model as existing DefaultRuntimeJdbcEndpointResolver |
| T-06-SC | Tampering | Maven deps | mitigate | slopcheck on new dependencies |
</threat_model>

<verification>
- JDBC adapter tests green
- No service CRUD code moved into jdbc module
</verification>

<success_criteria>
- DS-01: JDBC connection abstraction lives in datasource-jdbc submodule
- D-16, D-19, D-20, D-22 implemented with unit tests
</success_criteria>

## Artifacts this phase produces

- Maven module `data-generator-datasource-jdbc`
- `org.gensokyo.data.datasource.jdbc.*` resolver/pool helper types
- JDBC adapter unit test classes

<output>
Create `.planning/phases/06-datasource-platform-core/06-02-SUMMARY.md` when done
</output>
