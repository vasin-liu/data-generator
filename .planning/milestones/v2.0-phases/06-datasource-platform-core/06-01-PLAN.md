---
phase: 06-datasource-platform-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - data-generator-datasource/pom.xml
  - data-generator-datasource/data-generator-datasource-api/pom.xml
  - data-generator-datasource/data-generator-datasource-api/src/main/java/org/gensokyo/data/datasource/api/**
autonomous: true
requirements:
  - DS-01
must_haves:
  truths:
    - data-generator-datasource-api publishes ConnectionCatalog resolve/list contracts without Spring or adapter deps
    - CatalogEntry uses shared global namespace with ConnectionKind discriminator (D-01, D-02)
  artifacts:
    - data-generator-datasource/data-generator-datasource-api/pom.xml
    - org.gensokyo.data.datasource.api.ConnectionCatalog
    - org.gensokyo.data.datasource.api.CatalogEntry
    - org.gensokyo.data.datasource.api.ConnectionKind
    - org.gensokyo.data.datasource.api.CatalogEntrySource
  key_links:
    - root pom.xml lists data-generator-datasource-api as reactor module
    - data-generator-calcite can depend on datasource-api only (D-15, D-27)
---

# Plan 06-01: Datasource API Module & Catalog Contracts

<objective>
Scaffold `data-generator-datasource-api` with unified catalog types and resolve-only `ConnectionCatalog` interface per D-01..D-11, D-12 (api submodule), D-27, D-32.

Purpose: Establish the abstraction boundary calcite and service share before adapter implementations land.
Output: Compilable api module with public catalog contracts and Maven wiring in parent reactor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-datasource-platform-core/06-CONTEXT.md
@.planning/ROADMAP.md
@data-generator-datasource/pom.xml
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add datasource-api submodule to Maven reactor</name>
  <files>pom.xml,data-generator-datasource/pom.xml,data-generator-datasource/data-generator-datasource-api/pom.xml</files>
  <read_first>
    - pom.xml (parent modules list)
    - data-generator-datasource/pom.xml (empty aggregator)
    - data-generator-common/data-generator-core/pom.xml (submodule POM pattern)
  </read_first>
  <action>
    Per D-12: create submodule `data-generator-datasource-api` under `data-generator-datasource/`.
    Parent `data-generator-datasource/pom.xml`: set packaging `pom`, add module `data-generator-datasource-api` (jdbc/kafka/es modules added in later plans).
    Api module POM: parent `data-generator-datasource`, artifactId `data-generator-datasource-api`, Java 25, minimal deps (no Spring, no Druid, no Kafka client).
    Ensure root reactor still builds with `mvn -pl data-generator-datasource/data-generator-datasource-api -am compile`.
  </action>
  <acceptance_criteria>
    - `data-generator-datasource/data-generator-datasource-api/pom.xml` exists with artifactId `data-generator-datasource-api`
    - `data-generator-datasource/pom.xml` lists module `data-generator-datasource-api`
    - `.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-api -am compile` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-api -am compile -q</automated>
  </verify>
  <done>Api submodule compiles in isolation within reactor.</done>
</task>

<task type="auto">
  <name>Task 2: Define ConnectionCatalog and entry model types</name>
  <files>data-generator-datasource/data-generator-datasource-api/src/main/java/org/gensokyo/data/datasource/api/*.java</files>
  <read_first>
    - data-generator-service/src/main/java/org/gensokyo/data/datasource/DataSourceConfigService.java (managed JDBC naming)
    - data-generator-service/src/main/java/org/gensokyo/data/messaging/MessagingClusterConfigService.java (Kafka/ES cluster naming)
    - .planning/phases/06-datasource-platform-core/06-CONTEXT.md decisions D-01..D-11
  </read_first>
  <action>
    Per D-01/D-02: add `ConnectionKind` enum values `JDBC`, `KAFKA`, `ELASTICSEARCH`.
    Per D-06/D-26: add `CatalogEntrySource` enum `BOOTSTRAP`, `MANAGED`.
    Add immutable `CatalogEntry` record with fields: `name` (String), `kind` (ConnectionKind), `source` (CatalogEntrySource), plus type-specific metadata payloads (e.g., JDBC url/driver hints for list display only — no secrets).
    Per D-05/D-07: define `ConnectionCatalog` interface with `resolve(String name, ConnectionKind kind)` returning opaque runtime handle type(s) via kind-specific result types OR a sealed `ResolvedConnection` hierarchy; and `listAll()` returning `List&lt;CatalogEntry&gt;`.
    `resolve` must document that unknown name throws `IllegalArgumentException` with name, kind, and hint text (D-07).
    Do NOT expose secret values from api types (D-10).
    Follow Java copyright + public Javadoc rules from `.cursor/rules/java-copyright-class-javadoc.mdc`.
  </action>
  <acceptance_criteria>
    - `ConnectionCatalog.java` declares `resolve` and `listAll`
    - `CatalogEntry.java` includes `kind` and `source` fields
    - `ConnectionKind` has exactly JDBC, KAFKA, ELASTICSEARCH
    - `CatalogEntrySource` has BOOTSTRAP and MANAGED
    - No dependency on Spring/Kafka/ES/Druid in api module POM
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-api test -q</automated>
  </verify>
  <done>Public catalog contracts published for downstream adapters and calcite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog API → callers | Resolved handles must not embed plaintext secrets in entry list types (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | CatalogEntry list payloads | mitigate | Exclude password/apiKey fields from list DTOs; metadata only |
| T-06-02 | Spoofing | resolve(name, kind) | mitigate | Require kind match at resolve; actionable IllegalArgumentException on mismatch (D-07, D-08) |
| T-06-SC | Tampering | Maven dependency additions | mitigate | slopcheck + human review for new external deps in api module |
</threat_model>

<verification>
- Api module compiles without adapter dependencies
- grep confirms no `spring-` or `kafka-` imports in api sources
</verification>

<success_criteria>
- DS-01 partial: unified abstraction contracts exist for JDBC/Kafka/ES
- All D-01..D-11 type-level decisions reflected in api module
</success_criteria>

## Artifacts this phase produces

- `org.gensokyo.data.datasource.api.ConnectionCatalog`
- `org.gensokyo.data.datasource.api.CatalogEntry`
- `org.gensokyo.data.datasource.api.ConnectionKind`
- `org.gensokyo.data.datasource.api.CatalogEntrySource`
- `org.gensokyo.data.datasource.api.ResolvedConnection` (or kind-specific resolve result types)
- Maven module `data-generator-datasource-api`

<output>
Create `.planning/phases/06-datasource-platform-core/06-01-SUMMARY.md` when done
</output>
