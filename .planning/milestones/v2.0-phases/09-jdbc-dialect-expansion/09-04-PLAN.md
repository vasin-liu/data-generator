---
phase: 09-jdbc-dialect-expansion
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts
  - data-generator-console-web/package.json
  - scripts/verify-phase9-uat-jdbc-dialect.ps1
autonomous: true
requirements:
  - RW-05
  - RW-06
must_haves:
  truths:
    - verify-phase9-uat-jdbc-dialect.ps1 runs Maven dialect test slice with -SkipPlaywright exit 0 (D-16)
    - Playwright covers at least one preset select → form fill → save path, not five live-engine E2Es (D-12)
    - npm script e2e:phase9-jdbc-dialect wired for optional Podman UAT
    - UAT script test list includes SQL builder, validator, preset catalog, connectivity, and dialect IT classes
  artifacts:
    - scripts/verify-phase9-uat-jdbc-dialect.ps1
    - data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts
    - package.json e2e:phase9-jdbc-dialect script entry
  key_links:
    - verify script Invoke-RepoMaven -pl data-generator-service -am -Dtest= comma list mirrors 09-01..09-03 test classes
    - Playwright uses TestIds and /api/datasources/driver-presets like Phase 7/8 UAT specs
---

<objective>
Ship Phase 9 UAT verify script and one Playwright preset E2E path covering console datasource preset workflow (RW-05, RW-06, D-12, D-16).

Purpose: Operators and CI can verify dialect expansion via a single script; console preset UX smoke-tested.
Output: verify-phase9-uat-jdbc-dialect.ps1, jdbc-dialect-preset.spec.ts, npm e2e script.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md
@.planning/phases/09-jdbc-dialect-expansion/09-PATTERNS.md
@.planning/phases/09-jdbc-dialect-expansion/09-VALIDATION.md
@scripts/verify-phase8-uat-rw-streaming-upsert.ps1
@data-generator-console-web/e2e/specs/datasource-managed-crud.spec.ts
@scripts/lib/repo-maven.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-phase9-uat-jdbc-dialect.ps1 Maven slice</name>
  <files>scripts/verify-phase9-uat-jdbc-dialect.ps1</files>
  <read_first>
    - scripts/verify-phase8-uat-rw-streaming-upsert.ps1 (Write-Step, Invoke-RepoMaven, -SkipPlaywright early exit)
    - scripts/lib/repo-maven.ps1
    - .planning/phases/09-jdbc-dialect-expansion/09-PATTERNS.md verify script section
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-16
  </read_first>
  <action>
    Create scripts/verify-phase9-uat-jdbc-dialect.ps1 mirroring Phase 8 structure per D-16.
    Parameters: -SkipBuild, -SkipPlaywright, -KeepContainer, -ImageTag, -ContainerName, -HostPort, -SpringProfiles (defaults matching Phase 8 Podman pattern).
    Maven slice (always run first): Invoke-RepoMaven -RepoRoot -pl data-generator-service -am with -Dtest comma list:
    JdbcSinkSqlBuilderTests, ChunkedPipelinePostgresUpsertTests, ClickHouseInsertBulkWriterIntegrationTests, ChunkedPipelineKingbaseDialectTests, TemplateV2ValidatorTests, JdbcDriverPresetCatalogTests, ConsoleDataSourceControllerTest, ConnectionCatalogTestTests
    Use -Dsurefire.failIfNoSpecifiedTests=false.
    When -SkipPlaywright: exit 0 after Maven success (CI merge-friendly gate).
    When Playwright enabled: package service if needed, Podman/local health wait on /healthz, set DG_E2E_BASE_URL and DG_E2E_API_URL, run npm run e2e:phase9-jdbc-dialect from data-generator-console-web.
    Print step banners with Write-Step; throw on non-zero exit codes.
  </action>
  <verify>
    <automated>powershell -NoProfile -File scripts/verify-phase9-uat-jdbc-dialect.ps1 -SkipPlaywright</automated>
  </verify>
  <acceptance_criteria>
    - Script exists at scripts/verify-phase9-uat-jdbc-dialect.ps1
    - -SkipPlaywright runs Maven dialect slice and exits 0 when implementation complete
    - Script header documents Phase 9 scope and -SkipPlaywright CI usage
    - Test class list matches 09-01 through 09-03 deliverables
  </acceptance_criteria>
  <done>Phase 9 UAT verify script runs embedded Maven dialect gate successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright preset E2E and npm script wiring</name>
  <files>data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts, data-generator-console-web/package.json</files>
  <read_first>
    - data-generator-console-web/e2e/specs/datasource-managed-crud.spec.ts (preset dialog, save flow)
    - data-generator-console-web/e2e/support/testIds.ts (TestIds.nav.datasources, actions)
    - data-generator-console-web/package.json (e2e:phase8-rw-streaming-upsert script pattern)
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-12
  </read_first>
  <action>
    Create e2e/specs/jdbc-dialect-preset.spec.ts with one test: navigate to Datasources, click New, select one preset from grouped dropdown (prefer dm8 or postgresql16), assert driverClassName and url fields auto-populate from /api/datasources/driver-presets response, fill name/username/password with H2 or unreachable test values, submit save, expect POST /api/datasources success true (D-12 — one path only, not five engine live tests).
    Reuse gotoConsoleHome, navigateViaTopNav, TestIds patterns from datasource-managed-crud.spec.ts.
    Add npm script e2e:phase9-jdbc-dialect in package.json pointing to playwright test jdbc-dialect-preset.spec.ts (mirror Phase 8 e2e script naming).
    Wire verify script Playwright branch to invoke this npm script when -SkipPlaywright not set.
    Do not add dialect capability-hint UI tests (deferred).
  </action>
  <verify>
    <automated>cd data-generator-console-web; npx tsc -p tsconfig.json --noEmit; npm run build; npm run e2e:phase9-jdbc-dialect -- --list</automated>
    <manual>Per .cursor/rules/console-verify.mdc: task verify includes `tsc --noEmit` + `npm run build` (automated above). Full Playwright against running service: `.\scripts\verify-phase9-uat-jdbc-dialect.ps1` without `-SkipPlaywright`; broader console regression via `.\scripts\verify-console.ps1` when touching shared E2E helpers.</manual>
  </verify>
  <acceptance_criteria>
    - jdbc-dialect-preset.spec.ts exists with preset selection and save assertion
    - package.json contains e2e:phase9-jdbc-dialect script
    - Playwright --list finds the spec (does not require live Podman for list check)
    - Test selects preset and verifies auto-fill behavior before save
    - Console-web `npx tsc -p tsconfig.json --noEmit` and `npm run build` exit 0 before E2E list check (console-verify.mdc minimum)
  </acceptance_criteria>
  <done>One Playwright preset path exists and npm script registered; console-web build green; full UAT via verify-phase9-uat-jdbc-dialect.ps1 without -SkipPlaywright.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright E2E → console API | E2E uses test credentials only |
| verify script → Maven/npm | Script invokes repo-local build tools only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-11 | Information Disclosure | Playwright test fixtures | medium | mitigate | Use synthetic passwords in E2E; never commit production JDBC URLs |
| T-09-12 | Tampering | E2E save without connectivity | low | accept | Save path validates API envelope; connectivity test is separate operator action per D-09 |
</threat_model>

<verification>
- verify-phase9-uat-jdbc-dialect.ps1 -SkipPlaywright exits 0
- e2e:phase9-jdbc-dialect script registered and lists spec
- Console-web tsc + build pass per console-verify.mdc before Playwright list/run
- Full Playwright UAT: verify-phase9-uat-jdbc-dialect.ps1 without -SkipPlaywright; optional broader console via verify-console.ps1
</verification>

<success_criteria>
- D-12 and D-16 satisfied: API/unit coverage via Maven slice; one Playwright preset path
- RW-05 and RW-06 end-to-end verification entry point available
</success_criteria>

<output>
Create `.planning/phases/09-jdbc-dialect-expansion/09-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `scripts/verify-phase9-uat-jdbc-dialect.ps1`
- `data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts`
- `data-generator-console-web/package.json` e2e:phase9-jdbc-dialect entry
