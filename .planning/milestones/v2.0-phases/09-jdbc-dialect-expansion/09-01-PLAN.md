---
phase: 09-jdbc-dialect-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java
  - data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2Validator.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java
  - data-generator-service/src/test/java/org/gensokyo/data/template/TemplateV2ValidatorTests.java
autonomous: true
requirements:
  - RW-05
must_haves:
  truths:
    - Sink YAML must set explicit options.dialect for Phase 9 engines; SQL generation uses dialect key only, not JDBC URL (D-05, D-07)
    - kingbase and highgo dialect keys map internally to PostgreSQL ON CONFLICT upsert SQL (D-01, D-06)
    - dameng dialect generates MERGE INTO upsert from options.upsert and options.upsertKeys (D-02)
    - clickhouse and generic dialects reject upsert=true at publish and run with same severity (D-03, D-04, D-08)
    - Missing or invalid upsertKeys fail-fast before JDBC execute (D-04)
  artifacts:
    - org.gensokyo.data.calcite.sink.JdbcSinkSqlBuilder with dameng/kingbase/highgo/clickhouse/generic branches
    - org.gensokyo.data.calcite.sink.JdbcBulkWriteExecutor with extended countUpsertedRows for new dialects
    - org.gensokyo.data.template.TemplateV2Validator publish gate for dialect+upsert combinations
    - JdbcSinkSqlBuilderTests and TemplateV2ValidatorTests covering Phase 9 dialect matrix
  key_links:
    - JdbcSinkSqlBuilder.appendUpsertClause must stay aligned with TemplateV2Validator.validateJdbcUpsertOptions dialect rules (D-04)
    - WriterOptionResolver.stringOption(writer, dialect) is source of truth; no URL-family comparison in SQL builder (D-07)
---

<objective>
Extend V2 JDBC sink SQL generation and publish validation for Dameng MERGE, Kingbase/HighGo ON CONFLICT (PG path), ClickHouse upsert rejection, and generic+upsert fail-fast (RW-05, D-01–D-08).

Purpose: Operators get dialect-correct INSERT/upsert SQL without silent degradation.
Output: Updated JdbcSinkSqlBuilder, JdbcBulkWriteExecutor, TemplateV2Validator, and unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md
@.planning/phases/09-jdbc-dialect-expansion/09-PATTERNS.md
@.planning/phases/09-jdbc-dialect-expansion/09-VALIDATION.md
@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java
@data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2Validator.java
@docs/template-v2-jdbc-sink-guide.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend JdbcSinkSqlBuilder for five-engine dialect upsert matrix</name>
  <files>data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java, data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java</files>
  <read_first>
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java (appendUpsertClause, appendPostgresUpsert, resolveDialect)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java (postgres/mysql/clickhouse cases)
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-01, D-02, D-03, D-05, D-06, D-07, D-08
    - .planning/phases/09-jdbc-dialect-expansion/09-PATTERNS.md JdbcSinkSqlBuilder section
  </read_first>
  <behavior>
    - kingbase dialect with upsert=true produces SQL containing on conflict (id) do update set (D-01, D-06)
    - highgo dialect with upsert=true produces same ON CONFLICT path as kingbase (D-01, D-06)
    - dameng dialect with upsert=true produces MERGE INTO with upsertKeys in ON clause (D-02)
    - clickhouse dialect with upsert=true throws IllegalArgumentException mentioning clickhouse (D-03)
    - generic dialect with upsert=true throws IllegalArgumentException; no silent ignore (D-08)
    - resolveDialect uses options.dialect only; no JDBC URL comparison (D-07)
  </behavior>
  <action>
    Add unit tests first in JdbcSinkSqlBuilderTests: buildsKingbaseUsesOnConflictPath, buildsHighgoUsesOnConflictPath, buildsDamengMergeInto, genericDialectUpsertFailsFast (mirror existing clickhouseUpsertIsUnsupported pattern).
    Extend appendUpsertClause switch per D-01/D-06: case kingbase, highgo delegate to appendPostgresUpsert unchanged.
    Implement appendDamengMerge per D-02: MERGE INTO target USING (VALUES ...) AS src ON (key match from upsertKeys) WHEN MATCHED THEN UPDATE non-key columns WHEN NOT MATCHED THEN INSERT; use requireUpsertKeys validation already present.
    Keep clickhouse/click_house branch throwing unsupportedUpsertDialect (D-03).
    Tighten default branch: when upsert=true and dialect is generic or unknown, throw unsupportedUpsertDialect — do not fall through to plain INSERT (D-08).
    Preserve explicit options.dialect requirement in Javadoc; resolveDialect remains YAML source of truth without URL mismatch hard-fail (D-05, D-07).
    Follow java-copyright-class-javadoc.mdc for any new private methods (copyright, Javadoc, inline comments on MERGE bind semantics).
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcSinkSqlBuilderTests -Dsurefire.failIfNoSpecifiedTests=false -q</automated>
  </verify>
  <acceptance_criteria>
    - Kingbase and HighGo upsert SQL contains on conflict (id) do update set for fixture columns
    - Dameng upsert SQL contains MERGE INTO and references upsertKeys columns
    - ClickHouse and generic upsert=true throw with dialect name in message
    - No new URL-based dialect inference in JdbcSinkSqlBuilder
    - Maven JdbcSinkSqlBuilderTests exits 0
  </acceptance_criteria>
  <done>JdbcSinkSqlBuilder generates dialect-correct upsert SQL for dameng/kingbase/highgo and fail-fast rejects for clickhouse/generic upsert combinations.</done>
</task>

<task type="auto">
  <name>Task 2: Wire runtime upsert metrics and publish-time dialect validation</name>
  <files>data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java, data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2Validator.java, data-generator-service/src/test/java/org/gensokyo/data/template/TemplateV2ValidatorTests.java</files>
  <read_first>
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java (writeJdbcBatch, countUpsertedRows, writeClickHouseInsert)
    - data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2Validator.java (validateJdbcUpsertOptions)
    - data-generator-service/src/test/java/org/gensokyo/data/template/TemplateV2ValidatorTests.java (upsertMissingKeysThrows)
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-03, D-04, D-08
  </read_first>
  <action>
    Extend countUpsertedRows in JdbcBulkWriteExecutor so kingbase, highgo, and dameng follow the PostgreSQL branch (updateCount greater than 0 counts as upserted); keep MySQL equals-2 rule unchanged.
    Ensure writeClickHouseInsert and bulk paths still reject upsert=true at runtime with message aligned to JdbcSinkSqlBuilder (D-03).
    In TemplateV2Validator.validateJdbcUpsertOptions, after upsertKeys checks, read options.dialect (default generic) and fail publish for: clickhouse+upsert, generic+upsert, and any dialect JdbcSinkSqlBuilder would reject — mirror run-time messages and writerPath prefix (D-04, D-08).
    Add validator tests: clickhouseUpsertRejectedAtPublish, genericDialectUpsertRejectedAtPublish, damengUpsertAllowedWhenKeysPresent (positive path per D-02).
    Draft save warnings via collectWarnings unchanged; publish and run remain blocking (D-04).
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=TemplateV2ValidatorTests,JdbcSinkSqlBuilderTests -Dsurefire.failIfNoSpecifiedTests=false -q</automated>
    <manual>countUpsertedRows for kingbase/highgo/dameng (PostgreSQL-branch updateCount rule) is integration-proven in 09-03 Task 1 via UpsertParitySupport rowsUpserted &gt; 0 assertions in ChunkedPipelineKingbaseDialectTests — execute 09-03 verify after this task to close the metric loop.</manual>
  </verify>
  <acceptance_criteria>
    - Publish of template with dialect clickhouse and upsert true throws containing sink path and clickhouse
    - Publish of template with dialect generic and upsert true throws; no warn-only path
    - Dameng upsert template with valid upsertKeys passes publish validation
    - Dual fail-fast: same dialect+upsert rules enforced in validator and JdbcSinkSqlBuilder
    - Maven TemplateV2ValidatorTests and JdbcSinkSqlBuilderTests exit 0
    - countUpsertedRows kingbase/highgo/dameng path covered by cross-plan proof: 09-03 UpsertParitySupport asserts rowsUpserted &gt; 0 for dialect kingbase and highgo against PG container (dameng uses MERGE unit tests in JdbcSinkSqlBuilderTests; optional DM IT in 09-03)
  </acceptance_criteria>
  <done>Publish and run both block unsupported dialect+upsert combinations; Dameng upsert passes publish when keys valid; countUpsertedRows extended for kingbase/highgo/dameng with metric proof delegated to 09-03 UpsertParitySupport rowsUpserted assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template YAML → TemplateV2Validator | Untrusted operator sink options cross publish gate |
| Template runtime → JdbcBulkWriteExecutor | Validated template still executes JDBC against operator datasource |
| JDBC driver → error messages | Driver exceptions may echo connection strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | TemplateV2Validator | high | mitigate | Dual fail-fast publish+run for unsupported upsert per D-04; no silent INSERT fallback for generic/clickhouse |
| T-09-02 | Information Disclosure | JdbcBulkWriteExecutor / driver errors | high | mitigate | Upsert validation messages must not include passwords or full JDBC URLs; defer connectivity redaction to ConnectionConnectivityService (D-11 in 09-02) |
| T-09-03 | Denial of Service | MERGE SQL generation | medium | mitigate | requireUpsertKeys validation before batch execute; invalid keys fail fast with column list |
| T-09-04 | Elevation | dialect generic bypass | high | mitigate | generic+upsert publish and run both throw per D-08 |
</threat_model>

<verification>
- JdbcSinkSqlBuilderTests covers all five dialect upsert outcomes (support, MERGE, reject)
- TemplateV2ValidatorTests covers publish rejections mirroring run-time rules
- No silent upsert ignore for generic or clickhouse dialects
- countUpsertedRows kingbase/highgo integration proof linked to 09-03 UpsertParitySupport rowsUpserted assertions
</verification>

<success_criteria>
- RW-05: V2 JDBC sink generates dialect-appropriate INSERT/upsert for dameng, kingbase, highgo, postgres (existing), clickhouse insert-only with documented upsert reject
- D-01 through D-08 implemented in SQL builder and validator layers
</success_criteria>

<output>
Create `.planning/phases/09-jdbc-dialect-expansion/09-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Updated `org.gensokyo.data.calcite.sink.JdbcSinkSqlBuilder` (dameng MERGE, kingbase/highgo ON CONFLICT, fail-fast matrix)
- Updated `org.gensokyo.data.calcite.sink.JdbcBulkWriteExecutor` (upsert metrics for new dialects)
- Updated `org.gensokyo.data.template.TemplateV2Validator` (publish dialect+upsert gate)
- Extended `JdbcSinkSqlBuilderTests` and `TemplateV2ValidatorTests`
