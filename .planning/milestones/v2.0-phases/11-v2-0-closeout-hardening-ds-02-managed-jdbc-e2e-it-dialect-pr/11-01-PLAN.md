---
phase: 11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogSinkE2eIT.java
autonomous: true
requirements:
  - DS-02
must_haves:
  truths:
    - Dedicated @SpringBootTest ManagedJdbcCatalogSinkE2eIT exists and does not extend V2ScenarioTemplateIT (D-01)
    - Managed JDBC datasource is created via DataSourceConfigService.save (D-02)
    - Template JDBC sink uses managed dataSourceId only; writer has no inline dataSource block (D-03, D-07)
    - Spring config is classpath:/application-phase7-test.yaml with connectivity-before-save left off (D-04)
    - Run uses autowired TemplateV2Runner.run — no HTTP /api/templates/.../run, /task/run, TaskExecutionService.queueExecution, or WorkflowRunContext.bind (D-05)
    - Assertion uses COUNT(*) on the sink table through the managed catalog id (D-06)
    - Sink write mode is plain INSERT — no upsert / upsertKeys on JdbcWriterVO (D-08)
  artifacts:
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogSinkE2eIT.java
  key_links:
    - DataSourceConfigService.save → catalog.reload → HotReloadCoordinator.reloadJdbc → DynamicRoutingDataSource pool for managedName
    - JdbcRowSinkAdapter pushes resolveSinkDataSourceId(writer) onto DynamicDataSourceContextHolder
    - Unbound TemplateV2Runner keeps logical catalog name (not snap:{instanceId}:{name})
---

<objective>
Close DS-02 proof-depth gap (audit flow #1 / ROADMAP SC1) with a dedicated managed-catalog JDBC sink E2E IT.

Purpose: Prove a managed `dataSourceId` (not inline `InlineDataSourceVO`) reaches a V2 JDBC sink write and row counts — no new product capability.
Output: `ManagedJdbcCatalogSinkE2eIT` green under JDK 25 Maven surefire.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-RESEARCH.md
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-VALIDATION.md
@.planning/ROADMAP.md
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java
@data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java
@data-generator-service/src/test/resources/application-phase7-test.yaml
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/source/InlineRowsSourceTests.java
</context>

## Artifacts this phase produces

*(Phase-wide — this plan delivers the Wave 1 item)*

- `ManagedJdbcCatalogSinkE2eIT` — new `@SpringBootTest` class under `org.gensokyo.data.datasource.catalog`
- Private helpers in that class: `resetDatasourceRow`, `countRows`, `buildManagedSinkTemplate` (names discretionary if behavior matches)
- Constants: managed DS name (e.g. `managed-jdbc-catalog-sink-e2e-ds`), sink table (e.g. `managed_e2e_sink`), H2 mem URL unique to this IT
- *(Later waves)* `scripts/verify-phase11-uat-closeout-hardening.ps1`, Playwright `kingbase8` coverage, `AGENTS.md` + `v2.0-MILESTONE-AUDIT.md` surgical updates

<tasks>

<task type="auto">
  <name>Task 1: Add ManagedJdbcCatalogSinkE2eIT (managed save → TemplateV2Runner → COUNT(*))</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogSinkE2eIT.java</files>
  <read_first>
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md (D-01–D-08 — NON-NEGOTIABLE)
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-RESEARCH.md (Track A)
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java (`@SpringBootTest` bootstrap, `resetDatasourceRow`, `dataSourceConfigService.save`)
    - data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java (`countRows` at ~318–327 — copy pattern only; do NOT extend this class)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/source/InlineRowsSourceTests.java (InlineRowsSourceVO + SQL transform shape)
    - data-generator-service/src/test/resources/application-phase7-test.yaml (`require-connectivity-test-before-save: false`)
    - data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/model/vo/writer/JdbcWriterVO.java (confirm `dataSource` field must stay unset)
    - .cursor/rules/java-copyright-class-javadoc.mdc
  </read_first>
  <action>
    Create new class `org.gensokyo.data.datasource.catalog.ManagedJdbcCatalogSinkE2eIT` (package alongside `JdbcSnapshotExecutePathIT`).

    File layout (mandatory):
    1. PCI copyright block (exact company text from java-copyright rule).
    2. package + imports.
    3. Type Javadoc: short description that this closes DS-02 managed-catalog→sink-rows proof (audit flow #1); `@author Gensokyo`; `@version 3.0.0-SNAPSHOT`; `@since 2026-07-25` (or execute date).
    4. Public members: Javadoc on every public constructor/method if any are public (prefer package-visible class + `@Test` methods with brief Javadoc like `JdbcSnapshotExecutePathIT`).

    Bootstrap (D-01, D-04):
    - `@SpringBootTest(classes = DataGeneratorApplication.class, properties = "spring.config.location=classpath:/application-phase7-test.yaml")`
    - Do **not** extend `V2ScenarioTemplateIT`.
    - Do **not** annotate the write/`COUNT(*)` test with `@Transactional` (avoid metadata-DS transaction wrapping; follow `V2ScenarioTemplateIT` runner path).

    Autowire at minimum:
    - `DataSourceConfigService`
    - `DataSourceConfigRepository`
    - `DynamicRoutingDataSource`
    - `TemplateV2Runner`
    - `NamedParameterJdbcTemplate` (or `JdbcTemplate` via that bean — same as scenario IT)

    Constants (concrete values required; adjust only if collision):
    - `DS_NAME = "managed-jdbc-catalog-sink-e2e-ds"`
    - `TABLE = "managed_e2e_sink"`
    - H2 URL: `jdbc:h2:mem:managed-jdbc-catalog-sink-e2e;MODE=MySQL;DB_CLOSE_DELAY=-1` (or equivalent unique mem name)

    `@BeforeEach resetDatasourceRow` (copy from `JdbcSnapshotExecutePathIT` lines 82–88):
    - `dataSourceConfigRepository.findById(DS_NAME).ifPresent(dataSourceConfigRepository::delete)`
    - if `dynamicRoutingDataSource.getDataSources().containsKey(DS_NAME)` then `removeDataSource(DS_NAME)`

    Single `@Test` method (name discretionary, e.g. `managedCatalogSinkInsert_writesRowsCountableOnManagedPool`):
    1. **Save managed DS (D-02):** `dataSourceConfigService.save(DS_NAME, h2Url, "sa", "", null, "org.h2.Driver", null, null)` — do **not** call `registerToRuntime` alone; do **not** require a prior connectivity-test fingerprint (D-04).
    2. **DDL on managed pool:** `DynamicDataSourceContextHolder.push(DS_NAME)` then execute `create table managed_e2e_sink (id int primary key, label varchar(64))` (column names must match inline rows); `clear()` in finally.
    3. **Build TemplateV2VO in Java** (no YAML scenario file):
       - Source: `InlineRowsSourceVO` with ≥2 rows, e.g. `{id:1,label:"a"}`, `{id:2,label:"b"}` — **no** managed `dataSourceId` on source (D-03).
       - Transform: `SqlTransformVO` selecting those columns from the inline source alias (e.g. `SELECT id, label FROM seed`).
       - Sink: `WriteStageVO` with one `JdbcWriterVO`:
         - `writer.setDataSourceId(DS_NAME)`
         - `writer.setTarget(TABLE)`
         - **Do not** call `writer.setDataSource(...)` (D-07)
         - **Do not** set upsert / upsertKeys / merge options (D-08 plain INSERT)
    4. **Run (D-05):** `TemplateV2RunResult result = templateV2Runner.run(template)` with **no** `WorkflowRunContext.bind`, **no** `TaskExecutionService.queueExecution`, **no** HTTP run.
    5. **Assert (D-06):** private `countRows(DS_NAME, TABLE)` copied from `V2ScenarioTemplateIT` spirit — `DynamicDataSourceContextHolder.push` + `select count(*) from managed_e2e_sink` + `clear()`; expect count equal to seeded inline row count (e.g. `2L`). Optionally also assert `result.getMetrics().getRowsWritten()` if metrics expose that field and are stable; COUNT(*) is the required proof.

    Inline `//` comments only for non-obvious invariants (e.g. why WorkflowRunContext must stay unbound; why sink must omit inline dataSource).

    Out of scope for this file: Dameng, Playwright, dual-resolver edits, P0 matrix, HTTP run path.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am "-Dtest=ManagedJdbcCatalogSinkE2eIT" -Dsurefire.failIfNoSpecifiedTests=false test</automated>
  </verify>
  <acceptance_criteria>
    - Class `ManagedJdbcCatalogSinkE2eIT` exists under `org.gensokyo.data.datasource.catalog` and does not extend `V2ScenarioTemplateIT` (D-01)
    - Test calls `DataSourceConfigService.save` with H2 mem URL and `org.h2.Driver` (D-02)
    - Sink `JdbcWriterVO` has non-blank `dataSourceId` equal to `DS_NAME` and `getDataSource()` is null / unset (D-03, D-07)
    - Bootstrap uses `classpath:/application-phase7-test.yaml` without overriding `require-connectivity-test-before-save` to true (D-04)
    - Test body invokes `templateV2Runner.run` and never binds `WorkflowRunContext` or queues a task (D-05)
    - Assertion includes `COUNT(*)` on `managed_e2e_sink` via managed id (D-06)
    - No upsert configuration on the writer (D-08)
    - PCI copyright + type Javadoc present; Maven command above exits 0 (BUILD SUCCESS)
  </acceptance_criteria>
  <done>DS-02 managed JDBC catalog → V2 sink INSERT → COUNT(*) proof IT is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IT → managed catalog | Creates disposable H2 catalog rows; must not embed production credentials |
| IT → DynamicRoutingDataSource | Must clean up DS_NAME in `@BeforeEach` to avoid pool leaks across tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure | ManagedJdbcCatalogSinkE2eIT | medium | mitigate | Use H2 mem URL + empty password `""`; never assert or log secrets |
| T-11-02 | Tampering | Resolve path confusion | high | mitigate | Forbid WorkflowRunContext.bind so resolve stays logical name; assert COUNT on DS_NAME |
| T-11-03 | Elevation of Privilege | Governance bypass | low | accept | Connectivity-before-save intentionally off in phase7-test (D-04); production gate unchanged |
</threat_model>

<verification>
- `.\mvnw-jdk25.ps1 -pl data-generator-service -am "-Dtest=ManagedJdbcCatalogSinkE2eIT" -Dsurefire.failIfNoSpecifiedTests=false test` → BUILD SUCCESS
- Source review: sink has managed id only; no inline `dataSource`; no upsert; no snap-context bind
</verification>

<success_criteria>
- ROADMAP SC1 satisfied under CONTEXT override (managed id on **sink only**)
- DS-02 proof depth: managed catalog → in-process V2 run → sink rows
- Ready for Wave 2 UAT script to include this class in `-Dtest=` list
</success_criteria>

<output>
Create `.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-01-SUMMARY.md` when done
</output>
