---
phase: 11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - AGENTS.md
  - .planning/v2.0-MILESTONE-AUDIT.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DS-02
  - RW-05
  - RW-06
must_haves:
  truths:
    - AGENTS.md lists .\scripts\verify-phase11-uat-closeout-hardening.ps1 -SkipPlaywright as supplementary UAT (D-18)
    - v2.0-MILESTONE-AUDIT.md flows #1 and #8 updated surgically with evidence pointers; disposition moves PARTIAL → OK (or documents remaining accepted limits) (D-20, ROADMAP SC3)
    - Audit may keep overall status tech_debt if Dameng/Nyquist items remain (D-20)
    - P0 / verify-harness.ps1 / test-matrix.yaml still untouched (D-17)
    - ROADMAP Phase 11 section lists plans 11-01..11-03 and verification command
  artifacts:
    - AGENTS.md phase11 UAT command entry
    - .planning/v2.0-MILESTONE-AUDIT.md flow #1 and #8 surgical updates
    - .planning/ROADMAP.md Phase 11 plan registry
  key_links:
    - Flow #1 evidence → ManagedJdbcCatalogSinkE2eIT (in-process TemplateV2Runner; not HTTP task)
    - Flow #8 evidence pack → jdbc-dialect-preset.spec.ts kingbase8 + ConnectionCatalogTestTests + ChunkedPipelineKingbaseDialectTests + verify-phase11 script
---

<objective>
Close ROADMAP SC3 / D-18 / D-20: register the phase11 UAT command in AGENTS.md and surgically update milestone audit flows #1 and #8 after evidence from plans 11-01 and 11-02.

Purpose: Archive-ready traced evidence pointers; no product code; no full `/gsd-audit-milestone` rewrite.
Output: Updated `AGENTS.md`, surgical `v2.0-MILESTONE-AUDIT.md`, ROADMAP Phase 11 plan registry.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-RESEARCH.md
@.planning/v2.0-MILESTONE-AUDIT.md
@AGENTS.md
@.planning/ROADMAP.md
@scripts/verify-phase11-uat-closeout-hardening.ps1
</context>

## Artifacts this phase produces

*(Phase-wide — this plan delivers Wave 3 items)*

- `AGENTS.md` Commands entry for `verify-phase11-uat-closeout-hardening.ps1`
- Surgical disposition updates for audit flows #1 and #8 (plus matching tech_debt bullet disposition text)
- ROADMAP Phase 11 plans/waves/verification registry
- No new Java symbols; no P0 matrix edits

<tasks>

<task type="auto">
  <name>Task 1: Register phase11 UAT command in AGENTS.md</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (Phase 8/9 verify entries ~lines 86–90 and supplementary UAT note ~168)
    - scripts/verify-phase11-uat-closeout-hardening.ps1 (confirm path + -SkipPlaywright from plan 11-02)
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md (D-17, D-18)
  </read_first>
  <action>
    In `AGENTS.md` Commands section, immediately after the Phase 9 verify block, add:

    ```
    # Phase 11 v2.0 closeout hardening UAT (managed JDBC E2E IT + Kingbase dialect evidence pack; optional Podman Playwright)
    .\scripts\verify-phase11-uat-closeout-hardening.ps1 -SkipPlaywright
    ```

    Update the existing supplementary-UAT sentence (~line 168) that currently names only Phase 8/9 scripts so it also names `verify-phase11-uat-closeout-hardening.ps1`, and keep the statement that these are **supplementary UAT** / **not** the merge gate — canonical merge gate remains `.\scripts\verify-harness.ps1` (D-17, D-18).

    Do not remove or rewrite Phase 8/9 entries. Do not add Dameng live-CI instructions as mandatory.
  </action>
  <verify>
    <automated>rg -n "verify-phase11-uat-closeout-hardening" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains `.\scripts\verify-phase11-uat-closeout-hardening.ps1 -SkipPlaywright` (D-18)
    - Supplementary-UAT note still states phase11 script is not the P0 merge gate (D-17)
    - Phase 8/9 verify entries remain intact
  </acceptance_criteria>
  <done>Operators and agents can find the Phase 11 UAT entry point in AGENTS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Surgical audit flows #1/#8 + ROADMAP Phase 11 registry</name>
  <files>.planning/v2.0-MILESTONE-AUDIT.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/v2.0-MILESTONE-AUDIT.md (flow table ~119–126; tech_debt bullets ~135–136; executive summary PARTIAL wording)
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md (D-20, D-03, D-05, D-13 — CONTEXT overrides ROADMAP SC wording)
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-01-SUMMARY.md (if present — evidence class name)
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-02-SUMMARY.md (if present — script + Playwright)
    - .planning/ROADMAP.md Phase 11 section (already registers 11-01..11-03 + verify-phase11 command — confirm/refresh, do not treat as TBD)
  </read_first>
  <action>
    **A. Surgical audit update (D-20) — edit only what SC3 needs:**

    Flow table:
    1. Flow #1 `Managed JDBC DS → template run → rows` — change Status from PARTIAL to **OK**. Evidence pointer must cite `ManagedJdbcCatalogSinkE2eIT`: managed `DataSourceConfigService.save` → in-process `TemplateV2Runner` → sink `COUNT(*)` on managed id. Explicitly note accepted limit: primary proof is **in-process runner**, not HTTP `/api/.../run` or `/task/run` (CONTEXT D-05 overrides older “task” wording).
    2. Flow #8 `Dialect preset + upsert (≥1 dialect)` — change Status from PARTIAL to **OK**. Evidence pack pointer:
       - Playwright `jdbc-dialect-preset.spec.ts` covers `postgresql16` **and** `kingbase8`
       - Maven `ConnectionCatalogTestTests.jdbcDraftTest_kingbaseDriverFailureIsActionableWithoutSecrets`
       - Maven `ChunkedPipelineKingbaseDialectTests` (PG-proxy upsert; Docker-gated)
       - UAT entry `scripts/verify-phase11-uat-closeout-hardening.ps1`
       Document accepted limits: Dameng remains MERGE-unit / optional `-Ddm.it=true` (unchanged this phase, D-12); not a single-JVM Playwright Test Connection → live KB upsert chain (D-11, D-13).

    Also update:
    - Executive summary / Flows count text that still says “2 PARTIAL” / “7/9” so it matches the new OK dispositions (e.g. 9/9 OK for E2E flows, or “9/9 OK with documented accepted limits”).
    - Prior-blocker tech_debt rows for managed-catalog→rows and dialect preset→upsert: mark **CLOSED** via Phase 11 evidence (or equivalent closed wording), while allowing overall audit `status: tech_debt` to remain if Dameng/Nyquist / VALIDATION hygiene items stay listed (D-20).

    Do **not** run a full `/gsd-audit-milestone` rewrite. Do **not** invent green Dameng live-CI claims.

    **B. ROADMAP Phase 11 registry (confirm/refresh — already planned):**
    ROADMAP already lists Plans 11-01..11-03, waves, and `verify-phase11-uat-closeout-hardening.ps1`. Confirm those entries remain accurate after execute; refresh only if drift (e.g. checkbox completion when plans finish, wording sync with SUMMARY evidence). Do **not** replace a TBD block — none exists for Phase 11 plans.
    Expected registry shape (must stay present):
    - Plans: 3 plans in 3 waves — `11-01` ManagedJdbcCatalogSinkE2eIT; `11-02` Playwright kingbase8 + phase11 UAT script; `11-03` AGENTS + surgical audit
    - Waves: Wave 1 `11-01`; Wave 2 `11-02` (depends on 11-01); Wave 3 `11-03` (depends on 11-01, 11-02)
    - Verification: `.\scripts\verify-phase11-uat-closeout-hardening.ps1 -SkipPlaywright`

    Fence: do not edit `test-matrix.yaml` or `verify-harness.ps1` (D-17).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command '& { $fail = $false; foreach ($c in @(@{p="ManagedJdbcCatalogSinkE2eIT";f=".planning/v2.0-MILESTONE-AUDIT.md"},@{p="verify-phase11-uat-closeout-hardening";f=".planning/v2.0-MILESTONE-AUDIT.md"},@{p="kingbase8";f=".planning/v2.0-MILESTONE-AUDIT.md"},@{p="11-01";f=".planning/ROADMAP.md"},@{p="11-02";f=".planning/ROADMAP.md"},@{p="11-03";f=".planning/ROADMAP.md"},@{p="verify-phase11-uat-closeout-hardening";f=".planning/ROADMAP.md"})) { rg -n $c.p $c.f; if ($LASTEXITCODE -ne 0) { Write-Error ("missing evidence: " + $c.p); $fail = $true } }; rg -q "PARTIAL \(resolve OK|PARTIAL \(pieces exist" ".planning/v2.0-MILESTONE-AUDIT.md"; if ($LASTEXITCODE -eq 0) { Write-Error "residual PARTIAL flow wording for flows #1/#8"; $fail = $true }; if ($fail) { exit 1 } else { exit 0 } }'</automated>
  </verify>
  <acceptance_criteria>
    - Audit flow #1 Status is OK and cites `ManagedJdbcCatalogSinkE2eIT` + in-process runner accepted limit (D-20, D-05)
    - Audit flow #8 Status is OK and cites kingbase8 Playwright + ConnectionCatalogTestTests + ChunkedPipelineKingbaseDialectTests + phase11 verify script; Dameng accepted limit documented (D-20, D-12, D-13)
    - No remaining `PARTIAL (resolve OK` / `PARTIAL (pieces exist` strings for flows #1/#8
    - AGENTS already updated in Task 1; ROADMAP Phase 11 lists 11-01..11-03 and verification command
    - Diff does not include `.planning/test-matrix.yaml` or `scripts/verify-harness.ps1` (D-17)
  </acceptance_criteria>
  <done>SC3 closeout: docs/audit point at Phase 11 evidence; ROADMAP registry ready for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit doc → milestone archive | Disposition claims must match real tests — no overclaim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Repudiation | Audit OK without evidence | high | mitigate | Require concrete class/script/spec pointers in flow Status notes (D-20) |
| T-11-08 | Tampering | Accidental full audit rewrite | medium | mitigate | Surgical edits to flows #1/#8 + related bullets only; overall status may stay tech_debt |
</threat_model>

<verification>
- `rg -n "verify-phase11-uat-closeout-hardening" AGENTS.md` matches
- Audit flows #1 and #8 are OK with evidence pointers; PARTIAL wording for those two flows gone
- ROADMAP Phase 11 lists three plans and verify command
- Optional smoke: `.\scripts\verify-phase11-uat-closeout-hardening.ps1 -SkipPlaywright` still exit 0
</verification>

<success_criteria>
- ROADMAP SC3 satisfied: audit disposition for flows #1/#8 moved toward OK with evidence
- D-17/D-18/D-20 honored
- DS-02, RW-05, RW-06 all appear in this plan's requirements field (docs closeout traces all three)
</success_criteria>

<output>
Create `.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-03-SUMMARY.md` when done
</output>
