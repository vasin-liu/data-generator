---
phase: 11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts
  - scripts/verify-phase11-uat-closeout-hardening.ps1
autonomous: true
requirements:
  - RW-05
  - RW-06
must_haves:
  truths:
    - Primary non-PG dialect proof is Kingbase/HighGo (PG-proxy + dialect keys), not Dameng live / not ClickHouse (D-09)
    - Playwright covers kingbase8 preset select → form fill → POST /api/datasources success AND keeps postgresql16 (D-10, D-15)
    - Playwright does not click Test Connection; connectivity evidence is ConnectionCatalogTestTests kingbase failure path (D-11)
    - No Dameng production/test code changes (D-12)
    - Evidence is a three-piece pack + narrative, not a single-JVM preset→test→upsert chain (D-13)
    - scripts/verify-phase11-uat-closeout-hardening.ps1 exists with -SkipPlaywright early exit after Maven (D-14)
    - No new upsert IT class — ChunkedPipelineKingbaseDialectTests reused as-is (D-16)
    - npm script remains e2e:phase9-jdbc-dialect only — no new package.json e2e entry (D-19)
    - P0 / verify-harness.ps1 / test-matrix.yaml untouched (D-17)
  artifacts:
    - data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts (kingbase8 + postgresql16)
    - scripts/verify-phase11-uat-closeout-hardening.ps1
  key_links:
    - Preset id kingbase8 / driver com.kingbase8.Driver / UI /Kingbase 8|金仓 8/i from JdbcDriverPresetCatalog + i18n
    - Maven -Dtest includes ManagedJdbcCatalogSinkE2eIT,ConnectionCatalogTestTests,ChunkedPipelineKingbaseDialectTests via -pl data-generator-service -am
    - Playwright branch invokes npm run e2e:phase9-jdbc-dialect (reuse)
---

<objective>
Close RW-05/RW-06 E2E-depth gap (audit flow #8 / ROADMAP SC2) with a Kingbase evidence pack: Playwright `kingbase8` preset path + phase11 UAT script reusing existing Maven connectivity and upsert ITs.

Purpose: Traced non-PostgreSQL dialect journey evidence without new product features or P0 gate changes.
Output: Extended `jdbc-dialect-preset.spec.ts`, new `verify-phase11-uat-closeout-hardening.ps1` green with `-SkipPlaywright`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-RESEARCH.md
@.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-VALIDATION.md
@scripts/verify-phase9-uat-jdbc-dialect.ps1
@scripts/lib/repo-maven.ps1
@data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts
@data-generator-console-web/package.json
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineKingbaseDialectTests.java
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ConnectionCatalogTestTests.java
</context>

## Artifacts this phase produces

*(Phase-wide — this plan delivers Wave 2 items)*

- Extended Playwright coverage for preset id `kingbase8` (retain `postgresql16`)
- `scripts/verify-phase11-uat-closeout-hardening.ps1` with `-SkipPlaywright`
- Script header narrative linking three evidence pieces: Playwright preset → Maven connectivity → `ChunkedPipelineKingbaseDialectTests` upsert
- No new Java upsert IT; no new npm script; no Dameng edits

<tasks>

<task type="auto">
  <name>Task 1: Expand jdbc-dialect-preset.spec.ts for kingbase8 (keep postgresql16)</name>
  <files>data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts</files>
  <read_first>
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md (D-09–D-12, D-15, D-19)
    - data-generator-console-web/e2e/specs/jdbc-dialect-preset.spec.ts (current PRESET_ID = postgresql16 flow)
    - data-generator-console-web/src/app/datasources/jdbcDriverPresets.ts (kingbase8 id, driverClassName, urlTemplate)
    - data-generator-console-web/src/i18n/locales/en.json (datasources.driver.kingbase8 = Kingbase 8)
    - data-generator-console-web/src/i18n/locales/zh-CN.json (金仓 8)
    - data-generator-console-web/package.json (confirm e2e:phase9-jdbc-dialect already points at this spec — do not add a new script)
  </read_first>
  <action>
    Expand `jdbc-dialect-preset.spec.ts` so both presets are covered (D-10, D-15):

    Preferred shape (discretion: `test.each` OR two `test(...)` blocks — either OK):
    - Cases: `{ id: 'postgresql16', label: /PostgreSQL 16\.x/i }` and `{ id: 'kingbase8', label: /Kingbase 8|金仓 8/i }`
    - For each case: GET `/api/datasources/driver-presets`, find preset by `id`, navigate Datasources → New, select preset via Ant Design dropdown using that case's label regex, assert driver + URL inputs equal `preset.driverClassName` and `preset.urlTemplate`, fill username `e2e_user` / password `e2e_test_only`, submit, expect POST `/api/datasources` JSON `success === true`, dialog hidden.

    Hard requirements:
    - Keep existing postgresql16 path behavior (do not delete it).
    - For kingbase8 assert `preset.driverClassName === 'com.kingbase8.Driver'` via API fixture (or at least value equality with catalog row).
    - **Do not** click Test Connection / connectivity UI (D-11).
    - **Do not** assert password fields in API responses.
    - **Do not** add a new npm script in `package.json` (D-19) — reuse `e2e:phase9-jdbc-dialect`.
    - **Do not** edit Dameng specs, Dameng Java, or live Kingbase upsert in Playwright (D-12).

    After edit: run console-web typecheck + build (tsc + vite) as the local compile gate for this file touch.
    **console-verify.mdc substitute (phase closeout):** do **not** run a second `.\scripts\verify-console.ps1` for this change. Phase 11 closeout verification is `.\scripts\verify-phase11-uat-closeout-hardening.ps1` (Maven slice; Playwright via reused `npm run e2e:phase9-jdbc-dialect` when not `-SkipPlaywright`) — see Task 2 and RESEARCH Track B / `console-verify.mdc` note.
  </action>
  <verify>
    <automated>cd data-generator-console-web; npx tsc -p tsconfig.json --noEmit; npm run build; npm run e2e:phase9-jdbc-dialect -- --list</automated>
  </verify>
  <acceptance_criteria>
    - Spec covers both `postgresql16` and `kingbase8` preset ids (D-10, D-15)
    - Kingbase UI selection uses `/Kingbase 8|金仓 8/i` (not PostgreSQL regex)
    - No Test Connection click in the spec (D-11)
    - `package.json` still has exactly the existing `e2e:phase9-jdbc-dialect` entry for this journey — no `e2e:phase11-*` script added (D-19)
    - `npx tsc -p tsconfig.json --noEmit` and `npm run build` exit 0; Playwright `--list` finds the spec
    - Closeout UAT path documented/used is `verify-phase11-uat-closeout-hardening.ps1` (Playwright via `e2e:phase9-jdbc-dialect`), not a duplicate `verify-console.ps1` run
  </acceptance_criteria>
  <done>Playwright dialect preset E2E proves kingbase8 save path alongside postgresql16.</done>
</task>

<task type="auto">
  <name>Task 2: Create verify-phase11-uat-closeout-hardening.ps1 evidence-pack UAT</name>
  <files>scripts/verify-phase11-uat-closeout-hardening.ps1</files>
  <read_first>
    - scripts/verify-phase9-uat-jdbc-dialect.ps1 (full structure: params, Write-Step, Invoke-RepoMaven, -SkipPlaywright exit, Podman Playwright branch)
    - scripts/lib/repo-maven.ps1
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-CONTEXT.md (D-13, D-14, D-16, D-17, D-19)
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-RESEARCH.md (Track B Maven slice + evidence narrative)
    - .planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-VALIDATION.md (T-11-01..T-11-05)
  </read_first>
  <action>
    Create `scripts/verify-phase11-uat-closeout-hardening.ps1` by cloning phase9 script shape (D-14):

    Parameters (same switches as phase9 unless noted): `-SkipBuild`, `-SkipPlaywright`, `-KeepContainer`, `-ImageTag` default `dg-phase11-closeout-hardening-uat:local`, `-ContainerName` default `dg-phase11-closeout-hardening-uat`, `-HostPort` default `9876`, `-SpringProfiles` default `e2e`.

    Header comment must state:
    - Phase 11 closeout hardening UAT (DS-02 managed E2E + RW-05/RW-06 Kingbase evidence pack)
    - **Supplementary UAT** — not the P0 merge gate; do not change `verify-harness.ps1` / `test-matrix.yaml` (D-17)
    - Evidence pack narrative (D-13) — three complementary proofs, not one JVM chain:
      1. Playwright: `kingbase8` (+ `postgresql16`) preset → save via `e2e:phase9-jdbc-dialect`
      2. Maven: `ConnectionCatalogTestTests` kingbase actionable failure without secrets (D-11)
      3. Maven/Testcontainers: `ChunkedPipelineKingbaseDialectTests` dialect-correct upsert via PG-proxy (D-16); note Docker required for that class (`@EnabledIf(DockerTestSupport#dockerAvailable)`)

    Maven slice (always run first) via `Invoke-RepoMaven -RepoRoot $RepoRoot -pl data-generator-service -am` with:
    ```
    -Dtest=ManagedJdbcCatalogSinkE2eIT,ConnectionCatalogTestTests,ChunkedPipelineKingbaseDialectTests,JdbcDriverPresetCatalogTests
    -Dsurefire.failIfNoSpecifiedTests=false
    ```
    (`JdbcDriverPresetCatalogTests` optional-but-recommended for narrative completeness — include it.)
    Throw if Maven exit code ≠ 0.

    When `-SkipPlaywright`: print SUCCESS and `exit 0` after Maven (CI-friendly).

    When Playwright enabled: mirror phase9 Podman flow (healthz wait, set `DG_E2E_BASE_URL` / `DG_E2E_API_URL`), then run **`npm run e2e:phase9-jdbc-dialect`** from `data-generator-console-web` (D-19 — do not invent phase11 npm script).

    Hard fences:
    - Do **not** edit `.planning/test-matrix.yaml`, `scripts/verify-harness.ps1`, or Dameng IT classes (D-12, D-17).
    - Do **not** create a new upsert IT (D-16).
    - Do **not** consolidate dual JDBC resolvers.
  </action>
  <verify>
    <automated>powershell -NoProfile -File scripts/verify-phase11-uat-closeout-hardening.ps1 -SkipPlaywright</automated>
  </verify>
  <acceptance_criteria>
    - Script exists at `scripts/verify-phase11-uat-closeout-hardening.ps1` with `-SkipPlaywright` parameter (D-14)
    - Maven `-Dtest=` list includes at least `ManagedJdbcCatalogSinkE2eIT`, `ConnectionCatalogTestTests`, `ChunkedPipelineKingbaseDialectTests` (D-11, D-16, Wave 1 IT)
    - Header documents three-piece Kingbase evidence pack and Docker note for upsert IT (D-13)
    - Playwright branch (when not skipped) invokes `e2e:phase9-jdbc-dialect` only (D-19)
    - `-SkipPlaywright` exits 0 when Maven slice is green
    - `git diff` for this plan does not modify `test-matrix.yaml` or `verify-harness.ps1` (D-17)
  </acceptance_criteria>
  <done>Phase 11 UAT script gates managed IT + Kingbase connectivity/upsert reuse; optional Playwright reuses phase9 npm script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → console API | Disposable e2e_user / e2e_test_only only |
| UAT script → Maven/Podman | Repo-local tools only; no production hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Information Disclosure | Playwright fixtures | medium | mitigate | Synthetic passwords; never assert password in API JSON |
| T-11-05 | Tampering | Accidental P0 expansion | high | mitigate | Script is supplementary only; forbid edits to verify-harness / test-matrix (D-17) |
| T-11-06 | Denial of Service | Live Test Connection / licensed KB | medium | mitigate | No Playwright Test Connection; upsert via existing PG-proxy IT only (D-11, D-16) |
</threat_model>

<verification>
- `.\scripts\verify-phase11-uat-closeout-hardening.ps1 -SkipPlaywright` exits 0
- Playwright list / optional full UAT: `npm run e2e:phase9-jdbc-dialect` covers postgresql16 + kingbase8
- No P0 gate file changes in the plan diff
</verification>

<success_criteria>
- ROADMAP SC2 satisfied under CONTEXT override (evidence pack, not single-JVM chain; Kingbase primary)
- RW-05 / RW-06 E2E depth: kingbase8 preset + actionable connectivity + reused dialect upsert IT
- Ready for Wave 3 AGENTS + audit surgical closeout
</success_criteria>

<output>
Create `.planning/phases/11-v2-0-closeout-hardening-ds-02-managed-jdbc-e2e-it-dialect-pr/11-02-SUMMARY.md` when done
</output>
