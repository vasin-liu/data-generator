---
phase: 07-datasource-governance-hot-reload
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - data-generator-console-web/src/app/pages/DatasourcesPage.tsx
  - data-generator-console-web/src/app/pages/AuditPage.tsx
  - data-generator-console-web/src/api/**
  - data-generator-console-web/src/i18n/**
  - data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleDataSourceController.java
  - data-generator-service/src/main/java/org/gensokyo/data/api/console/dto/**
  - data-generator-console-web/src/app/pages/TemplateDetailPage.tsx
autonomous: true
requirements:
  - DS-03
  - DS-04
  - DS-05
must_haves:
  truths:
    - Datasource list shows HEALTHY/DEGRADED badge, lastReloadAt, degradedReason on detail (D-26, 07-UI-SPEC)
    - Unified Test connection works for new JDBC rows and all three kinds before Save (D-18, D-21)
    - Common JDBC driver preset persists after save/create (D-21 bug fix)
    - Template run button remains valid after datasource CRUD in same session (D-21)
    - Audit page supports datasource category filter and deep-link from Datasources (D-25)
  artifacts:
    - Updated DatasourcesPage with health badges and reload detail panel
    - Updated AuditPage with category filter and query-param deep-link
    - Console API client types for healthStatus, lastReloadAt, degradedReason
    - Console REST DTO fields on datasource list/detail responses
  key_links:
    - GET /api/datasources (catalog list) returns health fields from CatalogEntry (07-UI-SPEC)
    - POST test endpoints delegate to catalog.test() (D-18)
    - Audit deep-link navigates to /console/audit?category=DATASOURCE&resourceId={name}
---

# Plan 07-04: Console Datasource UX, Bug Fixes & Audit Deep-Link

<objective>
Deliver operator-facing DS-03/DS-04/DS-05 console surfaces per 07-UI-SPEC: HEALTHY/DEGRADED badges, connectivity test gate UX, governance feedback, audit deep-link, and three known bug fixes (D-21, D-26).

Purpose: Operators see connection health and policy violations; save/test flows match backend gates.
Output: Updated Datasources and Audit pages with i18n strings; console unit/build passes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-datasource-governance-hot-reload/07-UI-SPEC.md
@.planning/phases/07-datasource-governance-hot-reload/07-CONTEXT.md
@data-generator-console-web/src/app/pages/DatasourcesPage.tsx
@data-generator-console-web/src/app/pages/AuditPage.tsx
@data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleDataSourceController.java
@data-generator-console-web/e2e/specs/datasource-*.spec.ts (Phase 6 patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend console API DTOs and datasource list/detail UX</name>
  <files>data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleDataSourceController.java,data-generator-service/src/main/java/org/gensokyo/data/api/console/dto/**,data-generator-console-web/src/api/datasources.ts,data-generator-console-web/src/app/pages/DatasourcesPage.tsx,data-generator-console-web/src/i18n/en.json,data-generator-console-web/src/i18n/zh.json</files>
  <read_first>
    - ConsoleDataSourceController.java (list, test, save endpoints)
    - DatasourcesPage.tsx (form, driver presets, test button)
    - CatalogEntry health fields from 07-01/07-02
    - 07-UI-SPEC.md States & Badges section
  </read_first>
  <action>
    Per 07-UI-SPEC / D-26: add REST list/detail fields `healthStatus`, `lastReloadAt`, `degradedReason`, `version` mapped from `CatalogEntry`.
    Per D-26: render HEALTHY (green) and DEGRADED (warning) badges on datasource list rows; detail drawer shows failure reason and copy "Using last known good configuration for new runs".
    Per D-18/D-19: wire **Test connection** to unified `POST .../test` for JDBC/Kafka/ES — support payload test for unsaved new JDBC row (fix D-21 invalid test on new row).
    Per D-21: fix driver preset loss — persist selected common driver key in form state and round-trip through save API request body; rehydrate on edit load.
    Per D-19: disable Save when profile requires passing test and last test result is failure; show inline success/error under form per 07-UI-SPEC.
    Add i18n keys for health badges, degraded detail, connectivity test messages.
    Ensure messaging cluster forms (Kafka/ES) reuse same test/save gate pattern.
  </action>
  <acceptance_criteria>
    - List API response includes `healthStatus` and `lastReloadAt` fields
    - DatasourcesPage renders HEALTHY/DEGRADED badges for managed entries
    - New JDBC row: Test button calls payload test endpoint and shows result without requiring prior save (D-21)
    - After save/create, selected common driver preset remains selected on reopen (D-21)
    - `npm run build` in data-generator-console-web exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd data-generator-console-web && npm run build</automated>
  </verify>
  <done>Datasource console surfaces health and connectivity test UX per UI contract.</done>
</task>

<task type="auto">
  <name>Task 2: Governance feedback, audit deep-link, template run button fix</name>
  <files>data-generator-console-web/src/app/pages/DatasourcesPage.tsx,data-generator-console-web/src/app/pages/AuditPage.tsx,data-generator-console-web/src/app/pages/TemplateDetailPage.tsx,data-generator-console-web/src/api/audit.ts,data-generator-console-web/src/i18n/**</files>
  <read_first>
    - TemplateDetailPage.tsx or template run trigger component
    - AuditPage.tsx existing filters
    - ConsoleAuditController category filter from 07-03
    - 07-UI-SPEC.md Governance feedback and Audit deep-link flows
    - Phase 6 template-run e2e helpers
  </read_first>
  <action>
    Per D-16 / 07-UI-SPEC: on draft save show non-blocking warnings for inline connections; on publish/run show blocking toast/modal when governance API returns 400 `R.fail(message)`.
    Per D-25: add "View audit history" control on Datasources page (per-row or global) navigating to `/console/audit?category=DATASOURCE` with optional `resourceId` query param.
    Per D-25: extend AuditPage to read `category` (and `resourceId`) from URL query on mount and apply filter to list API.
    Per D-21: fix template run button invalid state — ensure run action stays enabled after datasource CRUD session flows; clear stale form/validation state that disables Run; verify run API call fires with correct template id.
    Display governance connectivity gate errors from publish path with actionable server message.
  </action>
  <acceptance_criteria>
    - AuditPage applies `category=DATASOURCE` filter from URL query param (D-25)
    - Datasources page link navigates to filtered audit view
    - Template detail Run button triggers run API after datasource create/edit in same browser session (manual or unit test)
    - Publish governance violation shows error toast with server message (D-16)
  </acceptance_criteria>
  <verify>
    <automated>cd data-generator-console-web && npm run build</automated>
  </verify>
  <done>Console governance feedback, audit navigation, and template run regression fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Console list/detail → operator browser | No secrets in health/degraded fields; server already sanitizes (D-23) |
| Test result messages → UI | Display server message as-is; backend must not embed credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | degradedReason in UI | accept | Backend controls message content; no secrets by contract (D-23) |
| T-07-12 | Tampering | Save bypass via UI | mitigate | Save disabled client-side when gate required; server enforces (D-19) |
| T-07-SC | Tampering | DatasourcesPage changes | mitigate | Playwright regression in 07-05 |
</threat_model>

<verification>
- Console build passes TypeScript check
- REST list response shape matches 07-UI-SPEC API expectations
- i18n keys added for en and zh locales
</verification>

<success_criteria>
- DS-03 UX: DEGRADED visible with actionable detail (D-26)
- DS-04 UX: connectivity test gate + three bug fixes (D-21)
- DS-05 UX: audit filter and deep-link (D-25)
</success_criteria>

## Artifacts this phase produces

- Updated `DatasourcesPage.tsx` (badges, test gate, driver preset fix, audit link)
- Updated `AuditPage.tsx` (category filter, URL query sync)
- Updated `TemplateDetailPage.tsx` (run button fix)
- Console API client updates (`datasources.ts`, `audit.ts`)
- REST DTO extensions on `ConsoleDataSourceController`
- i18n strings for health/governance/audit UX

<output>
Create `.planning/phases/07-datasource-governance-hot-reload/07-04-SUMMARY.md` when done
</output>
