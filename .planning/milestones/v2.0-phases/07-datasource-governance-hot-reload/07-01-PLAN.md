---
phase: 07-datasource-governance-hot-reload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-datasource/data-generator-datasource-api/src/main/java/org/gensokyo/data/datasource/api/**
  - data-generator-service/src/main/java/org/gensokyo/data/model/po/TaskExecutionPO.java
  - data-generator-service/src/main/resources/db/migration/**
  - data-generator-service/src/test/resources/application-phase7-test.yaml
autonomous: true
requirements:
  - DS-03
  - DS-04
  - DS-05
must_haves:
  truths:
    - ConnectionCatalog exposes test(), reload(), and health/version fields on CatalogEntry (D-08, D-18, D-26)
    - ExecutionConnectionSnapshot JSON schema captures param-only JDBC/Kafka/ES refs with BOOTSTRAP/MANAGED tags (D-01, D-05, D-06)
    - task_execution persists connection_snapshot_json column for permanent audit/traceability (D-04)
  artifacts:
    - org.gensokyo.data.datasource.api.ConnectionHealthStatus
    - org.gensokyo.data.datasource.api.ConnectionTestRequest
    - org.gensokyo.data.datasource.api.ConnectionTestResult
    - org.gensokyo.data.datasource.api.ExecutionConnectionSnapshot
    - org.gensokyo.data.datasource.api.SnapshottedConnectionRef
    - Extended ConnectionCatalog with test and reload methods
    - Extended CatalogEntry with version, updatedAt, healthStatus, lastReloadAt, degradedReason
  key_links:
    - datasource-api types compile without Spring/adapter deps (Phase 6 D-12 pattern)
    - TaskExecutionPO.connectionSnapshotJson maps to DB column added via migration
    - application-phase7-test.yaml documents new governance test profile flags
---

# Plan 07-01: Catalog API Extensions & Execution Snapshot Schema

<objective>
Extend `data-generator-datasource-api` with health, version, connectivity-test, and hot-reload contracts plus the execution snapshot JSON model per D-01..D-08, D-18, D-26.

Purpose: Lock public types and persistence shape before service hot-reload, governance, and audit wiring land in later waves.
Output: Compilable api module with additive Catalog methods; `task_execution.connection_snapshot_json` column ready for Wave 2 writers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-datasource-governance-hot-reload/07-CONTEXT.md
@.planning/phases/07-datasource-governance-hot-reload/07-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@data-generator-datasource/data-generator-datasource-api/src/main/java/org/gensokyo/data/datasource/api/ConnectionCatalog.java
@data-generator-datasource/data-generator-datasource-api/src/main/java/org/gensokyo/data/datasource/api/CatalogEntry.java
@data-generator-service/src/main/java/org/gensokyo/data/model/po/TaskExecutionPO.java
@data-generator-service/src/main/java/org/gensokyo/data/task/RunLineageSupport.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add health, version, and test/reload types to datasource-api</name>
  <files>data-generator-datasource/data-generator-datasource-api/src/main/java/org/gensokyo/data/datasource/api/*.java</files>
  <read_first>
    - ConnectionCatalog.java (existing resolve/listAll)
    - CatalogEntry.java
    - CatalogEntrySource.java, ConnectionKind.java
    - .planning/phases/07-datasource-governance-hot-reload/07-CONTEXT.md decisions D-08, D-18, D-26
  </read_first>
  <action>
    Per D-26: add `ConnectionHealthStatus` enum `HEALTHY`, `DEGRADED`.
    Extend `CatalogEntry` (additive record fields or companion type) with `long version`, `Instant updatedAt`, `ConnectionHealthStatus healthStatus`, `Instant lastReloadAt`, `String degradedReason` — list payloads remain secret-free (D-01, D-10).
    Per D-18: add `ConnectionTestRequest` supporting either `(ConnectionKind kind, String name)` for existing entries OR `(ConnectionKind kind, Map payload)` for unsaved draft config; add `ConnectionTestResult` with `success`, `message`, optional `details` map (no secrets).
    Extend `ConnectionCatalog` interface:
    - `ConnectionTestResult test(ConnectionTestRequest request)` — unified entry point (D-18)
    - `CatalogEntry reload(String name, ConnectionKind kind)` — documents save-triggered hot-reload contract; impl deferred to 07-02 (D-09)
    - Optional `Optional&lt;CatalogEntry&gt; findEntry(String name, ConnectionKind kind)` for version lookup at snapshot time (D-08)
    Per D-11: document reload semantics — on failure entry becomes DEGRADED but DB config retained; runtime serves last known good until restored.
    Follow Java copyright + public Javadoc rules from `.cursor/rules/java-copyright-class-javadoc.mdc`.
    Do NOT add Spring or adapter dependencies to api POM.
  </action>
  <acceptance_criteria>
    - `ConnectionHealthStatus` enum exists with HEALTHY and DEGRADED
    - `ConnectionCatalog` declares `test(ConnectionTestRequest)` and `reload(String, ConnectionKind)`
    - `CatalogEntry` exposes version and healthStatus fields
    - `ConnectionTestRequest` supports name-based and payload-based constructors/factories
    - `.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-api -am compile -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-datasource/data-generator-datasource-api -am compile -q</automated>
  </verify>
  <done>Public catalog contracts extended; Phase 6 resolve/listAll signatures unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Define ExecutionConnectionSnapshot model and task_execution column</name>
  <files>data-generator-datasource/data-generator-datasource-api/src/main/java/org/gensokyo/data/datasource/api/snapshot/**,data-generator-service/src/main/java/org/gensokyo/data/model/po/TaskExecutionPO.java,data-generator-service/src/main/resources/db/migration/**,data-generator-service/src/test/resources/application-phase7-test.yaml</files>
  <read_first>
    - RunLineageSupport.java (existing lineage JSON patterns)
    - TaskExecutionPO.java (template_version, plugin_set_json, datasource_config_hash columns)
    - .planning/phases/07-datasource-governance-hot-reload/07-CONTEXT.md D-01..D-08
    - docs/testing-embedded-components.md (phase7 test profile)
  </read_first>
  <action>
    Per D-01/D-05/D-06: add api types `ExecutionConnectionSnapshot` and `SnapshottedConnectionRef` with fields:
    - connection key: `name`, `kind`, `source` (BOOTSTRAP/MANAGED)
    - pinned `catalogVersion` and `catalogUpdatedAt` from Catalog entry at RUNNING (D-08)
    - param-only config map per kind (JDBC url/username/secretRef/driver; Kafka bootstrap/cluster; ES hosts/secretRef) — NO resolved handles (D-01)
    - inline template blocks serialized with same param-freeze semantics when present in template (D-05)
    Per D-04: add `TaskExecutionPO.connectionSnapshotJson` (`@Column(name = "connection_snapshot_json", columnDefinition = "CLOB")`).
    Add Flyway/Liquibase migration (match existing project migration tool) adding nullable `connection_snapshot_json CLOB` to `task_execution`.
    Per D-04: document in type Javadoc that DB rows are retained permanently; in-process cache is Wave 2 concern.
    Extend `application-phase7-test.yaml` with placeholder `data.generator.governance.*` keys aligned to D-14 (dev OFF defaults) for downstream ITs.
    Add unit test in api or service test compiling snapshot JSON round-trip via `TemplateJsonCodec` pattern.
  </action>
  <acceptance_criteria>
    - `ExecutionConnectionSnapshot` and `SnapshottedConnectionRef` types exist in datasource-api
    - `TaskExecutionPO` has `connectionSnapshotJson` field mapped to new DB column
    - Migration script adds `connection_snapshot_json` column to `task_execution`
    - Snapshot JSON schema test proves JDBC/Kafka/ES ref round-trip without secret values in serialized output when using secretRef fields
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service,data-generator-datasource/data-generator-datasource-api -am test -Dtest=ExecutionConnectionSnapshotTests -q</automated>
  </verify>
  <done>Persistence column and snapshot schema ready for Wave 2 RUNNING capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot JSON → task_execution DB | Param-only freeze; no resolved pool handles or plaintext secrets (D-01, D-23 prep) |
| Catalog list/test DTOs → console | Metadata and actionable messages only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | ExecutionConnectionSnapshot JSON | mitigate | Exclude password/apiKey values; use secretRef fields only (D-01, D-23) |
| T-07-02 | Tampering | Snapshot version pinning | mitigate | Store catalogVersion/updatedAt per ref at RUNNING (D-08) |
| T-07-03 | Denial of Service | Oversized snapshot CLOB | accept | Bound template connection ref count in Wave 2 builder; document limit |
| T-07-SC | Tampering | Api module dependency creep | mitigate | Enforce no Spring/adapter deps in api POM compile check |
</threat_model>

<verification>
- Api module compiles; grep confirms no `spring-` imports in api sources
- Migration applies cleanly on H2 test profile
- Phase 6 ConnectionCatalog.resolve/listAll signatures unchanged (additive only)
</verification>

<success_criteria>
- DS-03 foundation: snapshot schema and Catalog health/version/test contracts defined
- DS-04 foundation: ConnectionTestRequest/Result types for unified connectivity gate
- DS-05 foundation: snapshot JSON persisted on task_execution for traceability
</success_criteria>

## Artifacts this phase produces

- `org.gensokyo.data.datasource.api.ConnectionHealthStatus`
- `org.gensokyo.data.datasource.api.ConnectionTestRequest`
- `org.gensokyo.data.datasource.api.ConnectionTestResult`
- `org.gensokyo.data.datasource.api.ExecutionConnectionSnapshot`
- `org.gensokyo.data.datasource.api.SnapshottedConnectionRef`
- Extended `org.gensokyo.data.datasource.api.ConnectionCatalog` (`test`, `reload`, optional `findEntry`)
- Extended `org.gensokyo.data.datasource.api.CatalogEntry` (version, health, reload metadata)
- `TaskExecutionPO.connectionSnapshotJson` + DB migration
- `ExecutionConnectionSnapshotTests` (or equivalent round-trip test)
- Updated `application-phase7-test.yaml` governance placeholders

<output>
Create `.planning/phases/07-datasource-governance-hot-reload/07-01-SUMMARY.md` when done
</output>
