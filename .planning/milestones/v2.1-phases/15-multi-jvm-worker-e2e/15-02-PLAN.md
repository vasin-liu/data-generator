---
phase: 15-multi-jvm-worker-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - .planning/test-matrix.yaml
  - docs/test-matrix.md
autonomous: true
requirements:
  - DIST-01
must_haves:
  truths:
    - "P1 matrix row dist-multi-jvm-worker exists in .planning/test-matrix.yaml with tier P1 (D-03)"
    - "Row notes reference scripts/verify-multi-jvm-worker.ps1 as primary evidence; linked_tests empty or script-only (no P0 promotion) (D-03, D-04)"
    - "scripts/verify-harness.ps1 run surfaces dist-multi-jvm-worker in target/test-matrix-summary.json (D-04)"
    - "P0 row count and membership unchanged — still 15 P0 rows (D-03, D-10)"
    - "docs/test-matrix.md regenerated from matrix YAML per existing harness flow (D-03)"
  prohibitions:
    - statement: "Do not promote dist-multi-jvm-worker to P0 or edit existing P0 rows"
      status: resolved
      verification: "rg -n 'dist-multi-jvm-worker' .planning/test-matrix.yaml shows tier: P1 only; P0 row ids unchanged vs pre-plan git show HEAD:.planning/test-matrix.yaml count"
    - statement: "Do not change verify-harness.ps1 P0 merge-gate semantics"
      status: resolved
      verification: "git diff scripts/verify-harness.ps1 is empty OR diff only adds non-blocking comment; p0.pass logic unchanged"
  artifacts:
    - path: ".planning/test-matrix.yaml"
      provides: "dist-multi-jvm-worker P1 row"
    - path: "docs/test-matrix.md"
      provides: "Regenerated operator matrix doc"
    - path: "target/test-matrix-summary.json"
      provides: "Harness summary including new row (generated at verify time)"
  key_links:
    - from: ".planning/test-matrix.yaml"
      to: "scripts/verify-multi-jvm-worker.ps1"
      via: "row notes field"
      pattern: "verify-multi-jvm-worker"
    - from: "scripts/verify-harness.ps1"
      to: ".planning/test-matrix.yaml"
      via: "Get-MatrixRows / New-TestMatrixSummary"
      pattern: "test-matrix.yaml"
---

<objective>
Wire DIST-01 into the feature matrix as a **P1** row and confirm harness summary visibility without touching the P0 merge gate (D-03, D-04).

Purpose: Operators and CI can see multi-JVM proof status in `target/test-matrix-summary.json`; P1 failures remain non-blocking per `docs/test-harness.md`.
Output: New matrix row, regenerated `docs/test-matrix.md`, evidence that `verify-harness.ps1` lists the row.
Depends on plan 15-01 delivering `scripts/verify-multi-jvm-worker.ps1`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md
@.planning/phases/15-multi-jvm-worker-e2e/15-VALIDATION.md
@.planning/phases/15-multi-jvm-worker-e2e/15-01-PLAN.md
@.planning/test-matrix.yaml
@docs/test-harness.md
@scripts/verify-harness.ps1
@scripts/lib/test-matrix-summary.ps1
@scripts/generate-test-matrix-doc.ps1
</context>

## Artifacts this phase produces

- `dist-multi-jvm-worker` row in `.planning/test-matrix.yaml` — **this plan**
- Updated `docs/test-matrix.md` — **this plan**
- `target/test-matrix-summary.json` entry (generated) — **this plan (verify step)**

## Non-goals (D-10)

- P0 promotion of multi-JVM row
- Changes to Playwright distributed specs as harness gate
- Podman script linkage as required harness evidence
- Modifications to `verify-harness.ps1` merge-gate / `p0.pass` logic unless a one-line comment is needed
- Optional env-gated `MultiJvmWorkerE2eIT` (discretion: defer unless flake-free script exists first)

<tasks>

<task type="auto">
  <name>Task 1: Add dist-multi-jvm-worker P1 row to test-matrix.yaml</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md (D-03, D-04, D-10)
    - .planning/test-matrix.yaml (schema columns; existing P1 row examples e.g. console-api-jobs, calcite-chunked-pipeline)
    - docs/test-harness.md (P0 vs P1 semantics)
    - scripts/verify-multi-jvm-worker.ps1 (from plan 15-01 — confirm path for notes)
  </read_first>
  <action>
    Append one row to `.planning/test-matrix.yaml` `rows:` list (do **not** edit any existing row, especially P0 rows):

    ```yaml
    - id: dist-multi-jvm-worker
      capability: distributed-multi-jvm-worker
      adapter: host-jvm
      test_types: [integration]
      owner_module: data-generator-service
      status: pending
      tier: P1
      linked_tests: []
      notes: >
        Phase 15 DIST-01 — coordinator JVM enqueues via POST /task/run; worker JVM
        (DataGeneratorWorkerApplication) leases and executes to SUCCESS. Primary proof:
        scripts/verify-multi-jvm-worker.ps1 (host two-JVM + shared file H2). P1 non-blocking;
        Podman drill scripts/e2e-distributed-podman.ps1 remains optional. Single-JVM ITs
        (DistributedSplitRoleIntegrationTests) unchanged.
    ```

    Initial `status: pending` is acceptable until plan 15-01 script is green; executor may flip to `covered` after successful verify run (D-03 discretion).

    Confirm P0 row count remains **15** (grep `tier: P0` count unchanged from baseline).
  </action>
  <verify>
    <automated>rg -n "id: dist-multi-jvm-worker" .planning/test-matrix.yaml; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -A6 "id: dist-multi-jvm-worker" .planning/test-matrix.yaml | Select-String "tier: P1"; if (-not $?) { exit 1 }; (rg -c "tier: P0" .planning/test-matrix.yaml) -eq 15; if (-not $?) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - Row `dist-multi-jvm-worker` exists with `tier: P1`
    - Row `notes` contains `scripts/verify-multi-jvm-worker.ps1`
    - `linked_tests: []` (script-primary row; no Maven class linkage required for summary surfacing)
    - Exactly 15 rows with `tier: P0` (count unchanged)
    - No existing P0 row ids or tiers modified
  </acceptance_criteria>
  <done>P1 matrix row registered for DIST-01 without P0 gate changes.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate docs/test-matrix.md and confirm verify-harness summary includes new row</name>
  <files>docs/test-matrix.md</files>
  <read_first>
    - scripts/generate-test-matrix-doc.ps1 (regeneration entry point)
    - scripts/verify-harness.ps1 (summary generation via New-TestMatrixSummary)
    - scripts/lib/test-matrix-summary.ps1 (all rows appear in summary.rows)
    - .planning/test-matrix.yaml (updated row from Task 1)
  </read_first>
  <action>
    1. Regenerate operator matrix doc:

       ```powershell
       powershell -NoProfile -File scripts/generate-test-matrix-doc.ps1
       ```

    2. Run harness summary generation (Maven slice may skip if no linked_tests — that is OK):

       ```powershell
       powershell -NoProfile -File scripts/verify-harness.ps1 -SkipPlaywright
       ```

       If `-SkipPlaywright` is unsupported, run `verify-harness.ps1` with flags documented in `docs/test-harness.md` for fastest path.

    3. Confirm `target/test-matrix-summary.json` contains an object with `"id": "dist-multi-jvm-worker"` inside `rows` array.

    4. Confirm `p0.pass` / P0 total unchanged (still 15 P0 rows). P1 row status may be `pending` — non-blocking per D-04.

    Do **not** edit `verify-harness.ps1` unless summary omits script-only rows (bug fix only; preserve P0 gate logic).
  </action>
  <verify>
    <automated>rg -n "dist-multi-jvm-worker" docs/test-matrix.md; if ($LASTEXITCODE -ne 0) { exit 1 }; if (Test-Path target/test-matrix-summary.json) { Select-String -Path target/test-matrix-summary.json -Pattern 'dist-multi-jvm-worker' -Quiet; if (-not $?) { exit 1 } else { exit 0 } } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `docs/test-matrix.md` lists `dist-multi-jvm-worker`
    - `target/test-matrix-summary.json` includes `dist-multi-jvm-worker` in `rows`
    - `summary.p0.total` equals 15 and P0 row ids match pre-plan baseline
    - `git diff scripts/verify-harness.ps1` shows no merge-gate logic changes (empty diff preferred)
  </acceptance_criteria>
  <done>Harness linkage visible; P0 merge gate semantics preserved.</done>
</task>

</tasks>

## Verification

```powershell
rg -n "dist-multi-jvm-worker" .planning/test-matrix.yaml docs/test-matrix.md
powershell -NoProfile -File scripts/verify-harness.ps1 -SkipPlaywright
Select-String -Path target/test-matrix-summary.json -Pattern "dist-multi-jvm-worker"
(rg -c "tier: P0" .planning/test-matrix.yaml) -eq 15
```

## Success criteria

- [ ] P1 row `dist-multi-jvm-worker` in matrix + summary JSON
- [ ] P0 gate unchanged (15 rows, non-blocking P1)
- [ ] Matrix doc regenerated
