---
phase: 12-http-execute-path-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - data-generator-service/pom.xml
  - data-generator-service/src/test/java/org/gensokyo/data/support/DockerTestSupport.java
  - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpPostgresUpsertIT.java
autonomous: true
requirements:
  - EXEC-02
must_haves:
  truths:
    - Separate IT from EXEC-01 H2 class proves managed dataSourceId + postgres dialect upsert (ON CONFLICT) on HTTP /task/run spine (D-08, D-09, D-10)
    - Testcontainers PostgreSQL (postgres:16-alpine) backs the managed catalog JDBC URL (D-09)
    - Same HTTP evidence bar as EXEC-01: publish → MockMvc POST /task/run/{id} → parse instanceId → poll SUCCESS (30–60s, fail-fast FAILED/CANCELLED) → COUNT(*) (D-01–D-07, D-10)
    - IT is Docker-gated (@EnabledIf / DockerTestSupport); skips cleanly without Docker rather than failing CI hard
    - No snap:{instanceId} asserts (D-11); Phase 11 ManagedJdbcCatalogSinkE2eIT remains unmodified regression
  artifacts:
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpPostgresUpsertIT.java
    - data-generator-service/src/test/java/org/gensokyo/data/support/DockerTestSupport.java
    - data-generator-service/pom.xml (test-scoped testcontainers postgresql + junit-jupiter 1.20.6)
  key_links:
    - PostgreSQLContainer JDBC URL → DataSourceConfigService.save managed id
    - JdbcWriterVO options dialect=postgres, upsert=true, upsertKeys=[id] → JdbcSinkSqlBuilder ON CONFLICT path
    - MockMvc POST /task/run/{id} → TaskController → runV2Tracked → TemplateV2Runner (real bean)
---

<objective>
Prove EXEC-02: CI-friendly PostgreSQL dialect upsert (ON CONFLICT) through the same HTTP execute spine with a managed catalog `dataSourceId`, in a separate IT from EXEC-01.

Purpose: Close ROADMAP SC2 without serializing H2+PG into one fragile test (D-08).
Output: Docker-gated `ManagedJdbcCatalogHttpPostgresUpsertIT` green when Docker available; service pom Testcontainers test deps aligned with calcite 1.20.6.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-http-execute-path-proof/12-CONTEXT.md
@.planning/phases/12-http-execute-path-proof/12-RESEARCH.md
@.planning/phases/12-http-execute-path-proof/12-PATTERNS.md
@.planning/phases/12-http-execute-path-proof/12-VALIDATION.md
@.planning/phases/12-http-execute-path-proof/12-01-PLAN.md
@.planning/ROADMAP.md
@data-generator-calcite/pom.xml
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DockerTestSupport.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java
</context>

## Artifacts this phase produces

*(Phase-wide — this plan delivers Wave 2 / EXEC-02)*

- `ManagedJdbcCatalogHttpPostgresUpsertIT` — separate `@SpringBootTest` + `@Testcontainers` class under `org.gensokyo.data.datasource.catalog`
- `org.gensokyo.data.support.DockerTestSupport` — tiny copy of calcite’s Docker availability check for service-module `@EnabledIf` (calcite test-scoped class is not on service classpath)
- `data-generator-service/pom.xml` — test-scoped `org.testcontainers:postgresql` + `junit-jupiter` at version `1.20.6` (same as calcite; no new Maven groups)
- Reuses Plan 01 HTTP spine patterns (publish, MockMvc, parse, poll, COUNT) with PG-managed URL + upsert writer options

<tasks>

<task type="auto">
  <name>Task 1: Add Testcontainers test deps and DockerTestSupport for service module</name>
  <files>data-generator-service/pom.xml, data-generator-service/src/test/java/org/gensokyo/data/support/DockerTestSupport.java</files>
  <read_first>
    - .planning/phases/12-http-execute-path-proof/12-RESEARCH.md (Standard Stack + Package Legitimacy — existing monorepo artifacts only)
    - .planning/phases/12-http-execute-path-proof/12-PATTERNS.md (pom + DockerTestSupport sections)
    - data-generator-calcite/pom.xml (Testcontainers 1.20.6 test dependency block)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DockerTestSupport.java (copy source of truth)
    - data-generator-service/pom.xml (confirm Testcontainers absent today)
    - .cursor/rules/java-copyright-class-javadoc.mdc
  </read_first>
  <action>
    1. **POM (test scope only):** Add to `data-generator-service/pom.xml` the same Testcontainers artifacts calcite already uses — `org.testcontainers:junit-jupiter` and `org.testcontainers:postgresql`, version `1.20.6`, `<scope>test</scope>`. Do **not** introduce new Maven groupIds or bump unrelated deps. Package Legitimacy: existing monorepo packages only (RESEARCH N/A for new product packages).

    2. **Docker gate helper:** Create `data-generator-service/src/test/java/org/gensokyo/data/support/DockerTestSupport.java` as a surgical copy of calcite’s `DockerTestSupport` (`dockerAvailable()` via `DockerClientFactory.instance().isDockerAvailable()`, catch RuntimeException/LinkageError → false). Apply PCI copyright + type Javadoc stating it exists because calcite’s test-scoped helper is not on the service test classpath. Do **not** invent a new gate API shape.

    Out of scope: harness matrix / P0 rows, calcite module changes, production runtime deps.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests -Dskip.console.frontend=true dependency:resolve-plugins</automated>
  </verify>
  <acceptance_criteria>
    - `data-generator-service/pom.xml` contains test-scoped `org.testcontainers:postgresql` version 1.20.6
    - `data-generator-service/pom.xml` contains test-scoped `org.testcontainers:junit-jupiter` version 1.20.6
    - Class `org.gensokyo.data.support.DockerTestSupport` exists with public static `dockerAvailable()` method
    - No production (non-test) Testcontainers dependency added
    - PCI copyright + type Javadoc on DockerTestSupport
  </acceptance_criteria>
  <done>Service module can compile Testcontainers PG ITs and gate on Docker availability.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ManagedJdbcCatalogHttpPostgresUpsertIT (HTTP spine + ON CONFLICT)</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpPostgresUpsertIT.java</files>
  <read_first>
    - .planning/phases/12-http-execute-path-proof/12-CONTEXT.md (D-08, D-09, D-10, D-01–D-07, D-11)
    - .planning/phases/12-http-execute-path-proof/12-PATTERNS.md (EXEC-02 section)
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java (Plan 01 HTTP spine — reuse patterns, separate class)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java (PostgreSQLContainer postgres:16-alpine, @EnabledIf, @Testcontainers)
    - data-generator-calcite UpsertParitySupport (or equivalent) writer options: dialect, upsert, upsertKeys — shrink row count to 2–10
    - .cursor/rules/java-copyright-class-javadoc.mdc
  </read_first>
  <action>
    Create **separate** class `ManagedJdbcCatalogHttpPostgresUpsertIT` (D-08) in `org.gensokyo.data.datasource.catalog`.

    Annotations / gate (D-09):
    - `@EnabledIf("org.gensokyo.data.support.DockerTestSupport#dockerAvailable")`
    - `@Testcontainers`
    - `@SpringBootTest` same as Plan 01: `DataGeneratorApplication` + `classpath:/application-phase7-test.yaml` + `data.generator.governance.require-published-for-task-run=true`
    - Static `@Container` `PostgreSQLContainer<>("postgres:16-alpine")` with small db name / test user / test password (match ChunkedPipelinePostgresUpsertTests spirit)

    Type Javadoc: same maintainer-obvious order as Plan 01, plus dialect upsert ON CONFLICT / Testcontainers PG; note Docker gate and deferred snap asserts (D-11).

    Happy-path `@Test` (name e.g. `httpTaskRun_managedPostgresUpsert_reachesSuccessWithCountableRows`):
    1. `dataSourceConfigService.save(dsName, POSTGRES.getJdbcUrl(), POSTGRES.getUsername(), POSTGRES.getPassword(), null, "org.postgresql.Driver", null, null)` — managed id only.
    2. DDL on managed pool: `DROP TABLE IF EXISTS` + `CREATE TABLE` with PK on `id` (required for ON CONFLICT).
    3. Persist TemplatePO V2 YAML: small inline seed (2–10 rows, **not** UpsertParitySupport ROW_COUNT=500); JDBC writer `dataSourceId` = managed name; options map `dialect=postgres`, `upsert=true`, `upsertKeys=["id"]` (D-10). No inline writer dataSource.
    4. `templateLifecycleService.publish(id)` (D-02).
    5. MockMvc `POST /task/run/{id}` → success + parse `instanceId=` (D-01, D-03, D-04).
    6. Poll TaskExecution to SUCCESS; fail-fast FAILED/CANCELLED; 30–60s (D-05, D-07).
    7. COUNT(*) on managed pool ≥ expected (D-06, D-10). Optional stronger check: second HTTP run with same keys leaves count unchanged (idempotent upsert) — in-scope if cheap.
    8. Do **not** assert snap keys (D-11). Do **not** call `templateV2Runner.run` as primary. Do **not** merge this into EXEC-01 class. Do **not** edit Phase 11 IT or P0 matrix.

    Kingbase-proxy dialect key is **optional** — CONTEXT locks PG engine (D-09); do not require Kingbase in this IT.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am "-Dtest=ManagedJdbcCatalogHttpPostgresUpsertIT,ManagedJdbcCatalogHttpExecuteIT,ManagedJdbcCatalogSinkE2eIT" -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true test</automated>
  </verify>
  <acceptance_criteria>
    - Class `ManagedJdbcCatalogHttpPostgresUpsertIT` is separate from `ManagedJdbcCatalogHttpExecuteIT` (D-08)
    - IT uses Testcontainers `PostgreSQLContainer` with image `postgres:16-alpine` and is Docker-gated via service `DockerTestSupport` (D-09)
    - Managed catalog save uses container JDBC URL/user/password and `org.postgresql.Driver`
    - Writer options include dialect postgres, upsert true, and upsertKeys containing id (D-10)
    - Test uses MockMvc POST /task/run/{id}, publish before run, instanceId parse, TaskExecution poll to SUCCESS, and managed-pool COUNT(*) (D-01–D-07, D-10)
    - Test does not assert snap-prefixed datasource keys (D-11)
    - When Docker is available, Maven command exits 0; when Docker is absent, IT is skipped (not a hard failure of the gated class)
    - `ManagedJdbcCatalogSinkE2eIT` and `ManagedJdbcCatalogHttpExecuteIT` remain green / unmodified by unintended edits
  </acceptance_criteria>
  <done>EXEC-02 proven: managed catalog + PG ON CONFLICT upsert via HTTP /task/run spine (Docker-gated).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IT → Testcontainers PostgreSQL | Ephemeral container credentials; must not use production DB URLs |
| MockMvc → TaskController `/task/run` | Same enqueue surface as Plan 01; disposable published templates only |
| Docker host → Testcontainers | Local Docker socket access; tests skip when unavailable |

## ASVS

- Level: 1
- Block on: high severity threats

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Information Disclosure | PG container credentials in IT | medium | mitigate | Use Testcontainers-generated disposable user/password only; never commit real host credentials |
| T-12-06 | Tampering | Upsert key / dialect options | high | mitigate | Explicit writer options dialect=postgres, upsert=true, upsertKeys=[id]; assert COUNT(*) (optional second-run idempotency) so wrong SQL path fails the IT |
| T-12-07 | Denial of Service | Unbounded PG fixture size | medium | mitigate | Cap seed rows at 2–10; do not import UpsertParitySupport ROW_COUNT=500 into HTTP IT |
| T-12-01 | Elevation of Privilege | require-published-for-task-run | high | mitigate | Same as Plan 01: IT property true + publish before run |
| T-12-02 | Tampering | Fake TemplateV2Runner | high | mitigate | Real runner bean only; no capturing @Primary override |
| T-12-SC | Tampering | testcontainers Maven installs | high | mitigate | Only add existing monorepo group/artifact/version (1.20.6) already used by calcite; no new groups; Package Legitimacy N/A for novel packages |
</threat_model>

<verification>
- With Docker: `.\mvnw-jdk25.ps1 -pl data-generator-service -am "-Dtest=ManagedJdbcCatalogHttpPostgresUpsertIT" -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true test` → BUILD SUCCESS
- Combined: `-Dtest=ManagedJdbcCatalog*IT` includes Phase 11 + EXEC-01 + EXEC-02
- Without Docker: EXEC-02 skipped via `@EnabledIf`; EXEC-01 still required green
</verification>

<success_criteria>
- EXEC-02 satisfied under D-08..D-10 (+ shared D-01..D-07, D-11)
- ROADMAP Phase 12 SC2 met for CI-friendly PG dialect path on HTTP spine
- Phase 12 DoD complete when Plan 01 + Plan 02 both summarized green (EXEC-02 may document skip-without-Docker)
</success_criteria>

<output>
Create `.planning/phases/12-http-execute-path-proof/12-02-SUMMARY.md` when done
</output>
