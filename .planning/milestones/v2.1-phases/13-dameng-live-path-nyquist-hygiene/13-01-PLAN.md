---
phase: 13-dameng-live-path-nyquist-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-calcite/pom.xml
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java
autonomous: true
requirements:
  - DIAL-01
must_haves:
  truths:
    - "ChunkedPipelineDamengUpsertIT connects to a real external Dameng JDBC endpoint from DG_DM_JDBC_URL/DG_DM_USER/DG_DM_PASSWORD and can PASS when a host is configured (D-01, D-06, D-07)"
    - "When the opt-in flag is on but connection env is missing or the host is unreachable, the IT FAILS the build — it never reports as skipped (D-02)"
    - "PASS evidence is chunked upsert idempotency produced by the shared UpsertParitySupport.assertUpsertIdempotent helper, not a Dameng-specific scenario (D-03, D-04)"
    - "Default CI without the flag still skips the IT via the existing class-level DamengTestSupport gate, so the MERGE-unit bar is unchanged (D-01, D-05)"
    - "dm-jdbc is resolvable at test scope in data-generator-calcite so dm.jdbc.driver.DmDriver loads at runtime"
  artifacts:
    - path: "data-generator-calcite/pom.xml"
      provides: "test-scope com.dameng:dm-jdbc dependency (version inherited from root BOM)"
      contains: "dm-jdbc"
    - path: "data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java"
      provides: "Real Dameng live upsert idempotency IT with hard-fail-on-misconfig"
      min_lines: 40
    - path: "data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java"
      provides: "Opt-in gate with Javadoc documenting the full env contract"
  key_links:
    - from: "ChunkedPipelineDamengUpsertIT"
      to: "UpsertParitySupport.assertUpsertIdempotent"
      via: "delegation with driver class dm.jdbc.driver.DmDriver and dialect key dameng"
      pattern: "assertUpsertIdempotent"
    - from: "ChunkedPipelineDamengUpsertIT"
      to: "DamengTestSupport#damengItEnabled"
      via: "class-level @EnabledIf opt-in gate (unchanged)"
      pattern: "EnabledIf"
    - from: "data-generator-calcite/pom.xml"
      to: "root pom.xml dependencyManagement"
      via: "com.dameng:dm-jdbc with no explicit version (inherits ${dm.version})"
      pattern: "dm-jdbc"
---

<objective>
Replace the `ChunkedPipelineDamengUpsertIT` placeholder with real external-JDBC wiring so the opt-in Dameng live IT can genuinely PASS on a configured host (D-01), delegating all upsert assertions to the shared `UpsertParitySupport` helper (D-03, D-04), and make a flag-on-but-misconfigured run a hard build FAILURE rather than a skip (D-02).

Purpose: Today enabling the flag produces `Assumptions.abort`, so "I turned on the Dameng live IT" yields a green-looking skip. That is the exact dishonesty DIAL-01 exists to remove.
Output: test-scope `dm-jdbc` in `data-generator-calcite/pom.xml`, a real IT body, and a `DamengTestSupport` Javadoc that states the complete env contract.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-CONTEXT.md
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-RESEARCH.md
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-VALIDATION.md
@.planning/ROADMAP.md
@data-generator-calcite/pom.xml
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java
@.cursor/rules/java-copyright-class-javadoc.mdc
</context>

## Artifacts this phase produces

*(Phase-wide inventory; this plan delivers the Wave 1 DIAL-01 code slice)*

- `data-generator-calcite/pom.xml` — test-scope `com.dameng:dm-jdbc` (no `<version>`; inherits `${dm.version}` = 1.8 from root BOM) — **this plan**
- `ChunkedPipelineDamengUpsertIT` — real external-JDBC live IT, hard-fail on misconfig, delegates to `UpsertParitySupport` — **this plan**
- `DamengTestSupport` — Javadoc-only refresh documenting the `DG_DM_IT` + `DG_DM_JDBC_URL`/`DG_DM_USER`/`DG_DM_PASSWORD` contract — **this plan**
- `scripts/verify-phase13-uat-dameng-live.ps1` — opt-in UAT wrapper, non-zero exit when unconfigured (plan 13-02)
- `docs/template-v2-jdbc-sink-guide.md` — Dameng live IT recipe section + stale test-class reference fix (plan 13-02)
- `AGENTS.md` — one Commands-section line pointing at the opt-in Dameng verify script (plan 13-02)
- `.planning/milestones/v2.0-phases/07-.../07-VALIDATION.md` refresh + `07.1-.../07.1-VALIDATION.md` new (plan 13-03)
- `.planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md` new + `.planning/milestones/v2.0-MILESTONE-AUDIT.md` Nyquist sync (plan 13-04)

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Reproducible Dameng opt-in live IT green path, no P0 promotion | 13-01, 13-02 | COVERED | |
| GOAL | — | Backfill Nyquist/VALIDATION hygiene for lagging v2.0 phases | 13-03, 13-04 | COVERED | |
| REQ | DIAL-01 | Documented opt-in Dameng green path; default CI stays MERGE-unit | 13-01, 13-02 | COVERED | |
| REQ | DIAL-02 | Nyquist/VALIDATION hygiene for 07, 07.1, 08 | 13-03, 13-04 | COVERED | |
| RESEARCH | — | Wire IT via `UpsertParitySupport.assertUpsertIdempotent(..., "dm.jdbc.driver.DmDriver", "dameng")` | 13-01 | COVERED | |
| RESEARCH | — | Add `dm-jdbc` test scope to `data-generator-calcite/pom.xml` (Pitfall 4: right module) | 13-01 | COVERED | |
| RESEARCH | — | Hard FAIL, not the in-tree Ollama soft-skip precedent (Pitfall 1) | 13-01 | COVERED | |
| RESEARCH | — | Verify script named `verify-phase13-uat-dameng-live.ps1` (Open Question 1 recommendation) | 13-02 | COVERED | |
| RESEARCH | — | Env-only connection config; no `-D` mirrors (Open Question 2 recommendation) | 13-01, 13-02 | COVERED | Discretion resolved to env-only |
| RESEARCH | — | Fix stale docs reference to a nonexistent Dameng MERGE test class (Pitfall 5) | 13-02 | COVERED | |
| RESEARCH | — | No P0/harness/test-matrix inflation (Pitfall 2) | 13-02 | COVERED | Asserted by verify gate |
| RESEARCH | — | Nyquist backfill transcribes existing VERIFICATION evidence only (Pitfall 3) | 13-03, 13-04 | COVERED | |
| RESEARCH | — | Credential-leak controls on env docs and failure messages (Security Domain) | 13-01, 13-02 | COVERED | threat_model T-13-01/T-13-03 |
| CONTEXT | D-01 | Real JDBC wiring so the IT can PASS; default CI still skips | 13-01 | COVERED | |
| CONTEXT | D-02 | Hard FAIL when flag on + unreachable/misconfigured | 13-01 | COVERED | |
| CONTEXT | D-03 | PASS evidence = chunked upsert idempotency | 13-01 | COVERED | |
| CONTEXT | D-04 | Must reuse `UpsertParitySupport.assertUpsertIdempotent` | 13-01 | COVERED | |
| CONTEXT | D-05 | Honest done criteria with no host: documented path + MERGE-unit bar | 13-01, 13-02 | COVERED | |
| CONTEXT | D-06 | External JDBC URL/env only | 13-01 | COVERED | |
| CONTEXT | D-07 | `DG_DM_JDBC_URL` / `DG_DM_USER` / `DG_DM_PASSWORD` | 13-01 | COVERED | |
| CONTEXT | D-08 | Plaintext env acceptable; docs must warn never commit secrets | 13-02 | COVERED | |
| CONTEXT | D-09 | Testcontainers / licensed image out of scope | 13-01 | COVERED | Explicit non-goal in action |
| CONTEXT | D-10 | Strict DIAL-02 scope: 07, 07.1, 08 only; Phase 12 untouched | 13-03, 13-04 | COVERED | |
| CONTEXT | D-11 | Honest green from existing tests; no Nyquist-only product tests | 13-03, 13-04 | COVERED | |
| CONTEXT | D-12 | Write VALIDATION in place under `.planning/milestones/v2.0-phases/` | 13-03, 13-04 | COVERED | |
| CONTEXT | D-13 | Sync `v2.0-MILESTONE-AUDIT.md` Nyquist table after backfill | 13-04 | COVERED | |
| CONTEXT | D-14 | Recipe lives in `docs/template-v2-jdbc-sink-guide.md` | 13-02 | COVERED | |
| CONTEXT | D-15 | Add `scripts/verify-*-dameng*.ps1` wrapper | 13-02 | COVERED | |
| CONTEXT | D-16 | Script exits non-zero with usage when unconfigured | 13-02 | COVERED | |
| CONTEXT | D-17 | AGENTS.md Commands line for the opt-in Dameng path | 13-02 | COVERED | |

All source items COVERED. Deferred Ideas (Dameng Testcontainers, Phase 12 VALIDATION refresh, DIAL-03 P0 promotion, dual-resolver consolidation) are correctly absent from all plans.

<tasks>

<task type="auto">
  <name>Task 1: Add test-scope dm-jdbc and wire the Dameng live IT to real JDBC</name>
  <files>data-generator-calcite/pom.xml, data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java</files>
  <read_first>
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-CONTEXT.md (locked D-01..D-09)
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-RESEARCH.md (Pattern 1, Pitfall 1, Pitfall 4, Installation snippet)
    - data-generator-calcite/pom.xml (existing test-scope JDBC driver block: mysql-connector-j, postgresql, clickhouse-jdbc)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java (placeholder being replaced)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java (helper signature and the 500-row DDL fixture it creates)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java (sibling call-site precedent)
    - .cursor/rules/java-copyright-class-javadoc.mdc
  </read_first>
  <action>
    1. **POM (per RESEARCH Installation + Pitfall 4):** In `data-generator-calcite/pom.xml`, add a `com.dameng:dm-jdbc` dependency with `<scope>test</scope>` alongside the existing test-scope JDBC drivers. Omit `<version>` — it inherits `${dm.version}` from the root POM `dependencyManagement`. Do **not** add it to `data-generator-service` (already present there at non-test scope) and do **not** introduce Testcontainers Dameng artifacts (out of scope per D-09). No new Maven groupIds; package legitimacy is pre-approved in RESEARCH.

    2. **IT body (D-01, D-03, D-04, D-06, D-07):** Rewrite the single test method in `ChunkedPipelineDamengUpsertIT` to read `DG_DM_JDBC_URL`, `DG_DM_USER`, and `DG_DM_PASSWORD` from the environment and then delegate to `UpsertParitySupport.assertUpsertIdempotent(url, user, password, "dm.jdbc.driver.DmDriver", "dameng")`. Env vars are the canonical and only supported connection input (D-06, D-07); do not add system-property mirrors for URL/user/password — RESEARCH Open Question 2 resolves this discretion to env-only to avoid PowerShell `-D` quoting problems.

    3. **Hard fail on misconfiguration (D-02):** Add a small private static env-reading helper that throws `IllegalStateException` when a required variable is null or blank. The message must name the missing variable and point at the recipe section in `docs/template-v2-jdbc-sink-guide.md`; it must **never** include the variable's value, so a real password can never reach Surefire output (threat T-13-01). Remove the soft-skip abort entirely and remove its JUnit import — the class must contain no soft-skip escape hatch. An unreachable host must be allowed to surface as a propagating JDBC exception; do not catch and convert connection failures into skips. The only legal skip is the pre-existing class-level `@EnabledIf` gate firing when the opt-in flag itself is off.

    4. **Do not touch `UpsertParitySupport`.** It is shared with the PostgreSQL, MySQL, Kingbase, and HighGo ITs; D-04 requires reusing it exactly as-is. If the shared 500-row fixture DDL turns out to be incompatible with a particular Dameng schema, that is an operator prerequisite to document in plan 13-02, not a code change here.

    5. **Javadoc + header (project rules):** Keep the PCI copyright block. Update the type Javadoc to state: default-skipped, how to opt in, the three connection env vars, that a flag-on-but-misconfigured run fails rather than skips (D-02), and that the MERGE SQL unit proof remains the default CI bar (D-05). Keep `@author Gensokyo` and bump `@since` to the current date.

    Out of scope for this task: `.planning/test-matrix.yaml`, `scripts/verify-harness.ps1`, any `src/main` file, and any Testcontainers Dameng work.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "dm-jdbc" data-generator-calcite/pom.xml` shows a dependency with `<scope>test</scope>` and no `<version>` element
    - `rg -n "assertUpsertIdempotent" data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java` matches, with arguments `"dm.jdbc.driver.DmDriver"` and `"dameng"`
    - `rg -n "DG_DM_JDBC_URL|DG_DM_USER|DG_DM_PASSWORD" data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java` matches all three variables
    - `rg -c "^import org.junit.jupiter.api.Assumptions" data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java` returns 0 (no soft-skip import remains)
    - Class-level `@EnabledIf` still references `org.gensokyo.data.calcite.support.DamengTestSupport#damengItEnabled`
    - `data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java` is unmodified
    - `.planning/test-matrix.yaml` and `scripts/verify-harness.ps1` are unmodified
    - `test-compile` exits 0 and the PCI copyright block plus type Javadoc are present on the IT
  </acceptance_criteria>
  <done>The opt-in Dameng IT is real code against an external JDBC endpoint, reusing the shared upsert-parity helper, with no soft-skip path left in the class.</done>
</task>

<!-- planner-discipline-allow: Assumptions -->
<!-- planner-discipline-allow: ^import org.junit.jupiter.api.Assumptions -->

<task type="auto">
  <name>Task 2: Prove flag-on-misconfigured fails the build, and document the env contract on the gate</name>
  <files>data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java</files>
  <read_first>
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-CONTEXT.md (D-02, D-05, D-07, and the Claude's Discretion note allowing minor Javadoc updates on this class)
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-RESEARCH.md (Pitfall 1 warning signs: skipped-versus-failure in Surefire XML)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java (current Javadoc mentions only the boolean flag)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java (as rewritten in Task 1)
    - .cursor/rules/java-copyright-class-javadoc.mdc
  </read_first>
  <action>
    1. **Negative-path evidence (D-02):** Run the IT with the opt-in flag on and `DG_DM_JDBC_URL` unset and confirm Maven reports BUILD FAILURE. Then inspect `data-generator-calcite/target/surefire-reports/` for `ChunkedPipelineDamengUpsertIT` and confirm the result element is a failure, not a skip — this is the concrete warning sign RESEARCH Pitfall 1 names. Record both observations in the plan SUMMARY, including the Surefire report path you checked.

    2. **Gate Javadoc (D-07, D-05, discretion):** Update the `DamengTestSupport` type Javadoc so it documents the complete opt-in contract rather than only the boolean switch: the flag (`-Ddm.it=true` or `DG_DM_IT=true`), the three required connection variables `DG_DM_JDBC_URL` / `DG_DM_USER` / `DG_DM_PASSWORD`, the fact that the gate answers only "is opt-in on?" while the IT itself validates configuration and fails hard when it is incomplete (D-02), and that the default CI bar remains the MERGE SQL unit test (D-05). Point readers at the recipe section in `docs/template-v2-jdbc-sink-guide.md`. Also refresh the stale reference to deferred Testcontainers wiring, which no longer describes the class's role.

    3. **Behavior stays frozen.** Do not change `damengItEnabled()` logic, its signature, or its visibility, and do not move configuration validation into this class — RESEARCH assigns env validation to the IT tier. This task is Javadoc plus verification only. Never write a real credential value into the Javadoc; use placeholder host/schema text only (threat T-13-01).
  </action>
  <verify>
    <automated>$env:DG_DM_IT='true'; Remove-Item Env:DG_DM_JDBC_URL -ErrorAction SilentlyContinue; .\mvnw-jdk25.ps1 -pl data-generator-calcite -am "-Dtest=ChunkedPipelineDamengUpsertIT" "-Dsurefire.failIfNoSpecifiedTests=false" test; $rc = $LASTEXITCODE; Remove-Item Env:DG_DM_IT -ErrorAction SilentlyContinue; if ($rc -ne 0) { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - With the opt-in flag on and no JDBC URL set, Maven exits non-zero (the inverted verify command above exits 0)
    - The Surefire report for `ChunkedPipelineDamengUpsertIT` records a failure result, not a skipped result, under that condition
    - Without the opt-in flag, `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am "-Dtest=ChunkedPipelineDamengUpsertIT" "-Dsurefire.failIfNoSpecifiedTests=false" test` exits 0 with the IT skipped by the class-level gate
    - `rg -n "DG_DM_JDBC_URL" data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java` matches in the Javadoc
    - `damengItEnabled()` remains `public static boolean` with unchanged flag-reading logic
    - No credential-looking value appears in the Javadoc or in the IT failure message
  </acceptance_criteria>
  <done>D-02 is demonstrated by observed evidence, and the gate class documents the full opt-in env contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell env → test JVM | `DG_DM_JDBC_URL` / `DG_DM_USER` / `DG_DM_PASSWORD` cross into the test process as plaintext (accepted per D-08) |
| Test JVM → external Dameng host | Real database credentials and DDL/DML traffic leave the build machine |
| Test JVM → Surefire reports and CI logs | Failure messages and stack traces are persisted and may be shared |

## ASVS

- Level: 1
- Block on: high severity threats

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Information Disclosure | Env-reading helper failure message in `ChunkedPipelineDamengUpsertIT` | high | mitigate | Failure message names only the missing variable and the recipe doc path; the value is never interpolated, so a real `DG_DM_PASSWORD` cannot reach Surefire output or CI logs |
| T-13-02 | Repudiation | Opt-in result reporting | high | mitigate | Soft-skip abort and its import are removed; Task 2 asserts the Surefire result is a failure rather than a skip when the flag is on and config is incomplete, so a false green is not representable |
| T-13-04 | Elevation of Privilege | P0 merge gate scope | high | mitigate | `.planning/test-matrix.yaml` and `scripts/verify-harness.ps1` are declared unmodified in acceptance criteria; DIAL-03 stays deferred |
| T-13-06 | Tampering | Shared `UpsertParitySupport` helper | medium | mitigate | Helper is declared unmodified in acceptance criteria so the PostgreSQL, MySQL, Kingbase, and HighGo ITs cannot regress through this phase |
| T-13-07 | Information Disclosure | Dameng host credentials at rest in the repo | medium | accept | Plaintext env is explicitly accepted for opt-in local use (D-08); no env file or sample credential file is created in this plan, and the docs warning lands in plan 13-02 |
| T-13-SC | Tampering | Maven dependency install (`com.dameng:dm-jdbc`) | high | mitigate | Pre-approved root-BOM-managed artifact already used by `data-generator-service` and `data-generator-writer-database`; version inherited, no new groupId, RESEARCH Package Legitimacy Audit verdict OK |
</threat_model>

<verification>
- `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test-compile` → BUILD SUCCESS
- Flag off: `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am "-Dtest=ChunkedPipelineDamengUpsertIT" "-Dsurefire.failIfNoSpecifiedTests=false" test` → BUILD SUCCESS with the IT skipped (default CI bar unchanged, D-05)
- Flag on, URL unset: same command → BUILD FAILURE with a Surefire failure result (D-02)
- Flag on with a real host configured: same command → BUILD SUCCESS proving chunked upsert idempotency (D-01, D-03). Not exercisable in this environment; RESEARCH Environment Availability records no reachable Dameng host, and D-05 keeps phase completion honest without one
- Default MERGE-unit bar still green: `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test "-Dtest=JdbcSinkSqlBuilderTests" "-Dsurefire.failIfNoSpecifiedTests=false" -q`
</verification>

<success_criteria>
- DIAL-01 code half complete: the IT is real, env-driven, and reuses `UpsertParitySupport` (D-01, D-03, D-04, D-06, D-07)
- A flag-on-but-misconfigured run fails the build with observed Surefire evidence (D-02)
- Default CI behavior and the MERGE-unit merge bar are unchanged (D-05); no P0, harness, or test-matrix file is touched
- Testcontainers Dameng remains absent (D-09)
</success_criteria>

<output>
Create `.planning/phases/13-dameng-live-path-nyquist-hygiene/13-01-SUMMARY.md` when done
</output>
