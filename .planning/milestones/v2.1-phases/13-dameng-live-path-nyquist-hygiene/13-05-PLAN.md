---
phase: 13-dameng-live-path-nyquist-hygiene
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcUpsertSmokeTests.java
autonomous: true
gap_closure: true
requirements:
  - DIAL-01
must_haves:
  truths:
    - "Dameng dialect upsert metrics treat dm-jdbc batch updateCount==0 as one successful upsert row (MERGE WHEN MATCHED UPDATE quirk) while preserving SUCCESS_NO_INFO and updateCount>0 behavior"
    - "MySQL duplicate-key heuristic (updateCount==2), PostgreSQL/Kingbase/HighGo postgres-style heuristic (updateCount>0 only), and negative/EXECUTE_FAILED batch counts remain unchanged"
    - "Unit tests prove countUpsertedRows(..., \"dameng\") with zero, SUCCESS_NO_INFO, and positive update counts without requiring a live Dameng host in CI"
    - "UpsertParitySupport second-run rowsUpserted > 0 assertion can pass when dm-jdbc returns zero counts for successful MERGE updates (live verification optional for maintainers)"
  artifacts:
    - path: "data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java"
      provides: "Dameng-aware upsertCountAsRows branch with inline dm-jdbc MERGE quirk comment"
      contains: "dameng"
    - path: "data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcUpsertSmokeTests.java"
      provides: "Focused countUpsertedRows dameng unit coverage plus postgres/mysql regression guards"
      contains: "countUpsertedRows"
  key_links:
    - from: "JdbcBulkWriteExecutor.upsertCountAsRows"
      to: "UpsertParitySupport.assertUpsertIdempotent"
      via: "writeJdbcBatch → countUpsertedRows → writeStats.addRowsUpserted → second-run SinkWriteMetric.rowsUpserted"
      pattern: "rowsUpserted"
    - from: "JdbcUpsertSmokeTests"
      to: "JdbcBulkWriteExecutor.countUpsertedRows"
      via: "package-private static helper assertions (no live JDBC)"
      pattern: "countUpsertedRows"
---

<objective>
Close the Phase 13 UAT gap: `ChunkedPipelineDamengUpsertIT` fails only on the `rowsUpserted > 0` metrics assertion because `dm-jdbc` 1.8 returns batch `updateCount == 0` for successful Dameng `MERGE WHEN MATCHED THEN UPDATE` while data updates correctly.

Purpose: Fix the dialect metric interpretation in `JdbcBulkWriteExecutor.upsertCountAsRows` and lock the behavior with unit tests — no live Dameng required for CI verification.
Output: Dameng-specific zero-count handling with documented driver quirk, plus `countUpsertedRows` unit tests in `JdbcUpsertSmokeTests`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-UAT.md
@.planning/debug/dameng-rows-upserted-metric.md
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-01-PLAN.md
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcUpsertSmokeTests.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java
@.cursor/rules/java-copyright-class-javadoc.mdc
</context>

## Artifacts this phase produces

*(Gap-closure slice — plan 13-05 only)*

- `JdbcBulkWriteExecutor.upsertCountAsRows` — new `"dameng"` branch before the shared postgres-style rule — **this plan**
- `JdbcUpsertSmokeTests` — `countUpsertedRows` tests for `dameng` (zero / SUCCESS_NO_INFO / positive) plus postgres and mysql regression guards — **this plan**

## Gap source

| Source | Truth | Root cause | Missing |
|--------|-------|------------|---------|
| 13-UAT.md test 1 | Second MERGE run records `rowsUpserted > 0` on live Dameng | dm-jdbc 1.8 returns `updateCount==0` for successful MERGE UPDATE; `upsertCountAsRows` only counts `>0` or `SUCCESS_NO_INFO` | Dameng zero-count metric fix + unit coverage |

**Do not** weaken `UpsertParitySupport` data assertions; fix the counter only (per debug writeup).

<tasks>

<task type="auto">
  <name>Task 1: Dameng-aware upsertCountAsRows for zero batch counts</name>
  <files>data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java</files>
  <read_first>
    - .planning/debug/dameng-rows-upserted-metric.md (root cause and suggested fix)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java (current upsertCountAsRows and isPostgresStyleUpsertDialect)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java (second-run rowsUpserted assertion at line 106)
  </read_first>
  <action>
    In `JdbcBulkWriteExecutor.upsertCountAsRows`, add a **Dameng-specific branch** after the existing `updateCount &lt; 0` guard and **before** the `"mysql"` check:

    1. When `dialect` equals `"dameng"` (exact match — dialect is already normalized lowercase in `resolveDialect`), return `1` for any non-negative `updateCount` (i.e. `updateCount == 0` or `updateCount &gt; 0`). The existing top-of-method `Statement.SUCCESS_NO_INFO` branch still handles `-2` before this path is reached.

    2. Add an inline `//` comment on the Dameng branch documenting the dm-jdbc 1.8 quirk: successful `MERGE WHEN MATCHED THEN UPDATE` batch operations can report `updateCount == 0` despite persisting row changes.

    3. Update the stale class-level Javadoc on `countUpsertedRows` (lines 93–95) so it no longer claims Dameng reports successful ops only as `updateCount &gt; 0`; mention the Dameng zero-count exception briefly.

    4. Remove or replace the misleading inline comment at line 121 (`PostgreSQL ON CONFLICT and Dameng MERGE report successful row ops as updateCount &gt; 0`) — Dameng must no longer share that postgres-style `updateCount &gt; 0` rule because `"dameng"` is handled in its own branch first.

    5. Leave `isPostgresStyleUpsertDialect` membership for `"dameng"` unchanged unless removing it simplifies the branch order; the Dameng early return must execute before the postgres-style `updateCount &gt; 0` rule either way.

    **Out of scope:** Do not change `UpsertParitySupport`, live IT wiring, harness/P0 matrix, or postgres/mysql/kingbase/highgo counting semantics.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcUpsertSmokeTests -DfailIfNoTests=false</automated>
  </verify>
  <acceptance_criteria>
    - `upsertCountAsRows(0, "dameng")` returns `1` (verifiable via new unit test in Task 2)
    - `upsertCountAsRows(Statement.SUCCESS_NO_INFO, "dameng")` still returns `1`
    - `upsertCountAsRows(1, "dameng")` returns `1`
    - `upsertCountAsRows(-3, "dameng")` returns `0`
    - `upsertCountAsRows(0, "postgres")` still returns `0` (postgres-style rule unchanged)
    - `upsertCountAsRows(2, "mysql")` still returns `1` and `upsertCountAsRows(1, "mysql")` still returns `0`
    - Inline comment documents dm-jdbc MERGE zero-count quirk on the Dameng branch
    - No edits to `UpsertParitySupport.java` or live IT classes
  </acceptance_criteria>
  <done>Second-run Dameng MERGE batches with all-zero update counts accumulate `rowsUpserted &gt; 0` when wired through `writeJdbcBatch`.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for countUpsertedRows dameng and dialect regressions</name>
  <files>data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcUpsertSmokeTests.java</files>
  <read_first>
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcUpsertSmokeTests.java (existing mysqlUpsertCountHeuristicTreatsDuplicateUpdateAsUpsert test)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java (countUpsertedRows signature)
    - .planning/debug/dameng-rows-upserted-metric.md (expected inputs: {0, 0, SUCCESS_NO_INFO, 1})
    - .cursor/rules/java-copyright-class-javadoc.mdc (copyright, class Javadoc, public test method docs if any new public helpers — existing class is package-private tests only)
  </read_first>
  <action>
    Extend `JdbcUpsertSmokeTests` with focused `countUpsertedRows` unit tests (no live Dameng JDBC):

    1. Add test `damengUpsertCountTreatsZeroAsSuccessfulUpsert`:
       - `int[] counts = {0, 0, 0};`
       - Assert `JdbcBulkWriteExecutor.countUpsertedRows(counts, "dameng") == 3L`

    2. Add test `damengUpsertCountHandlesSuccessNoInfoAndPositive`:
       - `int[] counts = {java.sql.Statement.SUCCESS_NO_INFO, 0, 1};`
       - Assert total `== 3L`

    3. Add test `damengUpsertCountIgnoresNegativeBatchCounts`:
       - Assert `countUpsertedRows(new int[]{-3}, "dameng") == 0L`

    4. Add test `postgresStyleUpsertCountStillRequiresPositive` (regression guard):
       - Assert `countUpsertedRows(new int[]{1, 0}, "postgres") == 1L`
       - Assert `countUpsertedRows(new int[]{1, 0}, "kingbase") == 1L` and same for `"highgo"`

    5. Keep existing `mysqlUpsertCountHeuristicTreatsDuplicateUpdateAsUpsert` unchanged and green.

    Import `java.sql.Statement` if needed for `SUCCESS_NO_INFO` (prefer static constant over magic `-2`).
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcUpsertSmokeTests -DfailIfNoTests=false</automated>
  </verify>
  <acceptance_criteria>
    - At least four new `@Test` methods cover dameng zero counts, SUCCESS_NO_INFO + mixed counts, negative counts, and postgres/kingbase/highgo zero-count non-counting
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcUpsertSmokeTests` exits 0
    - No new live Dameng IT or Testcontainers dependency is introduced
  </acceptance_criteria>
  <done>CI can prove the Dameng metrics fix without `DG_DM_IT=true` or a reachable Dameng host.</done>
</task>

</tasks>

<verification>
## Plan-level verification

**Automated (CI-safe):**
```powershell
.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcUpsertSmokeTests -DfailIfNoTests=false
```

**Optional maintainer live confirmation (not required for plan done):**
```powershell
$env:DG_DM_IT = "true"
$env:DG_DM_JDBC_URL = "<reachable dm jdbc url>"
$env:DG_DM_USER = "<user>"
$env:DG_DM_PASSWORD = "<password>"
.\scripts\verify-phase13-uat-dameng-live.ps1
```
Expect `ChunkedPipelineDamengUpsertIT` green including `rowsUpserted > 0` on the second run.
</verification>

<success_criteria>
1. `JdbcBulkWriteExecutor.upsertCountAsRows` counts Dameng `updateCount == 0` as one upsert row with documented dm-jdbc quirk
2. Postgres/MySQL/Kingbase/HighGo counting behavior unchanged (regression tests pass)
3. Unit tests in `JdbcUpsertSmokeTests` prove dameng zero/SUCCESS_NO_INFO/positive/negative cases
4. No P0 harness or live-Dameng CI requirement added
</success_criteria>
