---
phase: 17-p1-harness-expansion-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/test-matrix.yaml
autonomous: true
requirements:
  - TEST-09
must_haves:
  truths:
    - "Three new P1 rows exist: exec-http-managed-catalog, exec-http-postgres-dialect, rbac-enable-path (D-01, D-02, D-08)"
    - "HTTP rows link Maven IT classes per D-04: ManagedJdbcCatalogHttpExecuteIT, ManagedJdbcCatalogHttpPostgresUpsertIT"
    - "RBAC row links ConsoleAuthorizationIntegrationIT, ConsoleSecurityDefaultOffIT, ConsoleAuthorizationFilterTest (D-05)"
    - "dist-multi-jvm-worker row retained; notes/status verified only — no duplicate row (D-03, D-06)"
    - "P0 row count remains 15; no P0 id/membership changes (D-07, D-09)"
  prohibitions:
    - statement: "Do not promote any v2.1 proof row to P0 or edit existing P0 rows"
      status: resolved
      verification: "(rg -c 'tier: P0' .planning/test-matrix.yaml) -eq 15; git diff .planning/test-matrix.yaml shows no tier: P0 changes on existing rows"
    - statement: "Do not modify verify-harness.ps1 merge-gate semantics"
      status: resolved
      verification: "git diff scripts/verify-harness.ps1 empty after plan 17-01"
    - statement: "Do not link single-JVM distributed ITs to dist-multi-jvm-worker"
      status: resolved
      verification: "rg -A8 'id: dist-multi-jvm-worker' .planning/test-matrix.yaml shows linked_tests: []"
    - statement: "Do not wire verify-rbac-enable.ps1 or verify-multi-jvm-worker.ps1 into linked_tests"
      status: resolved
      verification: "rbac and dist rows use Maven classes or empty linked_tests only; notes cite supplementary scripts"
  artifacts:
    - path: ".planning/test-matrix.yaml"
      provides: "Three new P1 rows + verified dist-multi-jvm-worker row"
  key_links:
    - from: "exec-http-managed-catalog"
      to: "ManagedJdbcCatalogHttpExecuteIT"
      via: "linked_tests"
      pattern: "ManagedJdbcCatalogHttpExecuteIT"
    - from: "exec-http-postgres-dialect"
      to: "ManagedJdbcCatalogHttpPostgresUpsertIT"
      via: "linked_tests"
      pattern: "ManagedJdbcCatalogHttpPostgresUpsertIT"
    - from: "rbac-enable-path"
      to: "ConsoleAuthorizationIntegrationIT"
      via: "linked_tests"
      pattern: "ConsoleAuthorizationIntegrationIT"
---

<objective>
Wire v2.1 proof paths into `.planning/test-matrix.yaml` as **P1-only** rows: HTTP execute (EXEC-01/02), RBAC enable (SEC-01), and confirm existing multi-JVM row (DIST-01) — without touching the 15-row P0 merge gate (D-01–D-09, D-17).

Purpose: TEST-09 matrix SSOT reflects finished Phase 12–16 proofs; harness can aggregate linked Maven ITs on next verify run.
Output: Updated test-matrix.yaml only. Doc regen and harness smoke deferred to plan 17-02.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-p1-harness-expansion-closeout/17-CONTEXT.md
@.planning/phases/17-p1-harness-expansion-closeout/17-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-http-execute-path-proof/12-CONTEXT.md
@.planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md
@.planning/phases/16-rbac-enable-path/16-CONTEXT.md
@.planning/test-matrix.yaml
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpPostgresUpsertIT.java
@data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationIntegrationIT.java
@data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleSecurityDefaultOffIT.java
@data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationFilterTest.java
</context>

## Artifacts this phase produces

*(Phase-wide inventory; this plan delivers Wave 1 matrix wiring)*

- `exec-http-managed-catalog` P1 row — **this plan**
- `exec-http-postgres-dialect` P1 row — **this plan**
- `rbac-enable-path` P1 row — **this plan**
- Verified `dist-multi-jvm-worker` row (notes/status only) — **this plan**
- *(Wave 2)* `docs/test-feature-matrix.md` regen + harness summary — plan 17-02
- *(Wave 3)* `docs/test-harness.md`, AGENTS.md, milestone state — plan 17-03

## Non-goals (D-17 — out of scope for this plan)

- Regenerating `docs/test-feature-matrix.md` (plan 17-02)
- Running full `verify-harness.ps1` (plan 17-02)
- Editing Phase 12–16 IT implementations unless linkage is broken
- P0 promotion, Surefire skip parsing, workflow edits
- New matrix rows for Dameng live, resolver docs, Nyquist, TEST-V2

<tasks>

<task type="auto">
  <name>Task 1: Capture P0 baseline before edits</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/phases/17-p1-harness-expansion-closeout/17-CONTEXT.md (D-07, D-09)
    - docs/test-harness.md (P0 row id list — 15 rows)
  </read_first>
  <action>
    Before modifying the matrix, record the P0 baseline for post-edit diff:

    ```powershell
    rg -n "tier: P0" .planning/test-matrix.yaml | ForEach-Object { $_ -replace '.*id: ', '' } | Sort-Object
    (rg -c "tier: P0" .planning/test-matrix.yaml)
    ```

    Save the 15 P0 row ids mentally or in plan summary — executor must confirm identical set after Task 3. Expected ids match `docs/test-harness.md` P0 list: `calcite-scenario-v2`, `udf-sql`, `udf-script`, `udf-java-plugin`, `transform-json`, `transform-mask`, `transform-lookup`, `v2-streaming-csv`, `v2-streaming-json`, `v2-jdbc-upsert-pg-mysql`, `v2-dialect-dameng`, `v2-dialect-kingbase`, `v2-dialect-highgo`, `v2-dialect-postgres`, `v2-dialect-clickhouse`.

    **Do not edit any file in this task** — baseline capture only.
  </action>
  <verify>
    <automated>(rg -c "tier: P0" .planning/test-matrix.yaml) -eq 15; if (-not $?) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - Baseline P0 count is 15 before any matrix edits
    - Executor has P0 id list for post-edit comparison
  </acceptance_criteria>
  <done>P0 baseline captured; merge-gate invariant documented for diff review.</done>
</task>

<task type="auto">
  <name>Task 2: Add exec-http-managed-catalog and exec-http-postgres-dialect P1 rows</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/phases/17-p1-harness-expansion-closeout/17-CONTEXT.md (D-01, D-02, D-04, D-08, D-10)
    - .planning/phases/12-http-execute-path-proof/12-CONTEXT.md (D-08 separate ITs)
    - .planning/test-matrix.yaml (append after dist-multi-jvm-worker row)
    - ManagedJdbcCatalogHttpExecuteIT.java (EXEC-01 evidence bar)
    - ManagedJdbcCatalogHttpPostgresUpsertIT.java (@EnabledIf Docker — D-10)
  </read_first>
  <action>
    Append **two** rows to `.planning/test-matrix.yaml` `rows:` (after existing `dist-multi-jvm-worker` entry). Do **not** modify any existing row.

    **Row 1 — EXEC-01:**

    ```yaml
    - id: exec-http-managed-catalog
      capability: http-execute-managed-catalog
      adapter: rest
      test_types: [integration]
      owner_module: data-generator-service
      status: covered
      tier: P1
      linked_tests: [ManagedJdbcCatalogHttpExecuteIT]
      notes: >
        Phase 12 EXEC-01 — POST /task/run/{id} on published V2 template with managed JDBC
        dataSourceId; poll TaskExecution to SUCCESS; COUNT(*) on managed H2 sink pool.
        P1 non-blocking; not P0 until future milestone decision (Pitfall 8).
    ```

    **Row 2 — EXEC-02:**

    ```yaml
    - id: exec-http-postgres-dialect
      capability: http-execute-postgres-dialect
      adapter: rest
      test_types: [integration]
      owner_module: data-generator-service
      status: covered
      tier: P1
      linked_tests: [ManagedJdbcCatalogHttpPostgresUpsertIT]
      notes: >
        Phase 12 EXEC-02 — same HTTP spine with managed catalog + PostgreSQL Testcontainers
        ON CONFLICT upsert (separate IT per Phase 12 D-08). @EnabledIf(DockerTestSupport)
        may skip without Docker — row may report skipped-conditional in harness summary;
        acceptable and non-blocking for P1 (D-10). Does not affect P0 merge gate.
    ```

    Initial `status: covered` is acceptable when ITs are known green from Phase 12; executor may set `skipped-conditional` after first harness run if Docker IT skipped (D-10 discretion).

    Confirm P0 count still **15** and no existing row changed.
  </action>
  <verify>
    <automated>rg -n "id: exec-http-managed-catalog|id: exec-http-postgres-dialect" .planning/test-matrix.yaml; rg -A6 "id: exec-http-managed-catalog" .planning/test-matrix.yaml | Select-String "ManagedJdbcCatalogHttpExecuteIT"; rg -A6 "id: exec-http-postgres-dialect" .planning/test-matrix.yaml | Select-String "ManagedJdbcCatalogHttpPostgresUpsertIT"; (rg -c "tier: P0" .planning/test-matrix.yaml) -eq 15</automated>
  </verify>
  <acceptance_criteria>
    - Both HTTP rows exist with `tier: P1`
    - linked_tests match D-04 IT class names exactly
    - Notes cite Phase 12, P1 non-blocking, EXEC-02 Docker skip acceptable
    - P0 count unchanged at 15
  </acceptance_criteria>
  <done>EXEC-01 and EXEC-02 proofs wired to harness matrix as P1 rows.</done>
</task>

<task type="auto">
  <name>Task 3: Add rbac-enable-path P1 row and verify dist-multi-jvm-worker</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/phases/17-p1-harness-expansion-closeout/17-CONTEXT.md (D-03, D-05, D-06)
    - .planning/phases/16-rbac-enable-path/16-CONTEXT.md (deferred matrix row)
    - .planning/test-matrix.yaml (existing dist-multi-jvm-worker block lines 602–616)
    - scripts/verify-rbac-enable.ps1 (supplementary — notes only)
  </read_first>
  <action>
    1. **Append RBAC row** (SEC-01):

    ```yaml
    - id: rbac-enable-path
      capability: console-rbac-enable
      adapter: console-api
      test_types: [integration, unit]
      owner_module: data-generator-service
      status: covered
      tier: P1
      linked_tests:
        - ConsoleAuthorizationIntegrationIT
        - ConsoleSecurityDefaultOffIT
        - ConsoleAuthorizationFilterTest
      notes: >
        Phase 16 SEC-01 — header RBAC when data.generator.console-security.enabled=true.
        Default-off guard (ConsoleSecurityDefaultOffIT) + deny/allow HTTP IT + filter unit.
        Supplementary operator UAT: scripts/verify-rbac-enable.ps1 (Maven + optional
        Playwright); not wired into verify-harness.ps1 Maven aggregation. P1 non-blocking.
    ```

    **Discretion (D-05):** Optionally add `ConsoleUdfAuthorizationFilterTest` to `linked_tests` if already green in Phase 16 verify script — not required for done.

    2. **Verify `dist-multi-jvm-worker`** (D-03): Do **not** add a duplicate row. Confirm existing row has:
       - `tier: P1`
       - `linked_tests: []`
       - `notes` references `scripts/verify-multi-jvm-worker.ps1`
       - `status: covered` (update from `pending` only if harness summary drifted)

    Touch `dist-multi-jvm-worker` notes only if Phase 17 cross-reference to TEST-09 is missing; do not link Maven distributed ITs (D-06).

    3. **Final P0 invariant check:** Compare P0 row ids to Task 1 baseline — must be identical set of 15.
  </action>
  <verify>
    <automated>rg -n "id: rbac-enable-path" .planning/test-matrix.yaml; rg -A10 "id: rbac-enable-path" .planning/test-matrix.yaml | Select-String "ConsoleAuthorizationIntegrationIT|ConsoleSecurityDefaultOffIT|ConsoleAuthorizationFilterTest"; rg -A8 "id: dist-multi-jvm-worker" .planning/test-matrix.yaml | Select-String "tier: P1"; rg -A8 "id: dist-multi-jvm-worker" .planning/test-matrix.yaml | Select-String "linked_tests: \[\]"; (rg -c "tier: P1" .planning/test-matrix.yaml) -ge 12</automated>
  </verify>
  <acceptance_criteria>
    - `rbac-enable-path` row exists with tier P1 and three linked IT/unit classes
    - Notes cite verify-rbac-enable.ps1 as supplementary, not harness gate
    - `dist-multi-jvm-worker` unchanged except optional notes tweak; still script-primary
    - P0 count 15; P0 ids unchanged from baseline
    - Total P1 rows ≥ 12 (was 9 + 3 new)
  </acceptance_criteria>
  <done>All four TEST-09 capability areas represented in matrix; P0 gate frozen.</done>
</task>

</tasks>

## Verification

```powershell
# New rows present
rg -n "exec-http-managed-catalog|exec-http-postgres-dialect|rbac-enable-path|dist-multi-jvm-worker" .planning/test-matrix.yaml

# P0 invariant
(rg -c "tier: P0" .planning/test-matrix.yaml) -eq 15

# No harness script edits
git diff scripts/verify-harness.ps1
```

## Success criteria

- [ ] Three new P1 rows appended; dist-multi-jvm-worker verified not duplicated
- [ ] HTTP + RBAC linked_tests match Phase 12/16 IT class names
- [ ] P0 membership/count unchanged (15 rows)
- [ ] verify-harness.ps1 untouched
