---
phase: 16-rbac-enable-path
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - docs/staging-console-rbac.md
  - AGENTS.md
  - docs/operator-console-usage.md
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "docs/staging-console-rbac.md is the single focused operator enable runbook (D-01)"
    - "Doc covers property keys, X-Console-Role / X-Console-Actor headers, role→permission summary, profile names, verify one-liners (D-01)"
    - "docs/operator-console-usage.md cross-links to new doc without duplicating full recipe (D-02)"
    - "AGENTS.md Commands section has comment + path pointer to verify script and doc (Phase 13–15 pattern) (D-01)"
    - "Profile contract table documents D-09: base/e2e/distributed off; staging/e2e-rbac on (D-09)"
    - "Doc states verify-rbac-enable.ps1 is SEC-01 proof; not P0 merge gate (D-08, D-10)"
  artifacts:
    - path: "docs/staging-console-rbac.md"
      provides: "Header RBAC enable + verify runbook"
      min_lines: 60
    - path: "AGENTS.md"
      provides: "Commands catalog entry for RBAC verify"
    - path: "docs/operator-console-usage.md"
      provides: "Cross-link from Configuration reference"
  key_links:
    - from: "docs/operator-console-usage.md"
      to: "docs/staging-console-rbac.md"
      via: "Configuration reference link near console-security block"
      pattern: "staging-console-rbac"
    - from: "AGENTS.md"
      to: "scripts/verify-rbac-enable.ps1"
      via: "Commands fenced block"
      pattern: "verify-rbac-enable"
    - from: "docs/staging-console-rbac.md"
      to: "application-staging.yaml"
      via: "Profile enable section"
      pattern: "spring.profiles.active=staging"
---

<objective>
Package SEC-01 for operators: focused staging RBAC runbook, AGENTS.md pointer, and operator-console cross-link (D-01, D-02, D-09).

Purpose: Close the gap between shipped RBAC code and discoverable enable/verify documentation without duplicating content across two long docs.
Output: `docs/staging-console-rbac.md`, AGENTS.md entry, one cross-link in operator-console-usage.md. Depends on plan 16-01 (verify script exists).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-rbac-enable-path/16-CONTEXT.md
@.planning/phases/16-rbac-enable-path/16-VALIDATION.md
@.planning/phases/16-rbac-enable-path/16-01-PLAN.md
@.planning/ROADMAP.md
@docs/operator-console-usage.md
@AGENTS.md
@data-generator-service/src/main/resources/application-staging.yaml
@data-generator-service/src/main/resources/application-e2e-rbac.yaml
@data-generator-service/src/main/java/org/gensokyo/data/security/ConsoleRole.java
@data-generator-service/src/main/java/org/gensokyo/data/security/ConsolePermission.java
@scripts/verify-rbac-enable.ps1
</context>

## Artifacts this phase produces

- `docs/staging-console-rbac.md` — enable runbook — **this plan**
- `AGENTS.md` — Commands pointer — **this plan**
- `docs/operator-console-usage.md` — cross-link only — **this plan**

## Non-goals (D-10)

- P1 matrix row / test-matrix.yaml (Phase 17)
- Full duplicate of operator-console-usage Configuration section
- README.md drive-by churn
- Changing Spring profiles or yaml files (document only)

<tasks>

<task type="auto">
  <name>Task 1: Create docs/staging-console-rbac.md enable runbook</name>
  <files>docs/staging-console-rbac.md</files>
  <read_first>
    - .planning/phases/16-rbac-enable-path/16-CONTEXT.md (D-01, D-06, D-07, D-09)
    - data-generator-service/src/main/resources/application-staging.yaml
    - data-generator-service/src/main/resources/application-e2e-rbac.yaml
    - data-generator-service/src/main/java/org/gensokyo/data/security/ConsoleRole.java
    - data-generator-service/src/main/java/org/gensokyo/data/config/ConsoleSecurityProperties.java
    - docs/operator-console-usage.md (Configuration reference — avoid duplicating prose)
  </read_first>
  <action>
    Create `docs/staging-console-rbac.md` with these sections:

    1. **Purpose** — SEC-01: opt-in header RBAC for `/api/**`; default-off locally.

    2. **Property keys** — table or list:
       - `data.generator.console-security.enabled` (default `false` in Java)
       - `data.generator.console-security.role-header` (default `X-Console-Role`)
       - `data.generator.console-security.actor-header` (default `X-Console-Actor`, optional audit)

    3. **Required headers** — example curl:
       ```bash
       curl -H "X-Console-Role: VIEWER" http://localhost:8080/api/templates/scenarios
       curl -H "X-Console-Role: ADMIN" -H "X-Console-Actor: ops-alice" ...
       ```

    4. **Role → permission summary** — compact table sourced from `ConsoleRole` / `ConsolePermission`:
       - VIEWER: TEMPLATE_READ, JOB_READ, AUDIT_READ
       - EDITOR: + TEMPLATE_EDIT
       - OPERATOR: + TEMPLATE_RUN
       - DATASOURCE_ADMIN: DATASOURCE_ADMIN + reads
       - ADMIN: all permissions (publish requires ADMIN)

    5. **Profile contract (D-09)** — table:

       | Profile file | `spring.profiles.active` | `console-security.enabled` |
       |--------------|--------------------------|----------------------------|
       | base `application.yaml` | (default) | false (Java default, no yaml block) |
       | `application-e2e.yaml` | `e2e` | **false** |
       | `application-e2e-distributed.yaml` | `e2e-distributed` | **false** |
       | `application-distributed-staging.yaml` | `distributed-staging` | **false** |
       | `application-staging.yaml` | `staging` | **true** (port 8080) |
       | `application-e2e-rbac.yaml` | `e2e-rbac` | **true** |

    6. **Enable staging on host** — `spring.profiles.active=staging` (tarball: `conf/application-staging.yaml`).

    7. **Verify one-liners:**
       ```powershell
       .\scripts\verify-rbac-enable.ps1 -SkipPlaywright
       ```
       Note: Maven slice is primary SEC-01 proof; Playwright optional (plan 16-03 / `-SkipPlaywright`).

    8. **E2E env distinction (D-07)** — short subsection:
       - `DG_E2E_RBAC=true` — dedicated RBAC Podman profile (`e2e-rbac`); runs `rbac.console.spec.ts` + `rbac.ui.spec.ts`
       - `DG_E2E_GOVERNANCE_STAGING=true` — injects ADMIN headers in default e2e helpers (`e2e/helpers/api.ts`); **not** the same as full RBAC profile
       - Default `verify-console.ps1` / `e2e` profile: RBAC **off** (`application-e2e.yaml`)

    9. **Non-goals / deferred** — P1 matrix row Phase 17; SEC-02 default-on production; OAuth2/JWT out of scope.

    Link to `docs/operator-console-usage.md` for general console usage; this doc owns the enable recipe.
  </action>
  <verify>
    <automated>Test-Path docs/staging-console-rbac.md; rg -n "verify-rbac-enable|X-Console-Role|application-staging|application-e2e-rbac|enabled: false|enabled: true" docs/staging-console-rbac.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc exists with property keys, headers, roles, profile table, verify one-liner
    - Profile contract matches current yaml files (scout values)
    - States script is not P0 merge gate
  </acceptance_criteria>
  <done>Operators have a single SEC-01 enable runbook under docs/.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-link operator-console-usage.md Configuration reference</name>
  <files>docs/operator-console-usage.md</files>
  <read_first>
    - docs/operator-console-usage.md lines ~296–306 (console-security block + staging mention)
    - .planning/phases/16-rbac-enable-path/16-CONTEXT.md (D-02)
  </read_first>
  <action>
    In the **Configuration reference** section, after the existing staging sample profile sentence (~line 304):

    **Replace or augment** the verification line to cross-link instead of being the sole enable guide:

    - Keep the yaml example block and role summary bullets (they are useful quick reference).
    - Change staging verification text to point at the focused doc:
      - Add: `See [staging-console-rbac.md](staging-console-rbac.md) for enable profiles, header contract, and `verify-rbac-enable.ps1`.`
    - Do **not** paste the full profile table or verify script flow — D-02 forbids duplication.
    - Optionally update the line referencing `verify-console.ps1 (includes e2e-rbac Playwright)` to note that is the **full console pipeline**, while SEC-01-focused verify is `verify-rbac-enable.ps1`.

    Surgical edit only — no unrelated section rewrites.
  </action>
  <verify>
    <automated>rg -n "staging-console-rbac" docs/operator-console-usage.md</automated>
  </verify>
  <acceptance_criteria>
    - Cross-link present in Configuration reference
    - Full enable recipe not duplicated (profile table lives in staging-console-rbac.md only)
  </acceptance_criteria>
  <done>Operator console doc points maintainers to the focused RBAC runbook.</done>
</task>

<task type="auto">
  <name>Task 3: Add AGENTS.md Commands pointer for verify-rbac-enable.ps1</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md Commands section (Phase 15 verify-multi-jvm-worker.ps1 pointer pattern ~line 106)
    - .planning/phases/15-multi-jvm-worker-e2e/15-03-PLAN.md (comment + path pattern)
  </read_first>
  <action>
    In AGENTS.md **Commands** fenced block, add after the Phase 15 multi-JVM entry (or adjacent verify scripts):

    ```powershell
    # Phase 16 SEC-01 RBAC enable path (Maven IT slice + optional Podman Playwright; not P0 merge gate)
    .\scripts\verify-rbac-enable.ps1 -SkipPlaywright
    ```

    Add a one-line docs pointer in the same area or a short "Docs" bullet if the file has a docs subsection:

    ```
    # RBAC staging enable runbook: docs/staging-console-rbac.md
    ```

    Match Phase 13–15 style: comment describes phase/requirement, path is copy-paste ready.

    Do **not** add to merge criteria section as a blocker — reinforce supplementary nature near Phase 8/9 UAT note if helpful.
  </action>
  <verify>
    <automated>rg -n "verify-rbac-enable|staging-console-rbac" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md contains verify script command with -SkipPlaywright
    - Doc path pointer present
    - No claim that script is P0 merge gate
  </acceptance_criteria>
  <done>Contributors discover SEC-01 verify path from AGENTS.md.</done>
</task>

</tasks>

## Verification

After all tasks:

```powershell
rg -n "staging-console-rbac|verify-rbac-enable" docs/ AGENTS.md
rg -n "console-security" docs/staging-console-rbac.md
```

## Success criteria

- [ ] Single focused operator doc with D-09 profile contract
- [ ] operator-console-usage cross-link without duplication
- [ ] AGENTS.md pointer matches Phase 13–15 pattern
