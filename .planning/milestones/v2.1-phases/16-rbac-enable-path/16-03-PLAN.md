---
phase: 16-rbac-enable-path
plan: 03
type: execute
wave: 3
depends_on:
  - 16-01
  - 16-02
files_modified:
  - scripts/verify-rbac-enable.ps1
  - docs/staging-console-rbac.md
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "verify-rbac-enable.ps1 optional Playwright leg runs rbac.console.spec.ts + rbac.ui.spec.ts with DG_E2E_RBAC=true (D-06)"
    - "Podman container uses DG_SPRING_PROFILES_ACTIVE=e2e-rbac (application-e2e-rbac.yaml) mirroring e2e-podman.ps1 (D-06)"
    - "-SkipPlaywright exits after Maven slice with success — CI-friendly default (D-08)"
    - "docs/staging-console-rbac.md documents full vs Maven-only verify and Podman prerequisites (D-06, D-07)"
    - "No new Playwright specs unless audit finds uncovered scenarios (D-06 — reuse existing)"
  prohibitions:
    - statement: "Podman-only proof is not sole SEC-01 gate"
      status: resolved
      verification: "Doc + script header state Maven slice is primary; Playwright optional"
    - statement: "Do not wire verify-rbac-enable into verify-harness P0 gate"
      status: resolved
      verification: "git diff scripts/verify-harness.ps1 empty"
  artifacts:
    - path: "scripts/verify-rbac-enable.ps1"
      provides: "Full SEC-01 verify: Maven + optional Podman Playwright RBAC specs"
  key_links:
    - from: "scripts/verify-rbac-enable.ps1"
      to: "scripts/e2e-podman.ps1"
      via: "Same e2e-rbac profile + DG_E2E_RBAC pattern (extract/minimize duplication)"
      pattern: "e2e-rbac"
    - from: "scripts/verify-rbac-enable.ps1"
      to: "data-generator-console-web/e2e/specs/rbac.console.spec.ts"
      via: "npx playwright test rbac.console.spec.ts rbac.ui.spec.ts"
      pattern: "DG_E2E_RBAC"
---

<objective>
Complete SEC-01 verify packaging: optional Podman Playwright leg in `verify-rbac-enable.ps1` and doc notes for full vs Maven-only runs (D-06, D-07, D-08).

Purpose: Operators who have Podman can run the same RBAC E2E specs as `e2e-podman.ps1` without the full console verify pipeline; Maven remains primary proof.
Output: Extended verify script + doc subsection. Depends on 16-01 (Maven script) and 16-02 (base doc).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-rbac-enable-path/16-CONTEXT.md
@.planning/phases/16-rbac-enable-path/16-VALIDATION.md
@.planning/phases/16-rbac-enable-path/16-01-PLAN.md
@.planning/phases/16-rbac-enable-path/16-02-PLAN.md
@scripts/e2e-podman.ps1
@scripts/verify-phase8-uat-rw-streaming-upsert.ps1
@data-generator-console-web/e2e/specs/rbac.console.spec.ts
@data-generator-console-web/e2e/specs/rbac.ui.spec.ts
@data-generator-console-web/e2e/helpers/api.ts
@data-generator-service/src/main/resources/application-e2e-rbac.yaml
@docs/staging-console-rbac.md
</context>

## Artifacts this phase produces

- `scripts/verify-rbac-enable.ps1` — Playwright leg — **this plan**
- `docs/staging-console-rbac.md` — Playwright/Podman subsection update — **this plan**

## Non-goals (D-10)

- New Playwright specs (unless audit documents a gap and adds minimal test — unlikely)
- Wiring into verify-harness P0 or test-matrix.yaml
- Full `verify-console.ps1` replacement
- Multi-JVM + RBAC combined Podman run

<tasks>

<task type="auto">
  <name>Task 1: Audit existing RBAC E2E coverage vs IT deny/allow matrix</name>
  <files>docs/staging-console-rbac.md</files>
  <read_first>
    - data-generator-console-web/e2e/specs/rbac.console.spec.ts
    - data-generator-console-web/e2e/specs/rbac.ui.spec.ts
    - data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationIntegrationIT.java
    - .planning/phases/16-rbac-enable-path/16-CONTEXT.md (D-06 — new specs only if gap)
  </read_first>
  <action>
    Compare IT deny paths (403 missing header, VIEWER POST, EDITOR publish) and governance (draft run 400) against Playwright RBAC specs.

    **Expected scout outcome:** Playwright already covers API deny/allow + UI role picker; no new spec files required.

    Document audit conclusion in `docs/staging-console-rbac.md` under **E2E coverage** (2–3 sentences): list spec files and what they prove; state Maven IT is primary backend evidence, Playwright is secondary opt-in.

    If a genuine gap is found (unlikely), add **one** focused Playwright test to existing spec file — do not create new spec files without gap proof.
  </action>
  <verify>
    <automated>rg -n "E2E coverage|rbac\.console\.spec|rbac\.ui\.spec" docs/staging-console-rbac.md</automated>
  </verify>
  <acceptance_criteria>
    - Audit documented in staging-console-rbac.md
    - No new Playwright files unless gap proven in doc
  </acceptance_criteria>
  <done>SEC-01 E2E scope is explicit; no speculative spec expansion.</done>
</task>

<task type="auto">
  <name>Task 2: Add optional Podman Playwright leg to verify-rbac-enable.ps1</name>
  <files>scripts/verify-rbac-enable.ps1</files>
  <read_first>
    - scripts/e2e-podman.ps1 lines 89–104 (e2e-rbac restart + DG_E2E_RBAC Playwright block)
    - scripts/verify-phase8-uat-rw-streaming-upsert.ps1 (Podman build/run + SkipPlaywright gate)
    - scripts/verify-rbac-enable.ps1 (16-01 Maven slice — extend, do not rewrite)
  </read_first>
  <action>
    Extend `scripts/verify-rbac-enable.ps1` after Maven slice succeeds:

    **When `-SkipPlaywright`:** keep existing early exit (unchanged behavior).

    **When Playwright requested (no `-SkipPlaywright`):**

    1. Params to add/confirm:
       - `[switch]$SkipBuild` — skip Maven package + podman build when image exists
       - `[string]$ImageTag = 'dg-rbac-enable-verify:local'`
       - `[string]$ContainerName = 'dg-rbac-enable-verify'`
       - `[int]$HostPort = 9876`

    2. `Write-Step "Checking Podman"` — `podman info | Out-Null`

    3. Build unless `-SkipBuild`:
       - `Invoke-RepoMaven` console-web package + service `-am -DskipTests package` (mirror phase 8)

    4. `podman build` from `data-generator-service/Containerfile`

    5. Run container:
       ```powershell
       podman run -d --name $ContainerName -p "${HostPort}:9876" `
         -e DG_SPRING_PROFILES_ACTIVE=e2e-rbac $ImageTag
       ```
       Wait health on `http://127.0.0.1:${HostPort}/healthz`

    6. Playwright (mirror `e2e-podman.ps1` RBAC block):
       ```powershell
       Push-Location data-generator-console-web
       $env:DG_E2E_BASE_URL = "http://127.0.0.1:${HostPort}/console/"
       $env:DG_E2E_API_URL = "http://127.0.0.1:${HostPort}"
       $env:DG_E2E_RBAC = 'true'
       npx playwright test e2e/specs/rbac.console.spec.ts e2e/specs/rbac.ui.spec.ts
       ```
       Clean up env vars in `finally`; `podman rm -f $ContainerName`

    7. Success message:
       `[SUCCESS] SEC-01 RBAC enable-path verification passed (Maven + Playwright).`

    **Do not** set `DG_E2E_GOVERNANCE_STAGING` for this leg — that is a different profile contract (D-07).

    Reuse `Wait-Health` helper from phase 8 scripts (inline copy OK to avoid cross-script dependency).
  </action>
  <verify>
    <automated>rg -n "e2e-rbac|DG_E2E_RBAC|rbac\.console\.spec|SkipPlaywright" scripts/verify-rbac-enable.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `-SkipPlaywright` still Maven-only, exit 0
    - Full run invokes Podman with `e2e-rbac` profile and both RBAC spec files
    - Script does not call full `verify-console.ps1` or `e2e-podman.ps1` wholesale
  </acceptance_criteria>
  <done>Optional Playwright leg completes SEC-01 verify script packaging.</done>
</task>

<task type="auto">
  <name>Task 3: Update staging-console-rbac.md with full verify instructions</name>
  <files>docs/staging-console-rbac.md</files>
  <read_first>
    - docs/staging-console-rbac.md (16-02 output)
    - scripts/verify-rbac-enable.ps1 (final flags)
  </read_first>
  <action>
    Add/update **Verification** section in `docs/staging-console-rbac.md`:

    1. **Maven-only (recommended for CI / no Podman):**
       ```powershell
       .\scripts\verify-rbac-enable.ps1 -SkipPlaywright
       ```

    2. **Full verify (Maven + Playwright RBAC specs):**
       ```powershell
       .\scripts\verify-rbac-enable.ps1
       ```
       Prerequisites: Podman, Node 22+, Chromium via `npx playwright install chromium`

    3. **Flags:** `-SkipBuild`, `-SkipPlaywright`, `-KeepContainer` (if implemented), `-HostPort`

    4. **Relation to verify-console.ps1:** full console pipeline includes RBAC leg among many specs; use `verify-rbac-enable.ps1` for focused SEC-01 sign-off.

    5. Reinforce D-07: `DG_E2E_RBAC` vs `DG_E2E_GOVERNANCE_STAGING` — link to `e2e/helpers/api.ts` behavior.

    Keep edit surgical — append/update Verification section only.
  </action>
  <verify>
    <automated>rg -n "SkipPlaywright|verify-rbac-enable|Podman|DG_E2E_RBAC" docs/staging-console-rbac.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc describes both Maven-only and full verify paths
    - Podman prerequisites stated
    - Script not described as P0 merge gate
  </acceptance_criteria>
  <done>Operators can choose Maven-only or full RBAC E2E verify from the runbook.</done>
</task>

</tasks>

## Verification

After all tasks:

```powershell
# Script contract
rg -n "e2e-rbac|DG_E2E_RBAC|SkipPlaywright" scripts/verify-rbac-enable.ps1

# Doc contract
rg -n "verify-rbac-enable|SkipPlaywright|DG_E2E" docs/staging-console-rbac.md

# Maven-only still green
powershell -NoProfile -File scripts/verify-rbac-enable.ps1 -SkipPlaywright
```

## Success criteria

- [ ] Optional Playwright leg mirrors e2e-podman RBAC block
- [ ] `-SkipPlaywright` remains the CI-friendly path
- [ ] Doc covers full vs Maven-only verify
- [ ] No harness/matrix changes; no new specs unless audit found gap
