---
phase: 14-resolver-ownership-docs
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - AGENTS.md
  - docs/template-v2-datasource-and-secret-governance.md
  - data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
autonomous: true
requirements:
  - RES-01
must_haves:
  truths:
    - "AGENTS.md Commands section includes a maintainer pointer to docs/jdbc-resolver-ownership.md (D-01, D-02, Phase 13 packaging precedent)"
    - "Optional sibling cross-link from template-v2-datasource-and-secret-governance.md helps operators find the ownership doc (D discretion)"
    - "Optional one-line @see on DefaultRuntimeJdbcEndpointResolver points to the doc without making Javadoc the source of truth (D-02)"
    - "P0 gate, test-matrix, verify-harness, and product behavior remain untouched (D-09)"
  artifacts:
    - path: "AGENTS.md"
      provides: "Commands/docs pointer to jdbc-resolver-ownership.md"
    - path: "docs/jdbc-resolver-ownership.md"
      provides: "Related docs back-link if sibling cross-link added"
  key_links:
    - from: "AGENTS.md"
      to: "docs/jdbc-resolver-ownership.md"
      via: "Commands section comment + path (Phase 13 Dameng pattern)"
      pattern: "jdbc-resolver-ownership"
    - from: "docs/template-v2-datasource-and-secret-governance.md"
      to: "docs/jdbc-resolver-ownership.md"
      via: "optional Related reading bullet"
      pattern: "jdbc-resolver-ownership"
---

<objective>
Package RES-01 for discoverability: AGENTS.md pointer, optional governance-doc cross-link, and optional lightweight Javadoc `@see` on `DefaultRuntimeJdbcEndpointResolver` (D-01, D-02).

Purpose: Ownership content buried only in `docs/` is easy to miss; Phase 13 established the comment+path Commands pattern for maintainer recipes.
Output: one AGENTS.md entry, optional sibling link, optional single-line Javadoc cross-reference. Depends on plan 14-01 delivering the primary doc.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-resolver-ownership-docs/14-CONTEXT.md
@.planning/phases/14-resolver-ownership-docs/14-VALIDATION.md
@.planning/phases/14-resolver-ownership-docs/14-01-PLAN.md
@docs/jdbc-resolver-ownership.md
@AGENTS.md
@docs/template-v2-datasource-and-secret-governance.md
@data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
</context>

## Artifacts this phase produces

*(Phase-wide inventory; this plan delivers packaging / discoverability)*

- `docs/jdbc-resolver-ownership.md` — primary doc — plan 14-01
- `AGENTS.md` — Commands pointer — **this plan**
- `docs/template-v2-datasource-and-secret-governance.md` — optional cross-link — **this plan (discretion)**
- `DefaultRuntimeJdbcEndpointResolver.java` — optional `@see` only — **this plan (discretion)**

<tasks>

<task type="auto">
  <name>Task 1: Add AGENTS.md Commands pointer and optional sibling cross-link</name>
  <files>AGENTS.md, docs/template-v2-datasource-and-secret-governance.md, docs/jdbc-resolver-ownership.md</files>
  <read_first>
    - .planning/phases/14-resolver-ownership-docs/14-CONTEXT.md (D-01, D-02)
    - AGENTS.md (Commands fenced block — mirror Phase 13 comment style at lines ~99–100 for Dameng)
    - docs/jdbc-resolver-ownership.md (as produced by plan 14-01)
    - docs/template-v2-datasource-and-secret-governance.md (find Related reading / see also section — add one bullet if natural)
  </read_first>
  <action>
    1. **AGENTS.md (required, D-02):** In the Commands section fenced code block, add one entry matching the existing pattern:

       ```markdown
       # JDBC resolver ownership + call-site inventory (catalog-side vs V2 execute-path; see docs/jdbc-resolver-ownership.md)
       # (no runnable script — documentation only)
       ```

       Use a short comment line naming the doc purpose and the path `docs/jdbc-resolver-ownership.md`. Do **not** add a verify script or reorder unrelated entries aggressively — one surgical insertion near other docs/UAT pointers (after Phase 13 Dameng block is fine).

    2. **Sibling cross-link (discretion):** If `docs/template-v2-datasource-and-secret-governance.md` has a Related reading / See also section, add one bullet linking to `docs/jdbc-resolver-ownership.md` with text like "JDBC resolver ownership (catalog vs execute-path)". If no natural section exists, skip rather than invent a new doc structure — note skip in SUMMARY.

    3. **Back-link (if sibling link added):** In `docs/jdbc-resolver-ownership.md` Related docs section, add reciprocal link to `docs/template-v2-datasource-and-secret-governance.md`.

    Out of scope: README.md churn, new verify scripts, test-matrix or harness edits.
  </action>
  <verify>
    <automated>rg -n "jdbc-resolver-ownership" AGENTS.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "verify-harness|test-matrix" AGENTS.md | Select-String "jdbc-resolver"; if ($?) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "jdbc-resolver-ownership" AGENTS.md` matches inside or adjacent to the Commands section
    - AGENTS entry is comment-style (documentation pointer); no fake runnable command implied as a gate
    - If governance doc cross-link added, `rg -n "jdbc-resolver-ownership" docs/template-v2-datasource-and-secret-governance.md` matches
    - `.planning/test-matrix.yaml`, `scripts/verify-harness.ps1`, and `.github/workflows/**` are unmodified
  </acceptance_criteria>
  <done>Maintainers can discover the ownership doc from AGENTS.md Commands without searching the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Optional Javadoc @see cross-link on DefaultRuntimeJdbcEndpointResolver</name>
  <files>data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java</files>
  <read_first>
    - .planning/phases/14-resolver-ownership-docs/14-CONTEXT.md (D-02: doc is source of truth; Javadoc lightly cross-linked only)
    - data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java (existing class Javadoc block)
    - .cursor/rules/java-copyright-class-javadoc.mdc (preserve copyright; minimal edit)
  </read_first>
  <action>
    If the class Javadoc does not already reference the maintainer doc, add **one** line to the existing class Javadoc block:

    `@see <a href="classpath:...">` is wrong — use plain relative doc reference in Javadoc:

    `{@code docs/jdbc-resolver-ownership.md} — maintainer ownership model and call-site inventory`

    Or standard `@see` with a short sentence in the Javadoc body pointing to `docs/jdbc-resolver-ownership.md`. Do **not** expand Javadoc into a second full ownership narrative; the doc remains authoritative.

    If the Javadoc already sufficiently points maintainers to external docs after plan 14-01 cross-references, skip this edit and note skip in SUMMARY.

    Compile check after edit: `.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q`

    Out of scope: any logic change in the resolver, Spring wiring, or calcite SPI.
  </action>
  <verify>
    <automated>git diff --name-only -- .planning/test-matrix.yaml scripts/verify-harness.ps1; if ($LASTEXITCODE -ne 0) { exit 1 }; $diff = git diff --name-only -- "data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java"; if ($diff) { rg -n "jdbc-resolver-ownership" "data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java" } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - If `DefaultRuntimeJdbcEndpointResolver.java` is modified, diff is Javadoc-only (no method body changes)
    - Javadoc references `docs/jdbc-resolver-ownership.md` without duplicating the full ownership narrative
    - `.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q` exits 0 when Javadoc was edited
    - `.planning/test-matrix.yaml` and `scripts/verify-harness.ps1` remain unmodified
  </acceptance_criteria>
  <done>IDE readers of the execute-path authority can jump to the maintainer doc; behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md Commands → perceived merge gate | A new script-like entry could be mistaken for CI/harness requirement |
| Javadoc edit → behavior drift | Doc packaging task must not change resolver logic |

## ASVS

- Level: 1
- Block on: high severity threats

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Elevation of Privilege | AGENTS.md entry | medium | mitigate | Comment-only pointer; no verify-harness registration; acceptance criteria grep |
| T-14-05 | Tampering | DefaultRuntimeJdbcEndpointResolver | high | mitigate | Javadoc-only diff; compile verify; harness/matrix untouched |
</threat_model>

<verification>
- AGENTS pointer: `rg -n "jdbc-resolver-ownership" AGENTS.md`
- P0 scope guard: `rg -n "jdbc-resolver|14-resolver" .planning/test-matrix.yaml scripts/verify-harness.ps1` → no matches
- Optional compile: `.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q` (only if Javadoc touched)
</verification>

<success_criteria>
- D-01/D-02 satisfied: durable doc + AGENTS.md pointer
- RES-01 fully packaged for maintainers
- No P0 / verify-harness / test-matrix / behavior changes
</success_criteria>

<output>
Create `.planning/phases/14-resolver-ownership-docs/14-02-SUMMARY.md` when done
</output>
