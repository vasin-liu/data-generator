---
phase: 14-resolver-ownership-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/jdbc-resolver-ownership.md
autonomous: true
requirements:
  - RES-01
must_haves:
  truths:
    - "Maintainers have a durable ownership doc under docs/ describing catalog-side vs execute-path JDBC resolver roles (D-01, D-06)"
    - "Call-site inventory is grouped into execute-path production, catalog-side, and tests/stubs tables derived from rg on main (D-03, D-04)"
    - "The doc states honestly that JdbcCatalogResolver has no production Spring injection sites beyond the class itself — only JdbcCatalogResolverTests (D-05 scout note)"
    - "The execute-path story includes snap:{instanceId}:… run-start snapshot routing (DS-03) and the HTTP /task/run spine with Phase 12 proof reference (D-05, D-06)"
    - "A Deferred: RES-02 section and explicit non-goals forbid code merge, wiring changes, and P0/test-matrix edits in this milestone (D-08, D-09)"
  artifacts:
    - path: "docs/jdbc-resolver-ownership.md"
      provides: "Maintainer-facing ownership model + rg-derived call-site inventory"
      min_lines: 80
  key_links:
    - from: "docs/jdbc-resolver-ownership.md"
      to: "data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java"
      via: "ownership narrative promotes existing class Javadoc without contradicting it"
      pattern: "DefaultRuntimeJdbcEndpointResolver"
    - from: "docs/jdbc-resolver-ownership.md"
      to: "data-generator-datasource/data-generator-datasource-jdbc/src/main/java/org/gensokyo/data/datasource/jdbc/JdbcCatalogResolver.java"
      via: "catalog-side inventory row"
      pattern: "JdbcCatalogResolver"
    - from: "docs/jdbc-resolver-ownership.md"
      to: "data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java"
      via: "HTTP execute-path evidence citation (Phase 12)"
      pattern: "ManagedJdbcCatalogHttpExecuteIT"
---

<objective>
Create `docs/jdbc-resolver-ownership.md` — the single maintainer-facing source of truth for dual JDBC resolver ownership and a code-derived call-site inventory (RES-01, D-01, D-03–D-09).

Purpose: Phase 07.1 and Phase 12 deferred ownership documentation (12-CONTEXT D-11) into a durable product doc so maintainers stop guessing which resolver to call and accidental consolidation PRs are discouraged.
Output: one new doc with ownership narrative, three inventory table groups, RES-02 deferral, and non-goals. No AGENTS.md pointer yet (plan 14-02).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-resolver-ownership-docs/14-CONTEXT.md
@.planning/phases/14-resolver-ownership-docs/14-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-http-execute-path-proof/12-CONTEXT.md
@data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
@data-generator-datasource/data-generator-datasource-jdbc/src/main/java/org/gensokyo/data/datasource/jdbc/JdbcCatalogResolver.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/RuntimeJdbcEndpointResolver.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/NoopRuntimeJdbcEndpointResolver.java
@data-generator-service/src/main/java/org/gensokyo/data/config/CoreConfig.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/plugin/JdbcTemplateTemplateV2RuntimePluginProvider.java
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/JdbcSnapshotExecutePathIT.java
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java
</context>

## Artifacts this phase produces

*(Phase-wide inventory; this plan delivers the ownership doc body)*

- `docs/jdbc-resolver-ownership.md` — ownership + inventory tables — **this plan**
- `AGENTS.md` — Commands/docs pointer — plan 14-02
- Optional sibling cross-link in `docs/template-v2-datasource-and-secret-governance.md` — plan 14-02 discretion
- Optional `@see` on `DefaultRuntimeJdbcEndpointResolver` — plan 14-02 discretion

<tasks>

<task type="auto">
  <name>Task 1: Scout call sites with rg and draft the inventory tables</name>
  <files>docs/jdbc-resolver-ownership.md</files>
  <read_first>
    - .planning/phases/14-resolver-ownership-docs/14-CONTEXT.md (D-03, D-04, D-05, scout note on sparse JdbcCatalogResolver production callers)
    - data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java (existing ownership Javadoc — promote, do not contradict)
    - data-generator-datasource/data-generator-datasource-jdbc/src/main/java/org/gensokyo/data/datasource/jdbc/JdbcCatalogResolver.java
  </read_first>
  <action>
    Run fresh scouts on `main` before writing tables (D-04). Record commands in a short "Inventory methodology" subsection at the bottom of the doc.

    **Required scout commands (PowerShell-friendly):**

    ```powershell
    rg -l "JdbcCatalogResolver" --glob "*.java"
    rg -l "DefaultRuntimeJdbcEndpointResolver|RuntimeJdbcEndpointResolver" --glob "*.java"
    rg -l "NoopRuntimeJdbcEndpointResolver" --glob "*.java"
    ```

    **Table 1 — Execute-path production** (`DefaultRuntimeJdbcEndpointResolver` / `RuntimeJdbcEndpointResolver` consumers on `src/main`):

    Include at minimum these rows (verify line numbers at write time; adjust if rg differs):

    | Path | Symbol / role | Notes |
    |------|---------------|-------|
    | `data-generator-service/.../CoreConfig.java` | `runtimeJdbcEndpointResolver` `@Bean` | Wires `DefaultRuntimeJdbcEndpointResolver` when no override bean |
    | `data-generator-service/.../CoreConfig.java` | `templateV2RuntimeContext` | Injects `RuntimeJdbcEndpointResolver` into V2 runtime context |
    | `data-generator-calcite/.../JdbcTemplateTemplateV2RuntimePluginProvider.java` | `sourceFactories` / `sinkFactories` | Passes `context.runtimeJdbcEndpointResolver()` into `QuerySourceFactory`, `PostGisQuerySourceFactory`, `JdbcSinkFactory` |
    | `data-generator-calcite/.../QuerySourceFactory.java` | constructor field | Calls resolver for managed/inline JDBC source routing keys |
    | `data-generator-calcite/.../PostGisQuerySourceFactory.java` | constructor field | Same execute-path contract for PostGIS sources |
    | `data-generator-calcite/.../JdbcSinkFactory.java` | constructor field | Builds `JdbcRowSinkAdapter` with injected resolver |
    | `data-generator-calcite/.../JdbcRowSinkAdapter.java` | field + ctor | Production sink adapter; default no-arg ctor uses `NoopRuntimeJdbcEndpointResolver` only for tests — note that in Notes column |
    | `data-generator-service/.../E2eV2ScenarioFixtureService.java` | injected field | Service-side fixture helper on execute-path SPI |

    Add an **HTTP run-path narrative** subsection (not a table row pretending to be a resolver): document the spine `TaskController` `/task/run` (or console `/api/templates/{id}/run`) → `TaskExecutionService` → `TemplateV2Runner` → calcite factories above → `DefaultRuntimeJdbcEndpointResolver`. Cite `ManagedJdbcCatalogHttpExecuteIT` as Phase 12 HTTP proof (D-05).

    **Table 2 — Catalog-side** (`JdbcCatalogResolver`):

    Scout expectation on current `main`: **only** `JdbcCatalogResolver.java` (definition) and `JdbcCatalogResolverTests.java` (unit test). Javadoc cross-reference in `DefaultRuntimeJdbcEndpointResolver` is documentation only, not a runtime caller. State plainly: *no production Spring bean injects `JdbcCatalogResolver` today* (D-05). Do not invent catalog/admin callers.

    **Table 3 — Tests and stubs**:

    Include representative rows (summarize bulk `NoopRuntimeJdbcEndpointResolver` calcite tests rather than listing every file — D discretion):

    | Path | Symbol | Notes |
    |------|--------|-------|
    | `data-generator-service/.../JdbcSnapshotExecutePathIT.java` | `@Autowired RuntimeJdbcEndpointResolver` | DS-03 snap: execute-path proof |
    | `data-generator-service/.../ManagedJdbcCatalogHttpExecuteIT.java` | HTTP IT | Phase 12 managed JDBC HTTP execute proof |
    | `data-generator-service/.../RuntimeJdbcEndpointResolverTests.java` | Spring context test | Inline registration through `QuerySourceFactory` |
    | `data-generator-service/.../V2ScenarioTemplateIT.java` | autowired resolver | Scenario IT on real bean |
    | `data-generator-datasource/.../JdbcCatalogResolverTests.java` | direct `new JdbcCatalogResolver(...)` | Catalog-side unit tests |
    | `data-generator-calcite/.../NoopRuntimeJdbcEndpointResolver.java` | stub impl | Test/noop authority — **not** a third production resolver |
    | `data-generator-calcite/src/test/**` (summary row) | `NoopRuntimeJdbcEndpointResolver` | Calcite unit/IT stubs — see `rg -l NoopRuntimeJdbcEndpointResolver --glob "*.java"` for full list |

    Column set is planner discretion (path / symbol / role / notes) but every row must map to a real file from scout output.
  </action>
  <verify>
    <automated>rg -n "Execute-path production|Catalog-side|Tests and stubs" docs/jdbc-resolver-ownership.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "CoreConfig|JdbcTemplateTemplateV2RuntimePluginProvider|QuerySourceFactory|ManagedJdbcCatalogHttpExecuteIT|JdbcCatalogResolverTests" docs/jdbc-resolver-ownership.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/jdbc-resolver-ownership.md` exists with three inventory table groups (or equivalent H2 sections)
    - Table 1 includes `CoreConfig.java`, `JdbcTemplateTemplateV2RuntimePluginProvider.java`, `QuerySourceFactory.java`, `PostGisQuerySourceFactory.java`, `JdbcSinkFactory.java`, and `JdbcRowSinkAdapter.java`
    - Table 2 states honestly that `JdbcCatalogResolver` has no production injection sites beyond its own class (callers: unit test only on current main)
    - Table 3 includes `JdbcSnapshotExecutePathIT.java`, `ManagedJdbcCatalogHttpExecuteIT.java`, and a summary row for `NoopRuntimeJdbcEndpointResolver` calcite tests
    - HTTP run-path narrative cites `/task/run` and `ManagedJdbcCatalogHttpExecuteIT`
    - Doc includes "Inventory methodology" noting rg commands used
    - No row references a file that fails `Test-Path` on the stated path
  </acceptance_criteria>
  <done>Inventory tables are rg-derived, grouped by role, and honest about sparse catalog-side production usage.</done>
</task>

<task type="auto">
  <name>Task 2: Write ownership narrative, snap: routing, RES-02 deferral, and non-goals</name>
  <files>docs/jdbc-resolver-ownership.md</files>
  <read_first>
    - .planning/phases/14-resolver-ownership-docs/14-CONTEXT.md (D-06, D-07, D-08, D-09)
    - .planning/REQUIREMENTS.md (RES-01 active, RES-02 deferred)
    - data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java (snap: Javadoc, coexistence wording)
    - .planning/phases/12-http-execute-path-proof/12-CONTEXT.md (D-11 deferral to Phase 14)
  </read_first>
  <action>
    Extend the same file with sections **above** the inventory (recommended doc outline):

    1. **Title + purpose** — "JDBC resolver ownership and call-site inventory"; docs-only RES-01; not a refactor plan.
    2. **Two authorities** — Short role definitions:
       - `JdbcCatalogResolver` — datasource-module catalog-side helper; catalog-first / inline semantics; **not** injected on V2 execute path today.
       - `DefaultRuntimeJdbcEndpointResolver` — V2 Template **execute-path authority** implementing `RuntimeJdbcEndpointResolver`; wired in `CoreConfig`.
    3. **Coexistence** — Both coexist by design; neither delegates to the other; similar catalog-resolve / register-if-absent semantics implemented independently (D-07). No line-by-line algorithm diff required.
    4. **snap: run-start snapshot routing (DS-03)** — When `WorkflowRunContext` binds an `instanceId`, managed JDBC returns `snap:{instanceId}:{name}` so in-flight runs keep pre-reload pools. Reference DS-03 / Phase 07.1 gap closure; point to `JdbcSnapshotExecutePathIT` for automated proof.
    5. **Which resolver to use** — Decision guide: extending V2 sources/sinks / HTTP runs → execute-path SPI and `DefaultRuntimeJdbcEndpointResolver`; catalog-module or standalone catalog resolve experiments → `JdbcCatalogResolver` (tests today).
    6. **Deferred: RES-02** — High-level only: future consolidation could mean single authority and removing duplication; **no** migration plan, tickets, or code sketches (D-08).
    7. **Non-goals (this milestone)** — Explicit list: no Spring bean merge, no deleting either class, no P0/test-matrix/`verify-harness` changes, no behavior changes (D-09).
    8. **Related docs** — Placeholder bullet for sibling governance doc cross-link (filled in plan 14-02 if added).

    Tone: ownership + inventory, not refactor plan (CONTEXT specifics). Promote `DefaultRuntimeJdbcEndpointResolver` Javadoc facts; do not contradict them.

    Out of scope this task: `AGENTS.md`, `.planning/test-matrix.yaml`, `scripts/verify-harness.ps1`, product Java edits (except plan 14-02 optional `@see`).
  </action>
  <verify>
    <automated>rg -n "snap:|RES-02|Non-goals|coexist|WorkflowRunContext" docs/jdbc-resolver-ownership.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "migration plan|merge the two|delete JdbcCatalogResolver" docs/jdbc-resolver-ownership.md; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - Ownership section names both resolver classes and their intended roles
    - Section explains coexistence without delegation and mentions independent mirror semantics (not a full algorithm diff)
    - `snap:{instanceId}:` snapshot routing is documented with DS-03 / execute-path context
    - "Deferred: RES-02" section exists with high-level consolidation meaning only — no migration steps or code sketches
    - "Non-goals" explicitly forbids Spring merge, class deletion, P0/test-matrix changes, and behavior changes
    - Doc does not contain implementation language like "merge the two resolvers in this milestone"
    - `.planning/test-matrix.yaml` and `scripts/verify-harness.ps1` are unmodified
  </acceptance_criteria>
  <done>RES-01 ownership narrative is complete and aligned with existing class Javadoc; inventory and narrative live in one doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rg scout output → committed inventory | Fabricated callers mislead maintainers into wrong extension points |
| Ownership doc → consolidation PRs | Doc reads like an implementation plan and triggers out-of-scope code changes |

## ASVS

- Level: 1
- Block on: high severity threats

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Spoofing | Catalog-side inventory | high | mitigate | Acceptance criteria require honest "no production injection" statement; scout commands documented |
| T-14-02 | Repudiation | RES-02 deferral | medium | mitigate | Explicit "Deferred: RES-02" section; grep rejects migration/merge implementation language |
| T-14-03 | Tampering | P0 / harness scope | high | mitigate | Acceptance criteria assert test-matrix and verify-harness unmodified |
</threat_model>

<verification>
- Inventory section headers: `rg -n "Execute-path production|Catalog-side|Tests and stubs" docs/jdbc-resolver-ownership.md`
- Narrative keywords: `rg -n "snap:|RES-02|Non-goals|coexist" docs/jdbc-resolver-ownership.md`
- P0 untouched: `rg -n "jdbc-resolver|resolver-ownership" .planning/test-matrix.yaml scripts/verify-harness.ps1` → no matches
- Spot-check catalog honesty: `rg -l JdbcCatalogResolver --glob "*.java"` → expect only `JdbcCatalogResolver.java`, `JdbcCatalogResolverTests.java`, and Javadoc reference in `DefaultRuntimeJdbcEndpointResolver.java`
</verification>

<success_criteria>
- ROADMAP Phase 14 success criteria 1 and 2 satisfied (ownership doc + call-site inventory)
- Success criteria 3 satisfied by non-goals (no code merge)
- Success criteria 4 satisfied by RES-02 deferral section
</success_criteria>

<output>
Create `.planning/phases/14-resolver-ownership-docs/14-01-SUMMARY.md` when done
</output>
