---
phase: 07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
autonomous: true
requirements:
  - DS-03
must_haves:
  truths:
    - Managed JDBC resolve returns connectionName() from catalog.resolve (snap:{instanceId}:{name} under WorkflowRunContext) for source and sink paths (D-01, D-02, D-04)
    - Dynamic routing pool registered via registerIfAbsent before returning connectionName (D-02)
    - Inline dataSource blocks unchanged; managed dataSourceId only (D-03)
    - JdbcCatalogResolver untouched; fix inside DefaultRuntime only; no inject/delegate (D-14, D-15)
    - Class Javadoc documents execute-path authority vs JdbcCatalogResolver catalog-side ownership (D-13, D-16)
  artifacts:
    - org.gensokyo.data.config.DefaultRuntimeJdbcEndpointResolver (managed path returns connectionName)
  key_links:
    - RuntimeJdbcEndpointResolver bean wired from service config to calcite V2 execute path
    - catalog.resolve + registerIfAbsent mirrors JdbcCatalogResolver semantics without calling that class
---

# Plan 07.1-01: Fix DefaultRuntime managed snap return + ownership Javadoc

<objective>
Fix `DefaultRuntimeJdbcEndpointResolver` managed JDBC path to return `connectionName()` after `catalog.resolve` and register-if-absent, mirroring `JdbcCatalogResolver` semantics without depending on that class (D-01..D-04, D-13..D-16).

Purpose: Close the DS-03 execute-path gap where resolver currently returns the logical catalog name instead of snapshot routing keys.
Output: Compilable service module with corrected managed resolve for source+sink and documented dual-resolver ownership.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-CONTEXT.md
@.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
@data-generator-datasource/data-generator-datasource-jdbc/src/main/java/org/gensokyo/data/datasource/jdbc/JdbcCatalogResolver.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/RuntimeJdbcEndpointResolver.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return connectionName() from managed JDBC resolve for source and sink</name>
  <files>data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java</files>
  <read_first>
    - DefaultRuntimeJdbcEndpointResolver.java (resolveSourceDataSourceId, resolveSinkDataSourceId, ensureManagedJdbc)
    - JdbcCatalogResolver.java resolveManagedDataSourceId and registerIfAbsent (reference only — do not edit D-15)
    - 07.1-CONTEXT.md decisions D-01, D-02, D-03, D-04, D-14, D-15
    - 07.1-PATTERNS.md DefaultRuntimeJdbcEndpointResolver gap and copy pattern
  </read_first>
  <action>
    Per D-01 and D-02: refactor managed path in both resolveSourceDataSourceId and resolveSinkDataSourceId so that when dataSourceId is non-blank the resolver calls connectionCatalog.resolve(name, ConnectionKind.JDBC), validates JdbcResolvedConnection, calls registerIfAbsent(connectionName, dataSource), and returns connectionName() — not the logical dataSourceId.
    Per D-03: leave inline dataSource block handling unchanged in this phase; managed dataSourceId only.
    Per D-04: when WorkflowRunContext is bound and catalog.resolve fails or returns non-JDBC, throw fail-fast IllegalArgumentException or IllegalStateException with actionable message — no fallback to logical catalog name.
    Per D-14 and D-15: implement register-if-absent and routing lookup inside DefaultRuntimeJdbcEndpointResolver; do not inject, delegate to, or modify JdbcCatalogResolver.
    Reuse existing DynamicRoutingDataSource provider pattern from the class or mirror JdbcCatalogResolver requireRouting semantics.
    Follow Java copyright and public Javadoc rules from .cursor/rules/java-copyright-class-javadoc.mdc.
  </action>
  <acceptance_criteria>
    - resolveSourceDataSourceId and resolveSinkDataSourceId return jdbc.connectionName() on managed path after registerIfAbsent
    - Logical dataSourceId is never returned after successful managed resolve
    - Inline dataSource code paths unchanged from pre-fix behavior
    - JdbcCatalogResolver.java has zero edits in this plan
    - No new dependency from DefaultRuntimeJdbcEndpointResolver to JdbcCatalogResolver
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q</automated>
  </verify>
  <done>Managed JDBC execute-path resolver returns snapshot routing keys and registers pools under connectionName.</done>
</task>

<task type="auto">
  <name>Task 2: Expand class Javadoc for execute-path vs catalog-side ownership</name>
  <files>data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java</files>
  <read_first>
    - DefaultRuntimeJdbcEndpointResolver.java existing type Javadoc
    - JdbcCatalogResolver.java type Javadoc (reference for contrast)
    - 07.1-CONTEXT.md D-13, D-16
    - RuntimeJdbcEndpointResolver.java interface Javadoc
  </read_first>
  <action>
    Per D-16: expand DefaultRuntimeJdbcEndpointResolver class-level Javadoc to state explicitly that this bean is the V2 Template execute-path authority for JDBC endpoint resolution when WorkflowRunContext.instanceId is set.
    Per D-13: document that JdbcCatalogResolver remains the datasource-module catalog-side resolver; both resolvers coexist; consolidation is deferred.
    Note managed-path returns connectionName including snap:{instanceId}:{name} when snapshot context is active.
    Keep wording concise; no behavioral changes beyond documentation in this task.
  </action>
  <acceptance_criteria>
    - Class Javadoc names DefaultRuntimeJdbcEndpointResolver as V2 execute-path JDBC authority
    - Class Javadoc references JdbcCatalogResolver as catalog-side resolver without implying delegation
    - Both D-13 keep-both and D-16 execute-path authority are unambiguous to a planner reading source only
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am -DskipTests compile -q</automated>
  </verify>
  <done>Dual-resolver ownership documented in DefaultRuntimeJdbcEndpointResolver Javadoc.</done>
</task>

</tasks>

<threat_model>
## Security Configuration

ASVS Level: 1 | block_on: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolver return value → DynamicDataSourceContextHolder / VO mutate | Must be connectionName snap key under active run context; logical name fallback forbidden (D-04) |
| catalog.resolve → registerIfAbsent | Only catalog-resolved DataSource instances registered under connectionName |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Tampering | high | DefaultRuntimeJdbcEndpointResolver managed return | mitigate | Return connectionName(); fail fast on resolve failure; no fallback to logical dataSourceId (D-04) |
| T-07.1-02 | Elevation of Privilege | medium | registerIfAbsent pool registration | mitigate | Register only DataSource from catalog.resolve JdbcResolvedConnection; reject non-JDBC resolved types |
| T-07.1-03 | Information Disclosure | low | Resolver error messages | accept | Actionable errors without secret values; existing CatalogResolveSupport patterns |
| T-07.1-04 | Denial of Service | medium | Fail-fast on bad catalog state | mitigate | Fail fast with clear IllegalArgumentException; avoid silent wrong-pool routing |
| T-07.1-SC | Tampering | n/a | Schema push | accept | Java JDBC only — no ORM schema push task this phase |
</threat_model>

<verification>
- Service module compiles with JDK 25 helper
- Grep confirms managed resolve paths return connectionName not dataSourceId parameter
- JdbcCatalogResolver.java unchanged in git diff for this plan
</verification>

<success_criteria>
- DS-03: execute-path resolver returns snap routing keys for managed JDBC when WorkflowRunContext is active
- D-13..D-16: ownership documented; catalog resolver untouched; fix localized to DefaultRuntime
</success_criteria>

## Artifacts this phase produces

- `org.gensokyo.data.config.DefaultRuntimeJdbcEndpointResolver` — managed path returns connectionName() with registerIfAbsent
- Updated class Javadoc documenting execute-path authority vs JdbcCatalogResolver (D-16)

<output>
Create `.planning/phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-01-SUMMARY.md` when done
</output>
