---
phase: 23.1-v2-3-tech-debt-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 23.1-01
files_modified:
  - docs/geo-assets.md
  - docs/geo-synthetic-v2-source.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Docs describe two-path dual-binding: editor/preview prefers asset-id; runtime mapper fail-fasts when both bindings set — not a silent runtime preference per D-06/D-07/D-08"
    - "docs/geo-assets.md Related API table names exact POST /api/console/geo-assets/preview/location and POST /api/console/geo-assets/preview/synthetic per D-09"
    - "docs/geo-assets.md notes synthetic preview body mirrors geo_synthetic fields including nested sample for LINE_SAMPLE per D-10"
    - "No runtime mapper or editor dual-binding behavior changes in this plan per D-06"
  artifacts:
    - path: docs/geo-assets.md
      provides: Exact preview paths + dual-binding / sample body notes
      contains: "/api/console/geo-assets/preview/location"
    - path: docs/geo-synthetic-v2-source.md
      provides: Two-path dual-binding clarity with cross-link to geo-assets
      contains: "fail-fast"
  key_links:
    - from: docs/geo-assets.md
      to: docs/geo-synthetic-v2-source.md
      via: short relative cross-link for YAML/runtime dual-binding depth
      pattern: "geo-synthetic-v2-source.md"
    - from: docs/geo-synthetic-v2-source.md
      to: docs/geo-assets.md
      via: short relative cross-link for console preview preference
      pattern: "geo-assets.md"
---

<objective>
Document dual-binding preview-vs-runtime semantics and exact locked preview API paths (D-06..D-10). Docs-only — no runtime `GeoSyntheticRequestMapper` or editor preference code changes (D-06). Cleanup of audit tech_debt; no new REQ-IDs. D-11: keep GEO-06, DATA-01, P0 inflation, Playwright→P1, Spring upload→run IT, Nyquist VALIDATION.md out of scope.

Purpose: Operators must not misread “asset-id wins” as silent runtime preference; maintainers must see the locked controller paths and that LINE_SAMPLE preview carries nested `sample`.
Output: Updated `docs/geo-assets.md` and `docs/geo-synthetic-v2-source.md` with consistent two-path notes and exact preview paths.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23.1-v2-3-tech-debt-closeout/23.1-CONTEXT.md
@.planning/phases/23.1-v2-3-tech-debt-closeout/23.1-01-PLAN.md
@.planning/v2.3-MILESTONE-AUDIT.md
@docs/geo-assets.md
@docs/geo-synthetic-v2-source.md
@data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleGeoAssetController.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/GeoSyntheticRequestMapper.java
</context>

## Artifacts this phase produces

| Artifact | Role |
|----------|------|
| Dual-binding two-path note | Editor/preview prefers asset-id; runtime fail-fast when both set (D-06..D-08) |
| Exact preview path rows in `docs/geo-assets.md` | `POST …/preview/location` and `POST …/preview/synthetic` (D-09) |
| Nested `sample` preview body note | LINE_SAMPLE preview mirrors YAML `sample` after plan 01 (D-10) |

<tasks>

<task type="auto">
  <name>Task 1: Clarify dual-binding two-path semantics in both geo docs</name>
  <files>docs/geo-synthetic-v2-source.md, docs/geo-assets.md</files>
  <read_first>docs/geo-synthetic-v2-source.md, docs/geo-assets.md, .planning/phases/23.1-v2-3-tech-debt-closeout/23.1-CONTEXT.md, .planning/phases/22-console-map-geo-synthetic-editor/22-CONTEXT.md</read_first>
  <action>
    Per D-06 this task is documentation only — do not edit `GeoSyntheticRequestMapper`, console editor dual-binding clearing logic, or any Java/TS runtime behavior.

    Per D-07 rewrite the ambiguous path-vs-asset-id precedence prose so operators see an explicit **two-path** model: (1) **Editor / preview** — when both bindings appear in the form, asset-id is preferred for preview calls (path cleared client-side); inline warning remains UX. (2) **Runtime execute** — mapper fail-fasts if both path and asset-id are present on the saved source for the same role; operators must leave one binding only in YAML/draft before run. Per D-08 prefer short cross-links between `docs/geo-synthetic-v2-source.md` and `docs/geo-assets.md` over duplicating long prose; wording must make it impossible to read “asset-id wins” as runtime silent preference. Keep YAML examples and GEO-03 path/classpath validity. Do not document GEO-06, DATA-01, P0 changes, Playwright matrix promotion, Spring upload→run IT, or Nyquist (D-11).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$a=Get-Content 'docs/geo-assets.md' -Raw; $s=Get-Content 'docs/geo-synthetic-v2-source.md' -Raw; $joined=$a+\"`n\"+$s; if ($joined -notmatch '(?i)preview') { exit 1 }; if ($joined -notmatch '(?i)fail-?fast') { exit 1 }; if ($joined -notmatch '(?i)asset-?id') { exit 1 }; if ($a -notmatch 'geo-synthetic-v2-source\.md') { exit 1 }; if ($s -notmatch 'geo-assets\.md') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    Both docs state editor/preview preference vs runtime fail-fast as distinct paths; cross-links present; no code behavior changes (D-06, D-07, D-08).
  </acceptance_criteria>
  <done>Dual-binding docs unambiguous for operators and maintainers.</done>
</task>

<task type="auto">
  <name>Task 2: Lock exact preview API paths and nested sample body note</name>
  <files>docs/geo-assets.md</files>
  <read_first>docs/geo-assets.md, data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleGeoAssetController.java, data-generator-service/src/main/java/org/gensokyo/data/api/console/dto/GeoSyntheticPreviewRequest.java</read_first>
  <action>
    Per D-09 replace the vague Related API preview row(s) in `docs/geo-assets.md` so the table names the exact locked paths (no hedge wording suggesting alternate controller paths):
    - `POST /api/console/geo-assets/preview/location`
    - `POST /api/console/geo-assets/preview/synthetic`
    Include both rows if location was previously omitted. Remove the follow-on sentence that defers to “Phase 22 controller wiring” / vague naming if it contradicts the locked paths.

    Per D-10 add a brief note that the synthetic preview JSON body mirrors `geo_synthetic` fields, including nested `sample` for LINE_SAMPLE (`strategy` / `spacingMeters`), after plan 01 wiring — point at `ConsoleGeoAssetController` / `GeoSyntheticPreviewRequest` for detail; do not invent a full OpenAPI chapter. Cross-link YAML sample semantics in `geo-synthetic-v2-source.md` if helpful. Docs only; no code edits.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$t=Get-Content 'docs/geo-assets.md' -Raw; if ($t -notmatch '/api/console/geo-assets/preview/location') { exit 1 }; if ($t -notmatch '/api/console/geo-assets/preview/synthetic') { exit 1 }; if ($t -notmatch '(?i)sample') { exit 1 }; if ($t -match '\.\.\./preview/synthetic') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    geo-assets.md lists both exact locked POST preview paths and mentions nested sample for LINE_SAMPLE preview body; ellipsis-style vague synthetic path row gone (D-09, D-10).
  </acceptance_criteria>
  <done>Exact preview paths + sample body note documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator templates | Examples must not embed secrets or real staging asset UUIDs |
| Docs → API contract | Path strings must match shipped controller mappings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23.1-04 | Spoofing | Documented API paths | low | mitigate | Cite locked ConsoleGeoAssetController mappings only (D-09) |
| T-23.1-05 | Information Disclosure | docs examples | low | accept | Placeholder UUIDs / classpath fixtures only |
| T-23.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this docs-only plan |

ASVS L1: documentation surface; residual risk low.
</threat_model>

<verification>
Read both docs: two-path dual-binding clear; exact preview paths present; nested sample note present; no Java/TS dual-binding behavior edits in the plan diff.
</verification>

<success_criteria>
- D-06..D-10 satisfied
- D-11 still honored (deferred audit items remain out)
- ROADMAP success criteria 2–4 covered for docs/scope
</success_criteria>

<output>
Create `.planning/phases/23.1-v2-3-tech-debt-closeout/23.1-02-SUMMARY.md` when done
</output>
