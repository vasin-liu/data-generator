# Phase 23.1: v2.3 Tech Debt Closeout - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-08
**Phase:** 23.1-v2-3-tech-debt-closeout
**Areas discussed:** LINE_SAMPLE preview sample surface, Dual-binding docs clarity, Exact preview API paths, Scope freeze
**Mode:** --auto (all areas; recommended defaults)

---

## LINE_SAMPLE preview sample surface

| Option | Description | Selected |
|--------|-------------|----------|
| Nested `sample` object | Mirror YAML/`GeoSyntheticSampleVO` (`strategy`, `spacingMeters`) on preview DTO + UI payload | ✓ |
| Flat top-level fields | `sampleStrategy` / `spacingMeters` on preview request only | |
| Strategy-only | Forward strategy string; ignore spacing for preview | |

**User's choice:** Nested `sample` object (auto / recommended)
**Notes:** Also default omitted LINE_SAMPLE strategy to `BY_COUNT`; wire editor preview call to send sample + related honesty fields; extend Maven tests.

---

## Dual-binding docs clarity

| Option | Description | Selected |
|--------|-------------|----------|
| Docs-only two-path note | Keep preview asset-id preference + runtime fail-fast; clarify in docs | ✓ |
| Align runtime to prefer asset-id | Change mapper to silent preference (product change) | |
| Align editor to fail-fast | Change UI to reject both-set like runtime | |

**User's choice:** Docs-only two-path note (auto / recommended)
**Notes:** Cross-link `docs/geo-assets.md` and `docs/geo-synthetic-v2-source.md`; avoid misreading “asset-id wins” as runtime behavior.

---

## Exact preview API paths

| Option | Description | Selected |
|--------|-------------|----------|
| Exact locked paths | Name `POST …/preview/location` and `…/preview/synthetic`; brief sample note | ✓ |
| Keep vague wording | Retain “or equivalent” language | |
| Full request schema chapter | Document complete JSON schemas in docs | |

**User's choice:** Exact locked paths (auto / recommended)

---

## Scope freeze

| Option | Description | Selected |
|--------|-------------|----------|
| Keep ROADMAP deferred out | GEO-06, DATA-01, P0, Playwright-P1, Spring IT, Nyquist stay deferred | ✓ |
| Pull Nyquist hygiene | Add VALIDATION.md for 21–23 | |
| Pull Spring upload→run IT | New metadata-DB IT | |

**User's choice:** Keep deferred out (auto / recommended)

---

## Claude's Discretion

- Nested Java type shape (`record` vs VO reuse)
- Primary doc home for dual-binding prose (must stay consistent across both docs)
- Test class split / plan packing

## Deferred Ideas

- GEO-06, DATA-01, P0 inflation, Playwright→P1, Spring upload→run IT, Nyquist VALIDATION.md (21–23)
