---
phase: 05-coverage-ramp-ci-gates
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleUdfControllerTest.java
  - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleTransformCatalogControllerTest.java
  - .planning/test-matrix.yaml
  - scripts/verify-harness.ps1
  - .github/workflows/harness-verify.yml
  - AGENTS.md
autonomous: true
requirements: [COV-03, COV-04]
must_haves:
  truths:
    - "ConsoleUdfControllerTest and ConsoleTransformCatalogControllerTest gain new slice assertions covering UDF + transform-metadata endpoints (version history / publish-deprecate / unknown-id 400; kind filter / invalid-kind 400 / metadata completeness) (COV-03, D-07)."
    - "The expanded console-api slice tests are linked into .planning/test-matrix.yaml rows tagged tier P1 (D-07)."
    - "scripts/verify-harness.ps1 exits non-zero when the summary p0Pass is false, with an explicit P0-failure message; P1/P2 failures do not trigger this gate (COV-04, D-08/D-09)."
    - ".github/workflows/harness-verify.yml enforces the P0 regression gate on pull_request as a named step so failing P0 rows block merge (COV-04, D-08)."
    - "AGENTS.md documents the merge criteria: merges are blocked when any P0 matrix row is not green (COV-04)."
  artifacts:
    - path: scripts/verify-harness.ps1
      provides: "P0 regression gate reading summary.p0.pass; non-zero exit on P0 failure"
    - path: .github/workflows/harness-verify.yml
      provides: "named P0 regression gate enforcement on pull_request"
    - path: AGENTS.md
      provides: "documented merge criteria + P0 gate behavior"
  key_links:
    - from: scripts/verify-harness.ps1 P0 gate
      to: target/test-matrix-summary.json p0.pass (from 05-01)
      via: "harness reads summary.p0.pass after New-TestMatrixSummary; exit 1 when false"
      pattern: "summary JSON → harness exit code"
    - from: harness-verify.yml
      to: verify-harness.ps1
      via: "named CI step runs the harness on pull_request; non-zero exit fails the check and blocks merge"
      pattern: "mirror existing harness-verify step, add explicit P0 gate naming"
    - from: ConsoleUdfControllerTest / ConsoleTransformCatalogControllerTest
      to: test-matrix.yaml console-api rows
      via: "linked_tests reference the slice classes; harness executes them"
      pattern: "matrix linked_tests → harness Surefire slice"
---

<objective>
**Purpose:** Expand console API slice coverage for the UDF and transform-metadata endpoints (COV-03) and add the regression merge gate that blocks merges when any P0 matrix row is not green (COV-04). The gate consumes the `p0Pass` signal produced in 05-01. Per the operator decision (D-08) the CI workflow is modified to enforce the gate; per D-09 the gate is P0-only and embedded-only (Playwright/Podman E2E stays opt-in).

**Output:** Additional assertions in `ConsoleUdfControllerTest` + `ConsoleTransformCatalogControllerTest`; matrix `linked_tests` wiring those classes into P1-tier console-api rows; a P0 gate in `scripts/verify-harness.ps1` (non-zero exit on `p0.pass == false`); a named P0-gate enforcement step in `.github/workflows/harness-verify.yml`; documented merge criteria in `AGENTS.md`.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-coverage-ramp-ci-gates/05-CONTEXT.md
@.planning/phases/05-coverage-ramp-ci-gates/05-01-PLAN.md
@data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleUdfControllerTest.java
@data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleTransformCatalogControllerTest.java
@data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleUdfController.java
@data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleTransformCatalogController.java
@data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleApiAdvice.java
@scripts/verify-harness.ps1
@.github/workflows/harness-verify.yml
@AGENTS.md
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Expand console API slice tests for UDF + transform metadata (COV-03, D-07)</name>
    <files>data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleUdfControllerTest.java
data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleTransformCatalogControllerTest.java</files>
    <read_first>
      - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleUdfControllerTest.java (existing slice style, MockMvc/direct, R<T> assertions)
      - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleTransformCatalogControllerTest.java (existing catalog assertions from Phase 4)
      - data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleUdfController.java (endpoints: list / version history / publish / deprecate — actual method signatures)
      - data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleTransformCatalogController.java (kind filter param)
      - data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleApiAdvice.java (IllegalArgumentException → 400 R.fail)
    </read_first>
    <action>
      Extend `ConsoleUdfControllerTest` with new `@Test` methods (camelCase behavior names per CONVENTIONS) covering endpoints not already asserted: version-history listing for a UDF id, publish→deprecate lifecycle transition reflected in the listing, and an unknown UDF id returning HTTP 400 via `ConsoleApiAdvice` (`R.fail`). Use only methods/routes that actually exist on `ConsoleUdfController` (read it first; do not invent endpoints).

      Extend `ConsoleTransformCatalogControllerTest` with new `@Test` methods covering: `kind=BUILTIN` returns only BUILTIN entries, `kind=UDF` returns only published UDF entries, an invalid `kind` value returns HTTP 400, and per-entry metadata completeness for json/mask/lookup (non-empty params + example). Reuse the existing test harness/seed setup in the class. Keep assertions on the `R<T>` envelope shape. Do not change controller production code.
    </action>
    <acceptance_criteria>
      - `ConsoleUdfControllerTest` has new tests for version history, publish/deprecate transition, and unknown-id 400.
      - `ConsoleTransformCatalogControllerTest` has new tests for kind=BUILTIN, kind=UDF, invalid-kind 400, and metadata completeness.
      - All new tests reference only existing controller endpoints; no production code changed.
    </acceptance_criteria>
    <verify><automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=ConsoleUdfControllerTest,ConsoleTransformCatalogControllerTest -Dsurefire.failIfNoSpecifiedTests=false</automated></verify>
    <done>Console UDF + transform-metadata endpoints have expanded slice coverage, all green.</done>
  </task>

  <task type="auto">
    <name>Task 2: Wire expanded slice tests into the matrix as P1 rows (COV-03, D-07)</name>
    <files>.planning/test-matrix.yaml</files>
    <read_first>
      - .planning/test-matrix.yaml (existing console-api-templates/console-api-jobs rows + the udf-* rows; tier field added in 05-01)
      - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleTransformCatalogControllerTest.java (class simple name for linked_tests)
      - .planning/phases/05-coverage-ramp-ci-gates/05-CONTEXT.md (D-07 — these rows are P1, not P0)
    </read_first>
    <action>
      Ensure the expanded slice tests are represented in the matrix so the harness executes them. Add two rows (or extend existing console-api rows if a better fit) `console-api-udf` (owner_module data-generator-service, adapter console-api, test_types [unit], `linked_tests: [ConsoleUdfControllerTest]`, `tier: P1`) and `console-api-transforms` (owner_module data-generator-service, adapter console-api, test_types [unit], `linked_tests: [ConsoleTransformCatalogControllerTest]`, `tier: P1`). Set `status` to the honest current value (`partial` or `covered` per how the harness computes it after Task 1). Do NOT tag these P0 — the literal P0 set from 05-01 D-03 is unchanged. Keep additive; do not modify unrelated rows.
    </action>
    <acceptance_criteria>
      - Matrix contains rows linking `ConsoleUdfControllerTest` and `ConsoleTransformCatalogControllerTest`, each `tier: P1`.
      - P0 row count remains exactly 7 (unchanged from 05-01).
      - New rows follow the existing row schema (id/capability/adapter/test_types/owner_module/status/tier/linked_tests/notes).
    </acceptance_criteria>
    <verify><automated>pwsh -NoProfile -Command ". ./scripts/lib/test-matrix-summary.ps1; $r = Parse-MatrixRows (Get-Content -Raw .planning/test-matrix.yaml); if (@($r | Where-Object { $_.tier -eq 'P0' }).Count -ne 7) { exit 1 }; $names = ($r | ForEach-Object { @($_.linked_tests) }) -join ','; if ($names -notmatch 'ConsoleUdfControllerTest' -or $names -notmatch 'ConsoleTransformCatalogControllerTest') { exit 1 }; exit 0"</automated></verify>
    <done>Expanded console API slices are matrix-linked and harness-executed as P1.</done>
  </task>

  <task type="auto">
    <name>Task 3: Add the P0 regression gate to the harness (COV-04, D-08/D-09)</name>
    <files>scripts/verify-harness.ps1</files>
    <read_first>
      - scripts/verify-harness.ps1 (summary generation ~line 75; existing `$mavenExit`/`$linkedFailed` exit logic ~lines 102-108)
      - scripts/lib/test-matrix-summary.ps1 (the `p0` block shape added in 05-01: p0.pass / p0.rows[].green)
      - .planning/phases/05-coverage-ramp-ci-gates/05-CONTEXT.md (D-08 gate behavior, D-09 P0-only/embedded-only)
    </read_first>
    <action>
      After `New-TestMatrixSummary` returns `$summary`, read the P0 rollup (`$summary.p0`). Compute `$p0Pass = [bool]$summary.p0.pass`. When `$p0Pass` is false, collect the non-green P0 row ids (`$summary.p0.rows | Where-Object { -not $_.green }`) and `Write-Host` an explicit red message naming them (e.g. `P0 regression gate FAILED: <ids> not green`). Fold the P0 result into the final exit decision: the harness must `exit 1` when `$mavenExit -ne 0` OR `$linkedFailed` OR `-not $p0Pass`. Keep the existing behavior for `$mavenExit`/`$linkedFailed` intact (do not weaken it). The gate is P0-only — do NOT fail on P1/P2 rows — and embedded-only — do NOT make E2E part of the exit decision (E2E stays behind `-IncludeE2e`). Emit a clear success line (`P0 regression gate passed (<n>/<n> green)`) when `$p0Pass` is true.
    </action>
    <acceptance_criteria>
      - Harness exits 1 when `$summary.p0.pass` is false, printing the offending P0 row ids.
      - Harness still exits 1 on `$mavenExit -ne 0` or `$linkedFailed` (unchanged).
      - P1/P2 row failures alone do not cause a non-zero exit; E2E is not part of the exit decision.
    </acceptance_criteria>
    <verify><automated>.\scripts\verify-harness.ps1</automated></verify>
    <done>The harness fails fast on any non-green P0 row and passes when all P0 rows are green.</done>
  </task>

  <task type="auto">
    <name>Task 4: Enforce the P0 gate in CI and document merge criteria (COV-04, D-08)</name>
    <files>.github/workflows/harness-verify.yml
AGENTS.md</files>
    <read_first>
      - .github/workflows/harness-verify.yml (existing steps: checkout, JDK 25, PowerShell, harness run, artifact upload; triggers push + pull_request)
      - AGENTS.md (Commands "Test harness" section + "Git workflow" + "Boundaries" sections)
      - scripts/verify-harness.ps1 (gate from Task 3 — the workflow relies on its exit code)
    </read_first>
    <action>
      In `.github/workflows/harness-verify.yml`, make the P0 regression gate explicit and merge-blocking on PRs. Rename/annotate the harness run step to "Harness verification + P0 regression gate" (it already runs `verify-harness.ps1`, whose non-zero exit now fails the job and blocks merge on `pull_request`). Keep `if-no-files-found: error` artifact upload but run it with `if: always()` so the summary is uploaded even when the gate fails (aids triage). Do not remove existing triggers; keep the change additive and minimal (operator-authorized CI edit, D-08).

      In `AGENTS.md`, add a "Merge criteria (P0 regression gate)" subsection (under Git workflow or Boundaries) stating: PRs are blocked when any P0 matrix row is not green; the gate is enforced by `scripts/verify-harness.ps1` via `.github/workflows/harness-verify.yml`; P1/P2 failures are tracked but non-blocking; the P0 set is defined in `.planning/test-matrix.yaml` (`tier: P0`). Reference `docs/test-harness.md` for tier definitions.
    </action>
    <acceptance_criteria>
      - `harness-verify.yml` runs the harness on `pull_request` with a named P0-gate step; summary upload uses `if: always()`.
      - `AGENTS.md` documents that P0 failures block merge and where the P0 set is defined.
      - No CI triggers removed; change is additive.
    </acceptance_criteria>
    <verify><automated>pwsh -NoProfile -Command "$y = Get-Content -Raw .github/workflows/harness-verify.yml; if ($y -notmatch 'verify-harness.ps1') { exit 1 }; if ((Get-Content -Raw AGENTS.md) -notmatch 'P0') { exit 1 }; exit 0"</automated></verify>
    <done>CI blocks merges on P0 failure and the merge criteria are documented in AGENTS.md.</done>
  </task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| TB-05-02-A | Console HTTP client → UDF + transform catalog endpoints (slice-tested surface) |
| TB-05-02-B | CI pull_request → P0 regression gate (merge authorization) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-02-01 | Information Disclosure | Slice tests asserting on UDF payload/secrets | mitigate | Tests assert on the `R<T>` projection (type/kind/metadata) only — never payload bytes; mirrors UdfVersionView D-14 contract from Phase 3/4. |
| T-05-02-02 | Elevation of Privilege | Merge bypassing the P0 gate | mitigate | Gate runs in `verify-harness.ps1` on `pull_request`; a non-green P0 row yields non-zero exit → failed required check. Branch protection (repo admin) makes the check required. |
| T-05-02-03 | Tampering | Gate weakened by making E2E/P1 failures silently pass or P0 ignored | mitigate | Task 3 keeps existing `$mavenExit`/`$linkedFailed` checks AND adds `-not $p0Pass`; gate is additive, never subtractive; reviewer diff shows any weakening. |
| T-05-02-04 | Tampering | Malformed `kind` query param | mitigate | Covered by new slice test asserting invalid `kind` → 400 via `ConsoleApiAdvice` (no reflection/dynamic load from request input). |
</threat_model>

<verification>
- `.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=ConsoleUdfControllerTest,ConsoleTransformCatalogControllerTest` is green with the new assertions.
- `.\scripts\verify-harness.ps1` exits 0 when all P0 rows are green; temporarily forcing a P0 row non-green makes it exit 1 with the offending id (manual smoke).
- `harness-verify.yml` contains a named P0-gate step on `pull_request`; `AGENTS.md` documents the merge criteria.
- Matrix still has exactly 7 P0 rows; `ConsoleUdfControllerTest` + `ConsoleTransformCatalogControllerTest` are matrix-linked.
</verification>

<success_criteria>
- COV-03: Console API slice tests expanded for UDF + transform metadata endpoints and matrix-linked.
- COV-04: A documented regression gate blocks merges when P0 rows fail, enforced via CI.
</success_criteria>

<output>Create .planning/phases/05-coverage-ramp-ci-gates/05-02-SUMMARY.md when done</output>
