---
phase: 15-multi-jvm-worker-e2e
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - docs/staging-distributed-deployment.md
  - AGENTS.md
autonomous: true
requirements:
  - DIST-01
must_haves:
  truths:
    - "docs/staging-distributed-deployment.md contains a DIST-01 local verify subsection with prerequisites, one-liner script command, expected SUCCESS signals, and cleanup notes (D-09)"
    - "Subsection states verify-multi-jvm-worker.ps1 is the primary DIST-01 gate; Podman drill remains optional (D-02, D-09)"
    - "AGENTS.md Commands section includes comment + path pointer to verify script and staging doc (Phase 13/14 pattern) (D-09)"
    - "P0 gate, test-matrix P0 rows, and verify-harness merge semantics remain untouched (D-10)"
  artifacts:
    - path: "docs/staging-distributed-deployment.md"
      provides: "DIST-01 local verify maintainer recipe"
    - path: "AGENTS.md"
      provides: "Commands catalog entry for multi-JVM verify"
  key_links:
    - from: "AGENTS.md"
      to: "scripts/verify-multi-jvm-worker.ps1"
      via: "Commands fenced block comment + path"
      pattern: "verify-multi-jvm-worker"
    - from: "docs/staging-distributed-deployment.md"
      to: "scripts/verify-multi-jvm-worker.ps1"
      via: "DIST-01 local verify subsection"
      pattern: "verify-multi-jvm-worker"
---

<objective>
Package DIST-01 for maintainers: extend the C2 staging runbook and add an AGENTS.md Commands pointer (D-09).

Purpose: Operators can discover and run the host two-JVM proof without reading plan docs; Podman path stays documented as optional supplement.
Output: Doc subsection + AGENTS.md entry. Depends on plans 15-01 (script) and 15-02 (matrix row exists for cross-reference in doc if useful).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md
@.planning/phases/15-multi-jvm-worker-e2e/15-VALIDATION.md
@.planning/phases/15-multi-jvm-worker-e2e/15-01-PLAN.md
@.planning/phases/15-multi-jvm-worker-e2e/15-02-PLAN.md
@docs/staging-distributed-deployment.md
@AGENTS.md
@scripts/verify-multi-jvm-worker.ps1
</context>

## Artifacts this phase produces

- `docs/staging-distributed-deployment.md` — DIST-01 local verify section — **this plan**
- `AGENTS.md` — Commands pointer — **this plan**

## Non-goals (D-10)

- Full AC-1..AC-7 staging checklist as DIST-01 done criteria
- Playwright distributed specs documentation as required gate
- RBAC-on distributed runs (Phase 16)
- README.md drive-by churn
- New verify scripts beyond 15-01

<tasks>

<task type="auto">
  <name>Task 1: Add DIST-01 local verify subsection to staging-distributed-deployment.md</name>
  <files>docs/staging-distributed-deployment.md</files>
  <read_first>
    - .planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md (D-02, D-07, D-09, D-10)
    - docs/staging-distributed-deployment.md (existing Local pre-flight, Podman dual-JVM sections — insert after Local pre-flight or before Podman)
    - scripts/verify-multi-jvm-worker.ps1 (actual flags: SkipBuild, SkipMavenPreflight, KeepWorkDir, HostPort)
  </read_first>
  <action>
    Add a new `## DIST-01 local verify (host two-JVM)` subsection to `docs/staging-distributed-deployment.md` covering:

    1. **Purpose** — one multi-JVM happy path; closes process-boundary gap vs single-JVM `DistributedSplitRoleIntegrationTests`.

    2. **Prerequisites** — JDK 25 (`mvnw-jdk25.ps1`), free coordinator port (default 9876), Windows PowerShell (primary); ~2–4 min first run for classpath build.

    3. **One-liner:**

       ```powershell
       .\scripts\verify-multi-jvm-worker.ps1
       ```

    4. **Faster iteration flags** — `-SkipBuild`, `-SkipMavenPreflight`, `-KeepWorkDir` (document when each applies).

    5. **Expected SUCCESS signals (D-07):**
       - Script prints `[SUCCESS] Multi-JVM worker E2E: instanceId=...` (and distributed job / worker id fields)
       - Exit code 0
       - Both `task_execution.status` and `distributed_job.status` are SUCCESS (mention explicitly)

    6. **Cleanup** — script stops JVMs and removes temp work dir by default; `-KeepWorkDir` for log inspection; note H2 file lock if processes killed manually (pointer to stop stray Java).

    7. **Harness** — P1 matrix row `dist-multi-jvm-worker` (non-blocking); link to `docs/test-harness.md` P1 semantics.

    8. **Optional paths** — `staging-distributed-smoke.ps1` (embedded IT preflight), `e2e-distributed-podman.ps1` (optional drill, **not** DIST-01 gate).

    Keep existing AC checklist and Podman sections intact — additive edit only.
  </action>
  <verify>
    <automated>rg -n "DIST-01 local verify|verify-multi-jvm-worker" docs/staging-distributed-deployment.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "distributed_job.status|task_execution.status" docs/staging-distributed-deployment.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/staging-distributed-deployment.md` contains heading or subsection title referencing DIST-01 local verify
    - Doc includes `.\scripts\verify-multi-jvm-worker.ps1` command
    - Doc mentions dual SUCCESS on `distributed_job` and `task_execution`
    - Doc states Podman is optional, not the primary DIST-01 gate
    - Existing Podman and AC sections not removed
  </acceptance_criteria>
  <done>Maintainers have a single runbook entry for host multi-JVM proof.</done>
</task>

<task type="auto">
  <name>Task 2: Add AGENTS.md Commands pointer (Phase 13/14 pattern)</name>
  <files>AGENTS.md</files>
  <read_first>
    - .planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md (D-09)
    - AGENTS.md (Commands fenced block — mirror Phase 13 Dameng ~line 99 and Phase 14 resolver ~line 102)
    - docs/staging-distributed-deployment.md (Task 1 output for cross-link text)
  </read_first>
  <action>
    In `AGENTS.md` Commands section fenced code block, add one surgical entry after the Phase 14 resolver pointer:

    ```markdown
    # Multi-JVM worker E2E — host coordinator + worker, shared file H2 (DIST-01; see docs/staging-distributed-deployment.md#dist-01-local-verify-host-two-jvm)
    .\scripts\verify-multi-jvm-worker.ps1
    ```

    Adjust anchor slug to match the actual markdown heading generated in Task 1. Use comment line naming DIST-01 + doc path; include runnable script line.

    Do **not** reorder unrelated entries aggressively. Do **not** edit P0/harness sections.
  </action>
  <verify>
    <automated>rg -n "verify-multi-jvm-worker" AGENTS.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "staging-distributed-deployment" AGENTS.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "verify-harness.*dist-multi" AGENTS.md; if ($?) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "verify-multi-jvm-worker" AGENTS.md` matches inside Commands section
    - Comment references `docs/staging-distributed-deployment.md`
    - Runnable line `.\scripts\verify-multi-jvm-worker.ps1` present
    - No changes to `.planning/test-matrix.yaml` P0 rows or `verify-harness.ps1` gate logic in this task
  </acceptance_criteria>
  <done>AGENTS.md catalog lists the DIST-01 verify recipe.</done>
</task>

</tasks>

## Verification

```powershell
rg -n "DIST-01|verify-multi-jvm-worker" docs/staging-distributed-deployment.md AGENTS.md
rg -n "tier: P0" .planning/test-matrix.yaml | Measure-Object | Select-Object -ExpandProperty Count  # expect 15
```

## Success criteria

- [ ] Runbook + AGENTS pointer complete
- [ ] Dual SUCCESS and optional Podman distinction documented
- [ ] P0/harness untouched
