---
phase: 15-multi-jvm-worker-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-multi-jvm-worker.ps1
  - scripts/lib/distributed-staging-rest.ps1
  - scripts/lib/distributed-host-jvm.ps1
autonomous: true
requirements:
  - DIST-01
must_haves:
  truths:
    - "scripts/verify-multi-jvm-worker.ps1 starts two host OS JVMs (coordinator + worker) with profiles distributed-staging,distributed-coordinator and distributed-staging,distributed-worker (D-01)"
    - "Both JVMs share an isolated temp file H2 db/ directory (AUTO_SERVER file URL), not in-memory H2 (D-01)"
    - "Coordinator trigger is POST /task/run/{templateId} against a published minimal V2 template (iterator → SQL → console sink) using real TemplateV2Runner on the worker (D-05, D-06)"
    - "Script polls until distributed_job.status == SUCCESS AND linked task_execution.status == SUCCESS within a bounded timeout (D-07)"
    - "Script exits 0 on SUCCESS and non-zero on failure; output echoes instanceId and distributed job id for debugging (D-02, CONTEXT specifics)"
    - "Existing single-JVM distributed ITs remain unchanged (D-08)"
  prohibitions:
    - statement: "Podman e2e-distributed-podman.ps1 is not the DIST-01 done gate"
      status: resolved
      verification: "rg -n 'e2e-distributed-podman' scripts/verify-multi-jvm-worker.ps1 returns no match OR only a comment marking optional drill"
    - statement: "Do not modify DistributedSplitRoleIntegrationTests or other embedded distributed ITs"
      status: resolved
      verification: "git diff --name-only data-generator-service/src/test/java/org/gensokyo/data/task/Distributed*.java is empty after plan 15-01"
  artifacts:
    - path: "scripts/verify-multi-jvm-worker.ps1"
      provides: "Primary DIST-01 proof — host two-JVM enqueue → lease → SUCCESS"
    - path: "scripts/lib/distributed-host-jvm.ps1"
      provides: "Host coordinator/worker java -cp spawn + cleanup helpers"
    - path: "scripts/lib/distributed-staging-rest.ps1"
      provides: "New-DistributedMinimalIteratorConsoleTemplate + dual-status poll helper"
  key_links:
    - from: "scripts/verify-multi-jvm-worker.ps1"
      to: "TaskController POST /task/run/{id}"
      via: "Start-DistributedTemplateRun in distributed-staging-rest.ps1"
      pattern: "/task/run/"
    - from: "scripts/verify-multi-jvm-worker.ps1"
      to: "DataGeneratorWorkerApplication"
      via: "host java -cp launch with distributed-staging,distributed-worker"
      pattern: "DataGeneratorWorkerApplication"
    - from: "coordinator JVM"
      to: "worker JVM"
      via: "shared jdbc:h2:file:./db/distributed-staging;AUTO_SERVER=TRUE"
      pattern: "AUTO_SERVER=TRUE"
---

<objective>
Deliver the primary DIST-01 proof: `scripts/verify-multi-jvm-worker.ps1` spawns coordinator + worker on the host OS, enqueues via HTTP `/task/run`, and polls dual SUCCESS across the process boundary (D-01, D-02, D-05–D-08).

Purpose: Close the gap left by single-JVM `DistributedSplitRoleIntegrationTests` (CapturingTemplateV2Runner stub) with real multi-process execution.
Output: Green verify script, reusable host-JVM lib, minimal-template REST helper. No harness or docs changes in this plan.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md
@.planning/phases/15-multi-jvm-worker-e2e/15-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@data-generator-service/src/main/resources/application-distributed-staging.yaml
@data-generator-service/src/main/resources/application-distributed-coordinator.yaml
@data-generator-service/src/main/resources/application-distributed-worker.yaml
@data-generator-service/src/test/java/org/gensokyo/data/task/DistributedSplitRoleIntegrationTests.java
@scripts/lib/distributed-staging-rest.ps1
@scripts/e2e-distributed-podman.ps1
@scripts/start-dev.ps1
@scripts/lib/repo-maven.ps1
</context>

## Artifacts this phase produces

*(Phase-wide inventory; this plan delivers Wave 1 core proof)*

- `scripts/verify-multi-jvm-worker.ps1` — primary DIST-01 gate script — **this plan**
- `scripts/lib/distributed-host-jvm.ps1` — host JVM spawn/stop/health — **this plan**
- `New-DistributedMinimalIteratorConsoleTemplate` — REST helper in `distributed-staging-rest.ps1` — **this plan**
- `Wait-DistributedDualSuccess` (or equivalent) — asserts both execution + distributedJob SUCCESS — **this plan**
- *(Wave 2)* `dist-multi-jvm-worker` P1 matrix row — plan 15-02
- *(Wave 3)* `docs/staging-distributed-deployment.md` DIST-01 subsection + AGENTS.md pointer — plan 15-03

## Non-goals (D-10 — out of scope for this plan)

- Full staging AC-1..AC-7 checklist automation
- Chaos / heartbeat expiry / lease-recovery drills (AC-4/5)
- Second worker JVM, Playwright distributed specs as gate, Podman-only proof
- RBAC-enabled runs, coordinator-local execution when worker disabled
- P0 matrix promotion or changes to `verify-harness.ps1` merge gate
- Modifications to `DistributedSplitRoleIntegrationTests`, `DistributedJob*IntegrationTests`, etc.

<tasks>

<task type="auto">
  <name>Task 1: Add minimal template REST helper and dual SUCCESS poll to distributed-staging-rest.ps1</name>
  <files>scripts/lib/distributed-staging-rest.ps1</files>
  <read_first>
    - .planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md (D-05, D-06, D-07)
    - scripts/lib/distributed-staging-rest.ps1 (Start-DistributedTemplateRun, Wait-DistributedJobSuccess, Invoke-DistributedEnqueueSmoke)
    - data-generator-service/src/test/java/org/gensokyo/data/task/DistributedSplitRoleIntegrationTests.java (iterator → sql → console YAML shape, lines 97–115)
    - data-generator-service/src/main/resources/application-distributed-staging.yaml (require-published-for-task-run: true)
  </read_first>
  <action>
    Extend `scripts/lib/distributed-staging-rest.ps1` with two functions (names discretionary if clearer):

    1. **`New-DistributedMinimalIteratorConsoleTemplate`** — creates + publishes a minimal V2 template via existing console REST helpers (`Invoke-DistributedApi`, `Publish-DistributedDraftTemplate` pattern). Draft body must match D-06 / `DistributedSplitRoleIntegrationTests` shape:
       - `sources.input.type: iterator`, `iterator.type: number`, `from: 1`, `to: 2`, `step: 1`
       - `transform.type: sql`, `sql: SELECT value FROM input`
       - `sink.writers[0].type: console`
       - `sinkExecutionPolicy.mode: CONTINUE_ON_ERROR`
       - Unique `name` suffix via timestamp (e.g. `dist-minimal-$(Get-Date -Format 'yyyyMMddHHmmss')`)
       Return `[pscustomobject]@{ TemplateId = [int64]; TemplateName = [string] }`.

    2. **`Wait-DistributedDualSuccess`** — wraps polling via `Get-DistributedJobDetail`. On SUCCESS return only when **both** `$data.execution.status -eq 'SUCCESS'` **and** `$data.distributedJob.status -eq 'SUCCESS'`. Fail fast on FAILED/CANCELLED for either side. Default timeout 180s (borrow from `Wait-DistributedJobSuccess`). Parameter names: `-BaseUrl`, `-InstanceId`, `-TimeoutSec`.

    Update `Wait-DistributedJobSuccess` only if needed for backward compatibility — prefer new function so Podman smokes stay unchanged.

    Do **not** change `Invoke-DistributedEnqueueSmoke` to use GF-A unless explicitly needed; keep GF-A path for Podman drill.
  </action>
  <verify>
    <automated>rg -n "New-DistributedMinimalIteratorConsoleTemplate|Wait-DistributedDualSuccess" scripts/lib/distributed-staging-rest.ps1; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SELECT value FROM input" scripts/lib/distributed-staging-rest.ps1; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "distributedJob\.status" scripts/lib/distributed-staging-rest.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/distributed-staging-rest.ps1` defines `New-DistributedMinimalIteratorConsoleTemplate` with iterator/number source, SQL transform, console sink
    - `Wait-DistributedDualSuccess` (or equivalent name) asserts both `execution.status` and `distributedJob.status` equal `SUCCESS`
    - Existing `Invoke-DistributedEnqueueSmoke` and Podman helpers compile unchanged (no breaking signature edits)
    - SQL text `SELECT value FROM input` appears in the new template helper
  </acceptance_criteria>
  <done>REST helpers exist for D-06 minimal template seeding and D-07 dual-status SUCCESS polling.</done>
</task>

<task type="auto">
  <name>Task 2: Create distributed-host-jvm.ps1 for classpath build and background process lifecycle</name>
  <files>scripts/lib/distributed-host-jvm.ps1</files>
  <read_first>
    - scripts/start-dev.ps1 (java -cp launch: package + dependency:copy-dependencies pattern, JDK 25 handling)
    - scripts/lib/repo-maven.ps1 (Invoke-RepoMaven)
    - data-generator-service/script/run-worker.ps1 (DG_SERVICE_ROLE=worker pattern)
    - data-generator-service/src/main/resources/application-distributed-staging.yaml (shared file H2 path `./db/distributed-staging`)
    - scripts/e2e-distributed-podman.ps1 (worker Spring args: poll-delay-ms, lease-seconds tuning for local runs)
  </read_first>
  <action>
    Create `scripts/lib/distributed-host-jvm.ps1` exporting:

    - **`Build-DistributedServiceClasspath`** — if `target/classes` + `target/dependency/*.jar` missing (or `-ForceBuild`), run `Invoke-RepoMaven -RepoRoot $RepoRoot -pl data-generator-service -am -DskipTests -Dskip.console.frontend=true -Dassembly.skipAssembly=true package dependency:copy-dependencies -DoutputDirectory=target/dependency -DincludeScope=runtime`. Return classpath string (`classes;dependency\*`).

    - **`Start-DistributedHostJvm`** — params: `-RepoRoot`, `-ServiceDir`, `-WorkingDirectory` (isolated run dir containing `db/`), `-MainClass`, `-SpringProfilesActive`, `-ServerPort` (coordinator only; worker may omit or use different port if profile binds one), `-ExtraSpringArgs` (string array), `-LogFile`. Launch via `Start-Process` (background, **not** interactive `-NoExit` window). Set `$env:JAVA_HOME` using same JDK-25 resolution as `Invoke-RepoMaven` / `mvnw-jdk25.ps1`. Pass `--spring.profiles.active=...` and `--server.port=...` when coordinator. For worker: main class `org.gensokyo.data.DataGeneratorWorkerApplication`, profiles `distributed-staging,distributed-worker`, unique `--data.generator.distributed.worker-id=host-worker-1`. Return process object + log path.

    - **`Stop-DistributedHostJvm`** — graceful stop then kill; remove pid tracking.

    - **`Wait-DistributedHostHealth`** — poll `http://127.0.0.1:{port}/healthz` until `opcode` 0 (reuse timing from `Wait-DistributedHealth` in distributed-staging-rest.ps1).

    Working directory for both JVMs must be the **same isolated temp folder** so `./db/distributed-staging` resolves identically (D-01). Create `db/` subfolder before start; H2 creates files on first connect.

    Tuning defaults (discretion, match Podman local stability): `--data.generator.distributed.poll-delay-ms=1000`, `--data.generator.distributed.lease-seconds=60`.
  </action>
  <verify>
    <automated>Test-Path scripts/lib/distributed-host-jvm.ps1; if (-not $?) { exit 1 }; rg -n "Start-DistributedHostJvm|Build-DistributedServiceClasspath|DataGeneratorWorkerApplication" scripts/lib/distributed-host-jvm.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/distributed-host-jvm.ps1` exists and defines classpath build + background JVM start/stop + health wait
    - Worker launch uses `org.gensokyo.data.DataGeneratorWorkerApplication`
    - Coordinator launch uses `org.gensokyo.data.DataGeneratorApplication`
    - Functions accept a shared `-WorkingDirectory` for shared `./db/` path
    - No Podman/docker invocations in this file
  </acceptance_criteria>
  <done>Reusable host two-JVM lifecycle helpers ready for the verify script.</done>
</task>

<task type="auto">
  <name>Task 3: Implement scripts/verify-multi-jvm-worker.ps1 end-to-end proof</name>
  <files>scripts/verify-multi-jvm-worker.ps1</files>
  <read_first>
    - .planning/phases/15-multi-jvm-worker-e2e/15-CONTEXT.md (D-01–D-08, D-10)
    - scripts/lib/distributed-host-jvm.ps1 (Task 2 output)
    - scripts/lib/distributed-staging-rest.ps1 (Task 1 output)
    - scripts/staging-distributed-smoke.ps1 (Write-Step pattern, Maven pre-flight optional slice)
    - data-generator-service/src/main/java/org/gensokyo/data/controller/TaskController.java (runById message shape with instanceId=)
  </read_first>
  <action>
    Create `scripts/verify-multi-jvm-worker.ps1` with params:
    - `[switch]$SkipBuild` — skip Maven classpath build when artifacts present
    - `[switch]$SkipMavenPreflight` — skip embedded distributed IT preflight
    - `[switch]$KeepWorkDir` — retain temp dir for debugging
    - `[int]$HostPort = 9876` — coordinator port (fail if port in use by non-DG process)
    - `[int]$TimeoutSec = 180` — poll budget for dual SUCCESS

    Flow:
    1. **Optional Maven preflight (D-08 guard, not replacement):** unless `-SkipMavenPreflight`, run same slice as `staging-distributed-smoke.ps1` (`DistributedJob*IntegrationTests,DistributedJobServiceTests,DistributedSplitRoleIntegrationTests,ConsoleDistributedControllerTest`) via `Invoke-RepoMaven`. This validates embedded ITs still green; does **not** satisfy DIST-01 alone.

    2. **Isolated work dir:** `$workDir = Join-Path $env:TEMP ("dg-dist-verify-" + [guid]::NewGuid().ToString("n"))`; create `db/` underneath; `Push-Location $workDir` for JVM cwd.

    3. **Build classpath** unless `-SkipBuild`.

    4. **Start coordinator** in `$workDir` with profiles `distributed-staging,distributed-coordinator`, port `$HostPort`. Wait health.

    5. **Start worker** in same `$workDir` with profiles `distributed-staging,distributed-worker`, unique worker-id. Wait until coordinator metrics or short sleep indicate worker registered (optional: GET `/api/console/distributed/metrics`).

    6. **Seed + enqueue (D-05, D-06):** `$baseUrl = "http://127.0.0.1:$HostPort"`. Call `New-DistributedMinimalIteratorConsoleTemplate`. Then `Start-DistributedTemplateRun -BaseUrl $baseUrl -TemplateId $templateId` (POST `/task/run/{id}`).

    7. **Poll (D-07):** `Wait-DistributedDualSuccess`. Capture `$detail.distributedJob.id` (or equivalent field from job detail API) and `$instanceId`.

    8. **Success output:** print `[SUCCESS] Multi-JVM worker E2E: instanceId=... distributedJobId=... workerId=...` (field names per actual API shape).

    9. **Cleanup:** `finally` block stops both JVMs; removes `$workDir` unless `-KeepWorkDir`. Exit 0 only on full SUCCESS path.

    Dot-source: `lib/repo-maven.ps1`, `lib/distributed-host-jvm.ps1`, `lib/distributed-staging-rest.ps1`.

    Filename: `verify-multi-jvm-worker.ps1` (D-02). Do **not** invoke `e2e-distributed-podman.ps1`.
  </action>
  <verify>
    <automated>rg -n "verify-multi-jvm-worker|Wait-DistributedDualSuccess|New-DistributedMinimalIteratorConsoleTemplate|distributed-staging,distributed-coordinator|distributed-staging,distributed-worker" scripts/verify-multi-jvm-worker.ps1; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "e2e-distributed-podman" scripts/verify-multi-jvm-worker.ps1; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-multi-jvm-worker.ps1` exists at repo root `scripts/`
    - Script uses isolated temp `db/` directory (not in-memory H2 URL)
    - Script calls POST `/task/run/` via `Start-DistributedTemplateRun`
    - Script asserts both execution and distributedJob SUCCESS before exit 0
    - Script does not call or require Podman
    - `git diff --name-only data-generator-service/src/test/java/org/gensokyo/data/task/` shows no modifications to existing distributed IT files
    - Manual smoke: `powershell -NoProfile -File scripts/verify-multi-jvm-worker.ps1 -SkipMavenPreflight` exits 0 on a JDK-25 Windows host with port 9876 free
  </acceptance_criteria>
  <done>DIST-01 primary proof script runs coordinator + worker on host OS and reaches dual SUCCESS.</done>
</task>

</tasks>

## Verification

After all tasks:

```powershell
# Static contract
rg -n "New-DistributedMinimalIteratorConsoleTemplate|Wait-DistributedDualSuccess" scripts/lib/distributed-staging-rest.ps1
rg -n "Start-DistributedHostJvm" scripts/lib/distributed-host-jvm.ps1
rg -n "distributed-staging,distributed-coordinator" scripts/verify-multi-jvm-worker.ps1

# Full proof (requires JDK 25, free port 9876)
powershell -NoProfile -File scripts/verify-multi-jvm-worker.ps1 -SkipMavenPreflight
```

## Success criteria

- [ ] Two host JVMs share file H2 and complete enqueue → lease → execute → dual SUCCESS
- [ ] Trigger path is HTTP POST `/task/run/{templateId}` with published iterator/SQL/console template
- [ ] Single-JVM ITs untouched; Podman not required for done gate
