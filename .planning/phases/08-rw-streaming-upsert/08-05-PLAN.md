---
id: 08-05
phase: 08
title: JDBC Upsert SQL Generation for PostgreSQL & MySQL
wave: 2
depends_on: [08-03]
requirements: [RW-03]
autonomous: true
files_modified:
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/WriterOptionResolver.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/SinkWriteMetric.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcUpsertSmokeTests.java
must_haves:
  truths:
    - Upsert configured via options.upsert true and options.upsertKeys list (D-12)
    - PostgreSQL generates ON CONFLICT (keys) DO UPDATE SET ... (D-13, D-15)
    - MySQL generates INSERT ... ON DUPLICATE KEY UPDATE ... (D-13, D-15)
    - Missing/invalid upsertKeys fail-fast at run with actionable IllegalArgumentException (D-14)
    - JdbcBulkWriteExecutor increments rowsUpserted during upsert batches in 08-05 — not deferred (W-05)
    - bulkMode postgres_copy rejects upsert=true (existing guard preserved)
    - PG/MySQL only — no ClickHouse/DM/Kingbase/HighGo upsert (Phase 9)
  artifacts:
    - Updated org.gensokyo.data.calcite.sink.JdbcSinkSqlBuilder
    - Updated org.gensokyo.data.calcite.sink.JdbcBulkWriteExecutor
    - JdbcSinkSqlBuilderTests upsertKeys cases
    - JdbcUpsertSmokeTests (H2 basic path per D-25)
---

# Plan 08-05: JDBC Upsert SQL Generation for PostgreSQL & MySQL

## Objective

Implement dialect-correct JDBC sink upsert/merge for PostgreSQL and MySQL with idempotent re-run semantics (RW-03, D-12–D-15).

**Output:** `JdbcSinkSqlBuilder` generates UPDATE-on-conflict SQL from `upsertKeys`; runtime validates keys; H2 smoke test.

## Context

@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@.planning/phases/08-rw-streaming-upsert/08-PATTERNS.md
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java
@docs/template-v2-jdbc-sink-guide.md

## Tasks

<task>
  <read_first>
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java (appendPostgresUpsert, appendMysqlUpsert, conflictColumns legacy)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/WriterOptionResolver.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-12, D-13, D-14
  </read_first>
  <action>
    Per D-12: standardize on `options.upsertKeys: [col1, col2]` list (YAML array).
    Deprecate but accept legacy `conflictColumns` comma-string for PG — map to upsertKeys internally with Javadoc note.
    Per D-13/D-15:
    - PostgreSQL: `INSERT ... ON CONFLICT (upsertKeys) DO UPDATE SET col = EXCLUDED.col` for non-key columns
    - MySQL: `INSERT ... ON DUPLICATE KEY UPDATE col = VALUES(col)` (or alias per project SQL style)
    Per D-14: when `upsert: true` and upsertKeys empty/null/contains unknown column → throw `IllegalArgumentException` with sink writer name and column list.
    Restrict upsert generation to dialect `postgres` and `mysql` only; other dialects throw unsupported (Phase 9 deferred).
    Extend `JdbcSinkSqlBuilderTests`:
    - `buildsPostgresUpsertWithUpsertKeysAndUpdateClause`
    - `buildsMysqlUpsertWithUpsertKeys`
    - `rejectsEmptyUpsertKeysWhenUpsertTrue`
  </action>
  <acceptance_criteria>
    - PG upsert SQL contains `ON CONFLICT (id) DO UPDATE SET` for fixture table with non-key columns updated
    - MySQL upsert SQL contains `ON DUPLICATE KEY UPDATE`
    - Empty upsertKeys throws message containing `upsertKeys` and `upsert`
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcSinkSqlBuilderTests -q` exits 0
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcBulkWriteExecutor.java
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/SinkWriteMetric.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresContainerTests.java (pattern reference)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-14, D-15, D-25
  </read_first>
  <action>
    Wire upsert SQL through `JdbcBulkWriteExecutor` batch insert path (non-COPY mode).
    Preserve guard: `bulkMode postgres_copy` + `upsert=true` → IllegalArgumentException.
    Per D-15: increment `SinkWriteMetric.rowsUpserted` in `JdbcBulkWriteExecutor` during batch execution — **required in 08-05, not deferred to 08-09**:
    - On upsert SQL batches, populate `rowsUpserted` from JDBC batch update counts (or driver-specific `Statement.SUCCESS_NO_INFO` heuristic documented in Javadoc)
    - Minimum acceptable: second-run idempotency IT in 08-09 must read `rowsUpserted > 0` from metrics produced here; do not leave field at zero with a comment deferring to later plans
    Add `JdbcUpsertSmokeTests` using H2 with `MERGE`-compatible stub or simplified insert-only smoke when H2 lacks ON CONFLICT — document H2 limitation; full dialect proof deferred to Testcontainers in 08-09 (D-25).
    Validate upsertKeys against transform output schema at first write batch (fail-fast D-14 at run).
  </action>
  <acceptance_criteria>
    - H2 smoke test executes upsert-enabled template without SQL builder exception when keys valid
    - Invalid upsert key `unknown_col` fails at run before JDBC execute with column name in message
    - `bulkMode postgres_copy` + upsert still throws unchanged error message
    - `JdbcBulkWriteExecutor` increments `SinkWriteMetric.rowsUpserted` on upsert batch path (unit or smoke test asserts counter > 0 on conflict re-run fixture)
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=JdbcSinkSqlBuilderTests,JdbcUpsertSmokeTests -q` exits 0
  </acceptance_criteria>
</task>

## Verification criteria

- upsertKeys YAML shape documented and enforced (D-12, D-14)
- PG/MySQL SQL dialect-correct (D-13)
- No upsert for non-PG/MySQL dialects in Phase 8

## Artifacts this phase produces

- Updated `org.gensokyo.data.calcite.sink.JdbcSinkSqlBuilder`
- Updated `org.gensokyo.data.calcite.sink.JdbcBulkWriteExecutor`
- Updated `org.gensokyo.data.calcite.runtime.SinkWriteMetric` (rowsUpserted field prep)
- Extended `JdbcSinkSqlBuilderTests`
- `org.gensokyo.data.calcite.sink.JdbcUpsertSmokeTests`
