---
id: 08-09
phase: 08
title: OOM Proof IT & Testcontainers Upsert Idempotency
wave: 4
depends_on: [08-08]
requirements: [RW-01, RW-02, RW-03]
autonomous: true
files_modified:
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/CsvJsonStreamingOomIT.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineMySqlUpsertTests.java
  - data-generator-calcite/pom.xml
  - data-generator-service/src/test/resources/application-phase7-test.yaml
must_haves:
  truths:
    - OOM proof IT runs at -Xmx256m with 10 MB fixture under CHUNKED or STREAMING for CSV and NDJSON sources (D-06, D-24)
    - Testcontainers PostgreSQL upsert idempotent re-run verified (D-25, D-15)
    - Testcontainers MySQL upsert idempotent re-run verified (D-25, D-15)
    - H2 smoke covered in 08-05 JdbcUpsertSmokeTests (D-25)
  artifacts:
    - org.gensokyo.data.calcite.runtime.CsvJsonStreamingOomIT
    - ChunkedPipelinePostgresUpsertTests
    - ChunkedPipelineMySqlUpsertTests
    - Surefire argLine profile or test JVM args for OOM IT
---

# Plan 08-09: OOM Proof IT & Testcontainers Upsert Idempotency

## Objective

Prove streaming CSV/JSON completes without OOM at 256 MB heap and verify dialect-correct upsert idempotency on real PostgreSQL and MySQL (D-24, D-25, RW-01–RW-03).

**Output:** Dedicated OOM IT; Testcontainers upsert tests gated on Docker availability.

## Context

@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@.planning/phases/08-rw-streaming-upsert/08-PATTERNS.md
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresContainerTests.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DockerTestSupport.java
@docs/testing-embedded-components.md
@data-generator-calcite/pom.xml

## Tasks

<task>
  <read_first>
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/StreamingPipelineTests.java (chunk/peak assertions)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresContainerTests.java
    - data-generator-calcite/pom.xml (surefire argLine, mockito.agentLine pattern)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-06, D-24
  </read_first>
  <action>
    Create `CsvJsonStreamingOomIT`:
    - Generate or copy **≥10 MB** CSV fixture (~100k rows) in `@BeforeAll` under temp dir
    - **Also generate ≥10 MB NDJSON fixture** (~100k lines) and add separate test method `streamingNdjsonCompletesUnder256mHeap` with mode STREAMING, NDJSON source → NDJSON or console sink (W-02 — both CSV and JSON source paths proven at OOM bar)
    - Template (CSV method): single CSV source, mode CHUNKED (and optionally separate test method STREAMING), SQL `SELECT * FROM incoming`, CSV or console sink
    - Annotate class or method with surefire `-Xmx256m` via `@Tag("oom")` + profile in pom OR dedicated surefire execution block with argLine `-Xmx256m` (D-24)
    - Assert run SUCCESS, expected row count ~100k, `peakRowsInMemory <= sourceChunkSize * safetyFactor`
    - Do NOT add IN_MEMORY OOM as CI gate (optional ignored test documented only)
    Document fixture bar in test Javadoc referencing D-06 operator expectation.
  </action>
  <acceptance_criteria>
    - `CsvJsonStreamingOomIT` passes with `-Xmx256m` when run via documented Maven command (CSV and NDJSON test methods)
    - Test fails fast or skips if fixture cannot be generated (clear message)
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=CsvJsonStreamingOomIT -Dsurefire.argLine="-Xmx256m" -q` exits 0
    - Heap OOM does not occur (test completes — implicit pass)
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresContainerTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/ChunkedJdbcParitySupport.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DockerTestSupport.java
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-15, D-25
  </read_first>
  <action>
    Create `ChunkedPipelinePostgresUpsertTests` (@EnabledIf dockerAvailable):
    - PostgreSQLContainer postgres:16-alpine
    - DDL: table with PRIMARY KEY on upsert key column
    - Run upsert template twice with changed non-key column values
    - Assert: row count after second run equals after first; updated column values match second run input; `rowsUpserted > 0` on second run metrics
    Create `ChunkedPipelineMySqlUpsertTests` similarly with MySQL container (mysql:8 or compatible image used elsewhere in repo).
    Extract shared helper `assertUpsertIdempotent(TemplateV2VO template, JdbcTemplate jdbc, int expectedRows)` in test support if duplication exceeds one screen.
    Gate with `@EnabledIf("org.gensokyo.data.calcite.support.DockerTestSupport#dockerAvailable")` matching existing pattern.
  </action>
  <acceptance_criteria>
    - PG upsert test passes when Docker available; skipped gracefully when not
    - MySQL upsert test passes when Docker available
    - Second run does not increase `SELECT COUNT(*)` from first run (D-15)
    - Generated SQL logged on failure contains `ON CONFLICT` (PG) or `ON DUPLICATE KEY` (MySQL)
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=ChunkedPipelinePostgresUpsertTests,ChunkedPipelineMySqlUpsertTests -q` exits 0 when Docker up
  </acceptance_criteria>
</task>

## Verification criteria

- Documented OOM bar proven at 256 MB heap (D-06, D-24)
- Real PG/MySQL upsert idempotency (D-25, D-15)
- Embedded-first: tests skip without Docker

## Artifacts this phase produces

- `org.gensokyo.data.calcite.runtime.CsvJsonStreamingOomIT`
- `org.gensokyo.data.calcite.runtime.ChunkedPipelinePostgresUpsertTests`
- `org.gensokyo.data.calcite.runtime.ChunkedPipelineMySqlUpsertTests`
- Optional `assertUpsertIdempotent` test support helper
- Surefire/OOM JVM arg configuration in calcite pom
