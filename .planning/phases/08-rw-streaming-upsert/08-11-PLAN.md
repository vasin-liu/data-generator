---
id: 08-11
phase: 08
title: Playwright E2E RW Streaming & Upsert Scenarios
wave: 5
depends_on: [08-08, 08-09]
requirements: [RW-01, RW-02, RW-03, RW-04]
autonomous: true
files_modified:
  - data-generator-console-web/e2e/specs/rw-streaming-upsert.spec.ts
  - data-generator-console-web/e2e/helpers/rw-streaming-upsert.ts
  - data-generator-console-web/e2e/helpers/template-run.ts
  - data-generator-console-web/package.json
  - data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleTemplateScenarioController.java
must_haves:
  truths:
    - Minimum 6 Playwright scenarios per D-23 covering CSV CHUNKED PG upsert MySQL upsert NDJSON failure report IN_MEMORY warn
    - IN_MEMORY warn scenario uses generated temp fixture via API inject — not committed 10 MB file (W-06)
    - API-first pattern using createPublishRunFromScenario helpers (Phase 6/7 pattern)
    - Job center shows extended sink metrics and actionable errors (D-17)
    - Scenario scaffolds registered for E2E scenario IDs
  artifacts:
    - data-generator-console-web/e2e/specs/rw-streaming-upsert.spec.ts
    - data-generator-console-web/e2e/helpers/rw-streaming-upsert.ts
    - npm script e2e:phase8-rw-streaming-upsert
---

# Plan 08-11: Playwright E2E RW Streaming & Upsert Scenarios

## Objective

Deliver Playwright E2E coverage for all Phase 8 operator paths including failure and warn scenarios (D-23, RW-01–RW-04).

**Output:** `rw-streaming-upsert.spec.ts` with 6+ scenarios; helpers; npm script.

## Context

@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@.planning/phases/08-rw-streaming-upsert/08-PATTERNS.md
@data-generator-console-web/e2e/specs/datasource-v2-template-run.spec.ts
@data-generator-console-web/e2e/helpers/template-run.ts
@data-generator-console-web/e2e/specs/datasource-governance.spec.ts
@scripts/verify-phase7-uat-datasource-governance.ps1

## Tasks

<task>
  <read_first>
    - data-generator-console-web/e2e/helpers/template-run.ts (createPublishRunFromScenario, waitForJobSuccess, fetchJobDetail)
    - data-generator-console-web/e2e/specs/datasource-v2-template-run.spec.ts (partial sink report assertions)
    - data-generator-service/src/main/resources/template/v2-scenarios/scenario-f-streaming-csv.yaml (from 08-08)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-23
  </read_first>
  <action>
    Create `e2e/helpers/rw-streaming-upsert.ts`:
    - `runScenarioTwice(request, scenarioId)` for upsert idempotency
    - `assertSinkMetrics(detail, sinkName, expected: { rowsOk?, rowsUpserted?, rowsFailed? })`
    - `publishLargeFileInMemoryDraft(page, fixturePath)` for warn toast scenario
    **IN_MEMORY large-file fixture strategy (W-06):** Scenario 6 must not rely on a 10 MB file committed to git. Use one of:
    - **API inject:** POST draft template with CSV/JSON source path pointing at temp file generated by test setup helper (preferred — mirrors operator path)
    - **Generate in helper:** `publishLargeFileInMemoryDraft` writes ≥10 MB CSV to OS temp dir before save/publish API call
    - **Podman mount:** if E2E runs in container, document volume mount of `e2e/fixtures/generated/large-orders.csv` produced by pre-test script in `verify-phase8-uat-*.ps1`
    Pick API inject + temp-file generate as default; document chosen strategy in helper Javadoc and spec header.
    Register scenario scaffold IDs in console scenario catalog API if needed (mirror GF-E pattern): `gf-f-streaming-csv`, `gf-f-streaming-ndjson`, `gf-g-upsert-pg`, `gf-g-upsert-mysql`.
    Create `rw-streaming-upsert.spec.ts` with `test.setTimeout(120_000)` and scenarios D-23:
    1. Large CSV CHUNKED → Job SUCCESS, report.executionMode CHUNKED, rowsWritten > 0
    2. PostgreSQL upsert idempotent re-run — two runs, same row count, rowsUpserted on second
    3. Failed job — intentional bad sink fixture scenario shows actionable errorSample in Job detail API/UI
    4. JSON NDJSON STREAMING → SUCCESS
    5. MySQL upsert idempotent re-run
    6. IN_MEMORY + large file publish → warning toast visible (UI test with generated temp fixture via API inject — not repo-committed 10 MB file)
  </action>
  <acceptance_criteria>
    - Spec file contains ≥6 test cases mapped to D-23 list
    - Scenarios 2 and 5 assert row count stable across two runs via API
    - Scenario 3 asserts errorSample or transformErrors non-empty with human-readable text (not empty stack)
    - Scenario 6 asserts Ant Design message warning visible containing CHUNKED or STREAMING recommendation
    - Scenario 6 uses generated ≥10 MB temp fixture (API inject or helper write) — no committed large binary in repo (W-06)
    - `cd data-generator-console-web && npx playwright test e2e/specs/rw-streaming-upsert.spec.ts --reporter=line` passes against local/Podman service
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - data-generator-console-web/src/app/pages/JobDetailPage.tsx (sink columns from 08-06)
    - data-generator-console-web/e2e/specs/datasource-governance.spec.ts (mixed API + UI structure)
    - data-generator-console-web/package.json (e2e:phase7-governance script pattern)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-17, D-18
  </read_first>
  <action>
    Add UI smoke test in same spec: navigate to Job detail after CSV streaming run; assert sink table shows rowsOk column populated (Job center UI path for D-17).
    Confirm no live progress bar assertions — only terminal SUCCESS/FAILED states (D-18).
    Add npm script `"e2e:phase8-rw-streaming-upsert": "playwright test e2e/specs/rw-streaming-upsert.spec.ts"`.
    Document required env vars (base URL, RBAC headers if staging) in spec file header comment matching Phase 7 specs.
  </action>
  <acceptance_criteria>
    - Job detail UI test locates sink row by name and reads rowsOk text content
    - package.json contains e2e:phase8-rw-streaming-upsert script
    - Full spec green in Podman E2E harness when service packaged with Phase 8 scenarios
    - No test asserts mid-run progress events (D-18)
  </acceptance_criteria>
</task>

## Verification criteria

- All 6 D-23 scenarios implemented
- API + UI coverage for RW-04 report visibility
- Follows Phase 6/7 E2E helper patterns

## Artifacts this phase produces

- `data-generator-console-web/e2e/specs/rw-streaming-upsert.spec.ts`
- `data-generator-console-web/e2e/helpers/rw-streaming-upsert.ts`
- Updated `data-generator-console-web/e2e/helpers/template-run.ts` (if assertion types extended)
- npm script `e2e:phase8-rw-streaming-upsert`
- Scenario scaffold API registrations (if required)
