---
id: 08-03
phase: 08
title: Pipeline CSV/JSON Eligibility & Registry Policy Wiring
wave: 1
depends_on: [08-01, 08-02]
requirements: [RW-01]
autonomous: true
files_modified:
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/StreamingPipeline.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/ChunkedPipeline.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/TemplateV2RuntimeRegistry.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/EffectiveExecutionPolicy.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/StreamingPipelineTests.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineTests.java
must_haves:
  truths:
    - StreamingPipeline accepts single CsvSourceVO or JsonSourceVO when source implements ChunkedRowSource (D-01, D-02, D-04)
    - ChunkedPipeline accepts CSV/JSON chunked sources for ROW_LOCAL per-chunk SQL (D-04)
    - TemplateV2RuntimeRegistry passes EffectiveExecutionPolicy to CsvSourceFactory and JsonSourceFactory (D-01)
    - CSV/JSON default sourceChunkSize resolves to 1000 when unset in policy (D-03)
    - No auto-promotion from IN_MEMORY based on file size (D-01)
  artifacts:
    - Updated StreamingPipeline.soleChunkedSource() (or equivalent generalization)
    - Updated ChunkedPipeline source validation
    - Updated TemplateV2RuntimeRegistry.createSource()
    - Updated EffectiveExecutionPolicy chunk default for file sources
---

# Plan 08-03: Pipeline CSV/JSON Eligibility & Registry Policy Wiring

## Objective

Wire CSV/JSON chunked sources into existing `StreamingPipeline` and `ChunkedPipeline` dispatch without new execution modes (RW-01, D-01–D-04).

**Output:** Pipelines run CSV/JSON templates in CHUNKED/STREAMING; registry passes policy to file source factories.

## Context

@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@.planning/phases/08-rw-streaming-upsert/08-PATTERNS.md
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/StreamingPipeline.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/ChunkedPipeline.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/TemplateV2RuntimeRegistry.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/EffectiveExecutionPolicy.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/QuerySourceFactory.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/StreamingPipelineTests.java

## Tasks

<task>
  <read_first>
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/StreamingPipeline.java (soleQuerySource, validateV1Scope, chunk loop)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/ChunkedPipeline.java (chunked source validation)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/ChunkedRowSource.java
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-01, D-02, D-04
  </read_first>
  <action>
    Generalize `StreamingPipeline.soleQuerySource()` → `soleChunkedFileOrQuerySource()`:
    - Accept exactly one source entry
    - Source VO may be `QuerySourceVO`, `CsvSourceVO`, or `JsonSourceVO`
    - Resolved `RowSource` from registry must implement `ChunkedRowSource`; else throw actionable `IllegalArgumentException` naming required mode (D-01)
    Keep v1 scope rules: single source, no cross-chunk joins (D-04) — `ExecutionShapeClassifier` / `validateChunkedCompatibility` unchanged for multi-source rejection.
    Update `ChunkedPipeline` parallel validation for CSV/JSON `ChunkedRowSource`.
    Preserve existing JDBC streaming tests — no behavior regression for `QuerySourceVO` paths.
  </action>
  <acceptance_criteria>
    - `StreamingPipelineTests.streamsQuerySourceToJdbcSinkInBatches` still passes
    - New test `streamsCsvSourceToConsoleSinkInBatches` (or equivalent) passes with CHUNKED CSV fixture
    - `StreamingPipelineTests.rejectsMultipleSources` still passes for CSV/JSON multi-source templates
    - Error message for IN_MEMORY CSV large file in STREAMING mode mentions explicit mode requirement (not auto-upgrade)
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=StreamingPipelineTests,ChunkedPipelineTests -q` exits 0
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/TemplateV2RuntimeRegistry.java (createSource policy passthrough for QuerySourceFactory)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/EffectiveExecutionPolicy.java (DEFAULT_SOURCE_CHUNK_SIZE = 5000 today)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/CsvSourceFactory.java
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/JsonSourceFactory.java
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-03
  </read_first>
  <action>
    Update `TemplateV2RuntimeRegistry.createSource()` to pass `EffectiveExecutionPolicy` to `CsvSourceFactory` and `JsonSourceFactory` (mirror `QuerySourceFactory` path).
    Per D-03: in `EffectiveExecutionPolicy.resolve()` or dedicated helper, when source is CSV/JSON and `sourceChunkSize` unset, default to **1000** (document if global DEFAULT_SOURCE_CHUNK_SIZE stays 5000 for JDBC-only).
    Add integration test: template with `executionPolicy.mode: STREAMING`, CSV source, SQL transform `SELECT * FROM incoming` — assert `chunksProcessed > 1` and `peakRowsInMemory <= sourceChunkSize`.
  </action>
  <acceptance_criteria>
    - Registry passes policy to CSV/JSON factories (grep confirms `create(name, source, policy)` invocation)
    - STREAMING CSV integration test asserts chunk metrics consistent with D-03 default 1000
    - JDBC streaming scenario `scenario-e-streaming-jdbc` behavior unchanged when run via existing tests
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=StreamingPipelineTests -q` exits 0
  </acceptance_criteria>
</task>

## Verification criteria

- CSV/JSON eligible for CHUNKED and STREAMING pipelines
- Per-chunk SQL transform loop unchanged (D-04)
- Explicit mode required — no silent upgrade (D-01)

## Artifacts this phase produces

- Updated `org.gensokyo.data.calcite.runtime.StreamingPipeline`
- Updated `org.gensokyo.data.calcite.runtime.ChunkedPipeline`
- Updated `org.gensokyo.data.calcite.runtime.TemplateV2RuntimeRegistry`
- Updated `org.gensokyo.data.calcite.runtime.EffectiveExecutionPolicy` (CSV/JSON chunk default D-03)
- New/updated tests in `StreamingPipelineTests`, `ChunkedPipelineTests`
