---
id: 08-07
phase: 08
title: Publish Validation, Warnings & Console Form Hints
wave: 3
depends_on: [08-05]
requirements: [RW-03, RW-04]
autonomous: true
files_modified:
  - data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2Validator.java
  - data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2ControlPlaneService.java
  - data-generator-service/src/test/java/org/gensokyo/data/template/TemplateV2ValidatorTests.java
  - data-generator-console-web/src/app/editor/ReviewPanel.tsx
  - data-generator-console-web/src/app/editor/**/*
  - data-generator-console-web/src/i18n/locales/en.json
  - data-generator-console-web/src/i18n/locales/zh-CN.json
must_haves:
  truths:
    - upsert true requires non-empty valid upsertKeys at publish and run — fail-fast (D-14, D-19)
    - Opaque SQL/JS transforms skip publish-time upsertKey column cross-check with run-time fallback warning (W-04)
    - IN_MEMORY plus large CSV/JSON file emits warn-only at draft save/publish (D-05, D-20, D-22)
    - Console form hints for executionPolicy.mode IN_MEMORY vs CHUNKED vs STREAMING (D-19)
    - Zero behavior change for small-file IN_MEMORY templates (D-21)
  artifacts:
    - Updated org.gensokyo.data.template.TemplateV2Validator
    - TemplateV2ValidatorTests for upsert and large-file warnings
    - Console editor hints and ReviewPanel warn toast wiring
---

# Plan 08-07: Publish Validation, Warnings & Console Form Hints

## Objective

Add publish-time upsert validation, large-file IN_MEMORY warnings, and execution policy form hints in the console (D-05, D-14, D-19, D-20, D-21, D-22).

**Output:** Hard-block invalid upsert config; warn-only large-file guidance; editor UX hints.

## Context

@.planning/phases/08-rw-streaming-upsert/08-CONTEXT.md
@.planning/phases/08-rw-streaming-upsert/08-PATTERNS.md
@data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2Validator.java
@data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2ControlPlaneService.java
@data-generator-console-web/src/app/editor/ReviewPanel.tsx
@docs/template-v2-streaming-execution-guide.md

## Tasks

<task>
  <read_first>
    - data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2Validator.java (validateExecutionPolicy, collectWarnings, validateGovernance throw pattern)
    - data-generator-service/src/test/java/org/gensokyo/data/template/TemplateV2ValidatorTests.java
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilder.java (upsertKeys validation mirror)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-05, D-06, D-14, D-21, D-22
  </read_first>
  <action>
    Add `validateJdbcUpsertOptions(TemplateV2VO template)` called from `validate()`:
    - For each JDBC sink writer with `options.upsert: true`, require non-empty `options.upsertKeys` list (D-14)
    - Validate each key is non-blank string
    - **Publish-time column cross-check (opaque SQL fallback):** when transform is a simple `SqlTransformVO` with parseable `SELECT` list, best-effort static extraction of output column names and verify each upsertKey is present — throw at publish if inferrable and key missing
    - When transform is opaque (JS/SpEL/complex SQL subquery/join), **skip publish-time column cross-check** and emit validator warning: `upsertKeys will be validated at run against transform output schema` — run-time validation in `JdbcBulkWriteExecutor` (08-05) remains fail-fast (D-14)
    - Throw `IllegalArgumentException` same severity as governance blocks for empty/malformed upsertKeys — message includes sink writer path e.g. `sink.orders.writer[0]`
    Add `appendLargeFileInMemoryWarnings(template, warnings)` in `collectWarnings()`:
    - When mode is IN_MEMORY and CSV/JSON source path resolves to file ≥ **10 MB** OR declared `estimatedRows >= 100_000` if VO field exists (D-05, D-06)
    - Warning text recommends explicit CHUNKED or STREAMING — no hard block (D-22)
    Ensure small-file IN_MEMORY templates produce zero new warnings (D-21).
    Add TemplateV2ValidatorTests: upsertMissingKeysThrows, largeFileInMemoryWarns, smallFileNoWarning, opaqueTransformUpsertKeysRunOnlyWarning.
  </action>
  <acceptance_criteria>
    - `TemplateV2Validator.validate()` throws when upsert=true and upsertKeys=[]
    - Simple SQL transform with inferrable columns throws at publish when upsertKey not in SELECT list
    - JS/SpEL transform with upsert=true passes publish (keys non-empty) with warning about run-time validation; run fails if key invalid (08-05)
    - `collectWarnings()` returns string containing `CHUNKED` or `STREAMING` for 10MB+ IN_MEMORY CSV fixture path
    - `scenario-c-csv-export` validation produces no Phase-8 warnings
    - `.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=TemplateV2ValidatorTests -q` exits 0
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - data-generator-console-web/src/app/editor/ReviewPanel.tsx (message.warning on validation.warnings)
    - data-generator-console-web/src/app/editor/**/* (execution policy form fields — locate via grep executionPolicy)
    - data-generator-service/src/main/java/org/gensokyo/data/template/TemplateV2ControlPlaneService.java (validate/publish API merges warnings)
    - .planning/phases/08-rw-streaming-upsert/08-CONTEXT.md D-19, D-20
  </read_first>
  <action>
    Per D-19: add form hints (Ant Design `Tooltip` or `Form.Item` extra) near `executionPolicy.mode` explaining:
    - IN_MEMORY: small datasets
    - CHUNKED: large CSV/JSON/JDBC export, batch SQL per chunk
    - STREAMING: strict single-source, bounded peak memory
    Add hints near JDBC sink upsert toggle for upsertKeys requirement (links to docs anchor).
    Per D-20: ensure publish flow calls validate API and surfaces `validation.warnings` via `message.warning()` toast (same as save in ReviewPanel) — include large-file IN_MEMORY warning.
    Wire publish mutation path if not already showing warnings.
    Add i18n strings en.json + zh-CN.json for hints and warning templates.
  </action>
  <acceptance_criteria>
    - Template editor execution policy field shows hint text in EN locale (grep i18n key used in component)
    - Save/publish with large-file IN_MEMORY draft triggers Ant Design warning toast containing recommendation text
    - Upsert keys field shows required hint when upsert enabled
    - `cd data-generator-console-web && npm run verify:unit` exits 0
  </acceptance_criteria>
</task>

## Verification criteria

- Invalid upsert blocked at publish (D-14)
- Large file IN_MEMORY warn-only (D-05, D-22)
- Small templates unchanged (D-21)

## Artifacts this phase produces

- Updated `org.gensokyo.data.template.TemplateV2Validator`
- `TemplateV2ValidatorTests` (upsert + large-file cases)
- Console editor hints (execution policy, upsert keys)
- Updated `ReviewPanel.tsx` / publish warning surfacing
- i18n entries for Phase 8 hints and warnings
