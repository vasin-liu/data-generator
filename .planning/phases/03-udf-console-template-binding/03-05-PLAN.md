---
phase: 03-udf-console-template-binding
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-04]
files_modified:
  - samples/udf-samples/README.md
  - samples/udf-samples/format-phone.js
  - samples/udf-samples/format-phone.schema.json
  - samples/udf-samples/mask-email.sql
  - data-generator-service/src/test/java/org/gensokyo/data/udf/UdfConsoleTemplateBindingE2ETests.java
  - .planning/test-matrix.yaml
autonomous: true
requirements: [UDF-08]
must_haves:
  truths:
    - "One sample UDF per type ships in-repo: the java-plugin sample reuses samples/template-v2-pf4j-plugin, plus a script sample and an sql sample under samples/ (D-17/D-18)"
    - "An embedded integration test proves register → publish (via the Phase 3 service path) → reference in a Template V2 → run → assert output for the sample UDFs (D-19)"
    - "The three existing matrix rows udf-sql / udf-script / udf-java-plugin have their linked_tests extended to include the Phase 3 console/template-binding E2E (D-20)"
    - "scripts/verify-harness.ps1 runs the new E2E via the matrix linked_tests fast path and reports BUILD SUCCESS"
  artifacts:
    - path: "data-generator-service/src/test/java/org/gensokyo/data/udf/UdfConsoleTemplateBindingE2ETests.java"
      provides: "Embedded register→publish→reference→run proof for sample UDFs"
      contains: "TemplateV2"
    - path: "samples/udf-samples/format-phone.js"
      provides: "Script UDF sample body (GraalJS callable)"
      min_lines: 3
    - path: "samples/udf-samples/mask-email.sql"
      provides: "SQL UDF sample definition referenced by sqlName"
      min_lines: 1
  key_links:
    - from: "data-generator-service/src/test/java/org/gensokyo/data/udf/UdfConsoleTemplateBindingE2ETests.java"
      to: "UdfPublishService / UdfRegistryService"
      via: "register draft then publish through the service path, then run a Template V2 referencing the UDF"
      pattern: "(publish|registerDraft)"
    - from: ".planning/test-matrix.yaml"
      to: "UdfConsoleTemplateBindingE2ETests"
      via: "linked_tests entry on udf-sql / udf-script / udf-java-plugin rows"
      pattern: "UdfConsoleTemplateBindingE2ETests"
---

<objective>
Ship one sample UDF per type in-repo and prove the full Phase 3 loop end-to-end with an embedded integration test (register → publish via the new service path → reference in a Template V2 → run → assert output), then link that proof into the existing three UDF matrix rows so the harness fast path exercises it.

Purpose: UDF-08 requires in-repo sample UDFs (one per type) with harness tests proving template reference works end-to-end. Decisions D-17–D-20 fix sample placement, java-plugin reuse, embedded-integration proof, and matrix-row reuse.
Output: `samples/udf-samples/` assets (script + sql; java reuses the PF4J sample), `UdfConsoleTemplateBindingE2ETests`, and extended `linked_tests` on `udf-sql`/`udf-script`/`udf-java-plugin`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-udf-console-template-binding/03-CONTEXT.md
@.planning/phases/03-udf-console-template-binding/03-PATTERNS.md
@.planning/phases/02-udf-platform-core/02-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/phases/03-udf-console-template-binding/03-02-SUMMARY.md
@.planning/phases/03-udf-console-template-binding/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add script and sql sample UDF assets (java reuses the PF4J sample)</name>
  <files>samples/udf-samples/README.md, samples/udf-samples/format-phone.js, samples/udf-samples/format-phone.schema.json, samples/udf-samples/mask-email.sql</files>
  <read_first>
    - samples/template-v2-pf4j-plugin/README.md (PF4J packaging reused as the java-plugin UDF sample, D-18 dual-path; do not break it)
    - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/udf/GraalJsScriptUdfExecutor.java (script UDF calling convention: shared engine, list-arg binding — from 02-02-SUMMARY; defines what a valid script body looks like)
    - data-generator-service/src/main/java/org/gensokyo/data/udf/UdfGovernanceSupport.java (publish-gate checks the samples must pass: no plaintext secrets, no dangerous script patterns, script JSON-Schema presence — from 02-03-SUMMARY)
    - .planning/phases/02-udf-platform-core/02-CONTEXT.md (D-18 sqlName metadata for SQL UDFs; D-12 script JSON Schema requirement)
  </read_first>
  <action>
    Create `samples/udf-samples/` holding one script sample and one sql sample (D-17); function semantics are planner discretion (D-18). Script sample `format-phone.js`: a small GraalJS callable that takes a phone-number argument and returns a normalized string, matching the `GraalJsScriptUdfExecutor` list-arg binding convention and containing NO dangerous patterns (`Runtime`, `ProcessBuilder`, reflection) so it passes `UdfGovernanceSupport` (Phase 2 D-22/D-23). Provide its parameter/return JSON Schema in `format-phone.schema.json` (required for script publish, D-12). SQL sample `mask-email.sql`: a SQL UDF body whose `sqlName` (e.g. `V2_MASK_EMAIL`) is the token templates reference (Phase 2 D-18); document the `sqlName` metadata value in the README. Add `samples/udf-samples/README.md` explaining: the three sample types, that the java-plugin sample is `samples/template-v2-pf4j-plugin/` (reused, D-18), the `udfId` (reverse-DNS) + `version` (semver) + `sqlName` to use when uploading, and that no real secrets appear in any sample. Keep all samples credential-free.
  </action>
  <acceptance_criteria>
    - `samples/udf-samples/format-phone.js` exists, defines a callable that returns a value from its input arg, and contains none of the tokens `Runtime`, `ProcessBuilder`, `java.lang.reflect`.
    - `samples/udf-samples/format-phone.schema.json` is valid JSON describing parameter and return types.
    - `samples/udf-samples/mask-email.sql` exists and the README documents its `sqlName` (e.g. `V2_MASK_EMAIL`), `udfId`, and `version`.
    - `samples/udf-samples/README.md` names `samples/template-v2-pf4j-plugin/` as the java-plugin sample (D-18) and contains no plaintext credentials.
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('samples/udf-samples/format-phone.schema.json','utf8')); const js=require('fs').readFileSync('samples/udf-samples/format-phone.js','utf8'); if(/Runtime|ProcessBuilder|java\.lang\.reflect/.test(js)) process.exit(1); console.log('samples ok')"</automated>
  </verify>
  <done>One sample UDF per type is present in-repo (java via the reused PF4J sample, plus script + sql under samples/udf-samples/), credential-free and governance-clean (D-17/D-18).</done>
</task>

<task type="auto">
  <name>Task 2: Embedded register → publish → reference → run E2E test</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/udf/UdfConsoleTemplateBindingE2ETests.java</files>
  <read_first>
    - data-generator-service/src/main/java/org/gensokyo/data/udf/UdfRegistryService.java, UdfPublishService.java (register draft + publish-gate path used to publish the sample UDFs)
    - data-generator-service/src/main/java/org/gensokyo/data/template/UdfReferenceValidator.java (publish-time binding from 03-04 — the template referencing the published UDF must pass)
    - data-generator-test-fixtures/ (FixtureTemplates.load / H2Seed.apply embedded helpers from Phase 1 / 01-02-SUMMARY — used to build the Template V2 + H2 data)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineTests.java (in-process TemplateV2Runner against H2 pattern — TESTING.md §5.5)
    - data-generator-service/src/test/resources/application-phase7-test.yaml (embedded service profile — TESTING.md §4)
    - samples/udf-samples/* (the sample payloads to register in the test)
  </read_first>
  <action>
    Create `UdfConsoleTemplateBindingE2ETests` in `data-generator-service/src/test/java/org/gensokyo/data/udf/` proving the D-19 loop with embedded infrastructure (H2; no external credentials, TESTING.md §1). For at least the SQL and script sample types: (1) register a draft via `UdfRegistryService.registerDraft(...)` loading the payload from the `samples/udf-samples/` asset, (2) publish it through `UdfPublishService.publish(...)` so the Phase 2 governance gate + Phase 3 persistence path run, (3) build a Template V2 (via `FixtureTemplates.load` or an inline `TemplateV2VO`) whose transform references the published UDF (SQL transform using the sample `sqlName`; script transform using `udfRef:{id,version}`), (4) run it through `TemplateV2Runner` against embedded H2, and (5) assert the output rows reflect the UDF transformation (e.g. masked email / normalized phone). Use `*Tests`/`*IT` naming per TESTING.md §3.2 — name the class `UdfConsoleTemplateBindingE2ETests`. The test also implicitly confirms the publish-time `UdfReferenceValidator` (03-04) accepts a template that references a genuinely published UDF.
  </action>
  <acceptance_criteria>
    - `UdfConsoleTemplateBindingE2ETests` registers and publishes the SQL and script sample UDFs through `UdfRegistryService`/`UdfPublishService` (not by direct registry mutation).
    - The test runs a Template V2 referencing the published UDF(s) through `TemplateV2Runner` against embedded H2 and asserts the transformed output (a concrete value assertion, not just non-empty).
    - The test uses no external services/credentials (embedded H2 only).
    - Test command passes.
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test "-Dtest=UdfConsoleTemplateBindingE2ETests" "-Dsurefire.failIfNoSpecifiedTests=false"</automated>
  </verify>
  <done>An embedded test proves the full register→publish→reference→run loop end-to-end for the sample UDFs (UDF-08, D-19).</done>
</task>

<task type="auto">
  <name>Task 3: Extend the three UDF matrix rows' linked_tests and run the harness</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - .planning/test-matrix.yaml (existing udf-sql / udf-script / udf-java-plugin rows with linked_tests at lines 386-413; udf-sql + udf-java-plugin owner_module = data-generator-service, udf-script owner_module = data-generator-calcite)
    - scripts/verify-harness.ps1 (the fast path auto-derives the Maven -Dtest list from each row's linked_tests, grouped by owner_module + -am; no script edit needed — adding the class to linked_tests is sufficient, D-20)
    - data-generator-service/src/test/java/org/gensokyo/data/udf/UdfConsoleTemplateBindingE2ETests.java (created in Task 2 — the class name to link)
  </read_first>
  <action>
    Edit the three existing UDF rows in `.planning/test-matrix.yaml` (do NOT add new rows, D-20): append `UdfConsoleTemplateBindingE2ETests` to the `linked_tests` of `udf-sql`, `udf-script`, and `udf-java-plugin`, keeping their existing linked tests (`UdfPublishServiceTests`, `GraalJsScriptUdfExecutorTests`, `UdfGovernanceSupportTests`). Because the harness combines all owner modules with `-am` and runs Surefire with `failIfNoSpecifiedTests=false`, the service-located E2E class resolves even for the calcite-owned `udf-script` row. Keep each row's `status: covered`. Then run the harness fast path to confirm the new E2E executes and the slice is green.
  </action>
  <acceptance_criteria>
    - `udf-sql`, `udf-script`, and `udf-java-plugin` rows each list `UdfConsoleTemplateBindingE2ETests` in `linked_tests` alongside their prior entries; no new matrix rows were added.
    - All three rows retain `status: covered`.
    - `scripts/verify-harness.ps1` prints `[SUCCESS] Harness verification passed.` and the run includes `UdfConsoleTemplateBindingE2ETests` in the linked Maven test list.
  </acceptance_criteria>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/verify-harness.ps1</automated>
  </verify>
  <done>The Phase 3 console/template-binding E2E is wired into the existing three UDF matrix rows and passes through the harness fast path (UDF-08, D-19/D-20).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo sample assets → registry (in tests) | Sample UDF payloads (script body, sql, JAR) are registered and published through the service path during the embedded E2E |
| Sample artifacts committed to the repo | Code-bearing sample files live in source control and could carry secrets or dangerous code if authored carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information disclosure | committed sample assets | mitigate | Samples are credential-free; README states no real secrets; the publish gate (`UdfGovernanceSupport`) would reject plaintext-secret payloads at publish in the E2E (Phase 2 D-22) |
| T-03-05-02 | Tampering / code execution | script sample body | mitigate | Script sample contains no `Runtime`/`ProcessBuilder`/reflection (Task 1 acceptance + Phase 2 dangerous-pattern scan D-23); E2E runs it through the sandboxed `GraalJsScriptUdfExecutor` (host access denied) |
| T-03-05-03 | Tampering | PF4J java-plugin sample dual-path | mitigate | The java sample reuses `samples/template-v2-pf4j-plugin/` unchanged, preserving the D-28 dual-path guarantee; no parallel classloader is introduced (D-18) |
| T-03-05-SC | Tampering | maven/npm dependency installs | accept | No new package-manager dependencies are added; the JSON-validity verify uses the already-present Node runtime; slopcheck N/A |
</threat_model>

<verification>
- `.\mvnw-jdk25.ps1 -pl data-generator-service -am test "-Dtest=UdfConsoleTemplateBindingE2ETests" "-Dsurefire.failIfNoSpecifiedTests=false"` — E2E green.
- `powershell -ExecutionPolicy Bypass -File scripts/verify-harness.ps1` — BUILD SUCCESS, harness verification passed, linked Maven test list includes `UdfConsoleTemplateBindingE2ETests`.
- `target/test-matrix-summary.json` shows `udf-sql`/`udf-script`/`udf-java-plugin` with passing linked results.
</verification>

<success_criteria>
- One sample UDF per type ships in-repo; java reuses the PF4J sample (D-17/D-18).
- Embedded E2E proves register→publish→reference→run for the samples (D-19).
- The three existing UDF matrix rows' linked_tests include the E2E and pass via the harness (D-20).
</success_criteria>

<output>
Create `.planning/phases/03-udf-console-template-binding/03-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New symbols / files introduced by this plan:

- `samples/udf-samples/README.md`, `samples/udf-samples/format-phone.js`, `samples/udf-samples/format-phone.schema.json`, `samples/udf-samples/mask-email.sql`
- `org.gensokyo.data.udf.UdfConsoleTemplateBindingE2ETests` (embedded integration test in `data-generator-service`)
- `.planning/test-matrix.yaml`: `linked_tests` extended on rows `udf-sql`, `udf-script`, `udf-java-plugin` with `UdfConsoleTemplateBindingE2ETests` (no new rows)
- Sample identifiers: script `udfId` (reverse-DNS) + `format-phone` body; sql `sqlName` `V2_MASK_EMAIL`; java-plugin = reused `samples/template-v2-pf4j-plugin/`
