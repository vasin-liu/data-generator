---
phase: 04-transform-operators-sql
plan: 05
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - docs/transform-operators.md
  - .planning/test-matrix.yaml
  - data-generator-test-fixtures/src/main/resources/fixtures/templates/transform-json.yaml
  - data-generator-test-fixtures/src/main/resources/fixtures/templates/transform-mask.yaml
  - data-generator-test-fixtures/src/main/resources/fixtures/templates/transform-lookup.yaml
  - data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformJsonExampleTests.java
  - data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformMaskExampleTests.java
  - data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformLookupExampleTests.java
autonomous: true
requirements: [XFORM-04, XFORM-06]
must_haves:
  truths:
    - "docs/transform-operators.md documents the new operator type/fields and notes the new fields are additive with no breaking template-version change (D-13, XFORM-04)."
    - "docs/transform-operators.md documents the internal V2_JSON_EXTRACT scalar function as internal-only (not cataloged) (D-12)."
    - ".planning/test-matrix.yaml has one row per new operator (transform-json, transform-mask, transform-lookup) with linked_tests populated (XFORM-06)."
    - "Each operator has an embedded fixture sample template and a passing Fixture*ExampleTests run via TemplateV2Runner (XFORM-06)."
    - "The harness entry script picks up the new rows as covered (no production credentials)."
  artifacts:
    - path: docs/transform-operators.md
      provides: "operator catalog + schema/version notes (additive D-13) + internal scalar function note (D-12)"
    - path: .planning/test-matrix.yaml
      provides: "transform-json / transform-mask / transform-lookup rows with linked_tests"
    - path: data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformJsonExampleTests.java
      provides: "embedded end-to-end json operator example via TemplateV2Runner"
  key_links:
    - from: .planning/test-matrix.yaml transform-json/mask/lookup rows
      to: FixtureTransform{Json,Mask,Lookup}ExampleTests
      via: "linked_tests: [FixtureTransform...ExampleTests]"
      pattern: "mirror transform-sql-basic row → FixtureTransformSqlExampleTests"
    - from: FixtureTransform*ExampleTests
      to: TemplateV2Runner + TemplateV2RuntimeRegistry(json/mask/lookup factories)
      via: "build template with named sources + operator transform + console sink; run; assert rows"
      pattern: "mirror FixtureTransformSqlExampleTests transformTemplate()/transformRegistry()"
---

<objective>
**Purpose:** Document the new operators and prove them end-to-end in the harness (XFORM-04, XFORM-06). Per D-13 the new operator `type`s and fields are additive to the Template V2 schema — document them in schema/version notes rather than bumping a breaking version. Per XFORM-06 the harness matrix gains a row per new operator with passing embedded tests.

**Output:** A new `docs/transform-operators.md` (operator reference + additive schema/version notes covering json/mask/lookup fields and the internal `V2_JSON_EXTRACT` note); three new matrix rows in `.planning/test-matrix.yaml` (`transform-json`, `transform-mask`, `transform-lookup`) with `linked_tests` populated; three embedded sample templates under the test-fixtures resources; and three `Fixture*ExampleTests` that run each operator through `TemplateV2Runner` and assert output. All embedded-first (H2/in-memory, no production credentials) per the existing fixtures policy.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-transform-operators-sql/04-CONTEXT.md
@.planning/phases/04-transform-operators-sql/04-PATTERNS.md
@.planning/test-matrix.yaml
@data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformSqlExampleTests.java
@docs/testing-embedded-components.md
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Embedded sample templates + Fixture*ExampleTests for each operator (XFORM-06)</name>
    <files>data-generator-test-fixtures/src/main/resources/fixtures/templates/transform-json.yaml
data-generator-test-fixtures/src/main/resources/fixtures/templates/transform-mask.yaml
data-generator-test-fixtures/src/main/resources/fixtures/templates/transform-lookup.yaml
data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformJsonExampleTests.java
data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformMaskExampleTests.java
data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformLookupExampleTests.java</files>
    <read_first>
      - data-generator-test-fixtures/src/test/java/org/gensokyo/data/testfixtures/FixtureTransformSqlExampleTests.java (transformTemplate() + transformRegistry() + H2 seed + assertions)
      - data-generator-test-fixtures/src/main/resources/fixtures/templates/transform-sql-basic.yaml (sample YAML shape)
      - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/transform/JsonTransformFactory.java (+ Mask/Lookup) (from 04-02 — registry wiring)
      - .planning/phases/04-transform-operators-sql/04-PATTERNS.md (Harness rows + sample templates + tests section)
    </read_first>
    <action>
      For each operator create a sample template YAML under `data-generator-test-fixtures/src/main/resources/fixtures/templates/` mirroring `transform-sql-basic.yaml`: `transform-json.yaml` (a source whose rows carry a JSON string column, a `type: json` transform with `flatten: true` and a `separator`, a console sink — exercises D-02), `transform-mask.yaml` (rows with email/phone/card columns, a `type: mask` transform with rules for each named strategy, console sink — D-03), `transform-lookup.yaml` (two named sources — a primary `input` and a reference source — and a `type: lookup` transform joining by key projecting columns, console sink — D-04). Use ONLY embedded/inline or H2-seeded sources (no production credentials).

      Create three JUnit 5 tests mirroring `FixtureTransformSqlExampleTests` (copyright + Javadoc): build a `TemplateV2VO` (programmatically or by loading the YAML via the fixtures loader), construct a `TemplateV2RuntimeRegistry` with the needed source factories + the operator factory under test + `ConsoleSinkFactory` (mirror `transformRegistry(...)`), run via `new TemplateV2Runner(registry)`, and assert the output rows: `FixtureTransformJsonExampleTests` asserts flattened `addr.city`-style columns exist; `FixtureTransformMaskExampleTests` asserts each column is masked per its strategy AND that the raw value is gone; `FixtureTransformLookupExampleTests` asserts each row gained the projected lookup columns by key. Each test must reference its matrix row id in a comment/Javadoc to keep matrix linkage explicit (mirror the SQL example).
    </action>
    <acceptance_criteria>
      - Three sample YAML templates exist and use embedded sources only.
      - Three `Fixture*ExampleTests` run each operator through `TemplateV2Runner` and assert operator-specific output (flatten / mask / join).
      - Mask test asserts the original value is not present in output (PII-safe).
    </acceptance_criteria>
    <verify><automated>.\mvnw-jdk25.ps1 -pl data-generator-test-fixtures -am test -Dtest=FixtureTransformJsonExampleTests+FixtureTransformMaskExampleTests+FixtureTransformLookupExampleTests</automated></verify>
    <done>All three operators pass embedded end-to-end fixture tests (XFORM-06).</done>
  </task>

  <task type="auto">
    <name>Task 2: Add matrix rows linking the new fixture tests (XFORM-06)</name>
    <files>.planning/test-matrix.yaml</files>
    <read_first>
      - .planning/test-matrix.yaml (rows 195-211 — transform-sql-basic + calcite-transform-js shape)
      - .planning/phases/04-transform-operators-sql/04-PATTERNS.md (matrix rows excerpt)
    </read_first>
    <action>
      Add three rows to `.planning/test-matrix.yaml` mirroring the `transform-sql-basic` row shape (id/capability/adapter/test_types/owner_module/status/linked_tests/notes). Rows: `transform-json` (capability `transform-json`, adapter `calcite-json`, test_types `[integration]`, owner_module `data-generator-test-fixtures`, status `partial` or `covered`, linked_tests `[FixtureTransformJsonExampleTests]`); `transform-mask` (capability `transform-mask`, adapter `calcite-mask`, linked_tests `[FixtureTransformMaskExampleTests]`); `transform-lookup` (capability `transform-lookup`, adapter `calcite-lookup`, linked_tests `[FixtureTransformLookupExampleTests]`). Notes should name the operator and reference D-02/D-03/D-04 respectively. Keep YAML valid and do not modify unrelated rows.
    </action>
    <acceptance_criteria>
      - Three new rows present with the exact ids transform-json/transform-mask/transform-lookup and populated linked_tests.
      - YAML parses (no indentation breakage); existing rows untouched.
    </acceptance_criteria>
    <verify><automated>.\mvnw-jdk25.ps1 -pl data-generator-test-fixtures -am test -Dtest=FixtureTransformJsonExampleTests+FixtureTransformMaskExampleTests+FixtureTransformLookupExampleTests</automated></verify>
    <done>Matrix has a row per new operator with linked tests (XFORM-06); harness can report them covered.</done>
  </task>

  <task type="auto">
    <name>Task 3: Operator reference + additive schema/version notes (XFORM-04, D-12, D-13)</name>
    <files>docs/transform-operators.md</files>
    <read_first>
      - data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/model/v2/JsonTransformVO.java + MaskTransformVO.java + MaskRuleVO.java + LookupTransformVO.java (from 04-01 — exact field names)
      - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sql/TemplateV2JsonSqlFunctions.java (from 04-02 — V2_JSON_EXTRACT)
      - docs/testing-embedded-components.md (docs tone/format)
    </read_first>
    <action>
      Create `docs/transform-operators.md` documenting the built-in transform operator catalog and schema/version notes (XFORM-04). For each of json/mask/lookup: the `type` discriminator string, every config field with type + required/optional + meaning (matching the 04-01 VOs), and a short YAML usage example (D-02/D-03/D-04). Add a "Schema & version notes" section stating EXPLICITLY that the new operator `type`s and fields are ADDITIVE to the Template V2 schema — existing templates parse and run unchanged and NO breaking template-version bump is introduced (D-13). Add a short "Internal SQL functions" note documenting `V2_JSON_EXTRACT(json, path)` as INTERNAL-ONLY — usable inside `sql` transforms but intentionally NOT listed in the `/api/console/transforms` catalog and reserved under the `V2_` prefix away from the UDF `sqlName` namespace (D-12). Cross-link the catalog endpoint `/api/console/transforms` (from 04-03) for discovery. Keep it concise and factual; no secrets in examples.
    </action>
    <acceptance_criteria>
      - docs/transform-operators.md covers json/mask/lookup fields + examples.
      - A schema/version notes section states the change is additive with no breaking version bump (D-13).
      - An internal-functions note documents V2_JSON_EXTRACT as internal-only/uncataloged (D-12).
    </acceptance_criteria>
    <verify><automated>powershell -NoProfile -Command "if (Select-String -Path docs/transform-operators.md -Pattern 'additive','V2_JSON_EXTRACT','lookup' -Quiet) { exit 0 } else { exit 1 }"</automated></verify>
    <done>Operators and additive schema notes documented (XFORM-04); internal scalar function noted (D-12).</done>
  </task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| TB-04-05-A | Sample template fixtures committed to the repo (must not contain real secrets/credentials) |
| TB-04-05-B | Documentation examples (must not encode plaintext secrets) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-05-01 | Information Disclosure | Sample YAML / docs containing credentials | mitigate | All fixtures use embedded/inline or H2-seeded sources only (no production datasource ids, no passwords); docs examples use placeholder data — consistent with docs/testing-embedded-components.md policy. |
| T-04-05-02 | Information Disclosure | Mask fixture leaking unmasked PII in assertions | mitigate | Mask fixture uses synthetic data and the test asserts the raw value is ABSENT from output, doubling as a PII-leak regression check. |
| T-04-05-03 | Tampering | Matrix YAML corruption breaking the harness | mitigate | Only additive rows mirroring an existing valid row; verify command runs the linked tests which the harness reads, catching structural breakage. |
</threat_model>

<verification>
- `.\mvnw-jdk25.ps1 -pl data-generator-test-fixtures -am test -Dtest=FixtureTransformJsonExampleTests+FixtureTransformMaskExampleTests+FixtureTransformLookupExampleTests` is green.
- `.planning/test-matrix.yaml` contains rows transform-json/transform-mask/transform-lookup with linked_tests.
- `docs/transform-operators.md` exists with additive schema notes (D-13) + V2_JSON_EXTRACT internal note (D-12).
- Optional full harness: `.\scripts\verify-harness.ps1` reports the new rows as covered.
</verification>

<success_criteria>
- Each new operator has a matrix row + passing embedded test (XFORM-06).
- New operator fields documented as additive, no breaking template-version change (XFORM-04, D-13).
</success_criteria>

<output>Create .planning/phases/04-transform-operators-sql/04-05-SUMMARY.md when done</output>
