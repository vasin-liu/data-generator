---
phase: 10-harness-coverage-ci-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - .planning/test-matrix.yaml
autonomous: true
requirements:
  - TEST-08
must_haves:
  truths:
    - verify-harness.ps1 exits 0 with p0.pass true after matrix expansion (D-04)
    - target/test-matrix-summary.json lists all 8 new row ids with status covered and green true (TEST-08)
    - p0.total equals 15 and p0.green equals 15 in summary JSON (D-04 strict gate)
    - harness-verify.yml unchanged — gate expands via matrix only (D-14)
    - CI merge gate fails when any new P0 row regresses to non-covered (D-04)
  artifacts:
    - target/test-matrix-summary.json (generated, validated)
  key_links:
    - verify-harness.ps1 Maven union includes all new linked_tests from data-generator-calcite
    - test-matrix-summary.ps1 p0.pass rollup matches script exit code
    - harness-verify.yml invokes verify-harness.ps1 without modification
---

<objective>
Prove the expanded P0 matrix gate works end-to-end: run `verify-harness.ps1`, validate `target/test-matrix-summary.json` coverage for new RW/dialect rows, and fix matrix links only if evidence classes fail (TEST-08, D-04, D-14).

Purpose: Merge-blocking CI must fail on any P0 regression for streaming, upsert, or dialect paths without rewriting harness-verify.yml.
Output: Green harness run with 15/15 P0 rows covered; documented verification commands for Nyquist automation.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md
@.planning/phases/10-harness-coverage-ci-gates/10-PATTERNS.md
@scripts/verify-harness.ps1
@scripts/lib/test-matrix-summary.ps1
@.github/workflows/harness-verify.yml
@.planning/phases/10-harness-coverage-ci-gates/10-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- Validated `target/test-matrix-summary.json` with p0.pass true and 15 P0 rows
- Optional surgical fix to `.planning/test-matrix.yaml` linked_tests if a class name mismatch is found
- Confirmation that `.github/workflows/harness-verify.yml` requires no edit (D-14)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run full harness and confirm strict P0 gate passes</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>
    - scripts/verify-harness.ps1 (Maven slice + p0.pass exit gate)
    - scripts/lib/test-matrix-summary.ps1 (status computation, p0 rollup)
    - .planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md (D-04, D-14)
    - .github/workflows/harness-verify.yml (reference only — do not modify)
  </read_first>
  <action>
    From repo root on Windows with JDK 25 available (use .\mvnw-jdk25.ps1 environment or set JAVA_HOME per AGENTS.md), run:
    .\scripts\verify-harness.ps1
    Expect: exit code 0, console line "P0 regression gate passed (15/15 green)", target/test-matrix-summary.json written.
    Per D-04 strict gate: any P0 row not computed covered must fail the script (exit 1). Per D-14: do not edit harness-verify.yml — the workflow already invokes verify-harness.ps1 and uploads summary artifact.
    If Maven fails on a linked class: verify simple class name spelling against data-generator-calcite/src/test tree; adjust linked_tests in test-matrix.yaml only (no new test classes unless a required row cannot be proven with existing Phase 8/9 classes per D-12). Re-run until green.
    If Testcontainers ITs skip due to missing Docker locally, note in SUMMARY — CI ubuntu-latest has Docker and must run ITs green.
  </action>
  <verify>
    <automated>.\scripts\verify-harness.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <acceptance_criteria>
    - verify-harness.ps1 exits 0 (D-04)
    - Console reports P0 regression gate passed with 15 total P0 rows
    - target/test-matrix-summary.json exists
    - harness-verify.yml file unchanged from pre-plan git state (D-14)
  </acceptance_criteria>
  <done>Full matrix-linked Maven slice passes and strict P0 gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: Validate summary JSON covers all new Phase 10 rows</name>
  <files>target/test-matrix-summary.json</files>
  <read_first>
    - target/test-matrix-summary.json (after Task 1)
    - docs/test-harness.md (p0 block field definitions)
    - .planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md (TEST-08 success criteria)
  </read_first>
  <action>
    Parse target/test-matrix-summary.json and assert:
    - p0.total is 15, p0.green is 15, p0.pass is true (D-04)
    - p0.rows contains entries for all eight new ids: v2-streaming-csv, v2-streaming-json, v2-jdbc-upsert-pg-mysql, v2-dialect-dameng, v2-dialect-kingbase, v2-dialect-highgo, v2-dialect-postgres, v2-dialect-clickhouse
    - Each new id has status covered and green true (TEST-08 covered vs pending reporting)
    - rows[] entries include linkedResults with outcome passed for Maven-linked classes (no failed outcomes)
    Document in SUMMARY: command used, p0 block snapshot, and any linked_tests correction applied.
    Do not merge phase8/9 UAT scripts into harness flow (D-16).
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "$s = Get-Content target/test-matrix-summary.json -Raw | ConvertFrom-Json; if (-not $s.p0.pass -or $s.p0.total -ne 15) { exit 1 }; $ids = @('v2-streaming-csv','v2-streaming-json','v2-jdbc-upsert-pg-mysql','v2-dialect-dameng','v2-dialect-kingbase','v2-dialect-highgo','v2-dialect-postgres','v2-dialect-clickhouse'); foreach ($id in $ids) { $r = $s.p0.rows | Where-Object { $_.id -eq $id }; if (-not $r -or -not $r.green) { exit 1 } }"</automated>
  </verify>
  <acceptance_criteria>
    - Summary JSON reports covered vs pending correctly for all 8 new rows (TEST-08)
    - p0.pass true only when every P0 row is green (D-04)
    - All eight new row ids present in p0.rows with green true
  </acceptance_criteria>
  <done>Machine-readable summary proves Phase 10 rows are covered and merge gate would pass in CI.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm CI gate wiring without workflow edits</name>
  <files>.github/workflows/harness-verify.yml</files>
  <read_first>
    - .github/workflows/harness-verify.yml
    - .planning/phases/10-harness-coverage-ci-gates/10-CONTEXT.md (D-14, D-16)
    - AGENTS.md Merge criteria section (pre plan 03 state)
  </read_first>
  <action>
    Read-only verification per D-14: confirm harness-verify.yml job harness-verify runs pwsh ./scripts/verify-harness.ps1 and uploads target/test-matrix-summary.json artifact. No YAML edits.
    Confirm gate semantics: PR blocked when verify-harness.ps1 exit 1 (maven failure, linkedFailed, or p0Pass false) — same as today but with expanded P0 row set from plan 01.
    Optionally simulate regression detection: temporarily set one new row status to pending in a local branch experiment (do not commit) and confirm p0.pass becomes false — or document that test-matrix-summary.ps1 logic already enforces this (D-04).
    Record in SUMMARY that CI gate auto-expands when tier P0 rows increase; no dual-run of verify-phase8/9 UAT scripts in harness-verify (D-16).
  </action>
  <verify>
    <automated>rg -n "verify-harness.ps1" .github/workflows/harness-verify.yml</automated>
  </verify>
  <acceptance_criteria>
    - harness-verify.yml still invokes verify-harness.ps1 unchanged (D-14)
    - SUMMARY documents that 15 P0 rows now gate merge via p0.pass
    - No phase UAT script added to harness-verify workflow (D-16)
  </acceptance_criteria>
  <done>CI merge gate path confirmed; workflow YAML untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local verify → CI harness-verify job | Same script must produce same p0.pass semantics |
| Summary JSON → merge gate decision | Tampered summary could misreport green status |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | p0.pass bypass | high | mitigate | Script exit 1 when p0.pass false; CI runs verify-harness.ps1 directly — no separate summary-only gate |
| T-10-05 | Denial of service | Expanded Maven -Dtest union | medium | accept | 15 P0 rows share classes across rows; verify-harness deduplicates by module — acceptable CI cost |
| T-10-06 | Spoofing | Wrong test class linked to capability | high | mitigate | Task 2 validates per-row linkedResults passed; fix matrix not skip failing links |
</threat_model>

<verification>
- .\scripts\verify-harness.ps1 exits 0
- JSON assertion script passes for all eight new row ids
- harness-verify.yml contains verify-harness.ps1 invocation
</verification>

<success_criteria>
- TEST-08: verify-harness.ps1 reports coverage status for new RW/dialect rows; P0 subset gates merge
- D-04 strict covered-only gate proven green locally
- D-14 confirmed — no workflow YAML change required
</success_criteria>

<output>
Create `.planning/phases/10-harness-coverage-ci-gates/10-02-SUMMARY.md` when done
</output>
