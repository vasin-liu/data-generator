---
phase: 20-pipeline-proof-docs-p1
plan: 03
type: execute
wave: 2
depends_on:
  - "20-01"
files_modified:
  - .planning/test-matrix.yaml
  - docs/test-feature-matrix.md
autonomous: true
requirements:
  - TEST-10
  - GEO-02
must_haves:
  truths:
    - "test-matrix row geo-synthetic is tier P1, status covered, adapter geo_synthetic, owner data-generator-calcite per D-09"
    - "linked_tests includes TemplateV2RunnerGeoSyntheticSourceTests plus Phase 19 GeoSyntheticRowSourceTests and GeoSyntheticRequestMapperTests per D-09"
    - "P0 row count remains 15; geo-synthetic is not promoted to P0 per D-10"
    - "Row notes describe V2 geo_synthetic source per D-11"
    - "docs/test-feature-matrix.md mirrors YAML after regeneration per D-08"
  artifacts:
    - path: .planning/test-matrix.yaml
      provides: Harness source of truth for geo-synthetic P1
      contains: "geo-synthetic"
    - path: docs/test-feature-matrix.md
      provides: Generated human-readable matrix mirror
      contains: "geo-synthetic"
  key_links:
    - from: .planning/test-matrix.yaml
      to: data-generator-calcite/src/test/java/org/gensokyo/data/calcite/TemplateV2RunnerGeoSyntheticSourceTests.java
      via: linked_tests entry
      pattern: "TemplateV2RunnerGeoSyntheticSourceTests"
    - from: docs/test-feature-matrix.md
      to: .planning/test-matrix.yaml
      via: generate-test-matrix-doc.ps1
      pattern: "GENERATED"
---

<objective>
Promote harness row geo-synthetic to P1 with real test linkage and sync generated matrix doc — TEST-10 — while freezing P0 at 15 rows (D-08, D-09, D-10, D-11).

Purpose: Feature matrix must reflect v2.2 geo pipeline proof without widening the merge gate.
Output: Updated `.planning/test-matrix.yaml`, regenerated `docs/test-feature-matrix.md`, verified P0 count unchanged.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-pipeline-proof-docs-p1/20-CONTEXT.md
@.planning/test-matrix.yaml
@docs/test-feature-matrix.md
@docs/test-harness.md
@.planning/phases/20-pipeline-proof-docs-p1/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update geo-synthetic row in test-matrix.yaml</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>.planning/test-matrix.yaml, docs/test-harness.md</read_first>
  <action>
    Per D-09 locate id geo-synthetic and update: tier P1 (from P2), status covered (once 20-01 IT exists), test_types [unit, integration], owner_module data-generator-calcite, adapter geo_synthetic (replace stale geojson label). Set linked_tests to at least TemplateV2RunnerGeoSyntheticSourceTests, GeoSyntheticRowSourceTests, GeoSyntheticRequestMapperTests (class simple names matching other rows). Per D-11 replace notes GeoJSON synthetic geometry with text describing Template V2 geo_synthetic source (four modes, path assets, calcite pipeline IT). Per D-10 do NOT add tier P0 to this row or any new P0 row; do not edit verify-harness.ps1 P0 gate logic. Before saving, count existing tier P0 entries — must remain 15.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$y=Get-Content .planning/test-matrix.yaml -Raw; if ($y -notmatch 'id:\s*geo-synthetic') { exit 1 }; if ($y -notmatch 'tier:\s*P1') { exit 1 }; (Select-String -Path .planning/test-matrix.yaml -Pattern 'tier:\s*P0').Count -eq 15"</automated>
  </verify>
  <done>YAML row reflects P1 covered geo_synthetic linkage per D-09/D-10/D-11.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate docs/test-feature-matrix.md from YAML</name>
  <files>docs/test-feature-matrix.md</files>
  <read_first>docs/test-feature-matrix.md, scripts/generate-test-matrix-doc.ps1</read_first>
  <action>
    Per D-08 run scripts/generate-test-matrix-doc.ps1 from repo root (or documented equivalent in test-feature-matrix.md header) to regenerate docs/test-feature-matrix.md from .planning/test-matrix.yaml. Do not hand-edit generated table cells. Confirm geo-synthetic row shows tier P1, adapter geo_synthetic, owner data-generator-calcite, status covered, and linked_tests includes TemplateV2RunnerGeoSyntheticSourceTests.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path docs/test-feature-matrix.md -Pattern '\| geo-synthetic \|' | ForEach-Object { if ($_.Line -match 'P1' -and $_.Line -match 'geo_synthetic|geo-synthetic' -and $_.Line -match 'data-generator-calcite' -and $_.Line -match 'TemplateV2RunnerGeoSyntheticSourceTests') { exit 0 } }; exit 1"</automated>
  </verify>
  <done>Human-readable matrix synced per D-08.</done>
</task>

<task type="auto">
  <name>Task 3: Verify harness P0 gate unchanged and linked tests exist</name>
  <files>.planning/test-matrix.yaml, docs/test-feature-matrix.md</files>
  <action>
    Per D-10 confirm p0.total semantics: re-count tier P0 rows in test-matrix.yaml (expect 15). Grep verify-harness.ps1 for geo-synthetic — must NOT appear in P0 gate list. Confirm linked test classes exist on disk: TemplateV2RunnerGeoSyntheticSourceTests (20-01), GeoSyntheticRowSourceTests, GeoSyntheticRequestMapperTests (Phase 19). Optional smoke: run linked calcite tests only to ensure matrix references are green — does not require full harness script if slow.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Select-String -Path .planning/test-matrix.yaml -Pattern 'tier:\s*P0').Count -eq 15; if ($LASTEXITCODE -ne 0) { exit 1 }; if (Select-String -Path scripts/verify-harness.ps1 -Pattern 'geo-synthetic' -Quiet) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>TEST-10 satisfied: P1 linkage live, P0 frozen at 15, verify-harness semantics untouched per D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-matrix.yaml → CI merge gate | P0 tier changes affect merge blocking |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Elevation of Privilege | tier P0 promotion | medium | mitigate | D-10 explicit freeze; verify P0 count == 15 in Task 1 and Task 3 |
| T-20-05 | Repudiation | stale linked_tests | low | mitigate | Assert test class files exist before marking covered |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan |
</threat_model>

<verification>
geo-synthetic row P1 covered in YAML and generated MD; P0 count 15; verify-harness.ps1 unchanged for P0 set.
</verification>

<success_criteria>
- TEST-10 complete: P1 matrix linkage without P0 gate change
- D-08 through D-11 honored
- Depends on 20-01 TemplateV2RunnerGeoSyntheticSourceTests existing for linked_tests accuracy
</success_criteria>

<output>
Create `.planning/phases/20-pipeline-proof-docs-p1/20-03-SUMMARY.md` when done
</output>
