---
phase: 02-udf-platform-core
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/udf/
  - data-generator-service/src/main/java/org/gensokyo/data/udf/
  - data-generator-service/src/test/java/org/gensokyo/data/udf/
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/udf/
  - .planning/test-matrix.yaml
  - data-generator-test-fixtures/
autonomous: true
requirements: [UDF-04, UDF-07]
must_haves:
  truths:
    - "SQL UDFs register as TemplateV2SqlFunction and merge with built-in; built-in wins on collision (D-07, D-11)"
    - "Publish runs governance: secrets, script blacklist, JAR checks; audit on publish/deprecate (D-21–D-25)"
    - "publish/deprecate triggers TemplateV2RuntimeRegistry.refresh() (D-06)"
    - "test-matrix.yaml has udf-java-plugin, udf-script-graaljs, udf-sql-calcite rows (D-29)"
    - "Job captures UDF snapshot at run start; publish affects subsequent runs only (D-20)"
    - "Governance scans plaintext secrets, script blacklist, JAR manifest per D-22, D-23, D-24"
    - "Typed ref contract documented for Phase 3: sqlName, udfRef, PF4J sqlName (D-27)"
    - "UDF test payloads live in data-generator-test-fixtures per D-30"
  artifacts:
    - "org.gensokyo.data.udf.UdfGovernanceSupport"
    - "org.gensokyo.data.calcite.udf.RegistrySqlFunctionContributor"
    - "org.gensokyo.data.udf.UdfPublishService"
  key_links:
    - "UdfPublishService.publish → UdfGovernanceSupport → AuditService.record"
    - "RegistrySqlFunctionContributor → TemplateV2SqlFunctionRegistry.merge (built-in first)"
---

<objective>
Deliver UDF-04 (SQL/Calcite UDF registration) and UDF-07 (governance + audit). Integrate registry
with runtime refresh, add harness matrix rows, and prove register→publish→resolve with embedded tests.
</objective>

<execution_context>
**Depends on:** 02-01 (registry service). Can run parallel to 02-02 after 02-01; integrates 02-02
artifacts when present (script validator called from publish gate).
</execution_context>

<context>
Locked: D-06, D-07, D-11, D-18 (sqlName metadata), D-21–D-26, D-29, D-30, D-20 (job snapshot —
document in runtime bridge: resolve at run start captures registry view).

Read: `TemplateV2SqlFunctionRegistry.java`, `TemplateGovernanceSupport.java`, `AuditService.java`,
`SqlTransformFactory`, Phase 1 `test-matrix.yaml` schema.
</context>

<tasks>

<task id="T1">
<name>SQL UDF registration and SqlFunctionRegistry merge</name>
<files>
- Create: data-generator-calcite/src/main/java/org/gensokyo/data/calcite/udf/RegistrySqlFunctionContributor.java
- Modify: data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sql/SqlTransformFactory.java (or registry factory wiring)
- Modify: data-generator-service/src/main/java/org/gensokyo/data/config/CoreConfig.java
- Create: data-generator-calcite/src/test/java/org/gensokyo/data/calcite/udf/RegistrySqlUdfTests.java
</files>
<read_first>
- data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sql/TemplateV2SqlFunctionRegistry.java
- data-generator-calcite/src/test/java/org/gensokyo/data/calcite/TemplateV2RunnerTests.java (V2_WRAP pattern)
- .planning/phases/02-udf-platform-core/02-CONTEXT.md (D-07, D-11, D-18)
</read_first>
<acceptance_criteria>
- Published SQL UDF payload defines Calcite operator metadata + evaluator; registered `sqlName` used in SQL text.
- `RegistrySqlFunctionContributor` builds merged registry: `TemplateV2SqlFunctionRegistry.builtIn()` then overlay
  registry UDFs; built-in names not overridden (D-07).
- `RegistrySqlUdfTests`: register SQL UDF `com.example.wrap` with sqlName `V2_REG_WRAP`, publish, run template
  SQL `SELECT V2_REG_WRAP('x')` via embedded pipeline — passes.
- Refresh called after publish updates SQL functions without service restart (D-06).
</acceptance_criteria>
<action>
SQL payload format: JSON or small DSL in String payload specifying sqlName, returnType, operandTypes,
and evaluator script/reference — planner chooses minimal JSON. udfId ≠ sqlName per D-18.
</action>
<done>
RegistrySqlUdfTests green.
</done>
</task>

<task id="T2">
<name>Governance, audit, publish gate, and harness matrix rows</name>
<files>
- Create: data-generator-service/src/main/java/org/gensokyo/data/udf/UdfGovernanceSupport.java
- Create: data-generator-service/src/main/java/org/gensokyo/data/udf/UdfPublishService.java
- Modify: data-generator-service/src/main/java/org/gensokyo/data/udf/UdfRegistryService.java
- Create: data-generator-service/src/test/java/org/gensokyo/data/udf/UdfGovernanceTests.java
- Modify: .planning/test-matrix.yaml
- Create: data-generator-test-fixtures/src/test/resources/fixtures/udf/ (minimal payloads)
</files>
<read_first>
- data-generator-service/src/main/java/org/gensokyo/data/template/TemplateGovernanceSupport.java
- data-generator-service/src/main/java/org/gensokyo/data/audit/AuditService.java
- data-generator-service/src/main/java/org/gensokyo/data/template/TemplateLifecycleService.java
- .planning/test-matrix.yaml (schema + row format from Phase 1)
- .planning/phases/02-udf-platform-core/02-CONTEXT.md (D-21–D-26, D-29)
</read_first>
<acceptance_criteria>
- `UdfGovernanceSupport.collectViolations(record)` per D-22 scans plaintext secrets (password/apiKey
  patterns) extending TemplateGovernanceSupport style; per D-23 script blacklist (Runtime,
  ProcessBuilder, reflection); per D-24 JAVA_PLUGIN JAR manifest sanity and no system classpath refs.
- `UdfPublishService.publish` runs governance; on failure throws with `UdfValidationError` list;
  on success transitions to PUBLISHED, calls `AuditService.record("udf.publish", ...)`, triggers runtime refresh.
- `deprecate` audits with action `udf.deprecate`.
- `UdfGovernanceTests`: publish rejects script with `Runtime.getRuntime`; publish accepts clean script.
- `.planning/test-matrix.yaml` adds rows `udf-java-plugin`, `udf-script-graaljs`, `udf-sql-calcite` with
  owner_module `data-generator-calcite` or `data-generator-service`, status `pending` or `covered` when
  linked_tests populated (RegistryPf4jPluginTests, GraalJsScriptUdfTests, RegistrySqlUdfTests).
</acceptance_criteria>
<action>
Publish gate per D-21; structured errors per D-26. Reuse AuditService signature:
`record(action, resourceType, resourceId, detail)`. Wire refresh via injected
`TemplateV2RuntimeRegistryProvider.refresh()` per D-06. Document D-27 typed ref contract in
`docs/` or plan context comment (sqlName / udfRef / PF4J). Runtime bridge captures resolved UDF
snapshot at job start per D-20. Place fixture payloads under data-generator-test-fixtures per D-30.
</action>
<done>
UdfGovernanceTests green; matrix YAML has 3 new UDF rows.
</done>
</task>

</tasks>

<verification_criteria>
- [ ] UDF-04: SQL UDF in registry resolves in Template V2 SQL transform
- [ ] UDF-07: governance rejects bad payloads at publish; audit on publish/deprecate
- [ ] D-29: three harness matrix rows present
- [ ] `scripts/verify-harness.ps1` still exits 0 (fast path)
</verification_criteria>

## Artifacts this phase produces

| Symbol | Kind | Module |
|--------|------|--------|
| `UdfGovernanceSupport` | class | data-generator-service |
| `UdfPublishService` | class | data-generator-service |
| `RegistrySqlFunctionContributor` | class | data-generator-calcite |
| Matrix rows | yaml | .planning/test-matrix.yaml |
