---
phase: 16-rbac-enable-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleSecurityDefaultOffIT.java
  - data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationIntegrationIT.java
  - scripts/verify-rbac-enable.ps1
autonomous: true
requirements:
  - SEC-01
must_haves:
  truths:
    - "Default-off regression proves ConsoleSecurityProperties.enabled is false when loading application-phase7-test.yaml without RBAC overrides (D-04)"
    - "Base application.yaml contains no console-security.enabled: true (D-04, D-09)"
    - "ConsoleAuthorizationIntegrationIT remains canonical RBAC-on HTTP deny/allow proof (D-03)"
    - "ConsoleAuthorizationFilterTest and ConsoleUdfAuthorizationFilterTest stay green (D-05)"
    - "scripts/verify-rbac-enable.ps1 runs Maven RBAC slice and exits 0 with -SkipPlaywright (D-08)"
    - "Profile contract preserved: e2e, e2e-distributed, distributed-staging overlays keep enabled: false (D-09)"
  prohibitions:
    - statement: "Do not flip default-on RBAC in application.yaml or phase7-test"
      status: resolved
      verification: "rg 'console-security' data-generator-service/src/main/resources/application.yaml returns no match; ConsoleSecurityDefaultOffIT passes"
    - statement: "Do not add P1 matrix row or edit verify-harness.ps1"
      status: resolved
      verification: "git diff .planning/test-matrix.yaml scripts/verify-harness.ps1 empty after plan 16-01"
    - statement: "Do not replace ConsoleAuthorizationIntegrationIT with filter-only tests as sole evidence"
      status: resolved
      verification: "ConsoleAuthorizationIntegrationIT still present and included in verify script -Dtest list"
  artifacts:
    - path: "data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleSecurityDefaultOffIT.java"
      provides: "Default-off guard against Pitfall 4 accidental RBAC default-on"
    - path: "scripts/verify-rbac-enable.ps1"
      provides: "SEC-01 Maven one-liner (Playwright leg deferred to plan 16-03)"
  key_links:
    - from: "ConsoleSecurityDefaultOffIT"
      to: "ConsoleSecurityProperties"
      via: "@Autowired bean assertion enabled == false"
      pattern: "consoleSecurityProperties.isEnabled()"
    - from: "scripts/verify-rbac-enable.ps1"
      to: "ConsoleAuthorizationIntegrationIT"
      via: "Maven -Dtest slice"
      pattern: "ConsoleAuthorizationIntegrationIT"
    - from: "ConsoleAuthorizationIntegrationIT"
      to: "ConsoleAuthorizationFilter"
      via: "MockMvc + explicit filter registration"
      pattern: "addFilter(consoleAuthorizationFilter)"
---

<objective>
Deliver SEC-01 backend proof: default-off regression test, green canonical RBAC IT, and `scripts/verify-rbac-enable.ps1` Maven slice (D-03–D-05, D-08, D-09).

Purpose: Guard Pitfall 4 (accidental default-on) while keeping the existing HTTP integration test as primary deny/allow evidence when RBAC is enabled.
Output: New default-off IT, optional positive allow-path assertion in existing IT, Maven-only verify script. No docs in this plan (16-02).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-rbac-enable-path/16-CONTEXT.md
@.planning/phases/16-rbac-enable-path/16-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@data-generator-service/src/main/java/org/gensokyo/data/config/ConsoleSecurityProperties.java
@data-generator-service/src/main/java/org/gensokyo/data/security/ConsoleAuthorizationFilter.java
@data-generator-service/src/main/java/org/gensokyo/data/security/ConsoleRole.java
@data-generator-service/src/main/java/org/gensokyo/data/security/ConsolePermission.java
@data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationIntegrationIT.java
@data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationFilterTest.java
@data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleUdfAuthorizationFilterTest.java
@data-generator-service/src/test/resources/application-phase7-test.yaml
@data-generator-service/src/main/resources/application.yaml
@data-generator-service/src/main/resources/application-e2e.yaml
@data-generator-service/src/main/resources/application-staging.yaml
@data-generator-service/src/main/resources/application-e2e-rbac.yaml
@scripts/verify-phase8-uat-rw-streaming-upsert.ps1
@scripts/lib/repo-maven.ps1
</context>

## Artifacts this phase produces

*(Phase-wide inventory; this plan delivers Wave 1 backend proof)*

- `ConsoleSecurityDefaultOffIT` — default-off regression — **this plan**
- `ConsoleAuthorizationIntegrationIT` — optional positive allow GET — **this plan (discretion)**
- `scripts/verify-rbac-enable.ps1` — Maven slice + `-SkipPlaywright` early exit — **this plan**
- *(Wave 2)* `docs/staging-console-rbac.md` + AGENTS + cross-link — plan 16-02
- *(Wave 3)* Playwright leg in verify script — plan 16-03

## Non-goals (D-10 — out of scope for this plan)

- P1 matrix row / `test-matrix.yaml` wiring (Phase 17 / TEST-09)
- `docs/staging-console-rbac.md`, AGENTS.md, operator-console doc edits
- Playwright/Podman leg in verify script (plan 16-03)
- Spring Security/OAuth2/JWT, permission matrix changes unless test gap forces bugfix
- Enabling RBAC on `application-distributed-staging.yaml` or multi-JVM combined proof

<tasks>

<task type="auto">
  <name>Task 1: Add ConsoleSecurityDefaultOffIT for base/phase7-test default-off guard</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleSecurityDefaultOffIT.java</files>
  <read_first>
    - .planning/phases/16-rbac-enable-path/16-CONTEXT.md (D-04, D-09)
    - data-generator-service/src/main/java/org/gensokyo/data/config/ConsoleSecurityProperties.java (Java field default `enabled = false`)
    - data-generator-service/src/test/resources/application-phase7-test.yaml (no console-security block)
    - data-generator-service/src/main/resources/application.yaml (no console-security block — scout confirms)
    - .planning/research/PITFALLS.md Pitfall 4 (accidental RBAC default-on)
  </read_first>
  <action>
    Create `ConsoleSecurityDefaultOffIT` as a focused `@SpringBootTest` using the same pattern as other service ITs:

    ```java
    @SpringBootTest(
        classes = DataGeneratorApplication.class,
        properties = "spring.config.location=classpath:/application-phase7-test.yaml"
    )
    ```

    **Do not** set `data.generator.console-security.enabled=true` in test properties.

    Tests (minimum):

    1. **`consoleSecurityDisabledByDefaultOnPhase7TestProfile`** — autowire `ConsoleSecurityProperties`; assert `!consoleSecurityProperties.isEnabled()`.

    2. **`baseApplicationYamlDoesNotEnableConsoleSecurity`** — static assertion: read `classpath:application.yaml` as string (or use `@Value` + `@TestPropertySource` absence check); assert content does **not** contain `console-security:` with `enabled: true`. Prefer a simple `ClassPathResource` read + `assertFalse(yaml.contains("console-security:") && yaml.contains("enabled: true"))` or split check: no `console-security` key at all in base yaml (current scout state).

    Follow repository Java file rules: copyright block, class Javadoc, public test method names in camelCase, inline `//` for non-obvious assertions.

    Class name discretion: `ConsoleSecurityDefaultOffIT` preferred over extending unrelated security tests.
  </action>
  <verify>
    <automated>Test-Path data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleSecurityDefaultOffIT.java; rg -n "ConsoleSecurityDefaultOffIT|isEnabled\(\)" data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleSecurityDefaultOffIT.java</automated>
  </verify>
  <acceptance_criteria>
    - New IT loads phase7-test profile without RBAC override and asserts `enabled == false`
    - Base `application.yaml` guard test passes on current tree (no console-security block)
    - Copyright + Javadoc present per project rules
  </acceptance_criteria>
  <done>Default-off regression guards Pitfall 4; fails if profile bleed enables RBAC globally.</done>
</task>

<task type="auto">
  <name>Task 2: Audit ConsoleAuthorizationIntegrationIT and add positive allow-path if missing</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationIntegrationIT.java</files>
  <read_first>
    - data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationIntegrationIT.java (existing deny tests: missing header 403, VIEWER POST 403, EDITOR publish 403, OPERATOR draft run 400)
    - data-generator-console-web/e2e/specs/rbac.console.spec.ts (VIEWER GET scenarios → 200 — mirror in IT if absent)
    - .planning/phases/16-rbac-enable-path/16-CONTEXT.md (D-03 discretion on positive allow)
  </read_first>
  <action>
    Keep all existing deny/governance tests unchanged.

    **Audit:** IT currently has no positive allow-path (VIEWER GET catalog → 200). Playwright `rbac.console.spec.ts` already covers this at E2E layer.

    **Discretion (recommended):** Add one test method:

    ```java
    @Test
    void viewerCanGetScenariosCatalogWhenSecurityEnabled() throws Exception {
        mockMvc.perform(get("/api/templates/scenarios")
                        .header("X-Console-Role", "VIEWER"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.success").value(true));
    }
    ```

    Do **not** expand to full endpoint matrix — D-03 says extend only if gaps found; single allow-path closes the obvious asymmetry.

    Do not change `@SpringBootTest` properties or filter wiring unless tests fail.
  </action>
  <verify>
    <automated>rg -n "viewerCanGetScenariosCatalogWhenSecurityEnabled|missingRoleHeaderReturnsForbidden" data-generator-service/src/test/java/org/gensokyo/data/security/ConsoleAuthorizationIntegrationIT.java; &amp; .\mvnw-jdk25.ps1 -pl data-generator-service -Dtest=ConsoleAuthorizationIntegrationIT -Dsurefire.failIfNoSpecifiedTests=false test</automated>
  </verify>
  <acceptance_criteria>
    - All four existing deny/governance tests still present and pass
    - Optional: new VIEWER GET allow test passes when added
    - No changes to filter permission matrix or production code
  </acceptance_criteria>
  <done>Canonical RBAC-on IT covers both deny and at least one allow path.</done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/verify-rbac-enable.ps1 Maven slice</name>
  <files>scripts/verify-rbac-enable.ps1</files>
  <read_first>
    - scripts/verify-phase8-uat-rw-streaming-upsert.ps1 (Invoke-RepoMaven slice + -SkipPlaywright early exit pattern)
    - scripts/lib/repo-maven.ps1
    - .planning/phases/16-rbac-enable-path/16-CONTEXT.md (D-05, D-08, D-10)
  </read_first>
  <action>
    Create `scripts/verify-rbac-enable.ps1` with params (align with sibling verify scripts):

    - `[switch]$SkipPlaywright` — **required param**; when set, exit 0 after Maven slice (Playwright leg added in plan 16-03)
    - `[switch]$SkipBuild` — reserved for wave 3 Podman build; no-op in wave 1

    **Maven slice** via `Invoke-RepoMaven` / `mvnw-jdk25.ps1`:

    ```powershell
    $testList = @(
      'ConsoleSecurityDefaultOffIT',
      'ConsoleAuthorizationIntegrationIT',
      'ConsoleAuthorizationFilterTest',
      'ConsoleUdfAuthorizationFilterTest'
    ) -join ','
    ```

    Run: `-pl data-generator-service -am -Dtest=$testList -Dsurefire.failIfNoSpecifiedTests=false test`

    On success with `-SkipPlaywright`:

    ```powershell
    Write-Host "[SUCCESS] SEC-01 RBAC enable-path Maven verification passed (-SkipPlaywright)." -ForegroundColor Green
    exit 0
    ```

    When `-SkipPlaywright` is **not** passed in wave 1, print a clear message that Playwright leg ships in plan 16-03 and exit 0 after Maven (or exit 0 with note — do not fail). Plan 16-03 replaces this stub with real Podman/Playwright.

    Dot-source `lib/repo-maven.ps1`. Use `Write-Step` helper matching phase 8 scripts.

    Script is **not** wired into `verify-harness.ps1` (D-08, D-10).
  </action>
  <verify>
    <automated>Test-Path scripts/verify-rbac-enable.ps1; rg -n "ConsoleSecurityDefaultOffIT|ConsoleAuthorizationIntegrationIT|SkipPlaywright" scripts/verify-rbac-enable.ps1; powershell -NoProfile -File scripts/verify-rbac-enable.ps1 -SkipPlaywright</automated>
  </verify>
  <acceptance_criteria>
    - Script exists and runs four security test classes green
    - `-SkipPlaywright` exits 0 with `[SUCCESS]` message
    - No edits to `verify-harness.ps1` or `test-matrix.yaml`
    - Profile yaml files untouched in this task
  </acceptance_criteria>
  <done>Operator Maven one-liner exists; CI-friendly `-SkipPlaywright` path green.</done>
</task>

</tasks>

## Verification

After all tasks:

```powershell
# Static contract
rg -n "ConsoleSecurityDefaultOffIT" data-generator-service/src/test
rg -n "verify-rbac-enable" scripts/

# Profile contract spot-check (must remain true)
rg -A2 "console-security:" data-generator-service/src/main/resources/application-e2e.yaml
rg -A2 "console-security:" data-generator-service/src/main/resources/application-staging.yaml

# Full Maven proof
powershell -NoProfile -File scripts/verify-rbac-enable.ps1 -SkipPlaywright
```

## Success criteria

- [ ] Default-off IT passes; base yaml has no RBAC-on leak
- [ ] ConsoleAuthorizationIntegrationIT + filter unit tests green via verify script
- [ ] verify-rbac-enable.ps1 is the Maven gate; Playwright deferred to 16-03
- [ ] No matrix/harness changes
