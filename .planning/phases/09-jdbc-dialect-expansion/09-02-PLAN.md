---
phase: 09-jdbc-dialect-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-service/src/main/java/org/gensokyo/data/datasource/JdbcDriverPresetCatalog.java
  - data-generator-service/src/main/java/org/gensokyo/data/datasource/catalog/ConnectionConnectivityService.java
  - data-generator-console-web/src/app/datasources/jdbcDriverPresets.ts
  - data-generator-console-web/src/app/datasources/DriverPresetFields.tsx
  - data-generator-service/src/test/java/org/gensokyo/data/datasource/JdbcDriverPresetCatalogTests.java
  - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleDataSourceControllerTest.java
  - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ConnectionCatalogTestTests.java
autonomous: true
requirements:
  - RW-06
must_haves:
  truths:
    - Console driver-presets API returns complete DM, Kingbase, HighGo, PostgreSQL, and ClickHouse presets with URL templates and driver class names (D-09)
    - Proprietary DM/KB/HG presets surface bundled:true when jdbc-bundled artifact present (D-10)
    - Connectivity test failures return actionable summaries without passwords or full JDBC URL userinfo (D-11)
    - Frontend fallback presets stay in sync with server catalog for the five engine groups (D-09)
  artifacts:
    - JdbcDriverPresetCatalog with verified five-engine preset entries
    - jdbcDriverPresets.ts fallback aligned with server catalog
    - ConnectionConnectivityService sanitized failure messages for DM/KB/HG driver classes
    - JdbcDriverPresetCatalogTests, ConsoleDataSourceControllerTest, ConnectionCatalogTestTests extensions
  key_links:
    - JdbcDriverPresetDto.from must reflect BundledJdbcDriverRegistry.hasBundle for dm/kingbase/highgo bundle keys (D-10)
    - ConsoleDataSourceController.driverPresets thin delegate to catalog; connectivity via ConnectionCatalog.test (D-09)
---

<objective>
Complete console JDBC driver presets and connectivity contracts for Dameng, Kingbase, HighGo, PostgreSQL, and ClickHouse (RW-06, D-09–D-11).

Purpose: Operators configure and test datasources from presets with correct URL/driver hints before running dialect templates.
Output: Updated preset catalog, connectivity hygiene, frontend fallback sync, and API/unit tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md
@.planning/phases/09-jdbc-dialect-expansion/09-PATTERNS.md
@.planning/phases/09-jdbc-dialect-expansion/09-VALIDATION.md
@.planning/phases/07-datasource-governance-hot-reload/07-CONTEXT.md
@data-generator-service/src/main/java/org/gensokyo/data/datasource/JdbcDriverPresetCatalog.java
@data-generator-service/src/main/java/org/gensokyo/data/datasource/catalog/ConnectionConnectivityService.java
@data-generator-console-web/src/app/datasources/jdbcDriverPresets.ts
@data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleDataSourceController.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete JdbcDriverPresetCatalog and bundled driver flags</name>
  <files>data-generator-service/src/main/java/org/gensokyo/data/datasource/JdbcDriverPresetCatalog.java, data-generator-service/src/test/java/org/gensokyo/data/datasource/JdbcDriverPresetCatalogTests.java, data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleDataSourceControllerTest.java</files>
  <read_first>
    - data-generator-service/src/main/java/org/gensokyo/data/datasource/JdbcDriverPresetCatalog.java (PRESETS list, matchesUrl, resolveDriverClassCandidates)
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/JdbcDriverPresetCatalogTests.java
    - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleDataSourceControllerTest.java (driverPresets_returnsCatalog)
    - .planning/codebase/STACK.md (dm-jdbc, kingbase, HighGo, clickhouse-jdbc versions)
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-09, D-10
  </read_first>
  <action>
    Audit and correct preset entries for dm8, kingbase8, kingbase9, highgo, clickhouse variants, and postgresql presets per D-09: URL templates, primary driverClassName, alternateDriverClassNames, groupKey, bundleKey.
    Verify bundleKey values match data-generator-service assembly jdbc-bundled layout; presets for DM/KB/HG must resolve bundled:true via BundledJdbcDriverRegistry when bundle exists (D-10).
    Extend JdbcDriverPresetCatalogTests: HighGo URL template and driver resolution, ClickHouse bundled key, PostgreSQL preset completeness, kingbase9 alternate drivers chain.
    Extend ConsoleDataSourceControllerTest jsonPath assertions for kingbase8, highgo, clickhouse24 (or current id), postgresql16 urlTemplate and bundled flags when registry mock returns true.
    Do not add dialect capability-hint UI or template-editor preset linking (deferred).
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=JdbcDriverPresetCatalogTests,ConsoleDataSourceControllerTest -Dsurefire.failIfNoSpecifiedTests=false -q</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/datasources/driver-presets returns all five engine groups with non-blank urlTemplate and driverClassName
    - dm8, kingbase8, highgo presets report bundled:true when registry has bundle
    - resolveDriverClassCandidates resolves jdbc:highgo:// and jdbc:dm:// URLs to expected driver classes
    - Maven preset catalog and controller tests exit 0
  </acceptance_criteria>
  <done>Server-side preset catalog is complete and tested for all five Phase 9 JDBC engines with bundled flags correct.</done>
</task>

<task type="auto">
  <name>Task 2: Sync console fallback presets and connectivity secret hygiene</name>
  <files>data-generator-console-web/src/app/datasources/jdbcDriverPresets.ts, data-generator-console-web/src/app/datasources/DriverPresetFields.tsx, data-generator-service/src/main/java/org/gensokyo/data/datasource/catalog/ConnectionConnectivityService.java, data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ConnectionCatalogTestTests.java</files>
  <read_first>
    - data-generator-console-web/src/app/datasources/jdbcDriverPresets.ts (JDBC_DRIVER_PRESETS_FALLBACK, JDBC_DRIVER_GROUP_KEYS)
    - data-generator-console-web/src/app/datasources/DriverPresetFields.tsx (applyPreset, bundledSelected hint)
    - data-generator-service/src/main/java/org/gensokyo/data/datasource/catalog/ConnectionConnectivityService.java (summarizeJdbcFailure, sanitizeMessage)
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ConnectionCatalogTestTests.java (jdbcDraftTest_failureHasActionableMessageWithoutSecrets)
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-09, D-11
  </read_first>
  <action>
    Align jdbcDriverPresets.ts fallback entries with JdbcDriverPresetCatalog for dm, kingbase, highgo, clickhouse, postgresql: same id, driverClassName, urlTemplate, groupKey; set bundled:true on DM/KB/HG fallbacks when matching server bundle policy (D-09, D-10).
    Keep DriverPresetFields bundled hint behavior; no new capability-hint panel after preset selection (out of scope).
    In ConnectionConnectivityService, ensure summarizeJdbcFailure and sanitizeMessage never return operator-facing messages containing password literals or full JDBC URL with embedded credentials; optionally enrich details map with driverClassName for DM/KB/HG failures (D-11).
    Add ConnectionCatalogTestTests draft connectivity cases using dm.jdbc.driver.DmDriver, com.kingbase8.Driver, com.highgo.jdbc.Driver against unreachable host: assert failure message is non-blank, actionable (host/driver hint), and does not contain test password or jdbc URL userinfo segment.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am test -Dtest=ConnectionCatalogTestTests -Dsurefire.failIfNoSpecifiedTests=false -q; cd data-generator-console-web; npx tsc -p tsconfig.json --noEmit; npm run build</automated>
    <manual>Per .cursor/rules/console-verify.mdc: task-level minimum is `tsc --noEmit` + `npm run build` (included in automated above). Full Playwright preset E2E runs via `.\scripts\verify-phase9-uat-jdbc-dialect.ps1` without `-SkipPlaywright` after 09-04 lands; broader console regression optional via `.\scripts\verify-console.ps1`.</manual>
  </verify>
  <acceptance_criteria>
    - jdbcDriverPresets.ts fallback URLs and driver classes match server catalog for five engine groups
    - JDBC_DRIVER_GROUP_KEYS order includes dm, kingbase, highgo, clickhouse, postgresql before mysql
    - Connectivity failure tests for DM/KB/HG drivers pass secret hygiene assertions per D-11
    - No new UI for dialect capability hints introduced
    - Maven ConnectionCatalogTestTests exits 0
    - Console-web preset sync compiles and builds: `npx tsc -p tsconfig.json --noEmit` and `npm run build` exit 0 (console-verify.mdc minimum)
  </acceptance_criteria>
  <done>Console fallback presets match server catalog; connectivity failures are actionable and secret-safe for proprietary drivers; frontend preset sync passes tsc + production build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → ConsoleDataSourceController | Untrusted JDBC URL, username, password in connectivity test |
| ConnectionCatalog → operator UI | Failure messages displayed in console toast/form |
| jdbc-bundled → runtime classpath | Bundled proprietary drivers loaded from distribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Information Disclosure | ConnectionConnectivityService | high | mitigate | sanitizeMessage strips JDBC userinfo; failure message must not echo password or full URL per D-11; contract tests assert no s3cr3t leakage |
| T-09-06 | Spoofing | JdbcDriverPresetCatalog | medium | mitigate | Preset driver class names match bundled artifact manifests; bundled flag only when registry confirms |
| T-09-07 | Tampering | Operator preset selection | low | accept | Preset fills form fields only; server validates on save/test — no runtime SQL impact from preset alone |
</threat_model>

<verification>
- driver-presets API JSON contract tests pass for five engines
- Connectivity draft tests prove no password/URL leakage for DM/KB/HG
- Frontend fallback preset file matches server catalog fields; console-web `tsc --noEmit` + `npm run build` green per console-verify.mdc
</verification>

<success_criteria>
- RW-06: Console presets and validation recognize Dameng, Kingbase, HighGo, PostgreSQL, and ClickHouse connection shapes
- D-09, D-10, D-11 satisfied without deferred capability-hint UI
</success_criteria>

<output>
Create `.planning/phases/09-jdbc-dialect-expansion/09-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Updated `JdbcDriverPresetCatalog` with verified five-engine presets and bundle keys
- Updated `jdbcDriverPresets.ts` fallback sync
- Hardened `ConnectionConnectivityService` failure summaries
- Extended `JdbcDriverPresetCatalogTests`, `ConsoleDataSourceControllerTest`, `ConnectionCatalogTestTests`
