---
phase: 09-jdbc-dialect-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineKingbaseDialectTests.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java
  - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/ClickHouseInsertBulkWriterIntegrationTests.java
autonomous: true
requirements:
  - RW-05
must_haves:
  truths:
    - PostgreSQL Testcontainers upsert idempotency passes (existing Phase 8 baseline retained)
    - ClickHouse Testcontainers insert bulk path passes; upsert=true rejected at runtime (D-03)
    - Kingbase and HighGo dialect keys tested via PostgreSQL container proxy plus mapping unit tests (D-13, D-15)
    - Dameng MERGE SQL proven by unit tests; optional real DM IT gated and skipped by default (D-13, D-14)
    - At least one read/write scenario per target dialect without production credentials (D-13, D-15)
  artifacts:
    - ChunkedPipelineKingbaseDialectTests (PG proxy for kingbase/highgo dialect keys)
    - Extended UpsertParitySupport parameterized for kingbase/highgo
    - ChunkedPipelineDamengUpsertIT optional with DamengTestSupport gate
    - ClickHouse upsert runtime contract test in integration suite
  key_links:
    - UpsertParitySupport.assertUpsertIdempotent must pass dialect kingbase/highgo against PG container (D-15)
    - DockerTestSupport.dockerAvailable gates Testcontainers ITs same as Phase 8
---

<objective>
Deliver layered embedded harness tests proving dialect read/write and upsert behavior without production credentials (RW-05, D-13–D-15).

Purpose: Success criterion 3 — embedded tests pass for at least one scenario per dialect engine.
Output: Testcontainers PG/CK ITs, Kingbase/HighGo PG-proxy ITs, Dameng MERGE unit/optional IT, ClickHouse upsert reject contract.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md
@.planning/phases/09-jdbc-dialect-expansion/09-PATTERNS.md
@.planning/phases/09-jdbc-dialect-expansion/09-VALIDATION.md
@docs/testing-embedded-components.md
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java
@data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/ClickHouseInsertBulkWriterIntegrationTests.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kingbase/HighGo PG-proxy upsert ITs and UpsertParitySupport extension</name>
  <files>data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java, data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineKingbaseDialectTests.java</files>
  <read_first>
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/UpsertParitySupport.java (assertUpsertIdempotent, upsertTemplate)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java (Testcontainers pattern)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java (kingbase/highgo ON CONFLICT from 09-01)
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-01, D-13, D-15
  </read_first>
  <behavior>
    - assertUpsertIdempotent with dialect kingbase against PG container: second run row count unchanged, rowsUpserted greater than 0
    - assertUpsertIdempotent with dialect highgo against same PG container: idempotent re-run
    - SQL builder unit test confirms kingbase maps to ON CONFLICT (companion assertion in same plan wave)
  </behavior>
  <action>
    Create ChunkedPipelineKingbaseDialectTests mirroring ChunkedPipelinePostgresUpsertTests: @EnabledIf DockerTestSupport, PostgreSQLContainer postgres:16-alpine.
    Add tests chunkedUpsertKingbaseDialectIsIdempotent and chunkedUpsertHighgoDialectIsIdempotent calling UpsertParitySupport.assertUpsertIdempotent with dialect kingbase and highgo respectively against PG JDBC URL (D-01, D-15).
    Extend UpsertParitySupport if needed so template writer options.dialect reflects passed dialect parameter while JDBC connection uses PG driver (document PG-proxy approach in class Javadoc per D-15).
    Add inline comment in test class stating Kingbase/HighGo fulfillment via PG ON CONFLICT proxy counts toward Phase 9 success criterion (D-15).
    Follow java-copyright-class-javadoc.mdc on new test classes.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=ChunkedPipelineKingbaseDialectTests,ChunkedPipelinePostgresUpsertTests -Dsurefire.failIfNoSpecifiedTests=false -q</automated>
  </verify>
  <acceptance_criteria>
    - Kingbase dialect IT passes idempotency against PG container when Docker available
    - Highgo dialect IT passes idempotency against PG container when Docker available
    - UpsertParitySupport.assertUpsertIdempotent asserts rowsUpserted &gt; 0 on second run for dialect kingbase and highgo — validates JdbcBulkWriteExecutor.countUpsertedRows path from 09-01 Task 2
    - Tests skip cleanly when Docker unavailable (@EnabledIf pattern)
    - UpsertParitySupport documents PG-proxy strategy for KB/HG (D-15)
  </acceptance_criteria>
  <done>Kingbase and HighGo dialect keys proven via PG-proxy upsert ITs fulfilling per-dialect harness requirement; rowsUpserted assertions close countUpsertedRows coverage for 09-01.</done>
</task>

<task type="auto">
  <name>Task 2: ClickHouse insert IT, upsert reject contract, optional Dameng IT gate</name>
  <files>data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/ClickHouseInsertBulkWriterIntegrationTests.java, data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelineDamengUpsertIT.java, data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/DamengTestSupport.java</files>
  <read_first>
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/ClickHouseInsertBulkWriterIntegrationTests.java
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/runtime/ChunkedPipelinePostgresUpsertTests.java (@EnabledIf gating)
    - data-generator-calcite/src/test/java/org/gensokyo/data/calcite/sink/JdbcSinkSqlBuilderTests.java (buildsDamengMergeInto from 09-01)
    - .planning/phases/09-jdbc-dialect-expansion/09-CONTEXT.md D-03, D-13, D-14
  </read_first>
  <action>
    Extend ClickHouseInsertBulkWriterIntegrationTests with clickhouseUpsertRejectedAtRuntime: template or direct JdbcSinkSqlBuilder call with dialect clickhouse and upsert true must throw IllegalArgumentException (complements unit test, D-03).
    Confirm existing writesRowsUsingClickHouseInsertBulkMode still passes for insert-only path.
    Create DamengTestSupport with static damengItEnabled() reading system property dm.it=true or env DG_DM_IT=true (D-14 discretion).
    Create ChunkedPipelineDamengUpsertIT skeleton @EnabledIf DamengTestSupport#damengItEnabled with @Testcontainers placeholder or documented skip — primary Dameng proof remains JdbcSinkSqlBuilderTests MERGE unit tests from 09-01 (D-13, D-14).
    Document in DamengTestSupport Javadoc how operators enable real DM IT when image/host available; default CI skips.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=ClickHouseInsertBulkWriterIntegrationTests,JdbcSinkSqlBuilderTests -Dsurefire.failIfNoSpecifiedTests=false -q</automated>
  </verify>
  <acceptance_criteria>
    - ClickHouse insert bulk IT still passes when Docker available
    - ClickHouse upsert=true runtime reject test passes
    - Dameng MERGE unit tests from 09-01 pass (included in JdbcSinkSqlBuilderTests run)
    - ChunkedPipelineDamengUpsertIT skipped by default without dm.it flag (D-14)
    - Maven test slice exits 0
  </acceptance_criteria>
  <done>ClickHouse insert and upsert-reject covered; Dameng MERGE unit-tested with optional gated real IT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testcontainers → local Docker | Test harness pulls public PG/CK images only |
| UpsertParitySupport → H2/PG test DB | Synthetic credentials in test fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08 | Tampering | ClickHouse upsert path | high | mitigate | Contract IT asserts upsert=true rejected; no silent insert-as-upsert (D-03) |
| T-09-09 | Denial of Service | Testcontainers in CI | low | accept | @EnabledIf dockerAvailable; SkipPlaywright Maven slice tolerates skip when Docker absent |
| T-09-10 | Information Disclosure | Test fixture JDBC URLs | low | accept | Test-only localhost URLs with fixed test credentials; never production secrets |
</threat_model>

<verification>
- PG, CK Testcontainers ITs pass when Docker available
- Kingbase/HighGo proxy ITs pass on same PG container; UpsertParitySupport rowsUpserted &gt; 0 proves 09-01 countUpsertedRows
- Dameng optional IT gated; unit MERGE tests always run
</verification>

<success_criteria>
- Embedded harness covers read/write or upsert contract for each of five dialect targets without production credentials (D-13, D-15)
- RW-05 integration proof complete pending 09-01 SQL builder merge
</success_criteria>

<output>
Create `.planning/phases/09-jdbc-dialect-expansion/09-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `ChunkedPipelineKingbaseDialectTests` (PG-proxy for kingbase/highgo)
- Extended `UpsertParitySupport` with dialect parameter documentation
- `DamengTestSupport` and optional `ChunkedPipelineDamengUpsertIT`
- Extended `ClickHouseInsertBulkWriterIntegrationTests` upsert reject contract
