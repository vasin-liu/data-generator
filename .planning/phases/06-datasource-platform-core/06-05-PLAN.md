---
phase: 06-datasource-platform-core
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - data-generator-calcite/pom.xml
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/TemplateV2RuntimeServices.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkFactory.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/KafkaSinkFactory.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/ElasticsearchSinkFactory.java
  - data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/QuerySourceFactory.java
  - data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleDataSourceController.java
  - data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java
  - data-generator-calcite/src/test/java/**
  - data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java
autonomous: true
requirements:
  - DS-01
  - DS-02
must_haves:
  truths:
    - TemplateV2RuntimeServices exposes ConnectionCatalog as primary connection access (D-28)
    - Jdbc/Kafka/ES factories resolve via Catalog with kind inferred from template context (D-08, D-30)
    - calcite depends only on datasource-api not adapter impls (D-27)
    - Zero template YAML changes; existing V2ScenarioTemplateIT passes (D-32, D-36)
    - Template reference fields unchanged — JDBC dataSourceId and Kafka/ES cluster fields only; Catalog resolves at runtime (D-03)
    - Console list endpoints delegate to Catalog.listAll() with unchanged REST shapes (D-11, D-37)
  artifacts:
    - org.gensokyo.data.calcite.runtime.InMemoryCatalog (test support)
    - Updated TemplateV2RuntimeServices with ConnectionCatalog
  key_links:
    - RuntimeJdbcEndpointResolver implemented atop Catalog (D-29)
    - PF4J plugins access connections via TemplateV2RuntimeServices (D-17)
---

# Plan 06-05: Calcite Runtime Integration & Regression Gate

<objective>
Wire `ConnectionCatalog` into Calcite runtime, sink/source factories, console list delegation, and full regression suite per D-08, D-17, D-27..D-33, D-36, D-37.

Purpose: Complete strangler cutover — templates resolve managed connections through catalog without YAML changes.
Output: Passing calcite + service integration tests using catalog resolution path exclusively.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-datasource-platform-core/06-CONTEXT.md
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/TemplateV2RuntimeServices.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/JdbcSinkFactory.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/KafkaSinkFactory.java
@data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/ElasticsearchSinkFactory.java
@data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleDataSourceController.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve TemplateV2RuntimeServices and JDBC resolver atop Catalog</name>
  <files>data-generator-calcite/pom.xml,data-generator-calcite/src/main/java/org/gensokyo/data/calcite/runtime/TemplateV2RuntimeServices.java,data-generator-calcite/src/main/java/org/gensokyo/data/calcite/RuntimeJdbcEndpointResolver.java,data-generator-service/src/main/java/org/gensokyo/data/config/DefaultRuntimeJdbcEndpointResolver.java,data-generator-calcite/src/test/java/org/gensokyo/data/calcite/support/InMemoryCatalog.java</files>
  <read_first>
    - TemplateV2RuntimeServices.java
    - RuntimeJdbcEndpointResolver.java
    - DefaultRuntimeJdbcEndpointResolver.java
    - ConnectionCatalog api
    - ChunkedJdbcParitySupport or similar calcite test wiring patterns
  </read_first>
  <action>
    Per D-27/D-28: add `data-generator-datasource-api` dependency to calcite POM only (not jdbc/kafka/es adapters).
    Extend `TemplateV2RuntimeServices` record to include `ConnectionCatalog connectionCatalog` as primary connection accessor; keep kafka/es helper methods delegating to catalog resolve (D-30).
    Per D-29: reimplement `DefaultRuntimeJdbcEndpointResolver` to resolve managed ids via `ConnectionCatalog.resolve(name, JDBC)` then apply inline fallback through jdbc adapter delegate.
    Per D-08: kind inferred from caller context (source/sink factory), not template field.
    Per D-31: add `InMemoryCatalog` test utility in calcite test sources registering H2/Kafka stub/ES stub connections without full Boot.
    Per D-33: remove feature flags — catalog is sole resolve path once tests pass.
    Update all calcite tests constructing `TemplateV2RuntimeServices` to pass catalog (use InMemoryCatalog where needed).
  </action>
  <acceptance_criteria>
    - `data-generator-calcite/pom.xml` depends on `data-generator-datasource-api` and NOT on `-jdbc`/`-kafka`/`-elasticsearch` adapter artifacts
    - `TemplateV2RuntimeServices` constructor/factory accepts `ConnectionCatalog`
    - `InMemoryCatalog` exists under calcite test or test-fixtures
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=TemplateV2RuntimeServicesTests,JdbcTemplateTemplateV2RuntimePluginProviderTests -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=TemplateV2RuntimeServicesTests,JdbcTemplateTemplateV2RuntimePluginProviderTests -q</automated>
  </verify>
  <done>Calcite runtime uses ConnectionCatalog; embedded tests updated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sink factories, console list delegation, regression gate</name>
  <files>data-generator-calcite/src/main/java/org/gensokyo/data/calcite/sink/**,data-generator-calcite/src/main/java/org/gensokyo/data/calcite/source/QuerySourceFactory.java,data-generator-service/src/main/java/org/gensokyo/data/api/console/ConsoleDataSourceController.java,data-generator-service/src/test/java/org/gensokyo/data/template/V2ScenarioTemplateIT.java</files>
  <read_first>
    - KafkaSinkFactory.java, ElasticsearchSinkFactory.java, JdbcSinkFactory.java
    - QuerySourceFactory.java (JDBC source resolution)
    - ConsoleDataSourceController.java (list endpoint)
    - V2ScenarioTemplateIT.java
    - Messaging cluster console controllers if separate list endpoints exist
  </read_first>
  <action>
    Per D-30: update `JdbcSinkFactory`, `KafkaSinkFactory`, `ElasticsearchSinkFactory`, and JDBC `QuerySourceFactory` paths to resolve cluster/dataSourceId via `runtimeServices.connectionCatalog()` with correct ConnectionKind.
    Per D-11/D-37: console datasource list endpoint(s) delegate aggregation to `catalog.listAll()` filtered by kind as needed; preserve existing REST paths and DTO JSON field names; add `source` field (BOOTSTRAP/MANAGED) for operator visibility (D-26).
    Per D-17: ensure PF4J runtime services injection passes same ConnectionCatalog instance.
    Per D-36: run regression slice: `DataSourceConfigServiceTests`, `ConsoleDataSourceControllerTest`, `V2ScenarioTemplateIT`, `KafkaSinkFactoryTests`, `ElasticsearchSinkFactoryTests`, messaging cluster tests.
    Per D-32/D-03: confirm no template YAML schema changes in samples or tests; factories continue reading `dataSourceId`/cluster fields from existing VOs — only resolution path changes to Catalog.
  </action>
  <acceptance_criteria>
    - Console list response includes `source` or equivalent field with BOOTSTRAP/MANAGED values
    - `.\mvnw-jdk25.ps1 -pl data-generator-service,data-generator-calcite -am test -Dtest=V2ScenarioTemplateIT,ConsoleDataSourceControllerTest,KafkaSinkFactoryTests,ElasticsearchSinkFactoryTests -q` exits 0
    - No edits to Template V2 YAML VO classes adding required new reference fields (D-03, D-32)
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service,data-generator-calcite -am test -Dtest=V2ScenarioTemplateIT,ConsoleDataSourceControllerTest,KafkaSinkFactoryTests,ElasticsearchSinkFactoryTests -q</automated>
  </verify>
  <done>End-to-end catalog resolution verified; regression gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template V2 run → Catalog resolve | Pipeline receives runtime handles not raw secrets |
| Console list → operator | Metadata only in list responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Spoofing | Wrong kind resolve | mitigate | Infer kind from factory context; validate kind on resolve (D-08) |
| T-06-10 | Repudiation | Connection source visibility | accept | BOOTSTRAP/MANAGED tag aids audit prep for Phase 7 |
| T-06-SC | Tampering | Calcite factory wiring | mitigate | Full regression test slice (D-36) |
</threat_model>

<verification>
- V2ScenarioTemplateIT passes without template YAML changes
- Console REST contract unchanged except additive source metadata field
- All phase requirement IDs DS-01 and DS-02 satisfied end-to-end
</verification>

<success_criteria>
- Operator template referencing dataSourceId/cluster resolves through catalog abstraction
- data-generator-datasource populated with api + jdbc + kafka + es submodules
- Existing console CRUD and V2 run paths pass regression (ROADMAP success criteria 1-3)
</success_criteria>

## Artifacts this phase produces

- Updated `TemplateV2RuntimeServices` with `ConnectionCatalog`
- `InMemoryCatalog` test support class
- Catalog-backed `DefaultRuntimeJdbcEndpointResolver`
- Console list delegation using `catalog.listAll()`
- Factory updates in Jdbc/Kafka/ES sink and JDBC source paths

<output>
Create `.planning/phases/06-datasource-platform-core/06-05-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Source | Item | Plan(s) |
|--------|------|---------|
| GOAL | Unified JDBC/Kafka/ES resolution via datasource module | 01-05 |
| REQ | DS-01 module abstractions | 01, 02, 03, 05 |
| REQ | DS-02 catalog resolves without breaking templates | 04, 05 |
| CONTEXT | D-01..D-37 | Distributed across 01-05 per decision mapping in CONTEXT |
