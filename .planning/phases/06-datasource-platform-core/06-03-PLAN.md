---
phase: 06-datasource-platform-core
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - data-generator-datasource/data-generator-datasource-kafka/pom.xml
  - data-generator-datasource/data-generator-datasource-kafka/src/main/java/**
  - data-generator-datasource/data-generator-datasource-elasticsearch/pom.xml
  - data-generator-datasource/data-generator-datasource-elasticsearch/src/main/java/**
  - data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/kafka/**
  - data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/elasticsearch/**
autonomous: true
requirements:
  - DS-01
must_haves:
  truths:
    - DynamicKafkaTemplateRegistry and DynamicElasticsearchClientRegistry live in adapter modules (D-14)
    - Kafka/ES resolve preserves primary-cluster fallback when cluster blank (D-04, D-21)
  artifacts:
    - data-generator-datasource-kafka module with relocated registry
    - data-generator-datasource-elasticsearch module with relocated registry
  key_links:
    - All modules importing registries updated to new package paths
    - Kafka/ES writers/readers still compile after package move
---

# Plan 06-03: Kafka & Elasticsearch Datasource Adapters

<objective>
Create kafka and elasticsearch datasource submodules and relocate `DynamicKafkaTemplateRegistry` / `DynamicElasticsearchClientRegistry` from core per D-12, D-14, D-04, D-21.

Purpose: Unified messaging/search connection adapters matching JDBC module structure.
Output: Compiling adapter modules with relocated registries and primary fallback semantics preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-datasource-platform-core/06-CONTEXT.md
@data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/kafka/support/DynamicKafkaTemplateRegistry.java
@data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/elasticsearch/support/DynamicElasticsearchClientRegistry.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate Kafka registry to datasource-kafka module</name>
  <files>data-generator-datasource/data-generator-datasource-kafka/**,data-generator-common/data-generator-core/**,data-generator-writer/data-generator-writer-kafka/**,data-generator-calcite/**,data-generator-service/**</files>
  <read_first>
    - DynamicKafkaTemplateRegistry.java
    - data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/config/DynamicKafkaConfig.java
    - Grep results for DynamicKafkaTemplateRegistry imports across repo
  </read_first>
  <action>
    Per D-14: move `DynamicKafkaTemplateRegistry` (and closely coupled kafka support types if required) from `data-generator-core` to `data-generator-datasource-kafka` under package `org.gensokyo.data.datasource.kafka` (or keep support subpackage — document choice in SUMMARY).
    Update all import sites: CoreConfig, KafkaWriter, KafkaSinkFactory, calcite tests, writer-kafka module.
    Per D-04: preserve `template(cluster)` primary fallback when cluster name blank/null.
    Add datasource-kafka POM depending on api + spring-kafka (versions from root BOM).
    Keep backward-compatible re-export shim in core ONLY if needed to avoid wide breakage — prefer single cutover per D-35 (update all imports, delete old class).
  </action>
  <acceptance_criteria>
    - `DynamicKafkaTemplateRegistry` source file path under `data-generator-datasource-kafka`
    - `grep -r "org.gensokyo.data.kafka.support.DynamicKafkaTemplateRegistry" --include="*.java"` returns 0 matches OR only deprecated shim with @Deprecated
    - `.\mvnw-jdk25.ps1 -pl data-generator-writer/data-generator-writer-kafka,data-generator-calcite -am test -Dtest=KafkaSinkFactoryTests -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=KafkaSinkFactoryTests -q</automated>
  </verify>
  <done>Kafka registry relocated; calcite Kafka sink tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate Elasticsearch registry to datasource-elasticsearch module</name>
  <files>data-generator-datasource/data-generator-datasource-elasticsearch/**,data-generator-common/data-generator-core/**,data-generator-writer/data-generator-writer-elasticsearch/**,data-generator-reader/data-generator-reader-elasticsearch/**,data-generator-calcite/**,data-generator-service/**</files>
  <read_first>
    - DynamicElasticsearchClientRegistry.java
    - data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/config/DynamicElasticsearchConfig.java
    - Grep results for DynamicElasticsearchClientRegistry imports
  </read_first>
  <action>
    Per D-14: move `DynamicElasticsearchClientRegistry` to `data-generator-datasource-elasticsearch` module.
    Update imports in CoreConfig, EsWriter, EsReader, ElasticsearchSinkFactory, related tests.
    Per D-04/D-21: preserve primary fallback in `llc(cluster)` (or renamed method) when cluster blank.
    Add adapter unit tests for primary fallback and unknown cluster IllegalArgumentException (D-07 pattern).
  </action>
  <acceptance_criteria>
    - Registry class lives under datasource-elasticsearch module
    - `.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=ElasticsearchSinkFactoryTests -q` exits 0
    - Adapter module has at least one unit test asserting primary fallback behavior
  </acceptance_criteria>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-calcite -am test -Dtest=ElasticsearchSinkFactoryTests -q</automated>
  </verify>
  <done>ES registry relocated with tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cluster config → Kafka/ES clients | Broker/ES endpoints from managed or inline config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | ES/Kafka API keys | mitigate | SecretResolver for auth refs (D-10, D-22) |
| T-06-06 | Denial of Service | Client registry | accept | Existing registry concurrency model preserved |
| T-06-SC | Tampering | Package relocation | mitigate | Compile + targeted tests after import updates |
</threat_model>

<verification>
- Kafka and ES targeted calcite tests pass
- No duplicate registry classes in core (single source of truth in adapters)
</verification>

<success_criteria>
- DS-01: Kafka and ES abstractions in dedicated submodules
- D-14, D-04, D-21 satisfied
</success_criteria>

## Artifacts this phase produces

- Maven modules `data-generator-datasource-kafka`, `data-generator-datasource-elasticsearch`
- Relocated `DynamicKafkaTemplateRegistry`, `DynamicElasticsearchClientRegistry` (new packages)
- Adapter unit tests for cluster resolution

<output>
Create `.planning/phases/06-datasource-platform-core/06-03-SUMMARY.md` when done
</output>
