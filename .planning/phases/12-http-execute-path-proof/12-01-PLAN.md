---
phase: 12-http-execute-path-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java
autonomous: true
requirements:
  - EXEC-01
must_haves:
  truths:
    - MockMvc POST /task/run/{id} enqueues a published V2 template that sinks via managed JDBC dataSourceId (D-01, D-02, D-03)
    - instanceId is parsed from R.ok message with Pattern instanceId=(\\d+) (D-04)
    - TaskExecution is polled via TaskExecutionService until SUCCESS within ~30–60s; FAILED and CANCELLED fail immediately (D-05, D-07)
    - Proof requires SUCCESS status plus managed-pool COUNT(*) ≥ seeded row count (D-06)
    - IT does not use TemplateV2Runner.run as the primary execution path and does not assert snap:{instanceId} bindings (D-11)
    - require-published-for-task-run is true for this IT; application-phase7-test.yaml default stays false
  artifacts:
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java
  key_links:
    - MockMvc POST /task/run/{id} → TaskController.postRunById → runByIdInternal(requirePublished=true)
    - TemplateLifecycleService.publish → PUBLISHED gate before enqueue
    - TaskExecutionService.getByInstanceId poll → markSuccess on production runV2Tracked
    - DataSourceConfigService.save → managed pool → JDBC sink COUNT(*) after SUCCESS
---

<objective>
Prove EXEC-01: managed JDBC catalog sink through the real HTTP task spine via MockMvc `POST /task/run/{id}`, not in-process `TemplateV2Runner` alone.

Purpose: Close the v2.0 accepted limit where `ManagedJdbcCatalogSinkE2eIT` stopped at in-process runner; leave Phase 11 IT unchanged as regression.
Output: Green `ManagedJdbcCatalogHttpExecuteIT` under JDK 25 Surefire (H2, no Docker).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-http-execute-path-proof/12-CONTEXT.md
@.planning/phases/12-http-execute-path-proof/12-RESEARCH.md
@.planning/phases/12-http-execute-path-proof/12-PATTERNS.md
@.planning/phases/12-http-execute-path-proof/12-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogSinkE2eIT.java
@data-generator-service/src/test/java/org/gensokyo/data/task/RunReportPersistenceTests.java
@data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleAuthorizationIntegrationIT.java
@data-generator-service/src/test/resources/application-phase7-test.yaml
</context>

## Artifacts this phase produces

*(Phase-wide — this plan delivers the Wave 1 / EXEC-01 item)*

- `ManagedJdbcCatalogHttpExecuteIT` — new `@SpringBootTest` under `org.gensokyo.data.datasource.catalog`
- Private helpers in that class (names discretionary): `extractInstanceId`, `awaitSuccess`, `countRows`, `resetDatasourceRow`, template YAML/PO builder
- Constants unique from Phase 11: e.g. DS name `managed-jdbc-catalog-http-execute-ds`, table `managed_http_execute_sink`, H2 mem URL `jdbc:h2:mem:managed-jdbc-catalog-http-execute;MODE=MySQL;DB_CLOSE_DELAY=-1`
- *(Wave 2 / Plan 02)* `ManagedJdbcCatalogHttpPostgresUpsertIT`, service Testcontainers test deps, optional `DockerTestSupport` under service tests

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ManagedJdbcCatalogHttpExecuteIT bootstrap, seed, publish, and poll helpers</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java</files>
  <read_first>
    - .planning/phases/12-http-execute-path-proof/12-CONTEXT.md (D-01–D-11 — NON-NEGOTIABLE)
    - .planning/phases/12-http-execute-path-proof/12-PATTERNS.md (Pattern A–D compose map)
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogSinkE2eIT.java (save, DDL, countRows, managed-id assert — reuse patterns; do not modify this file)
    - data-generator-service/src/test/java/org/gensokyo/data/task/RunReportPersistenceTests.java (INSTANCE_ID_PATTERN, extractInstanceId, awaitSuccess — extend timeout + CANCELLED)
    - data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleAuthorizationIntegrationIT.java (@SpringBootTest properties override for require-published-for-task-run)
    - data-generator-service/src/main/java/org/gensokyo/data/template/editor/TemplateEditorRunSupport.java (instanceId parse twin)
    - .cursor/rules/java-copyright-class-javadoc.mdc
  </read_first>
  <action>
    Create `org.gensokyo.data.datasource.catalog.ManagedJdbcCatalogHttpExecuteIT` beside Phase 11 IT. Do **not** edit `ManagedJdbcCatalogSinkE2eIT`.

    File layout (mandatory): PCI copyright → package → imports → type Javadoc → body.

    Type Javadoc (maintainer-obvious evidence path, per RESEARCH / D-11) must state in order:
    1. Entry: MockMvc `POST /task/run/{id}`
    2. Catalog: managed `dataSourceId` via `DataSourceConfigService.save`
    3. Gate: publish before run
    4. Async: poll `TaskExecutionService` to SUCCESS
    5. Rows: managed-pool COUNT(*)
    6. Explicit non-claims: not in-process-only primary path; snap key assertion deferred (D-11)
    Include `@author`, `@version 3.0.0-SNAPSHOT`, `@since` (execute date or 2026-07-25).

    Bootstrap (D-02 gate override — do **not** change `application-phase7-test.yaml` defaults):
    - `@SpringBootTest(classes = DataGeneratorApplication.class, properties = { "spring.config.location=classpath:/application-phase7-test.yaml", "data.generator.governance.require-published-for-task-run=true" })`
    - Do **not** extend `V2ScenarioTemplateIT`
    - Do **not** install a capturing/`@Primary` fake `TemplateV2Runner`

    Autowire at minimum: `DataSourceConfigService`, `DataSourceConfigRepository`, `DynamicRoutingDataSource`, `TemplateRepository`, `TemplateLifecycleService`, `TaskExecutionService`, `NamedParameterJdbcTemplate`, `WebApplicationContext`.

    Constants (distinct from Phase 11 to avoid same-JVM H2 clash):
    - DS name e.g. `managed-jdbc-catalog-http-execute-ds`
    - Table e.g. `managed_http_execute_sink`
    - H2 URL e.g. `jdbc:h2:mem:managed-jdbc-catalog-http-execute;MODE=MySQL;DB_CLOSE_DELAY=-1`

    `@BeforeEach` reset: delete catalog row + remove dynamic pool for DS name (same spirit as Phase 11 / `JdbcSnapshotExecutePathIT`).

    Helpers to add in this task (behavior required; names discretionary):
    - `Pattern.compile("instanceId=(\\d+)")` + `extractInstanceId(String)` matching `RunReportPersistenceTests` / `TemplateEditorRunSupport` (D-04)
    - `awaitSuccess(Long instanceId)`: poll `taskExecutionService.getByInstanceId` with short sleeps (100–250ms), total budget ~30–60s (D-07); return on SUCCESS; **immediately** fail/assert on FAILED **and** CANCELLED (D-05, D-07) — no fixed sleep without status checks
    - `countRows(dataSourceId, table)` via `DynamicDataSourceContextHolder` push/clear (D-06)
    - MockMvc field built in `@BeforeEach` via `MockMvcBuilders.webAppContextSetup(webApplicationContext).build()`

    Prefer `templateLifecycleService.publish(id)` for publish (D-03 discretion). Leave the happy-path `@Test` method body for Task 2 if splitting, or stub a failing/empty test only if needed for compile — prefer completing helpers here and the full test in Task 2.
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am "-Dtest=ManagedJdbcCatalogHttpExecuteIT" -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true test</automated>
  </verify>
  <acceptance_criteria>
    - File `ManagedJdbcCatalogHttpExecuteIT.java` exists under `org.gensokyo.data.datasource.catalog`
    - Class Javadoc mentions MockMvc POST /task/run, managed dataSourceId, publish, TaskExecution poll, COUNT(*), and deferred snap assert (D-11)
    - `@SpringBootTest` properties include `data.generator.governance.require-published-for-task-run=true` and `classpath:/application-phase7-test.yaml`
    - `application-phase7-test.yaml` on disk still has `require-published-for-task-run: false` (unchanged)
    - Source contains `instanceId=(\\d+)` parse helper and poll loop that checks FAILED and CANCELLED
    - `ManagedJdbcCatalogSinkE2eIT.java` git status shows no modifications from this plan
    - PCI copyright + type Javadoc present
  </acceptance_criteria>
  <done>HTTP IT scaffold with publish-gate override, parse, and 30–60s fail-fast poll helpers compiles under Surefire.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HTTP managed-catalog happy path (publish → MockMvc /task/run → SUCCESS + COUNT(*))</name>
  <files>data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogHttpExecuteIT.java</files>
  <read_first>
    - .planning/phases/12-http-execute-path-proof/12-CONTEXT.md (D-01–D-07, D-11)
    - .planning/phases/12-http-execute-path-proof/12-RESEARCH.md (Pattern A data-flow checklist; pitfalls 1–3, 7)
    - data-generator-service/src/test/java/org/gensokyo/data/datasource/catalog/ManagedJdbcCatalogSinkE2eIT.java (buildManagedSinkTemplate shape — inline rows + SQL + jdbc writer)
    - data-generator-service/src/test/java/org/gensokyo/data/task/RunReportPersistenceTests.java (TemplatePO persist pattern)
    - data-generator-service/src/main/java/org/gensokyo/data/controller/TaskController.java (postRunById return message shape)
    - data-generator-service/src/test/java/org/gensokyo/data/template/TemplatePublishUdfValidationTests.java (lifecycle publish after saveAndFlush)
  </read_first>
  <action>
    Complete a single primary `@Test` method (name encoding the path, e.g. `httpTaskRun_managedCatalogSink_reachesSuccessWithCountableRows`) that implements D-01..D-07 and D-11:

    1. **Managed DS (reuse Phase 11 spirit):** `dataSourceConfigService.save(DS_NAME, h2Url, "sa", "", null, "org.h2.Driver", null, null)`.
    2. **DDL:** `CREATE TABLE` (prefer `DROP TABLE IF EXISTS` first) on managed pool for TABLE with columns matching seed rows (e.g. `id int primary key, label varchar(64)`).
    3. **Persist V2 template (D-03):** `TemplatePO` with V2 YAML/content whose JDBC writer has **only** `dataSourceId` = DS_NAME (no inline writer `dataSource`). Shape: inline/small seed (≥2 rows) + SQL transform + jdbc sink target TABLE — lift from Phase 11 VO shape / PATTERNS YAML sketch; verify key names against existing draft parser used by service tests. `templateRepository.saveAndFlush`.
    4. **Publish (D-02):** `templateLifecycleService.publish(entity.getId())` with IT gate already true.
    5. **MockMvc enqueue (D-01, D-03):** `mockMvc.perform(post("/task/run/{id}", templateId)).andExpect(status().isOk()).andExpect(jsonPath("$.success").value(true))`. Read `$.message` (R.ok puts text in message). Do **not** use console `/api/templates/{id}/run` as primary evidence.
    6. **Parse (D-04):** `extractInstanceId` from message.
    7. **Poll (D-05, D-07):** `awaitSuccess(instanceId)` → SUCCESS within 30–60s; fail fast FAILED/CANCELLED.
    8. **Rows (D-06):** `countRows(DS_NAME, TABLE)` ≥ seeded row count (e.g. 2L) after SUCCESS.
    9. **Managed-id-only:** Assert writer/dataSourceId intent on the template under test (YAML parse or PO content contains managed id; no inline dataSource block for the proven sink).

    Hard constraints:
    - Do **not** call `templateV2Runner.run(...)` as the primary execution path (may not even autowire it).
    - Do **not** assert `snap:` pool/key strings (D-11). Do **not** copy Phase 11 “Keep WorkflowRunContext unbound” framing into this HTTP IT (production `runV2Tracked` binds context).
    - Do **not** promote this IT into P0 harness / test-matrix (Phase 17).
    - Plain INSERT sink — no upsert options on EXEC-01 (upsert is Plan 02 / EXEC-02).

    Inline `//` only for non-obvious invariants (publish gate vs phase7 default; why poll not fixed sleep; why COUNT after SUCCESS).
  </action>
  <verify>
    <automated>.\mvnw-jdk25.ps1 -pl data-generator-service -am "-Dtest=ManagedJdbcCatalogHttpExecuteIT,ManagedJdbcCatalogSinkE2eIT" -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true test</automated>
  </verify>
  <acceptance_criteria>
    - Test performs MockMvc `POST /task/run/{id}` and expects HTTP 200 with `$.success` true (D-01, D-03)
    - Test calls `templateLifecycleService.publish` (or equivalent publish that sets PUBLISHED) before run (D-02)
    - Test parses instanceId via `instanceId=(\\d+)` from response message (D-04)
    - Test polls `TaskExecutionService` to SUCCESS with FAILED/CANCELLED fail-fast and ~30–60s budget (D-05, D-07)
    - Test asserts managed-pool COUNT(*) ≥ seeded rows after SUCCESS (D-06)
    - Test source does not invoke `templateV2Runner.run` as primary path
    - Test source does not assert snap-prefixed datasource keys (D-11)
    - `ManagedJdbcCatalogSinkE2eIT` still green (regression) and unmodified by this plan
    - Maven command above exits 0 (BUILD SUCCESS)
  </acceptance_criteria>
  <done>EXEC-01 proven: managed catalog → publish → HTTP /task/run → SUCCESS + COUNT(*).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MockMvc → TaskController `/task/run` | Untrusted template id / enqueue surface; IT uses disposable fixtures only |
| IT → managed JDBC catalog | Creates disposable H2 catalog credentials; must not embed production secrets |
| Publish gate → task run | Governance boundary: unpublished templates must not run when gate enabled |

## ASVS

- Level: 1
- Block on: high severity threats

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | require-published-for-task-run | high | mitigate | IT sets gate true and publishes before MockMvc run; do not copy RunReportPersistenceTests unpublished shortcut; leave phase7-test yaml default false for other tests |
| T-12-02 | Tampering | CapturingTemplateV2Runner / fake runner | high | mitigate | Use real TemplateV2Runner bean only; no @Primary capture override in this IT |
| T-12-03 | Information Disclosure | ManagedJdbcCatalogHttpExecuteIT credentials | medium | mitigate | H2 mem URL + empty password only; never log secrets |
| T-12-04 | Spoofing | instanceId parse from R.message | low | accept | IT-only parse of trusted MockMvc response; production path unchanged |
| T-12-SC | Tampering | Maven package installs | high | mitigate | No new Maven groups this plan; N/A Package Legitimacy (no installs) |
</threat_model>

<verification>
- `.\mvnw-jdk25.ps1 -pl data-generator-service -am "-Dtest=ManagedJdbcCatalogHttpExecuteIT" -Dsurefire.failIfNoSpecifiedTests=false -Dskip.console.frontend=true test` → BUILD SUCCESS
- Phase 11 regression: `-Dtest=ManagedJdbcCatalogSinkE2eIT` still green
- Source review: MockMvc `/task/run`, publish gate true, poll+COUNT, no primary in-process runner, no snap asserts
</verification>

<success_criteria>
- EXEC-01 satisfied under locked D-01..D-07 + D-11
- ROADMAP Phase 12 SC1/SC3/SC4 partially met for H2 HTTP managed-catalog path
- Ready for Plan 02 EXEC-02 PG upsert IT on same spine
</success_criteria>

<output>
Create `.planning/phases/12-http-execute-path-proof/12-01-SUMMARY.md` when done
</output>
