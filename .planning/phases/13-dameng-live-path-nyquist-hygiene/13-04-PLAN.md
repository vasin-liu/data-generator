---
phase: 13-dameng-live-path-nyquist-hygiene
plan: 04
type: execute
wave: 2
depends_on:
  - 13-03
files_modified:
  - .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md
  - .planning/milestones/v2.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - DIAL-02
must_haves:
  truths:
    - "08-VALIDATION.md exists with nyquist_compliant: true, built only from the already-green tests recorded in 08-VERIFICATION.md and 08-UAT.md (D-11)"
    - "The Phase 8 accepted limits, including the Playwright PostgreSQL upsert scenario skipped on the H2 e2e path, are carried into Manual-Only Verifications rather than fixed (D-11)"
    - "The v2.0-MILESTONE-AUDIT.md Nyquist Compliance table shows 7, 07.1, and 8 as COMPLIANT, and its nyquist frontmatter block agrees with the table (D-13)"
    - "The audit tech-debt entries that existed only because of the missing validation documents are updated to record closure under DIAL-02 in Phase 13"
    - "Phase 12 validation state and the P0 merge gate are untouched (D-10)"
  artifacts:
    - path: ".planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md"
      provides: "New Phase 08 validation contract in the compliant 09-VALIDATION.md shape"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v2.0-MILESTONE-AUDIT.md"
      provides: "Nyquist Compliance table and frontmatter synced to the completed backfill"
      contains: "COMPLIANT"
  key_links:
    - from: ".planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md"
      to: ".planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VERIFICATION.md"
      via: "Per-Task Verification Map rows transcribed from Goal Achievement and the Automated Test Results Maven slice"
      pattern: "nyquist_compliant"
    - from: ".planning/milestones/v2.0-MILESTONE-AUDIT.md"
      to: "the three backfilled VALIDATION files"
      via: "Nyquist Compliance table rows plus the nyquist frontmatter compliant/partial/missing lists"
      pattern: "COMPLIANT"
---

<objective>
Complete DIAL-02: create the missing Phase 08 validation contract from its existing verification evidence (D-11, D-12), then sync the v2.0 milestone audit's Nyquist table and frontmatter so the recorded compliance status matches reality across 07, 07.1, and 08 (D-13) — with Phase 12 explicitly left alone (D-10).

Purpose: The milestone audit is the artifact maintainers actually read; leaving it saying MISSING after the backfill would make the whole exercise invisible and keep the tech-debt entry alive.
Output: new `08-VALIDATION.md`, and a `v2.0-MILESTONE-AUDIT.md` whose table, frontmatter, and tech-debt notes agree with the three backfilled files.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-CONTEXT.md
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-RESEARCH.md
@.planning/phases/13-dameng-live-path-nyquist-hygiene/13-03-PLAN.md
@.planning/ROADMAP.md
@.planning/milestones/v2.0-phases/09-jdbc-dialect-expansion/09-VALIDATION.md
@.planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VERIFICATION.md
@.planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-UAT.md
@.planning/milestones/v2.0-MILESTONE-AUDIT.md
</context>

## Artifacts this phase produces

*(Phase-wide inventory; this plan delivers the Wave 2 DIAL-02 closeout slice)*

- `.planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md` — new file, `nyquist_compliant: true` — **this plan**
- `.planning/milestones/v2.0-MILESTONE-AUDIT.md` — Nyquist Compliance table, `nyquist:` frontmatter block, and stale tech-debt entries synced — **this plan**
- 07 and 07.1 VALIDATION backfill — delivered by plan 13-03 (this plan depends on it)
- Dameng live IT wiring, wrapper script, docs, and AGENTS.md entry — plans 13-01 and 13-02

<tasks>

<task type="auto">
  <name>Task 1: Create 08-VALIDATION.md from existing Phase 08 verification and UAT evidence</name>
  <files>.planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-CONTEXT.md (D-10, D-11, D-12)
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-RESEARCH.md (Workstream B; DIAL-02 row notes 08-VERIFICATION carries 58 truths and a Maven slice command under Automated Test Results; Pitfall 3)
    - .planning/milestones/v2.0-phases/09-jdbc-dialect-expansion/09-VALIDATION.md (compliant shape template)
    - .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VERIFICATION.md (Goal Achievement, Requirements Coverage, Anti-Patterns Found, Automated Test Results, Human Verification Required, Gaps Summary)
    - .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-UAT.md (operator UAT record including the scenario skipped on the H2 e2e path)
    - .planning/milestones/v2.0-phases/07-datasource-governance-hot-reload/07-VALIDATION.md (as refreshed by plan 13-03 — keep the three backfilled documents consistent)
  </read_first>
  <action>
    Create `08-VALIDATION.md` in the archived Phase 08 directory (D-12). No such file exists today.

    1. **Group the evidence before writing.** Phase 08's VERIFICATION report carries far more truths than there were plan tasks, so do not emit one map row per truth. Group truths by the plan task and test class that proved them, using the Requirements Coverage table and the Automated Test Results section as the join. A readable map of roughly one row per plan task, each naming its real test classes, is the goal — a 58-row transcription is not.

    2. **Use the recorded Maven slice.** The Automated Test Results section states the command that was actually run and its green result; use that as the basis for the Automated Command column and for the Test Infrastructure full-suite entry rather than composing a new command that has never been executed.

    3. **Transcribe only (D-11, RESEARCH Pitfall 3).** Do not reopen Phase 08 streaming or upsert implementation, and do not write any new product test. In particular, the Playwright PostgreSQL upsert scenario that was skipped on the H2 end-to-end path is an accepted limit: copy it into Manual-Only Verifications with its recorded reason and its Testcontainers coverage note. Do not attempt to close it. Likewise carry forward the noted large-payload logging observation as recorded rather than fixing it.

    4. **Fill Test Infrastructure, Sampling Rate, and Wave 0 Requirements** from the phase's own records — the frameworks and configuration those tests used, the commands the SUMMARYs and VERIFICATION record, and the pre-existing infrastructure Phase 08 built on. Only the section shape comes from Phase 9.

    5. **Sign-off and provenance:** Set `nyquist_compliant: true` only once the grouped map is complete and the accepted limits are carried across, tick only the sign-off boxes that genuinely hold, and state that the document was backfilled in Phase 13 under DIAL-02 from the existing Phase 08 verification and UAT record.

    Out of scope: any file under `data-generator-*`, `data-generator-console-web/e2e/**`, the 07 and 07.1 files (plan 13-03 owns them), `.planning/test-matrix.yaml`, `scripts/verify-harness.ps1`, and Phase 12 validation (D-10).
  </action>
  <verify>
    <automated>rg -n "nyquist_compliant: true" .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "Per-Task Verification Map" .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "Manual-Only Verifications" .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `08-VALIDATION.md` exists in the archived Phase 08 directory with `nyquist_compliant: true`
    - The Per-Task Verification Map is grouped by plan task, not one row per verification truth, and every row names real test classes
    - Every test class named in the map appears in `08-VERIFICATION.md`, `08-UAT.md`, or an `08-0N-SUMMARY.md`
    - The Automated Command entries derive from the Maven slice recorded in the VERIFICATION Automated Test Results section
    - Manual-Only Verifications includes the Playwright PostgreSQL upsert scenario skipped on the H2 e2e path with its recorded reason and Testcontainers coverage note
    - The file uses the `09-VALIDATION.md` section set and states that it was backfilled in Phase 13 under DIAL-02
    - No file under `data-generator-*` or `data-generator-console-web/e2e/**` is modified
  </acceptance_criteria>
  <done>Phase 08 moves from MISSING to compliant, with its accepted limits preserved rather than papered over.</done>
</task>

<task type="auto">
  <name>Task 2: Sync the v2.0 milestone audit Nyquist table and frontmatter</name>
  <files>.planning/milestones/v2.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-CONTEXT.md (D-10, D-13)
    - .planning/phases/13-dameng-live-path-nyquist-hygiene/13-RESEARCH.md (Workstream B: the Nyquist Compliance table and the nyquist frontmatter block are the sync targets)
    - .planning/milestones/v2.0-MILESTONE-AUDIT.md (the nyquist frontmatter block with compliant/partial/missing phase lists near the top; the tech-debt entries for phases 07 and 08 that cite the missing or non-compliant validation documents; the Nyquist Compliance table and its overall verdict line; the Executive Summary sentence naming Nyquist hygiene as outstanding tech debt)
    - .planning/milestones/v2.0-phases/07-datasource-governance-hot-reload/07-VALIDATION.md (plan 13-03 output — confirm compliance before claiming it)
    - .planning/milestones/v2.0-phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-VALIDATION.md (plan 13-03 output)
    - .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md (Task 1 output)
  </read_first>
  <action>
    Sync the audit to the completed backfill (D-13). Verify each of the three files actually carries `nyquist_compliant: true` before changing any status — never assert compliance the files do not support.

    1. **Nyquist Compliance table:** Move the Phase 7 row from PARTIAL to COMPLIANT and the Phase 07.1 and Phase 8 rows from MISSING to COMPLIANT, updating each row's validation-document and flag columns to match the real files. Replace the overall verdict line, which currently recommends optional validation passes for 7, 8, and 07.1, with an accurate statement that all v2.0 phases in this table are compliant and that the backfill landed in Phase 13 under DIAL-02.

    2. **Frontmatter `nyquist:` block:** Update the compliant list to include 07, 07.1, and 08 alongside the phases already listed, and empty the partial and missing lists. The block and the table must agree — a reader or tool consuming either must reach the same conclusion.

    3. **Tech-debt entries:** Two entries exist only because of this documentation gap — the Phase 07 item about the non-compliant validation flag and the Phase 08 item recording the absent validation document. Update both to state that the gap was closed in Phase 13 under DIAL-02 rather than deleting the history. Leave every other tech-debt entry untouched, including the Dameng opt-in and dual-resolver items, which have their own requirement tracks.

    4. **Executive Summary:** Adjust only the clause that lists Nyquist hygiene as outstanding tech debt so it reflects closure. Do not restate requirement counts, harness results, or flow verdicts — those are unchanged by this phase.

    5. **Scope boundaries (D-10, RESEARCH Pitfall 2):** Do not add a Phase 12 row to this v2.0 audit and do not touch `.planning/phases/12-http-execute-path-proof/12-VALIDATION.md`. Do not touch `.planning/test-matrix.yaml` or `scripts/verify-harness.ps1`; the Dameng opt-in IT stays out of the P0 gate. `.planning/STATE.md` deferred-item bookkeeping is handled by the phase verification flow, not this plan.
  </action>
  <verify>
    <automated>rg -n "COMPLIANT" .planning/milestones/v2.0-MILESTONE-AUDIT.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "partial_phases: \[\]" .planning/milestones/v2.0-MILESTONE-AUDIT.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "missing_phases: \[\]" .planning/milestones/v2.0-MILESTONE-AUDIT.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "nyquist_compliant: false" .planning/phases/12-http-execute-path-proof/12-VALIDATION.md; if ($LASTEXITCODE -eq 0) { exit 0 } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - The Nyquist Compliance table shows COMPLIANT for phases 7, 07.1, and 8, with no PARTIAL or MISSING rows remaining
    - The frontmatter `nyquist:` block lists 07, 07.1, and 08 as compliant, with empty partial and missing lists that agree with the table
    - The overall verdict line no longer recommends optional validation passes for 7, 8, or 07.1
    - The Phase 07 and Phase 08 tech-debt entries record closure under DIAL-02 in Phase 13 rather than being deleted
    - The Dameng opt-in and dual-resolver tech-debt entries are unchanged
    - No Phase 12 row is added, and `.planning/phases/12-http-execute-path-proof/12-VALIDATION.md` still reads `nyquist_compliant: false`
    - `.planning/test-matrix.yaml` and `scripts/verify-harness.ps1` are unmodified
    - Every status claimed in the audit is backed by a `nyquist_compliant: true` flag in the corresponding VALIDATION file
  </acceptance_criteria>
  <done>The milestone audit's recorded Nyquist status matches the backfilled files, and DIAL-02 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backfilled VALIDATION files → milestone audit status claims | The audit is the summary maintainers trust; a claim ahead of the files is a durable false record |
| DIAL-02 doc scope → product code and e2e specs | Documentation work must not reach into module sources or Playwright specs |
| Phase 13 scope → Phase 12 validation and the P0 gate | D-10 and deferred DIAL-03 both draw hard lines here |

## ASVS

- Level: 1
- Block on: high severity threats

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Repudiation | `nyquist_compliant: true` claim on Phase 08 | high | mitigate | Grouped map rows must name test classes traceable to `08-VERIFICATION.md`, `08-UAT.md`, or an 08 SUMMARY; the flag is set only after the map is complete, and provenance text marks it a Phase 13 retrospective backfill |
| T-13-13 | Spoofing | Audit table asserting compliance the files do not support | high | mitigate | Task 2 requires verifying all three flags before editing status, and acceptance criteria bind every audit claim to a corresponding `nyquist_compliant: true` flag |
| T-13-10 | Tampering | Scope creep into Phase 08 implementation or e2e specs while documenting | high | mitigate | Accepted limits are copied, never resolved; acceptance criteria require no `data-generator-*` or e2e spec file to be modified |
| T-13-04 | Elevation of Privilege | P0 merge gate scope | high | mitigate | Acceptance criteria assert `.planning/test-matrix.yaml` and `scripts/verify-harness.ps1` are unmodified; DIAL-03 stays deferred |
| T-13-11 | Tampering | Out-of-scope edits to Phase 12 validation state | medium | mitigate | Both task verify commands assert `12-VALIDATION.md` still reads `nyquist_compliant: false`, enforcing D-10 |
| T-13-14 | Tampering | Loss of tech-debt history through deletion | low | mitigate | Closed entries are annotated with their DIAL-02 closure rather than removed, preserving the audit trail |
</threat_model>

<verification>
- All three backfilled files compliant: `rg -n "nyquist_compliant: true" .planning/milestones/v2.0-phases/07-datasource-governance-hot-reload/07-VALIDATION.md ".planning/milestones/v2.0-phases/07.1-close-gap-ds-03-jdbc-snapshot-routing-on-execute-path/07.1-VALIDATION.md" .planning/milestones/v2.0-phases/08-rw-streaming-upsert/08-VALIDATION.md`
- Audit synced: `rg -n "COMPLIANT|partial_phases|missing_phases" .planning/milestones/v2.0-MILESTONE-AUDIT.md` shows no PARTIAL or MISSING classifications and empty partial/missing lists
- Scope guards: `rg -n "nyquist_compliant: false" .planning/phases/12-http-execute-path-proof/12-VALIDATION.md` still matches (D-10); `.planning/test-matrix.yaml` and `scripts/verify-harness.ps1` unmodified (RESEARCH Pitfall 2)
- No product code touched: the plan's `files_modified` list contains only `.planning/**` paths
</verification>

<success_criteria>
- DIAL-02 fully satisfied: 07, 07.1, and 08 all carry honest `nyquist_compliant: true` from pre-existing evidence (D-11, D-12)
- The v2.0 milestone audit table, frontmatter, and tech-debt entries agree with the backfilled files (D-13)
- ROADMAP Phase 13 success criterion 3 is met
- Phase 12 validation and the P0 merge gate are provably untouched (D-10)
</success_criteria>

<output>
Create `.planning/phases/13-dameng-live-path-nyquist-hygiene/13-04-SUMMARY.md` when done
</output>
