---
phase: 23-docs-harness-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/test-matrix.yaml
  - docs/test-feature-matrix.md
  - docs/test-harness.md
autonomous: true
requirements:
  - TEST-11
must_haves:
  truths:
    - "test-matrix row geo-assets is tier P1, status covered, owner_module data-generator-service, test_types unit+integration per D-07"
    - "linked_tests includes ConsoleGeoAssetControllerIT, ConsoleGeoAssetPreviewIT, GeoAssetServiceTests, TemplateV2RunnerGeoAssetSourceTests, and GeoAssetReferenceScannerTests per D-08"
    - "P0 row count remains 15; geo-assets is not promoted to P0 per D-09"
    - "docs/test-feature-matrix.md regenerated from YAML after matrix edit per D-06"
    - "Linked Surefire class simple names resolve to existing test sources; harness summary path remains target/test-matrix-summary.json per D-10"
  artifacts:
    - path: .planning/test-matrix.yaml
      provides: Harness source of truth for geo-assets P1 row
      contains: "geo-assets"
    - path: docs/test-feature-matrix.md
      provides: Generated human-readable matrix mirror
      contains: "geo-assets"
  key_links:
    - from: .planning/test-matrix.yaml
      to: data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleGeoAssetControllerIT.java
      via: linked_tests entry
      pattern: "ConsoleGeoAssetControllerIT"
    - from: .planning/test-matrix.yaml
      to: data-generator-calcite/src/test/java/org/gensokyo/data/calcite/TemplateV2RunnerGeoAssetSourceTests.java
      via: linked_tests entry
      pattern: "TemplateV2RunnerGeoAssetSourceTests"
    - from: docs/test-feature-matrix.md
      to: .planning/test-matrix.yaml
      via: generate-test-matrix-doc.ps1
      pattern: "GENERATED"
---

<objective>
Deliver TEST-11: add a non-blocking P1 `geo-assets` matrix row linked to real Maven tests, regenerate the feature-matrix doc, and prove P0 stays at 15 (D-06 through D-10).

Purpose: Feature matrix must reflect Phase 21–22 geo-asset coverage without widening the merge gate.
Output: Updated `.planning/test-matrix.yaml`, regenerated `docs/test-feature-matrix.md`, optional brief `docs/test-harness.md` P1 evidence note; no new merge-gate script required.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-docs-harness-closeout/23-CONTEXT.md
@.planning/test-matrix.yaml
@docs/test-feature-matrix.md
@docs/test-harness.md
@scripts/generate-test-matrix-doc.ps1
@scripts/verify-harness.ps1
@.planning/milestones/v2.2-phases/20-pipeline-proof-docs-p1/20-03-PLAN.md
</context>

## Artifacts

| Artifact | Role |
|----------|------|
| `.planning/test-matrix.yaml` | Source of truth — new `geo-assets` P1 row (D-07/D-08) |
| `docs/test-feature-matrix.md` | Regenerated mirror (D-06) |
| `docs/test-harness.md` | Optional Phase 23 P1 evidence note (discretion; keep short) |
| `target/test-matrix-summary.json` | Proof path when linked modules are run (D-10; not committed) |

<tasks>

<task type="auto">
  <name>Task 1: Add geo-assets P1 row to test-matrix.yaml</name>
  <files>.planning/test-matrix.yaml</files>
  <read_first>.planning/test-matrix.yaml, docs/test-harness.md, .planning/phases/23-docs-harness-closeout/23-CONTEXT.md</read_first>
  <action>
    Per D-07 add a new matrix row id geo-assets near the existing geo-synthetic P1 row (capability/adapter naming aligned with geo-synthetic / console-api-* style). Set: tier P1, status covered, owner_module data-generator-service, test_types [unit, integration], adapter such as geo-assets or console-geo-assets (executor discretion; keep consistent with YAML style). Per D-08 set linked_tests to at least ConsoleGeoAssetControllerIT, ConsoleGeoAssetPreviewIT, GeoAssetServiceTests, TemplateV2RunnerGeoAssetSourceTests, and GeoAssetReferenceScannerTests (class simple names). Notes should describe metadata DB geo assets + console preview ITs + calcite execute-path asset-id proof; state P1 non-blocking and that Playwright/Podman is not required for this row. Per D-09 do NOT set tier P0 on this row; do not add any new P0 row; do not change verify-harness.ps1 P0 gate logic. Before saving, count existing tier P0 entries — must remain 15. Discretion: skip creating scripts/verify-phase23-uat-geo-assets.ps1 unless operator replay clearly needs it; default is matrix-only evidence (D-10).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$lines=Get-Content .planning/test-matrix.yaml; $i=0..($lines.Count-1) | Where-Object { $lines[$_] -match '^\s*-\s*id:\s*geo-assets\s*$' } | Select-Object -First 1; if ($null -eq $i) { exit 1 }; $block=$lines[$i..([Math]::Min($i+20,$lines.Count-1))] -join \"`n\"; if ($block -notmatch 'tier:\s*P1') { exit 1 }; if ($block -notmatch 'status:\s*covered') { exit 1 }; if ($block -notmatch 'owner_module:\s*data-generator-service') { exit 1 }; foreach ($c in @('ConsoleGeoAssetControllerIT','ConsoleGeoAssetPreviewIT','GeoAssetServiceTests','TemplateV2RunnerGeoAssetSourceTests','GeoAssetReferenceScannerTests')) { if ($block -notmatch [regex]::Escape($c)) { Write-Error \"missing $c\"; exit 1 } }; $n=(Select-String -Path .planning/test-matrix.yaml -Pattern 'tier:\s*P0').Count; if ($n -ne 15) { Write-Error \"P0 count=$n\"; exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    geo-assets row exists as P1 covered with required linked_tests; P0 count still 15 (D-07, D-08, D-09).
  </acceptance_criteria>
  <done>YAML row reflects P1 geo-assets linkage without P0 inflation.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate docs/test-feature-matrix.md and optionally note P1 in test-harness.md</name>
  <files>docs/test-feature-matrix.md, docs/test-harness.md</files>
  <read_first>docs/test-feature-matrix.md, scripts/generate-test-matrix-doc.ps1, docs/test-harness.md</read_first>
  <action>
    Per D-06 run scripts/generate-test-matrix-doc.ps1 from repo root to regenerate docs/test-feature-matrix.md from .planning/test-matrix.yaml. Do not hand-edit generated table cells. Confirm the geo-assets row shows tier P1, status covered, owner data-generator-service, and lists the linked Surefire class names. Discretion: add a short Phase 23 / v2.3 P1 evidence subsection to docs/test-harness.md mirroring the Phase 17 P1 table style (row id geo-assets, Maven IT/unit bar, P0 freeze reminder) — keep it brief; skip if overview already covers harness semantics and the generated matrix is enough.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path docs/test-feature-matrix.md -Pattern 'geo-assets' | ForEach-Object { if ($_.Line -match 'P1' -and $_.Line -match 'data-generator-service' -and $_.Line -match 'ConsoleGeoAssetControllerIT') { exit 0 } }; exit 1"</automated>
  </verify>
  <acceptance_criteria>
    Human-readable matrix mirrors YAML geo-assets P1 row after regeneration (D-06).
  </acceptance_criteria>
  <done>Generated matrix synced; optional harness doc note applied per discretion.</done>
</task>

<task type="auto">
  <name>Task 3: Prove P0=15 and linked Surefire classes exist for harness summary</name>
  <files>.planning/test-matrix.yaml, docs/test-feature-matrix.md</files>
  <read_first>.planning/test-matrix.yaml, scripts/verify-harness.ps1</read_first>
  <action>
    Per D-09 re-count tier P0 rows in test-matrix.yaml (expect 15). Confirm geo-assets is tier P1 only. Confirm scripts/verify-harness.ps1 was not edited to promote geo-assets or map E2E into the P0 set. Per D-08/D-10 prove each linked_tests simple name exists on disk under service or calcite test trees: ConsoleGeoAssetControllerIT, ConsoleGeoAssetPreviewIT, GeoAssetServiceTests, GeoAssetReferenceScannerTests, TemplateV2RunnerGeoAssetSourceTests. Optional focused Maven smoke (not full harness if slow): run those classes via mvnw-jdk25.ps1 -pl data-generator-service,data-generator-calcite -am -Dtest=ConsoleGeoAssetControllerIT,ConsoleGeoAssetPreviewIT,GeoAssetServiceTests,GeoAssetReferenceScannerTests,TemplateV2RunnerGeoAssetSourceTests test so Surefire results can feed target/test-matrix-summary.json when verify-harness is later run — do not require a new Phase 23 UAT script (D-10 default matrix-only). Do not add Playwright to linked_tests.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$n=(Select-String -Path .planning/test-matrix.yaml -Pattern 'tier:\s*P0').Count; if ($n -ne 15) { Write-Error \"P0 count=$n\"; exit 1 }; $paths=@( 'data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleGeoAssetControllerIT.java', 'data-generator-service/src/test/java/org/gensokyo/data/api/console/ConsoleGeoAssetPreviewIT.java', 'data-generator-service/src/test/java/org/gensokyo/data/geo/GeoAssetServiceTests.java', 'data-generator-service/src/test/java/org/gensokyo/data/geo/GeoAssetReferenceScannerTests.java', 'data-generator-calcite/src/test/java/org/gensokyo/data/calcite/TemplateV2RunnerGeoAssetSourceTests.java' ); foreach ($p in $paths) { if (-not (Test-Path $p)) { Write-Error \"missing $p\"; exit 1 } }; $ga=Select-String -Path .planning/test-matrix.yaml -Pattern 'id:\s*geo-assets' -Context 0,15; if ($ga -match 'tier:\s*P0') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    TEST-11 satisfied: P1 geo-assets linked to real classes, P0 frozen at 15, no Playwright matrix requirement (D-08, D-09, D-10).
  </acceptance_criteria>
  <done>Harness closeout proven: linked classes exist, P0=15, matrix doc regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-matrix.yaml → CI merge gate | P0 tier changes affect merge blocking |
| linked_tests → Surefire collection | Stale class names falsely claim coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03 | Elevation of Privilege | tier P0 promotion of geo-assets | medium | mitigate | D-09 explicit freeze; Task 1 and Task 3 verify P0 count == 15 |
| T-23-04 | Repudiation | stale linked_tests | low | mitigate | Assert test class files exist before marking covered (D-08/D-10) |
| T-23-05 | Denial of Service | accidental Playwright-as-gate | low | mitigate | D-08 forbids Playwright requirement for this P1 row |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan |

ASVS L1: matrix/docs low risk; primary control is P0 freeze verification.
</threat_model>

<verification>
geo-assets row P1 covered in YAML and generated MD; P0 count 15; linked Surefire sources exist; verify-harness.ps1 P0 semantics untouched.
</verification>

<success_criteria>
- TEST-11 complete: P1 matrix linkage without P0 gate change
- D-06 through D-10 honored
- Linked class names match Phase 21–22 shipped tests
</success_criteria>

<output>
Create `.planning/phases/23-docs-harness-closeout/23-02-SUMMARY.md` when done
</output>
