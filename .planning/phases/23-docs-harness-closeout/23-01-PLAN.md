---
phase: 23-docs-harness-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/geo-synthetic-v2-source.md
  - docs/geospatial-overview.md
  - docs/geo-assets.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "docs/geo-synthetic-v2-source.md documents boundaryAssetId / networkAssetId / geojson assetId YAML and asset:{uuid} wire format per D-01"
    - "Path vs asset-id precedence documents asset-id wins (Phase 21 D-02) and path/classpath remain valid per D-01"
    - "docs/geospatial-overview.md has a v2.3 phase row for asset registry + console map and no longer claims upload/map as deferred per D-02"
    - "Upload limits max-bytes default 52428800 and max-features default 10000 are documented from shipped config per D-04"
    - "Console map preview usage covers /geo-assets browse, template-editor geo_synthetic hybrid preview, and seed honesty per D-05"
    - "Map/upload/console prose either stays short in overview or lives in docs/geo-assets.md linked from overview per D-03"
  artifacts:
    - path: docs/geo-synthetic-v2-source.md
      provides: Primary YAML/asset-id reference for geo_synthetic and geojson binding
      contains: "boundaryAssetId"
    - path: docs/geospatial-overview.md
      provides: Landing page with v2.3 asset registry + map status
      contains: "v2.3"
    - path: docs/geo-assets.md
      provides: Optional sibling for upload limits + map preview when overview budget exceeded (D-03)
      contains: "max-bytes"
  key_links:
    - from: docs/geospatial-overview.md
      to: docs/geo-synthetic-v2-source.md
      via: relative markdown link
      pattern: "geo-synthetic-v2-source.md"
    - from: docs/geospatial-overview.md
      to: docs/geo-assets.md
      via: relative markdown link when sibling created
      pattern: "geo-assets.md"
    - from: docs/geo-synthetic-v2-source.md
      to: docs/geospatial-overview.md
      via: overview landing link
      pattern: "geospatial-overview.md"
---

<objective>
Deliver DOC-01 operator/maintainer documentation for asset-id YAML binding, path vs asset-id, map preview usage, and upload size limits (D-01 through D-05; D-03 placement heuristic).

Purpose: Close the v2.3 docs gap left after Phase 21–22 shipped code so operators can bind `asset:{uuid}` / dedicated asset-id fields and use console map preview without reading source.
Output: Updated `docs/geo-synthetic-v2-source.md` and `docs/geospatial-overview.md`; optional `docs/geo-assets.md` when new map/upload prose exceeds ~40 lines (D-03).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-docs-harness-closeout/23-CONTEXT.md
@.planning/milestones/v2.2-phases/20-pipeline-proof-docs-p1/20-CONTEXT.md
@docs/geo-synthetic-v2-source.md
@docs/geospatial-overview.md
@docs/test-harness.md
@.planning/phases/21-geo-asset-registry-runtime-resolution/21-CONTEXT.md
@.planning/phases/22-console-map-geo-synthetic-editor/22-CONTEXT.md
@data-generator-service/src/main/java/org/gensokyo/data/config/DataGeneratorProperties.java
@data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/model/v2/GeoSyntheticSourceVO.java
@data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/model/v2/GeoJsonSourceVO.java
</context>

## Artifacts

| Artifact | Role |
|----------|------|
| `docs/geo-synthetic-v2-source.md` | Primary YAML/asset-id reference (D-01) |
| `docs/geospatial-overview.md` | Landing page + v2.3 status + links (D-02) |
| `docs/geo-assets.md` | Optional sibling for upload/map/console usage when D-03 length heuristic trips |

<tasks>

<task type="auto">
  <name>Task 1: Replace v2.2 path-only wording with asset-id YAML in geo-synthetic-v2-source.md</name>
  <files>docs/geo-synthetic-v2-source.md</files>
  <read_first>docs/geo-synthetic-v2-source.md, data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/model/v2/GeoSyntheticSourceVO.java, data-generator-common/data-generator-core/src/main/java/org/gensokyo/data/model/v2/GeoJsonSourceVO.java, .planning/phases/21-geo-asset-registry-runtime-resolution/21-CONTEXT.md</read_first>
  <action>
    Per D-01 edit docs/geo-synthetic-v2-source.md as the primary YAML/asset-id reference. Remove or replace the v2.2 line that says asset upload is not in scope. Add concrete YAML examples for: (1) geo_synthetic BOUNDARY_POINTS with boundaryAssetId; (2) geo_synthetic LINE_SAMPLE with networkAssetId; (3) type geojson with assetId. Document wire format asset:{uuid} on path fields (boundaryPath / networkPath / path) as an alternate that resolves through the same GeoAssetResolver spine. Document path vs asset-id precedence exactly as Phase 21 D-02: when both path and asset-id are set for the same role, asset-id wins (fail-fast / clear validation message preferred over silent dual-read); path and classpath remain first-class when asset-id is absent. Keep existing mode/output/SQL companion sections; do not invent new modes, ST_* APIs, or product behavior. Do not document Playwright or console UI details here (those belong in Task 2/3).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='docs/geo-synthetic-v2-source.md'; if (-not (Test-Path $p)) { exit 1 }; $t=Get-Content $p -Raw; if ($t -notmatch 'boundaryAssetId') { exit 1 }; if ($t -notmatch 'networkAssetId') { exit 1 }; if ($t -notmatch 'assetId') { exit 1 }; if ($t -notmatch 'asset:\{uuid\}|asset:\{') { exit 1 }; if ($t -match '(?i)Asset upload is \*\*not\*\* in v2\.2 scope') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    geo-synthetic-v2-source.md shows dedicated asset-id fields, asset:{uuid} notes, and asset-id-wins precedence; obsolete v2.2 “upload not in scope” claim gone (D-01).
  </acceptance_criteria>
  <done>Primary YAML/asset-id reference updated per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Update geospatial-overview.md v2.3 landing and remove deferred upload/map wording</name>
  <files>docs/geospatial-overview.md</files>
  <read_first>docs/geospatial-overview.md, docs/geo-synthetic-v2-source.md, .planning/phases/23-docs-harness-closeout/23-CONTEXT.md</read_first>
  <action>
    Per D-02 update docs/geospatial-overview.md as the landing page. Add a Phase status row for v2.3 covering geo asset registry (metadata DB upload/list/get/delete) and console map preview (/geo-assets + template-editor geo_synthetic preview). In Modules and/or source-types sections, note asset-id binding for geo_synthetic and GEOJSON and link to geo-synthetic-v2-source.md. Remove or replace Deferred bullets that still say GeoJSON asset upload / console map UI are v2.2 follow-ups — those shipped in Phase 21–22. Keep GEO-06 polygon and other true deferrals. Keep overview as landing; do not dump long YAML here (Task 1 owns YAML depth). Optionally add a one-line pointer to console /geo-assets; detailed map/upload prose is Task 3 (D-03/D-05).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='docs/geospatial-overview.md'; $t=Get-Content $p -Raw; if ($t -notmatch 'v2\.3') { exit 1 }; if ($t -notmatch 'geo-assets|geo assets|/geo-assets') { exit 1 }; if ($t -match '(?i)GeoJSON asset upload, polygon synthesis, console map UI \(v2\.2 follow-ups\)') { exit 1 }; if ($t -notmatch 'geo-synthetic-v2-source\.md') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    Overview has v2.3 asset registry + map status, links geo-synthetic doc, and no longer lists upload/map as deferred (D-02).
  </acceptance_criteria>
  <done>Landing page reflects shipped v2.3 geo assets + map per D-02.</done>
</task>

<task type="auto">
  <name>Task 3: Document upload limits and map preview usage (overview section or geo-assets.md)</name>
  <files>docs/geospatial-overview.md, docs/geo-assets.md</files>
  <read_first>data-generator-service/src/main/java/org/gensokyo/data/config/DataGeneratorProperties.java, .planning/phases/21-geo-asset-registry-runtime-resolution/21-CONTEXT.md, .planning/phases/22-console-map-geo-synthetic-editor/22-CONTEXT.md, docs/geospatial-overview.md</read_first>
  <action>
    Per D-04 document shipped upload limits only: data.generator.geo-assets.max-bytes default 52428800 (50 MiB) and max-features default 10000 from DataGeneratorProperties.GeoAssets; mention Phase 21 Spring multipart raise behavior as already shipped (do not invent new knobs or change application.yaml). Per D-05 document console map preview usage: browse at /geo-assets; template-editor geo_synthetic hybrid preview; seed honesty so operators do not mistake preview sampling for full run output — point at Phase 22 behavior, no new UI work. Per D-03 length heuristic (same as Phase 20 D-06): if map preview + upload limits + console usage exceed ~40 lines of new prose in overview, create short sibling docs/geo-assets.md and link it from overview; if content stays short, keep a compact section in overview only and do not leave an empty unused geo-assets.md. Do not promote Playwright E2E to a merge gate; do not edit test-matrix.yaml in this plan (23-02 owns D-06/D-07).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$files=@('docs/geospatial-overview.md'); if (Test-Path 'docs/geo-assets.md') { $files+='docs/geo-assets.md' }; $joined=($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; if ($joined -notmatch '52428800|50 MiB|50MiB') { exit 1 }; if ($joined -notmatch '10000|10[, ]?000') { exit 1 }; if ($joined -notmatch 'max-bytes|maxBytes|max-features|maxFeatures') { exit 1 }; if ($joined -notmatch '(?i)preview' -or $joined -notmatch '(?i)seed') { exit 1 }; if ($joined -notmatch '/geo-assets|geo-assets') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    Operators can find max-bytes/max-features defaults and map preview + seed honesty docs either in overview or linked geo-assets.md (D-03, D-04, D-05).
  </acceptance_criteria>
  <done>DOC-01 coverage complete: limits + map preview documented per D-03/D-04/D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → operator templates | YAML examples must not embed secrets, production hostnames, or real asset UUIDs from staging |
| Docs → config knobs | Documented limits must match shipped DataGeneratorProperties only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Information Disclosure | example YAML / docs | low | accept | Use placeholder UUIDs and classpath/fixture names only; no real credentials |
| T-23-02 | Tampering | documented upload limits | low | mitigate | Cite shipped defaults from DataGeneratorProperties (D-04); do not invent knobs |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan |

ASVS L1: docs-only surface; low residual risk.
</threat_model>

<verification>
Read geo-synthetic-v2-source.md and geospatial-overview.md (and geo-assets.md if created): asset-id examples present, deferred upload/map wording gone, limits and preview honesty reachable from landing page.
</verification>

<success_criteria>
- DOC-01 satisfied: asset-id YAML, path vs asset-id, map preview, size limits documented
- D-01 through D-05 honored; D-03 placement applied via length heuristic
- No product/API code changes; no matrix edits (owned by 23-02)
</success_criteria>

<output>
Create `.planning/phases/23-docs-harness-closeout/23-01-SUMMARY.md` when done
</output>
